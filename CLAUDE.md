# roomstudio (working name — no product name has been chosen)

A spatial intelligence product that helps people discover the best version of their home: AI-powered room analysis, conversational redesign, and an immersive 3D representation of *their own* space.

This file is the always-current state of the project. Both Claude Code (reads it automatically) and Claude Chat (you upload it) consume it at the start of every session. If something in here is wrong, fix it before doing anything else.

## What we're building

**The thesis: every home contains a version of itself that its owner has never seen. This product makes that version visible, understandable, and achievable — one conversation at a time.** Every feature decision filters through this. The full founding vision lives at `docs/product/initial-idea-draft.md` (verbatim, with what's superseded vs durable mapped in decision 0055) — read it before making product-surface decisions.

This is NOT an "upload → generate a 3D scene" showcase. The 3D reconstruction is the *medium*; the product is helping people make AI-based decisions about improving their room. Three product layers frame everything: the **AI layer** (understands space structurally — object relationships, traffic flow, light, proportion — with algorithmic spatial analysis before any LLM is invoked, and reasoning traces on every design decision), the **emotional layer** (feels personal, not algorithmic — the experience bar is Linear/Vercel/Figma-tier premium consumer software; conversation is the primary post-reveal interface; the cinematic reveal is the defining moment; design language is Apple-grade restraint per decision 0056 — neutral chrome, content carries the color, one sans, mono only for machine data), and the **social layer** (rooms are identity — sharing, comparison, evolution over time). Direction, not yet commitments: room health scoring, taste graph, lighting simulation, budget-aware shopping, DAG version history. Deliberately out (per the founding draft, still sound): AR overlay, social feed, photorealistic image generation, floor plans, voice input; desktop-first.

**Naming:** "RoomMind" (the draft's name) was discarded; nothing has been chosen. "roomstudio" is a stand-in used for the repo, GCP project, and wordmark — the web wordmark is isolated in `web/src/components/Wordmark.tsx` for a one-file swap when the name lands.

Three technical surfaces today:

- **iOS capture app** (Swift + ARKit + RoomPlan) — capture-only, no viewer. The app's only job is producing a high-quality capture bundle and uploading it. Users come to the web for everything else.
- **Backend perception pipeline** (FastAPI on Cloud Run, `asia-southeast1`) — ingests bundles, runs SAM 3 segmentation + SAM 3D Objects reconstruction, places objects in the room's gravity-aligned metric frame using ARKit data (decision 0052), generates inpainted scene-3DGS for walls/floor (unbuilt). This is the modern substrate for the draft's perception + spatial-reasoning layers; the spatial relationship graph and design-generation layers above it are unbuilt.
- **Web app** (Next.js, static export + web splat rendering — WebGL2 via Spark, decision 0053 — hosted on Firebase Hosting) — the product surface: today rooms + viewer; next analysis, conversation, and redesign. Capture path is one screen: "Open the iOS app." Auth: same Firebase identity as iOS — requires upgrading iOS's anonymous auth to a real sign-in linked to the existing anonymous credential (see "Next on the board"); anonymous UIDs don't carry across devices.

Photo-upload (Android, no-iPhone users) is a deferred concern. Until the iOS path is solid we don't build the web-fallback capture.

## Capture bundle — the central contract

Everything between iOS and the backend flows through `packages/schemas/capture_bundle.proto`. The bundle is metadata; pixel data (frames, depth) lives in GCS by reference.

Frame of reference is **ARKit-native** end-to-end: right-handed, +Y up, camera looks down -Z in its local frame. The iOS client does NOT transform; the backend converts to downstream model frames (e.g. SAM 3D's per-object frame) when it has to.

Pose is **position + unit quaternion (x, y, z, w)**, not a 4×4 matrix. ARKit-native, ARCore-native, glTF-native. 7 floats instead of 16. The proto file's docstring carries the full reasoning.

Quaternion math is centralized in `packages/schemas/roomstudio_schemas/pose_math.py`. Any Python that touches a Pose imports from there. Do not re-implement.

## Repo layout

```
packages/schemas/                 capture bundle proto + generated Python + pose/placement math
  capture_bundle.proto              source of truth
  roomstudio_schemas/
    capture_bundle_pb2.py            generated; regen with ./tools/gen_proto.sh
    pose_math.py                     quaternion ops; one place to change
    placement_math.py                depth backprojection, single-view fits, ray triangulation
  tests/                              61 invariant tests, all green

packages/api-core/                shared logic consumed by both API services
  roomstudio_api_core/
    scene.py                         Scene model, SceneStatus, state machine
    scene_read_repo.py               SceneReadRepository ABC + Firestore/in-memory read-only impls
    upload_session_repo.py           UploadSessionRepository ABC + Firestore/in-memory impls + gcs_mint_resumable_uri
  tests/                              53 direct unit tests, all green

tools/                            local scripts (run from repo root)
  gen_proto.sh                      regenerate Python and Swift (ios/RoomStudioCapture/RoomStudioCapture/Generated/)
  build_test_bundle.py              synthesize a bundle from test_data/photos
  inspect_bundle.py                 verify a bundle parses + smoke-checks

services/
  api-public/                     client-facing API (--allow-unauthenticated, Firebase JWT verify)
  api-internal/                   internal API (--no-allow-unauthenticated, Cloud Run IAM)
  perception-obj/                 SAM 3 + SAM 3D Objects + placement/fusion (deployed pre-placement)
  perception-geom/                VGGT for the photo-upload path (deployed, photo-path only)

web/                              Next.js static-export web app (decision 0050); Spark splat viewer
                                  contained in src/components/SplatViewer.tsx (decision 0053)

infra/                            Cloud Build configs, deploy scripts
docs/decisions/                   short notes on dead-ends — see "When to write a decision note"
test_data/photos/                 9 HEIC photos of a real room, for synthesis testing
outputs/                          gitignored; generated artifacts
```

## What works right now

- The capture-bundle contract is defined, generated, and tested. `python tools/build_test_bundle.py && python tools/inspect_bundle.py outputs/test_bundle/bundle.pb` runs clean end-to-end.
- The old photo-upload HTTP surface is gone from `perception-obj`: `/segment`, `/segment-raw`, `/objects` and their dead helpers were removed on the review-findings-cleanup branch (`67b2f26`). `/process` (the iOS path) is the only perception entrypoint. `perception-geom` (VGGT) remains deployed but orphaned pending the photo-upload decision.
- 25 schema + math tests, all passing. Don't break them.
- **`services/api` two-service split deployed** (see `docs/decisions/0016`): `services/api-public/` (`--allow-unauthenticated`, Firebase JWT in-app verification, hosts `/upload_session`) + `services/api-internal/` (`--no-allow-unauthenticated`, Cloud Run IAM, hosts `/ingest/eventarc`; the legacy `/ingest` route was removed in the cleanup pass) + `packages/api-core/` (shared `UploadSessionRepository` + `gcs_mint_resumable_uri`). Both services live on Cloud Run `asia-southeast1`; see revision details on line below.
- `perception-obj` `/process` receiver: accepts Cloud Tasks HTTP POST (OIDC-verified), claims scenes atomically in Firestore with lease-TTL crash recovery, runs SAM 3 + SAM 3D Objects, writes outputs to GCS, updates Scene state, fires FCM on terminal transitions. System is functional end-to-end locally. Dockerfile, cloudbuild config, and deploy script env vars are all patched (see `docs/decisions/0005`, `0006`). Models are lazy-loaded on first `/process` call: `/health` returns 200 immediately for the startup probe; `/ready` reports per-model load state. DINOv2 weights (~1.13 GB) are pre-cached in the image at `TORCH_HOME=/opt/torch_hub`, eliminating the cold-start runtime fetch. Startup probe targets `httpGet /health`. 165 tests passing across both services.
- **Stuck-scene lease-semantics fix shipped and verified** (revision `perception-obj-00024-89b`, 2026-05-25). The bug from `docs/decisions/0011` and `0012` is fixed: `/process` reclaims stale leases atomically, `EnvironmentalError` eagerly releases the lease, SIGTERM resets held scenes to `queued`, and `holder_id` is checked defensively on all three lease-mutating paths. Verified production trace for scene `561c68ae`: `action=claim` → OOM → `action=release_error` → `action=reclaim_stale` → OOM → `action=release_error` → `action=reclaim_stale` → OOM → `action=release_error` (final, `is_final_attempt`) → scene `failed`. Note: the trace exercised the eager-release optimization path; the load-bearing expiration-check path (stale-but-not-cleared leases) is covered by unit tests only, not this production trace.
- **Smoke tool pass 3 scoping locked** (see `docs/decisions/0017`): manifest derivation, upload sequencing, and PUT semantics are pinned against the current `/upload_session` contract. Recon from a prior Code session covers the handler, request/response schemas, GCS minting, and Firestore session repo.
- **Smoke tool pass 4 scoping locked** (see `docs/decisions/0019`): polling contract pinned against a new `GET /scenes/by-bundle/{bundle_id}` endpoint on api-public. Recon from prior Code session covers the Scene document lifecycle, field population at ingest, idempotency under at-least-once Eventarc delivery, and the existing `SceneRepository` patterns in api-internal.
- **Smoke tool pass 5 scoping locked** (see `docs/decisions/0020`): four-mode CLI shape, `--cleanup` semantics, `--verbose`/`--json` output, and exit-code 2 (misconfig vs. system-under-test failure) pinned.
- **`tools/upload_test_bundle.py` written**: 1008-line substitute iOS client. Four modes (happy-path, skip-blob, duplicate-event, auth-rejection), two-phase upload sequencing per decision 0017, polling per decision 0019, failure semantics per decision 0020. `--cleanup`, `--verbose`, `--json` (NDJSON), exit codes 0–3. 27 unit tests in `tools/test_upload_test_bundle.py`. Verification against a deployed stack pending runbook execution.
- **`packages/api-core/roomstudio_api_core/test_fixtures/capture_bundle.py` added**: `build_capture_bundle()` + `TestBundleArtifacts`. Generates valid `CaptureBundle` protos for all three tiers with configurable per-frame blob kinds. 22 new invariant tests.
- **Deploy runbook preconditions landed**: `infra/eventarc_setup.sh` corrected — `INGESTER_SERVICE` changed from `roomstudio-api` to `api-internal`, `INGESTER_SA` changed to `api-internal-runtime@roomstudio.iam.gserviceaccount.com` (both were stale post-split). `services/api/` confirmed local-only (only gitignored `__pycache__/`) and removed. `upload_sessions.created_at` verified correct (`datetime.now(tz=timezone.utc)`) — TTL semantics match intent, no code change needed.
- **`infra/RUNBOOK.md` written**: 8-phase deploy runbook (Preflight + Phases 1–8) for the two-service iOS upload path (decisions 0014–0020). `eventarc_setup.sh` gains `--lifecycle-only`, `--ttl-only`, `--trigger-only` flags for per-phase invocation.
- **`GET /scenes/by-bundle/{bundle_id}` on api-public built** (board item 1, decision 0019). `SceneReadRepository` + `InMemorySceneReadRepository` + `FirestoreSceneReadRepository` in `packages/api-core/`. Scene model moved to `packages/api-core/roomstudio_api_core/scene.py`; `services/api-internal/scene.py` is now a re-export shim. `SceneRepository` in api-internal extends `SceneReadRepository`; `FirestoreSceneRepository` extends `FirestoreSceneReadRepository` (no duplication of read logic). 242 tests across all packages, all green (25 schema + 48 api-core + 40 api-public + 94 api-internal + 35 smoke tool). Runbook Phase 0 preflight gate now satisfied.
- **Phase 1 deploy infrastructure is live.** GCS lifecycle rule on `gs://roomstudio-captures` (delete after age=1, prefix `captures/`) is configured. Firestore TTL policy is set on `upload_sessions.created_at` (state `ACTIVE`). Automatic single-field index on `scenes.bundle_id` is confirmed unexempt. `infra/RUNBOOK.md` Phase 0 preflight passes; v2 PR hardening (all 20 findings) landed.
- **Firebase Auth is configured for the project and usable from the smoke tool.** Firebase added to the GCP project (previously GCP-native only). Web app `roomstudio-smoke-test` registered (`appId 1:502805861152:web:095d7e3b331e0e0ddcbd45`); anonymous sign-in provider enabled; `firebase.googleapis.com` and Identity Toolkit enabled. `tools/upload_test_bundle.py` can obtain valid `idToken` credentials against the real project. ADC quota project set to `roomstudio`.
- **Protobuf gencode/runtime mismatch fixed in both service images** (see `docs/decisions/0021`). Root cause: `pip install packages/api-core/` in Layer 3 triggered proto-plus's `<7.0.0dev` constraint and downgraded protobuf from 7.35.0 back to 6.33.6, undoing the Layer 2 force-reinstall from decision 0005. Fix: api-core install moved to Layer 2 (before the force-reinstall); schemas install remains last. Container-import smoke added to both `infra/cloudbuild/api-internal.yaml` and `infra/cloudbuild/api-public.yaml` (build → smoke → push).
- **Two-service iOS upload path deployed to `asia-southeast1`:** `api-public` (revision `api-public-00010-cag`, 100% traffic, Firebase verifier wired) and `api-internal` (revision `api-internal-00017-puw`, 100% traffic, Cloud Run IAM gated). `/health` 200 on both. Redeployed 2026-07-21 with the merged web-app stack — see the board-item-1 deploy bullet at the end of this section.
- **Eventarc trigger `captures-bundle-pb-finalized` live**, destination `api-internal/ingest/eventarc`. App-side path filtering working (bundle.pb → 200, non-bundle.pb → 200 with structured ignore-log, per decision 0023).
- **Decision 0021 verified end-to-end:** layer-swap fix landed cleanly, CI import smoke ran in Cloud Build, no protobuf downgrade recurrence.
- **Phase 0 verifier check is now a hard gate** enforcing decision 0016's trust boundary: positive grep that `FirebaseTokenVerifier` is wired into api-public (`server.py`), negative greps that it's absent from `services/api-internal/` and `packages/api-core/`. Exits non-zero with a labeled message on any failure. (v2 PR #9, commit d166ec0.)
- **Phase 0h is now a liveness-only gate** consistent with perception-obj's scale-to-zero lazy-load design (decision 0024). `gcloud describe` halts on anything but `Ready True`; `curl /ready` confirms reachability and accepts any HTTP response (200/503/500 all confirm the service is invokable). Broken-model failures are caught downstream by Phase 7 + the container-failed-to-start decision-tree branch from Cluster D.
- **v2 PR RUNBOOK hardening landed** (board item 3): all 20 findings closed. Deploy scripts now conditionally pass `--no-traffic` (only when the service already exists) and tag each revision `candidate` for reliable pre-flip smoke URLs (findings #5/#11/#10). Phase 0a IAM check uses effective-permission tests; Phase 0b Firebase setup and ADC quota-project fully specified (#1/#2/#3). Test collection is invocation-independent via pytest rootdir + conftest path setup (#20). 242 tests green from any CWD.
- **Service modules renamed** `server.py` → `services/api-internal/ingest_server.py` and `services/api-public/public_server.py` (resolves a test-collection module-name collision, #20). Dockerfile CMDs updated to the new entrypoints. Post-rename redeploy complete (revisions `api-internal-00007-nic` and `api-public-00004-nah`); both services boot the renamed entrypoints.
- **Phases 0–7 executed against production** (2026-05-29). Full upload→ingest→reconstruction path exercised end-to-end: bundle upload, Eventarc delivery, Cloud Tasks enqueue, SAM 3D model load (94.6 s from cold), reconstruction pipeline reached. skip-blob and auth-rejection modes confirmed green. happy-path and duplicate-event reach reconstruction but not `ready` (synthetic fixture pixels are non-decodable placeholders — by design; see "What does NOT work").
- **Ingest validation gate shipped and live-verified end-to-end** (board item 1). Validates image decodability pre-GPU at the `/ingest/eventarc` handler; fast-fails bundles with non-decodable image blobs to a new `failed_invalid` Scene state. Serving revisions: `api-internal-00009-bej` (gate) and `api-public-00006-quw` (reader rebuild required by the cross-service enum contract — see `docs/decisions/0027`). Phase 7 is now 4/4 green: happy-path and duplicate-event both reach `failed_invalid` end-to-end (~3–10 s including Eventarc delivery latency; perception-obj never wakes on either, confirming pre-GPU fast-fail). Note: `api-internal-runtime` holds `storage.objectViewer` on `gs://roomstudio-captures` at **bucket scope**, not project scope — a project-level IAM check will not show this binding.
- **iOS capture app P1 complete** (HEAD `9094ed1`, 2026-05-30). `ios/RoomStudioCapture/` Xcode project, iOS 16, SPM-only (SwiftProtobuf). `capture_bundle.pb.swift` generated by `./tools/gen_proto.sh` and committed as source. ARWorldTrackingConfiguration, gravity-aligned, `.sceneDepth` when available. Keyframe accumulation by pose delta (10 cm / 5°); `.limited`/`.notAvailable` tracking states skipped per decision 0028. Per keyframe: JPEG to temp dir (`frames/NNNNNN.jpg`), device-monotonic µs timestamp, `RSPose` + `RSIntrinsics` from ARCamera. Gravity field is a zero-vector stub — formula deferred to P1→P2 gate. Minimal SwiftUI: start/stop + frame counter + tracking-state badge (headless by design — no camera preview). On-device verified: pose-delta filter correct both directions, JPEG writes confirmed end-to-end (88 accepted / 88 written / 0 failures / 88 on-disk), all ARKit tracking states surface correctly. `PoseTests` 4/4 green (simd-only; quaternion unit-norm + component-order). Write observability (`WriteStats` + `do/catch` + `logWriteSummary`) kept as production code for P2.
- **`schema_version = "1"` enforcement gate closed and deployed** (see `docs/decisions/0031`). The ingest handler validates `schema_version == "1"` and rejects non-matching bundles with a `failed_invalid` Scene, structured log, and HTTP 200 (not a bare 400). A check already existed; this fixed the accepted value (`"1.0.0"` → `"1"`) and the rejection mechanism (bare 400 → `failed_invalid` + 200). Deployed: `api-internal-00013-qeg`, `api-public-00006-quw`.
- **iOS P2 chunk B complete** (see `docs/decisions/0032`, `0033`). Full `CaptureBundle` proto assembly: tier dispatch (ARKIT_ONLY/LIDAR_ARKIT), all fields populated including `schema_version = "1"`, `bundle_id` lowercased. Depth intrinsics corrected to scaled-RGB path per decision 0032 (the old `capturedDepthData` reference was wrong and would not have compiled). B-2 bundle_id casing closed. B-3 ARKIT_ONLY verified on-device (iPhone 16e): schema `"1"`, lowercased bundle_id, real-device provenance, clean quaternion norms, sane RGB intrinsics, gravity zero-stub as expected pre-chunk-C. LIDAR_ARKIT capture + inspect deferred pending hardware (decision 0033).
- **iOS P2 chunk C committed** (commit `a90418d`, see `docs/decisions/0030`, `0034`). `PoseExtractor.gravityInCameraFrame` implements R^T·world-down via `simd_quatf.inverse.act(simd_float3(0,-1,0))`. Both Gate-1 conditions met: hand-derived PoseTests at 1e-5 (green) + on-device sign check (floor/ceiling/landscape-horizon/roll, all clean). `#if DEBUG` gravity HUD in CaptureManager/ContentView; release builds unaffected. `ARCamera.transform` is a fixed landscapeRight frame — portrait reads ≈ (±1,0,0) and is correct (not a sign error); see decision 0034. P2 status: chunk A deployed, B code-complete (LiDAR depth residual deferred per decision 0033), C committed — P2 is mergeable.
- **iOS P3 complete — Firebase anon auth + `/upload_session`, live-contract-verified** (2026-05-31). Firebase iOS SDK 11.15.0 (Core+Auth) via SPM; offline-safe anonymous auth (cached UID, sign-in only when `currentUser` nil, never churns the UID — 0036); `UploadSessionClient` POSTs `/upload_session` with the 0038 retry/backoff status→action map; per-bundle session record persisted as an `NSFileProtectionComplete` file in Application Support, not Keychain (0037); Firebase-before-`configure()` ordering trap fixed (0039). Verified against live `api-public-00006-quw` (asia-southeast1) with a real anon idToken: happy-path 200, response mapped by `relative_path` not order, idempotent re-mint (same `session_uri`s for the same path-set); leading-slash manifest path → structured 400 (`invalid_manifest`), confirming 0038's fatal set unchanged; invalid-token 401 against the live `FirebaseTokenVerifier`. Integration tests run only when `RUN_INTEGRATION_TESTS=1` is set, using the `RoomStudioCapture-Integration` scheme — the only scheme in this project; there is no separate default scheme. Tests are run manually via `xcodebuild … -scheme RoomStudioCapture-Integration`; there is no automated CI gate at present. iOS test count and posture are tracked in the "iOS test policy" section (single source of truth).
- **iOS P4 ARKIT_ONLY complete — background blob upload, `bundle.pb` last, finalize hardware-verified** (main `0c0b39b`, 2026-06-03; decisions 0040/0041/0044). Single-shot whole-blob PUT over background `URLSession` (no chunking; `Content-Range: bytes 0-(N-1)/N`, no client `Content-Type` per F2); `bundle.pb` enqueued only after every non-`bundle.pb` blob is `uploaded`, the gate persisted in the 0037 record. On-device verified on a non-LiDAR iPhone17,5 (iOS 26.5): Gate 1 (upload-start handoff) PASS, Gate 4 (24h smoke soak, `failed_invalid` end-to-end) PASS, and **Gate 2 (finalize under suspension, alive process) PASS** — bundle `2fa51f70-…`: capture prefix + all frames + `bundle.pb` confirmed in GCS *during the locked interval*, verified from the Mac with no unlock (first hardware proof finalize completes while the app is non-active; the historical Anomaly-A failure mode is positively contradicted for the alive-process path). The Q1c POST-stall fix (two-window background-assertion architecture) is merged (`2586fc1`) and re-confirmed live. Under suspension GCS is authoritative over logs (Anomaly B: os_log buffers/coalesces/duplicates while suspended — operator tooling, not an app bug). ARKIT_ONLY tier only; LiDAR tiers have zero execution on a non-LiDAR device (see "Next on the board").
- **iOS P5 relaunch-recovery cluster code-complete** (decisions 0044/0045; (c) `5bb07c9`, (b) `cc7aba5`, (a) `658bc4a`, test-pin `bd8b86f`). Three coupled units: (c) idempotency hardening — `bundlePbEnqueueInFlight` in-process latch + `getAllTasks` cross-process reconciliation + context-preserve fix in `enqueuePhasOneBlobs`; (b) `onFatalBlobError` reclassified into TERMINAL/DEFERRED-INTERRUPTED/DEFERRED-TRANSIENT with `.failed` + `failureReason` persisted on terminal paths, sibling-task cancellation, and bounded cross-launch retry (N=10 per-launch, reset on progress); (a) launch-time rehydration via `rehydrateAllUnfinishedBundles()` triggered from `.task` in `RoomStudioCaptureApp`, with pre-checks for missing blob files before any enqueue. **Hardware gate pending:** the OS-kill route (background URLSession relaunch, `.task` fires without entering foreground) is unverified on-device — code-complete ≠ Gate-2b closed. See "What does NOT work" and decision 0045.
- **iOS P5(a) scene-status poll client + status UI complete** (commit `4556e44`). `ScenePoller` (@MainActor singleton, ObservableObject): lenient `SceneStatus` decode (unknown wire → `.unknown(raw)`, never throws — 0027); inner 0038 retry policy (401-refresh-once, 5xx bounded jittered backoff → `transientFail`); outer elapsed-gated cadence (2 s < 30 s / 10 s < 5 min / 30 s cap — placeholder values, not yet tuned against real production perception latencies); no hard give-up while foregrounded (FCM `ready`/`failed` is broken — polling is the sole completion channel). `ScenePollState` covers idle / polling(latest, since, longRunning, connectionTrouble) / succeeded / failedTerminal / recoverable / pollError. `SceneStatusView` renders all states premium-clean: live elapsed, long-running flip at 10 min + manual "Check now" (since replaced by pull-to-refresh — see the ios-upload-robustness bullet), connection-trouble note, success, hard-terminal failure, recoverable humanised to file count. A-nudge kick wired in `onBundleComplete` (one `Task { await MainActor.run { ScenePoller.shared.notifyBundleComplete } }` line); `SceneStatusView.onAppear` is the independent start path that reads `.complete` records from `UploadSessionStore`. 29 new `ScenePollTests`, all injected seams, no real clock/network. Suite: 191 tests, 0 failures (was 162; now 187 after the cleanup pass — see iOS test policy).
- **#17 Pub/Sub no-redelivery verified live** (2026-07-15). Uploaded a non-`bundle.pb` blob to `gs://roomstudio-captures`; confirmed a single Eventarc delivery, a single `200`, and no redelivery over several minutes (subscription `minimumBackoff=10s`). Found and fixed a real bug in the process: `services/api-internal/ingest_server.py` and `services/api-public/public_server.py` never called `logging.basicConfig()`, so every `logger.info(...)` call in both services was silently dropped in production (root logger default level `WARNING`) — the `eventarc_ignored` structured log decision 0023 relies on had never once fired in 30 days of history despite the code path executing correctly. Fixed by adding the same `logging.basicConfig(level=logging.INFO, ...)` call already used in `perception-obj/server.py`; both services redeployed (`api-internal-00015-quq`, `api-public-00008-qut`) and the fix re-verified live (the log now appears). 351 tests green (post-cleanup-merge count).
- **Review-findings cleanup pass complete on branch `review-findings-cleanup`** (17 commits, `41c8e59` → `f289b5d`, 2026-07-17). 198 of 207 findings from the untracked tasks.md resolved; the 9 remaining are deferred features/launch gaps recorded there. Highlights: pre-VGGT schemas deleted; `device_id` required end-to-end (new `DeviceIdentity.swift` Keychain UUID, backend `device_id_missing` validation, `DeviceIdSource`/hardware_id fallback removed); `fcm_token` threaded Scene → ClaimResult → notifiers (perception-obj no longer sends the device UUID as an FCM token); legacy `POST /ingest` removed + FAILED-redelivery duplicate-Scene bug fixed (FAILED → QUEUED retry with existing scene_id); perception-obj model loads moved off the event loop (`asyncio.to_thread`); lease-holder guard on `release_ready`/`release_failed` + per-object soft-fail + bounded `release_failed` retry (decision 0048); per-thread GCS client cache in ingest; api-public handlers un-async'd (event-loop unblock) + prod-gated sys.path shim; Cloud Tasks `dispatch_deadline` 930s ≥ Cloud Run 900s timeout; iOS `remintProvider` wired in production, stranded re-mint blobs fatal-routed, `UploadSessionRecord` migration shims removed (strict decode), scene-status poll-start deadlock fixed, phase-codename comments replaced throughout (decision 0049 for the re-mint failure semantics). Suites: 351 Python, 187 iOS, all green.
- **Object placement + scene fusion built in perception-obj** (merged to `main` and deployed 2026-07-21, decision 0052). Each SAM 3D object now gets a world transform in the ARKit frame: rotation from SAM 3D's layout output (`rotation`/`translation`/`scale` — previously discarded, only `"gs"` was read), scale+translation from the LiDAR depth cloud via `placement_math.fit_single_view` (single-view-aware: silhouette-true transverse extents + near-face anchoring; the generic PCA/centroid fit and iterated NN-ICP were evaluated and rejected — measured findings in 0052) + translation-only NN polish. ARKIT_ONLY frames emit per-object view rays; `fusion.py` clusters observations into one entry per physical object (per-frame-uniqueness + behind-camera triangulation guards) and fuses transforms. **Manifest is now v2**: top-level `manifest_version: 2` + fused scene-level `objects[]` (what the viewer renders) alongside `frames[]` (provenance). Every failure path yields explicit `placed: false` + reason — never a guessed transform. Synthetic-fixture accuracy: scale 0.2%, translation ~3 cm, robust to 5mm noise + 3% bleed; pinned as regression tests at achieved-accuracy tolerances.
- **Web read contract on api-public** (decision 0054): `GET /scenes` (newest-first list for the caller — the by-bundle-only read surface left a web client unable to discover scenes), `GET /scenes/{scene_id}/assets` (manifest verbatim + `asset_urls` map of V4-signed HTTPS splat URLs, TTL 1h, IAM-signBlob signing; 409 `scene_not_ready` until ready), and CORS gated on the new `CORS_ALLOWED_ORIGINS` env var (set in `infra/api-public.env.yaml`). Closes gap F4's web half. Shared `_scene_to_client_dict`/`_verify_bearer` helpers keep the three authed routes' shapes from drifting.
- **Web app scaffolded, redesigned product-true, and interactively verified** (`web/`, board item 6; decisions 0050/0053/0055). Next 16 static export (TS, App Router, Tailwind 4): typed API layer with three modes (`mock` fixtures covering all six SceneStatus values / `live-local` against a local uvicorn api-public / `live` Firebase anon); rooms-centric routes `/` (thesis-led landing with cursor-as-light ambient hero), `/rooms`, `/room?bundle=` (query-param routing — static export can't prerender unknown segments; the 3D space embeds inline when ready, with an object inventory and 10s polling while in flight), `/new` (scan instructions), `/viewer` (dev workbench, hidden from nav in live mode). Visual language now per decision 0057 (Good Guest — see the rehaul bullet below; this sentence's 0056 predecessor is superseded); all motion still through the single spring in `web/src/components/ui/spring.tsx` (`motion` lib). The room page is a full-bleed immersive stage with floating chrome (the narration card + disabled composer are the conversational interface's designated home). Status copy narrates analysis, not pipeline states. `SplatViewer.tsx` is the ONLY module importing three.js/Spark (renderer-agnostic `PositionedSplat` input); `Wordmark.tsx` is the single point of change for the placeholder name. Concrete CSP in `web/firebase.json` (connect-src allows api-public + storage.googleapis.com + Firebase auth; worker/child blob: for Spark). Verified in-browser across all routes and status fixtures, including the **assembled room** rendering inline on the room page (fused objects with correct transforms, no coordinate flip — ARKit and three.js share handedness/up-axis) and a live-local smoke (real CORS preflight + Bearer + empty-list 200). 17 vitest tests; lint + build green. `tools/make_synthetic_splat.py` generates INRIA-layout 3DGS PLY fixtures + a v2 manifest into gitignored `web/public/dev-fixtures/`.
- **Full Python suite: 460 tests green** (was 351; new coverage spans placement math incl. the single-view fit evaluation, the placement service layer, fusion incl. two regression guards, list_by_user, and the three new api-public surfaces). Dev toolchain this session: Node 22 via homebrew (keg-only, `/opt/homebrew/opt/node@22/bin`); local packages reinstalled **editable** in `.venv` (site-packages copies were stale); `uvicorn` added to `.venv`.
- **Web app visually rehauled to the "Good Guest" product system** (merged to `main` 2026-07-21; commits `d20b7c3`…`22bef7a`, 2026-07-20; source design file `Good Guest - Product System.dc.html`, untracked at repo root; decision 0057 supersedes 0056's visual language). Warm light-first: parchment/cream surfaces, warm-brown ink, rust primary actions, muted gold strictly as light-semantic indicators; Source Serif 4 = the guest's voice (italic) + display, Instrument Sans = UI, IBM Plex Mono = eyebrows + machine data (all next/font, self-hosted at build — CSP untouched). Surfaces: `/` = one-claim hero with the live demo room, plus a returning-visitor greeting grounded in the newest room's real state (localStorage has-rooms hint kills the pitch flash; `?pitch=1` dev escape in non-live modes); `/rooms` = the House (sibling cards on hatch stages, derived titles "the July 12 room", state as treatment + words); `/new` = the Bridge (declared-placeholder QR; a real listening panel that polls `GET /scenes` and walks into the wait when a new scan lands); `/room` = immersive, nav stands down: the Wait (arrival narration from real status + elapsed, breathing hatch, never a percentage), hold rule + objects-first reveal (once per room per browser via `lib/seen.ts`; assembly choreographed inside SplatViewer — largest first, each named as it lands; reduced motion collapses to the finished room), stage-1 layout (guest line grounded in real counts via `lib/voice.ts`, ledger inventory panel, disabled composer that says the guest hasn't arrived), and §8 failure treatments (partial = real missing-file count + the true iOS-resume claim; terminal = dark ink panel + rescan CTA → the bridge). StatusBadge deleted (state reads as treatment + words). 24 vitest tests (voice + seen libs covered), lint + static-export build green; also fixed a pre-existing `react-hooks/set-state-in-effect` lint error on HEAD (NewRoomSheet). SplatViewer stays the only three.js module (re-lit warm; new `frameless`/`reveal` props; `PositionedSplat` contract unchanged); Wordmark stays the one-file name seam (`❖ ROOMSTUDIO` explicit-placeholder treatment).
- **Board item 1 executed: `web-app` merged to `main` and the full backend stack deployed + live-verified (2026-07-21).** Merge was a fast-forward (`5dfa046` → `3c06fb2`; 460 Python tests green on the merged tree). Serving revisions: `api-internal-00017-puw`, `api-public-00010-cag`, `perception-obj-00025-tf6` (image `20260721-003900` — placement/fusion + manifest v2), all `/health` 200 at 100% traffic. RUNBOOK executed Phases 0–8a: preflight green (note: the operator account cannot `describe` Google-managed service agents — PERMISSION_DENIED is expected there; the live trigger + both IAM grants prove the delivery chain), lifecycle+TTL re-applied, candidates smoked pre-flip (401 `invalid_token` / platform 403), Eventarc trigger idempotently re-applied. Phase 7 all four smoke modes green first-attempt (happy-path + duplicate-event → `failed_invalid` ~3–5 s pre-GPU; skip-blob → `failed_incomplete` with the dropped path in `missing_paths`; auth-rejection → 401 `missing_token`); TTL belt-and-braces EXISTS; `--use-hardware-id-fallback` probe confirmed the `device_id_missing` rejection with the exact validation message on the Scene doc (read via ADC — the client API excludes `last_error` by design). **Web read contract live-verified end-to-end** with a manufactured ready scene: `GET /scenes` lists it, `GET /scenes/{id}/assets` returns the manifest verbatim + V4-signed URLs that fetch 200 at exact byte lengths (functionally proving the two new IAM grants: `serviceAccountTokenCreator` on `api-public-runtime` on itself + `objectViewer` on the outputs bucket), non-owner 403, artifacts cleaned. CORS confirmed live (allowed origin echoed on preflight; disallowed origin gets no CORS headers). Three deploy-path traps found and fixed in the same session (commits `7f6a74b`, `88a0de2`, `b3484bc`): `.gcloudignore` now excludes `web/` (gcloud reads only the top-level `.gitignore`, so `node_modules`/`.next` were adding ~1.3 GiB to every build context); the perception-obj Dockerfile now COPYs `placement.py`/`fusion.py` and runs a build-time `import placement, fusion` smoke (both are deferred imports — the missing COPYs passed `/health` and all probes and would have crashed the first real capture); `deploy_perception.sh` now forces `update-traffic --to-latest` after deploy (the service's traffic spec was pinned to revision name `00024-89b` from a 2026-05 rollback, so the new revision validated, was silently Retired at zero traffic, and gcloud printed "serving 100 percent" for the old code — caught and cleared same-session).
- **First real capture executed end-to-end (2026-07-21, scene `25a14caf`).** A real ARKIT_ONLY scan (iPhone17,5, 126 keyframes, 127 blobs) ran the whole chain: fresh install → capture → mint → background upload (127/127 in ~1 min) → **first real bundle ever through the ingest validation gate** → Cloud Tasks → GPU claim. **Decision 0052's result-keys check passed on real data**: every real SAM 3D reconstruct carries `rotation`/`translation`/`scale` (+ `gs`), the fields placement reads. ~20+ object observations reconstructed across frames 0–9 before memory saturation; per-object soft-fail (cleanup pass) contained every OOM exactly as designed. Real cost numbers now exist: ~3.5 min GPU cold start (inside the request budget), ~20 s/object, ~70–130 s/frame. **The capture is preserved at `outputs/real-capture-25a14caf/`** (126 frames + bundle.pb, gitignored, 62 MB) as the regression fixture for the envelope fix — the captures-bucket lifecycle rule would otherwise sweep it 2026-07-22.
- **Pre-launch gap (c) client half closed — re-mint sends real per-blob sizes** (branch `ios-remint-real-sizes`, commit `1b4d85c`, 2026-07-21). `BlobUploadManager.onSessionExpired` reads each manifest path's real on-disk size via `record.outputDir` (the `enqueuePhasOneBlobs`/`enqueueReput` pattern) and sends it as `expected_size_bytes`, replacing the deliberate placeholder 0; the full path-set is still sent (idempotency key unchanged), and both placeholder-describing comments are rewritten. A missing/unreadable/zero-size file routes to `onFatalBlobError("blob_unreadable_at_remint_manifest")` before the mint call — never a silent 0; the staleness pre-pass remains as TOCTOU defence-in-depth (a dedicated test pins it). No server change (0035 contract untouched). iOS suite 190 (+3), all green post-rebase in one run, the 4 live integration tests passing against the post-deploy stack (`api-public-00010-cag`).
- **Conversation stage 1 built, deployed, and live-verified (decisions 0058/0059; revision `api-public-00012-ziz`, 2026-07-21).** The room-page composer is alive. Server (commits `fc68e2d`…`697e6f9`): `scene_facts.py` — the model's ENTIRE world (inventory with confidence tiers from `frames_observed`/`cluster_spread_m`, framed center-to-center distances at honest 0.1 m rounding, relative-only vertical relations, provenance, per-scene limits; pure + deterministic, cached by `(scene_id, FACTS_VERSION)` with a loader so hits skip the GCS fetch); `guest_prompt.py` — PROMPT_VERSION 1 charter + five exemplars, pinned-hash test, observe-only telemetry (foreign-measurement detector with facts∪user-window allowlist, invitation heuristic → `flags` + structured log); `conversation_repo.py` — turns exist only completed; accept = dedupe → UTC-day quota → 150 s reservation, persist = sequential index + counters + holder-guarded lease clear (InMemory impl is the semantics oracle, Firestore mirrors it); `public_server.py` grows `GET /scenes/{id}/conversation` + `POST …/conversation/messages` — the service's FIRST async route (SSE `delta`/`done`/`error` in our vocabulary + `: ping` comments; disconnect shield = independent turn task + held-open request). Model call: `claude-sonnet-5`, zero tools, thinking disabled, 60 s wall clock, cache breakpoints charter/facts/newest-message (see 0059). Web: `GuestEvent` seam (SSE parsing only in `LiveApiClient`; typed pre-stream errors thrown before the iterable exists), pure composer reducer (idle/submitting/streaming/confirming/reoffer/rested/blocked; 27 vitest cases), `Conversation` card + stubs + live composer with ~200 ms delta batching (sentence-boundary under reduced motion), mock parity incl. `!error`/`!budget`/`!slow`/`!inflight`, degrade-to-settled on any conversation GET failure. Verified: 561 offline Python + 50 web tests; live-local e2e with the real key (happy turn, mid-stream-kill shield, 409 turn_in_flight, 429 with `resets_at`, dedupe replay at 0.02 s); voice evals 6/6 live at PROMPT_VERSION 1; deployed checks on the candidate pre-flip — streamed turn, **shield on Cloud Run** (client killed at 2.5 s → the complete turn in Firestore; 0058's recorded abort-on-disconnect fallback stays unneeded on first evidence), cache read 2268 tokens with `input_tokens=2` on a later turn. Ops: `anthropic-api-key` in Secret Manager (operator-created; secret-scoped `secretAccessor` on `api-public-runtime`), Cloud Run `--timeout 120`, `GUEST_MODEL`/`GUEST_DAILY_TURNS` in `infra/api-public.env.yaml`.
- **iOS upload robustness: three documented no-device gaps closed + a cold-launch auth race fixed** (branch `ios-upload-robustness`, commits `2e064a6`/`faea541`/`a29f9ec`/`c3a7396`, 2026-07-21; suite 190 → 212, green post-rebase in one run incl. the 4 live tests against `api-public-00012-ziz`). (1) **Blob PUTs honor GCS `Retry-After`** (`2e064a6`): 408/429 now retry in-process on the shared 0041 schedule (same as 5xx; exhaustion still DEFERRED-TRANSIENT under the cross-launch budget; no-context reclassified DEFERRED-INTERRUPTED — no budget burn for relaunch artifacts); `BlobUploadDelegate` threads the raw header through `handleTaskCompletion`; `parseRetryAfter` handles both RFC 9110 forms against the injected clock; a stated wait overrides the local backoff in full up to `maxRetryAfterHoldSec` (60 s), beyond which the blob defers cross-launch — never retry earlier than the server asked; 503-with-Retry-After honored by the same path. (Distinct from decision 0038's `/upload_session` item, still gated on pre-launch gap (b).) (2) **Upload-level terminal failure is visible** (`faea541`): `UploadFailureMonitor` mirrors the `.complete` architecture — kick from `onFatalBlobError` + independent `.task` scan of the store for `.failed`; `UploadFailureView` banner in ContentView shows honest copy + `failureReason` verbatim in mono; dismissal is session-local BY DESIGN (the record persists until P5 terminal-state cleanup; the failure resurfaces on relaunch). (3) **"Check now" → pull-to-refresh** (`a29f9ec`): ScrollView + GeometryReader minHeight preserves centering, `.refreshable` reuses `checkNow()` (unit-pinned), gesture enabled only while `.polling`; the long-running affordance is a quiet pull-down hint. (4) **Cold-launch auth race fixed** (`c3a7396`): `signInIfNeeded` gained a single-flight guard (two callers overlapping the sign-in suspension would have double-signed-in — 0036's UID churn), ScenePoller's default token path joins the launch sign-in, and a token-less tick is transient (connection-trouble sub-state) not fatal; 401-after-refresh and refresh-path token throws stay fatal per 0038, both test-pinned. All UI states exercised in the simulator with seeded records (failure banner found by the cold-launch scan; live `.polling` against production; the cold-launch repro that previously died at "Could not reach server" polls from the first tick).

## What does NOT work / what we're deliberately not doing

- **Sendable warnings in CaptureManager.swift.** `CVImageBuffer`/`CVPixelBuffer` cross the `DispatchQueue.main.async` boundary without `Sendable` conformance. Warnings in Swift 5 language mode; become errors under strict concurrency. Flagged for P1→P2 pre-flight.
- **iOS P2 LiDAR depth residual deferred** (hardware-gated, ~1–2 months, see `docs/decisions/0033`). B-3 LIDAR_ARKIT on-device capture + inspect not yet run. Covers: `sceneDepth` non-nil, 256×192 depth dimensions, one depth blob per frame, `depthIntrinsics` wrapper values correct on real hardware. No live depth consumer exists yet (P3–P5 unbuilt); deferral is low-risk.
- Scene `f077e9ed-d339-4be8-8dbf-37b952abfec2` is intentionally left in `processing` with an expired lease as a canonical stuck-scene reference. Re-enqueue manually if ever needed to verify the expiration-check reclaim path end-to-end.
- **`test_data/photos/` privacy is deferred.** 9 HEIC photos of a real room are tracked by git, used by `tools/build_test_bundle.py` for local synthesis testing. Privacy review (anonymise, replace with synthetic data, or remove from history) is a separate session. Do not act on this until explicitly scoped — it may require a history rewrite.
- **Web app: scaffolded, not deployed; live mode is dev-only.** No Firebase Hosting deploy has ever run (`firebase.json`/`.firebaserc` are committed but the `firebase` CLI isn't set up); no proper web-app Firebase registration (dev uses the `roomstudio-smoke-test` appId). `live` mode signs in with a web **anonymous** UID that owns no scenes — real cross-device login stays blocked on the iOS anonymous→linked-sign-in upgrade (decision 0051), which remains unbuilt. `lib/firebase.ts` is the marked seam where the real provider flow lands.
- **Bridge QR is a declared placeholder; reveal timing has had no real-browser watch.** The QR encodes nothing (no deep-link infra exists) and its caption says so. The objects-first assembly (§4 reveal) is verified by code + end state only — the dev preview pane throttles rAF/timers — so nobody has watched it play at real speed yet; do that once in a normal browser and tune the timing constants at the top of `web/src/components/SplatViewer.tsx` if the pacing feels off.
- **Conversation stage 1 is live; its recorded residue.** (1) Manifest v2 still carries no per-object extents — sizes, gaps, and clearances stay unspeakable (the charter forbids restating center distances as any of those); extents remain the top manifest addition (fast-follow after board item 1's verification event) and land with a `facts_version` bump. (2) The ledger deferral and its vocabulary ban are unchanged — no pin/keep/"put into words" anywhere; the "in this room" inventory panel stays as-is. (3) The reoffer/confirming composer states are pinned by reducer tests and mock triggers but haven't been watched against a real mid-stream server failure in a browser. (4) Client wake cadences (blocked 5 s, confirm 4 s, rested 15 s ticks) are untuned placeholder values. (5) Voice evals are live-gated (`RUN_VOICE_EVALS=1` + key; NOT in the offline suite); re-run on PROMPT_VERSION bump OR `GUEST_MODEL` change — the env yaml notes this at the `GUEST_MODEL` line. (6) Real users still can't reach it: prod exposure needs board 6(a) hosting + 6(b) 0051 sign-in — today only dev clients can talk to the deployed endpoint (CORS allows localhost + the not-yet-deployed Firebase Hosting domains).
- **perception-obj cannot finish a real-sized capture — envelope broken three ways (found by the first real capture, 2026-07-21; fix brief at `docs/briefs/perception-envelope-fix-brief.md`).** (1) The receiver iterates EVERY keyframe with no sampling policy — sized by 3-frame synthetic bundles; a real 126-frame capture is ~10× over the 900 s request budget, which ALSO contains the ~3.5 min GPU cold start (effective reconstruction budget ~11 min). (2) GPU memory climbs monotonically across objects and never returns — 22.03 GiB saturated by frame 10, then intermittent per-object OOM (soft-fail contains it; suspected cause: per-object results with on-GPU tensors retained in the accumulator all run — verify before fixing). (3) When the request times out, the handler thread keeps computing on the always-allocated CPU and holds the single concurrency slot, so Cloud Tasks retries sit platform-queued and 504 without ever reaching the app — the 0011/0012 reclaim machinery never runs, and a crashed zombie strands the scene in `processing` forever (a NEW stuck-scene route; the fix brief includes a re-enqueue tool as the cure). **Scene `25a14caf` is currently in exactly this limbo**: a zombie attempt (request cut 13:19:47Z, thread alive at frame 18/126 as of 13:48Z) may self-complete to `ready` hours late or strand — whoever touches perception next checks its state first (the fix brief says so).
- **iOS status surface: three honesty findings from the first real-user walk (2026-07-21; recorded mid-flight for the ios-upload-robustness branch, which wrapped without them — still open for the next iOS status-surface session).** (1) "Session ready" fires at the upload-session mint (~10 s in) and reads as "done" — it's the least meaningful of five milestones; copy fix. (2) "Queued for processing" headline over a "Processing your room…" subline — self-contradictory while the scene is still queued. (3) The elapsed clock anchors to the CLIENT poll-session start, so it ticks at poll cadence (10 s jumps mid-window) and resets to 0:00 on re-foreground/relaunch while the room has been processing for ~45 min — anchor it to the scene's `created_at`, which `_scene_to_client_dict` already puts on the wire, and tick the label on a 1 s display timer independent of the 10 s poll cadence (the cadence itself is by design, though its placeholder values now have real latency data to be tuned against).
- **SAM 3D layout conventions are HALF-verified.** Placement assumes layout quaternions are wxyz order and the layout camera frame is CV (+Z fwd/+Y down → diag(1,−1,−1) to ARKit). 2026-07-21: the result-keys half is now CONFIRMED on real data (see the first-real-capture bullet); the wxyz/CV-frame convention half still awaits the first completed placement run — it arrives with the envelope fix's re-enqueue of the preserved capture (`gravity_deviation_deg` verdict). Wrong conventions surface as systematically large `gravity_deviation_deg` (degraded orientation, never a crash). Placement accuracy remains synthetic-fixture-proven only — no real room has ever completed placement. The LiDAR-tier `depth_fit` path additionally stays parked on Pro hardware (board item 1).
- **Known fusion limits (v1, documented in 0052):** two same-label objects closer than 0.4 m (`FUSION_CLUSTER_DIST_M`) can merge into one; walls/floor don't render at all — the inpainted scene-3DGS for room shell geometry is unbuilt, so the viewer shows objects on a grid, not a full room.
- **Pre-launch gaps (nine gaps + one audit item, categorized).** See `docs/decisions/0015` and `0018` for full list and un-defer triggers. Abuse-surface (original 0015 set, trigger: first non-developer user): (a) TOCTOU race on `bundle_id` ownership in `/upload_session`; (b) no per-UID rate limit on `/upload_session` (the conversation endpoint ships its own local budget bounds per 0058; per-UID velocity limiting across expensive endpoints remains this gap, unchanged trigger); (c) `expected_size_bytes` optional server-side, defaults to 0 — **client half CLOSED** (2026-07-21, `1b4d85c`): the re-mint path reads real sizes off disk via `record.outputDir` and fatals (`blob_unreadable_at_remint_manifest`, pre-mint) on a missing/unreadable/zero-size file, and the two placeholder comments are updated; every client mint path (initial via `ManifestBuilder`, re-mint via `onSessionExpired`) now sends honest sizes. What remains is server-side only: the field is optional and unvalidated at `/upload_session` — requiring/enforcing it is a 0035 contract change that lands with the launch-hardening pass like the rest of this list. Contract-shape (from pass 3 + pass 4 recon, trigger: iOS development or web app build begins): (F1) `expires_at` not surfaced in `/upload_session` response — **on review, this likely doesn't need building as originally conceived**: the only consumer of a client-side mint timestamp (`BlobUploadManager`'s 12h staleness guard) measures time-since-mint, not URI expiry — a different quantity — and the 410-triggered re-mint path already handles genuine URI death; if anything from the server is worth surfacing here, it's the GCS object lifecycle window (currently a hardcoded 12h client-side mirror of a server config), not the resumable URI's own ~7-day expiry; (F2) `X-Upload-Content-Type` hardcoded to `application/octet-stream` server-side; (F3) no semantic manifest validation (unknown extensions, tier/path consistency) — server does independently enforce relative-path **format** (leading slash → structured 400, live-verified 2026-05-31); semantic checks remain client-owned and unverified; (F4) **web half CLOSED** (decision 0054): `GET /scenes/{scene_id}/assets` now serves the manifest + V4-signed splat URLs; `/scenes/by-bundle` still returns raw `gs://` in `result_uri`, which is fine — iOS treats it as an opaque readiness token and the web client uses the assets endpoint instead; nothing remains to build here unless a client someday needs to fetch `result_uri` directly. Production-hygiene (trigger: launch): (F5) no lifecycle rule on `gs://roomstudio-perception-outputs/scenes/`; (F6) no TTL on Firestore `scenes` collection — this isn't just a storage-hygiene gap: `receiver_repo.py`'s `claim()` currently treats a `NOT_FOUND` result (no scene document for a task's scene_id) as an unusual, essentially manual-deletion-only case, since nothing auto-deletes scenes today. Once F6 ships, the exact same `NOT_FOUND` response becomes routine, expected steady-state traffic — any Cloud Tasks redelivery arriving after its scene has aged out and been TTL-swept hits this path normally, not exceptionally. **When building F6**, update the docstring at `receiver_repo.py:91-96` (the `claim()` not-found case), which currently describes the pre-TTL "manually deleted only" framing and will go stale the moment TTL exists. **Also when building F6:** ship TTL-only first — a client-ack cleanup path (client acks after successfully fetching/consuming a ready/terminal result, triggering prompt deletion, with TTL remaining as the mandatory backstop since client cooperation isn't guaranteed) is a possible later optimization layered on top, not a substitute for TTL and not scoped or designed yet — do not build ack-only. **F6 also inherits (once conversation stage 1 is built):** Firestore never cascades deletes — scene TTL alone would orphan decision 0058's `conversations` docs and their `turns` subcollections; plan per-collection-group TTL on `turns.created_at` + `conversations`, or explicit recursive delete in the sweep. Audit item (during launch hardening): verify perception-obj runtime SA identity and storage IAM. All nine gaps close in the same launch-hardening pass.
- **happy-path and duplicate-event smoke modes terminate at `failed_invalid`, not `ready`.** The ingest validation gate fast-fails the synthetic fixture's non-decodable placeholder images pre-GPU (~3–10 s including Eventarc latency). This is the verified, expected terminal state for smoke runs. Reaching `ready` requires real image data from an iOS bundle.
- **24h post-deploy smoke soak not yet run for the 2026-07-21 deploy.** Phase 7 verified the merged stack end-to-end immediately post-deploy (4/4 green, 2026-07-21), but the RUNBOOK Phase 8c confirmation run — a happy-path re-run ≥24h after deploy, confirming stability under cold steady-state (scale-to-zero cold-start, GCS lifecycle age=1d and Firestore TTL having fired) — is due on or after 2026-07-22. Must be green before the iOS app's first real upload. One-command run: `python3 tools/upload_test_bundle.py happy-path … --cleanup` against the production URLs. (api-public has since moved to `api-public-00012-ziz`, 2026-07-21 conversation deploy — the soak run covers the current serving stack.)
- **iOS P3 401 recovery-success leaf unverified.** The live 401 test used a garbage token — exercises refresh→retry→propagate (give-up branch), not the expired→refresh→valid→200 case 0038 actually cites. Mechanism runs; the "refresh yields a working token" leaf is untested. Low risk. The path-violation 400 is confirmed for a leading slash only (`..` not separately confirmed; client emits neither).
- **`validate_bundle` bundle_id cross-check: CLOSED in code, not yet serving.** Built on branch `ingest-bundle-id-crosscheck` (commit `c30e317`, merged to `main` 2026-07-21): `validate_bundle(bundle, expected_bundle_id=…)` runs the proto-vs-URI bundle_id cross-check as check 2 (right after schema_version); any divergence → `failed_invalid` Scene with `bundle_id_mismatch` (both ids in `last_error`) + structured log + HTTP 200, per decision 0031's rejection pattern. Comparison is exact and **case-sensitive** — every downstream join on bundle_id (Firestore `scenes.bundle_id` queries, `upload_sessions` doc IDs, `captures/{bundle_id}/` GCS paths) is byte-wise, so a case-differing id breaks those joins like any other mismatch and is rejected, not normalized (rationale in `_check_bundle_id`'s docstring); an empty proto bundle_id is rejected by the same check. Passing `expected_bundle_id=None` skips the check — only for callers with no upload URI (the api-core fixture self-tests call `validate_bundle(b)` and were deliberately left untouched). **Not serving yet:** the change takes production effect at the next api-internal redeploy (serving revision `api-internal-00017-puw` predates it).
- **Staleness re-mint missing-file stall: CLOSED by P5(b)+(a).** `onFatalBlobError("blob_file_missing_at_staleness_remint")` now routes to the real terminal handler (sets `uploadPhase = .failed`, cancels sibling URLSession tasks, no record update without explicit save). The staleness pre-pass (Phase 1 of `onSessionExpired`'s `!loopGuardEnabled` branch) confirms all non-`bundle.pb` files exist before resetting any blob status or starting any upload task; a missing-file abort starts zero sibling uploads and leaves the store untouched. `bundle.pb` is never finalized against the hole. Gap was: `onFatalBlobError` was an unbuilt stub — now built by P5(b).
- **KNOWN GAP — completed-capture disk accumulation.** As of decision 0043, blob files live in Application Support (durable, CAFUFA), no longer tmp (which auto-purged). `CaptureStorageSweeper` reclaims ONLY abandoned captures (no `UploadSessionRecord`). Completed bundles keep their record — `onBundleComplete` is an unbuilt P5 stub that does not delete it — so the sweep keeps their directory. Each completed capture leaves one ~50–300 MB directory in Application Support that is never reclaimed until P5 ships. Bound: flat accumulation, one dir per completed capture; not runaway. Pre-launch, no real captures yet. **Closure trigger:** P5's `onBundleComplete`, when built, must delete the store record AND the session dir on successful upload. Do NOT pre-empt it by teaching the sweep to delete completed-bundle dirs — distinguishing "completed, safe to delete" from "completed, may need retry after failed_incomplete" is a P5 design decision tied to terminal-state handling, not a sweep heuristic.
- **KNOWN GAP — no automatic re-upload for `.recoverable` (failed_incomplete).** Nothing watches `pollState == .recoverable(missingPaths:)` and re-drives `BlobUploadManager` with the missing paths — this coordinator doesn't exist. `SceneStatusView.recoverableView` currently tells the user "Re-upload will be handled automatically," which is not true today; **fixing that copy is deferred** until either the coordinator ships or the copy is made honest about the gap, whichever comes first. **Dependency with the disk-accumulation gap above:** `onBundleComplete` fires the moment `bundle.pb`'s PUT succeeds — client-side, before the backend's async validation can determine `failed_incomplete`. If `onBundleComplete`'s eventual cleanup deletes the local session directory at that point, the files a re-upload coordinator needs may already be gone by the time `.recoverable` is observed. Whichever of these two gaps is built first must account for this, or the recoverable UI's promise stays false even after the coordinator ships. **Closure trigger:** build the coordinator AND defer `onBundleComplete`'s file/record deletion until a genuinely terminal backend state (`ready`/`failed`/`failed_invalid`), not just client-side upload success.
- **`bundle.pb` relaunch-recovery: code-complete, OS-kill gate unverified (Gate 2b).** Root cause (decision 0044): recovery path was unreachable on relaunch — wired only to `ContentView.onChange(of: capture.bundlePath)`, not a bare reopen. **Fixed in code by the P5 cluster** ((c) `5bb07c9`, (b) `cc7aba5`, (a) `658cc4a`): `rehydrateAllUnfinishedBundles()` fires on every launch from `.task` in `RoomStudioCaptureApp`; the OS-kill secondary route (`contexts[bundleId] == nil` → `onFatalBlobError`) now routes to the real terminal handler which sets `uploadPhase = .failed` and cancels siblings. **NOT VERIFIED ON HARDWARE:** the OS-kill route (background URLSession relaunch, `.task` fires without entering foreground) has not been staged on-device. The AppDelegate fallback is deferred pending the hardware gate — see decision 0045 Fork A ordering constraint. `diag-bundlepb-reason-public` (`5bdd12f`) is the parked tool for reading the redacted `reason=` on the OS-kill route — do not delete. Code-complete ≠ Gate-2b closed; the hardware gate is the close.

## iOS test policy

The iOS suite is **212 tests total** (was 190; the ios-upload-robustness branch added 11 `BlobUploadManagerTests`, 9 `UploadFailureMonitorTests`, and 2 `ScenePollTests`), run manually via `xcodebuild … -scheme RoomStudioCapture-Integration` — the only scheme in this project (no separate default scheme, no CI gate). That scheme bakes `RUN_INTEGRATION_TESTS=1`, so the 4 `UploadSessionClientTests` **execute live on every run**; they are NOT skipped in practice. They last ran live 2026-07-21 (ios-upload-robustness session, post-rebase) against the serving stack (`api-public-00012-ziz`).

**Posture: fail-closed-live, not fail-open.** Each integration test calls `XCTSkipIf(!RUN_INTEGRATION_TESTS)` — the fail-open default — but because the sole scheme always sets the flag, that skip path is never taken here. With the flag set they hit the live `/upload_session` contract and go **red if the backend is unreachable**. Running the suite therefore requires a reachable backend; an offline run will fail those 4 (expected, not a regression).

**Honest count:** report as "206 asserting offline unit tests + 2 boilerplate stubs (`testExample`/`testPerformanceExample`) + 4 live integration tests (require a reachable backend)", total 212 — not a bare total: the 2 stubs assert nothing and the 4 integration tests carry an external dependency the unit tests don't.

**Parallel-worktree note:** `GoogleService-Info.plist` is gitignored, so a fresh git worktree lacks it and the 4 live tests fail with a Firebase configure error that mimics a backend failure. Copy it from the main tree (`ios/RoomStudioCapture/RoomStudioCapture/`) before running the suite in a worktree; a rebuild picks it into the app bundle.

**Contract/CI note:** the `/upload_session` contract is frozen (decision 0035), so the live integration tests assert against a stable shape. When the contract next changes (launch hardening or any server-side change), or when CI is stood up, the live-integration posture is already in place — CI would set the same flag against a known-up backend. (The earlier fail-open rationale — that fail-closed-without-CI trains operators to ignore red — is superseded: the sole-scheme decision already made these intentionally fail-closed-live.)

## Conventions

- Python 3.11+. Ruff for lint/format (config at `pyproject.toml`).
- Frame of reference: ARKit-native everywhere on the wire. Convert at the boundary, not in transit.
- Coordinates: right-handed, +Y up, meters.
- Quaternions: `(x, y, z, w)`, unit norm within 1e-3.
- GCS paths in the bundle are RELATIVE to the bundle prefix, not full `gs://` URIs.
- File creation: every long-lived file gets a docstring explaining what it's for and who reads it. No "test code anyway" justifications for shortcuts.
- Tests pin invariants, not implementation. They should still pass after a refactor.

## Tooling conventions

Default model for routine work: **Sonnet 4.6**. Switch to **Opus 4.7** for hard reasoning (coordinate-frame conversions, perception-pipeline architecture, anything where a wrong answer propagates). Haiku 4.5 is not in use yet.

Default tool for code work: **Claude Code**. Default for strategy / architecture decisions: **Claude Chat**. See `.claude/WORKFLOW.md` for the full rubric and prompt templates.

## Git conventions

This repo is tracked with git. Local-only as of now; no remote yet.

**Claude Code's role with git:**

- Commit as part of normal work. One commit per logical unit (a feature, a fix, a refactor — not "end of session"). If a session produces multiple distinct changes, that's multiple commits.
- Write descriptive commit messages: what changed and why, not just what. Subject line under 72 chars; add a body if the why isn't obvious from the subject.
- Run `git status` and `git diff` before committing, and surface anything unexpected (e.g. a file changed that wasn't part of the task). Don't `git add -A` blindly.
- Run the relevant test suite after every code change. Show the full test output before proposing a commit. "Relevant" means: tests for the package or service that changed, plus any tests for packages that depend on it. If unsure which tests are relevant, run them all. Never commit untested code. If tests fail, fix and rerun until green; do not commit red, do not commit "mostly green," do not commit with an explanation of why a failure is fine. If a test is genuinely wrong, fix the test in the same commit and explain in the message.
- Do NOT push to remotes. The user pushes manually after reviewing. (As of now there is no remote anyway, but this rule pre-applies for when one is added.)
- Do NOT rewrite history (`git rebase`, `git commit --amend` on already-committed work, `git reset --hard` on commits you didn't make this session) without asking.
- Do NOT delete branches or force-anything.

**Session-end housekeeping commits:** the CLAUDE.md updates and any decision-note additions from session-end housekeeping should land in their own commit, separate from the code changes that prompted them. Message convention: `docs: session housekeeping — <one-line summary of what changed>`.

**What's gitignored:** see `.gitignore`. Notably: `outputs/`, virtualenvs, `.env`, `.claude/cache/` and `.claude/projects/` (Code's session state). Note that `CLAUDE.md`, `.claude/WORKFLOW.md`, and everything under `docs/decisions/` ARE tracked — they're project documentation, not local state.

## When to write a decision note

`docs/decisions/` holds the *why* behind decisions that aren't obvious from the code. One file per decision, filename `NNNN-short-slug.md` (zero-padded). Template at `docs/decisions/0000-template.md`.

The criteria for "is this worth a note?" live in the session-end housekeeping section below.

## Next on the board

When this section gets stale, the project's drifting. Keep it current.

**1 — Placement-verification event (PARKED on LiDAR hardware) + Phase 8c soak + one ops decision.** The 2026-07-21 deploy closed the software half of the old board item 1; three things remain. (a) **The placement-verification event — parked, hardware-gated:** the dev device (iPhone17,5) has no LiDAR, so the first real capture→assembled-room run waits for a Pro/LiDAR device. Ready-to-run checklist when hardware lands: capture a real room with the iOS app (LIDAR_ARKIT tier — note board item 3: that tier has zero on-device execution yet, so its runbook applies first); watch perception-obj logs for the SAM 3D result keys + `gravity_deviation_deg` (decision 0052's convention check); open the room in the web app (`live` mode) and confirm the first real assembled room. **UPDATE 2026-07-21 (later session): the ARKIT_ONLY variant of this event RAN — the full chain worked and the 0052 result-keys check passed on real data, but reconstruction broke the processing envelope before placement could run (see the envelope bullet in What-does-NOT-work). The placement/fusion verdict (`gravity_deviation_deg`) now arrives via the perception envelope fix session's re-enqueue of the preserved capture; the LiDAR variant stays parked on hardware for the `depth_fit` path.** (b) **Phase 8c soak (hardware-independent, due ≥2026-07-22):** happy-path re-run against production, expect exit 0 / `failed_invalid` (RUNBOOK 8c one-liner). (The former item (c) — deleting the pre-split `api` Cloud Run service — was operator-approved and executed 2026-07-21: it was publicly invokable (`allUsers` run.invoker), had zero requests in 30 days, and could not serve as a rollback target since its contract no longer exists; the image survives in Artifact Registry if ever needed. Cloud Run now runs exactly api-internal + api-public + perception-obj, plus the deliberately parked perception-geom.)

**2 — iOS P5 — OS-kill hardware gate** (decisions 0029, 0044, 0045). Core poll + status UI shipped (P5(a), commit `4556e44`). Remaining: **OS-kill hardware gate (cluster close item):** stage on-device: all blobs uploaded, `bundle.pb` PUT enqueued while alive, force-quit, reopen — confirm `bundle.pb` reaches GCS with no user interaction (GCS-authoritative, verified from Mac during locked interval); also verify `.task` fires on background OS-relaunch. `diag-bundlepb-reason-public` (`5bdd12f`) is the parked tool for reading the redacted `reason=`. AppDelegate fallback (decision 0045 Fork A) deferred pending this gate. After hardware gate: FCM `ready`/`failed` — backend threading is done (cleanup pass: Scene.fcm_token → ClaimResult → notifiers), so what remains is iOS-side FCM registration and passing the real `fcm_token` at `/upload_session`. Cold-start poll recovery is DONE (ios-upload-robustness, 2026-07-21): the SceneStatusView scan restarts polling across launches and the cold-launch auth race is fixed (`c3a7396`), simulator-verified; the open follow-up on this surface is the three status-surface honesty findings in What-does-NOT-work.

**3 — LiDAR-tier hardware verification (full-P4-close blocker for LiDAR tiers)** — needs a Pro device. ARKIT_ONLY green does NOT certify `captureDepth()`, LiDAR-tier dispatch, or the decision-0032 depth-intrinsics derivation (zero execution on a non-LiDAR device). The on-device runbook is parametric with `[LIDAR-UNVERIFIED]` tags so the same runbook re-runs on a Pro device. Subsumes the prior LIDAR_ARKIT depth-residual check (decision 0033). **Raised stakes since placement shipped:** the depth rasters this tier produces are now the metric authority for object placement (decision 0052's `depth_fit` path) — LiDAR-tier verification is what stands between the placement math and real data.

**4 — Close the nine pre-launch gaps + one audit item (decisions 0015 + 0018)** (before public traffic)

Decision 0018 extended to nine gaps: original three abuse-surface gaps ((c)'s
client half closed 2026-07-21 — re-mint sends real sizes; its server half, field
optional/unvalidated, stays in this pass) + F1/F2/F3
contract-shape gaps (F4's web half closed this session via the assets endpoint,
decision 0054 — see the gap list for what little remains) + F5 (no lifecycle rule on
`gs://roomstudio-perception-outputs/scenes/`)
+ F6 (no TTL on Firestore `scenes` collection — ship TTL-only first; a client-ack cleanup
optimization on top is a later, unscoped possibility, see "What does NOT work" for detail).
Plus one audit item: verify perception-obj
runtime SA identity and storage IAM. Abuse-surface trigger: first non-developer user.
Contract-shape trigger: iOS development or web app build begins (now met — F1/F2/F3
remain open). Production-hygiene and
audit: launch hardening. The remaining gaps close in the same launch-hardening pass.

**5 — test_data/photos/ privacy review** (deferred, low urgency)

9 HEIC photos of a real room are tracked by git and used by
`tools/build_test_bundle.py` for local synthesis testing. Review whether they
should be anonymised, replaced with synthetic data, or removed from history.
Do not act on this until explicitly scoped — it may require a history rewrite.

**6 — Web app: next increment** (decisions 0050, 0052–0057; Good Guest rehaul
live on branch `web-app`, verified against fixtures)

In rough order once board item 1's deploy lands: (a) Firebase Hosting first
deploy + PR preview channels (`firebase.json`/`.firebaserc` committed; CLI
setup + proper web-app Firebase registration needed); (b) iOS 0051
anonymous→linked sign-in (the blocker for real cross-device login; the web
seam is marked in `web/src/lib/firebase.ts`); (c) first real assembled room
in the viewer (falls out of board item 1's verification capture);
(d) **conversation fast-follows** (stage 1 SHIPPED 2026-07-21, decisions
0058/0059, revision `api-public-00012-ziz`): per-object extents into the
manifest (v2.x, after board item 1's verification event) → unlocks gap/
clearance speech with a `facts_version` bump; a real-browser watch of the
reveal AND the streaming composer at real speed (the dev preview pane
throttles both); tune the client wake cadences against real usage;
(e) interactive surface — per-object selection first
(buildable against fixtures now; this is the R3F/Drei adoption point per 0056,
with Theatre.js arriving for the reveal), then replace/rearrange; conversation
stage 2 (mutation) lands WITH the Design Specification contract + reactive
scene as one workstream; walls/floor once the scene-3DGS inpainting path
exists.

## Session-end housekeeping (Claude Code: do this before ending any session)

Before the session ends — when the user indicates they're wrapping up, when tests are green and the task is done, or when the conversation is winding down — propose the following without being asked:

**1. CLAUDE.md updates.** Show a proposed diff for:
- **"What works right now"** — add anything that started working this session.
- **"What does NOT work / what we're deliberately not doing"** — add anything broken, half-done, or explicitly deferred. Be honest. Half-done is half-done, not "in progress."
- **"Next on the board"** — replace, don't append. This section reflects the present, not history.

Show the diff and ask for approval before writing. Don't write speculatively.

**2. Decision note candidates.** Identify whether anything this session is worth a `docs/decisions/NNNN-slug.md` note. The bar:

- A path was explored and rejected — write one.
- A non-obvious convention was chosen — write one.
- A workaround was applied that looks wrong without context — write one.
- Routine implementation that's self-evident from the code — skip.
- Strategy already captured in CLAUDE.md — skip.

If the bar isn't met, say so explicitly ("nothing this session rises to a decision note") and move on. Don't write notes just to seem thorough. If the bar IS met, draft using `docs/decisions/0000-template.md`, pick the next number, propose, wait for approval.

**3. If neither applies**, say so and end the session cleanly. Not every session changes the state.

The user can short-circuit this with "skip housekeeping" if the session was trivial (e.g. a one-line fix). Otherwise, run through it.

## How history is preserved

CLAUDE.md is the present. Anything historical — abandoned approaches, the reasoning behind a current convention, "we tried X and it didn't work" — goes in `docs/decisions/`, not here. If you find yourself wanting to add "we used to do Y" to CLAUDE.md, that's a signal to write a decision note instead and keep CLAUDE.md focused on what's true now.
