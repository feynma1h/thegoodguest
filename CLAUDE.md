# roomstudio (working name — no product name has been chosen)

A spatial intelligence product that helps people discover the best version of their home: AI-powered room analysis, conversational redesign, and an immersive 3D representation of *their own* space.

This file is the always-current state of the project. Both Claude Code (reads it automatically) and Claude Chat (you upload it) consume it at the start of every session. If something in here is wrong, fix it before doing anything else.

## What we're building

**The thesis: every home contains a version of itself that its owner has never seen. This product makes that version visible, understandable, and achievable — one conversation at a time.** Every feature decision filters through this. The full founding vision lives at `docs/product/initial-idea-draft.md` (verbatim, with what's superseded vs durable mapped in decision 0055) — read it before making product-surface decisions.

This is NOT an "upload → generate a 3D scene" showcase. The 3D reconstruction is the *medium*; the product is helping people make AI-based decisions about improving their room. Three product layers frame everything: the **AI layer** (understands space structurally — object relationships, traffic flow, light, proportion — with algorithmic spatial analysis before any LLM is invoked, and reasoning traces on every design decision), the **emotional layer** (feels personal, not algorithmic — the experience bar is Linear/Vercel/Figma-tier premium consumer software; conversation is the primary post-reveal interface; the cinematic reveal is the defining moment; design language is Apple-grade restraint per decision 0056 — neutral chrome, content carries the color, one sans, mono only for machine data), and the **social layer** (rooms are identity — sharing, comparison, evolution over time). Direction, not yet commitments: room health scoring, taste graph, lighting simulation, budget-aware shopping, DAG version history. Deliberately out (per the founding draft, still sound): AR overlay, social feed, photorealistic image generation, floor plans, voice input; desktop-first.

**Naming:** "RoomMind" (the draft's name) was discarded; nothing has been chosen. "roomstudio" is a stand-in used for the repo, GCP project, and wordmark — the web wordmark is isolated in `web/src/components/Wordmark.tsx` for a one-file swap when the name lands.

Three technical surfaces today:

- **iOS capture app** (Swift + ARKit + RoomPlan) — capture-only, no viewer. The app's only job is producing a high-quality capture bundle and uploading it. Users come to the web for everything else.
- **Backend perception pipeline** (FastAPI on Cloud Run, `asia-southeast1`) — ingests bundles, runs SAM 3 segmentation + SAM 3D Objects reconstruction, places objects in the room's gravity-aligned metric frame using ARKit data (decision 0052), renders the room shell — walls/floor as textured quads from measured ARKit planes (decision 0066; BUILT, deploy pending). This is the modern substrate for the draft's perception + spatial-reasoning layers; the spatial relationship graph and design-generation layers above it are unbuilt.
- **Web app** (Next.js, static export + web splat rendering — WebGL2 via Spark, decision 0053 — hosted on Firebase Hosting) — the product surface: today rooms + viewer; next analysis, conversation, and redesign. Capture path is one screen: "Open the iOS app." Auth: same Firebase identity as iOS — requires upgrading iOS's anonymous auth to a real sign-in linked to the existing anonymous credential (see "Next on the board"); anonymous UIDs don't carry across devices.

Photo-upload (Android, no-iPhone users) is a deferred concern. Until the iOS path is solid we don't build the web-fallback capture.

## Capture bundle — the central contract

Everything between iOS and the backend flows through `packages/schemas/capture_bundle.proto`. The bundle is metadata; pixel data (frames, depth) lives in GCS by reference.

Frame of reference is **ARKit-native** end-to-end: right-handed, +Y up, camera looks down -Z in its local frame. The iOS client does NOT transform; the backend converts to downstream model frames (e.g. SAM 3D's per-object frame) when it has to.

Pose is **position + unit quaternion (x, y, z, w)**, not a 4×4 matrix. ARKit-native, ARCore-native, glTF-native. 7 floats instead of 16. The proto file's docstring carries the full reasoning.

Quaternion math is centralized in `packages/schemas/roomstudio_schemas/pose_math.py`. Any Python that touches a Pose imports from there. Do not re-implement.

## Repo layout

```
packages/schemas/                 capture bundle proto + generated Python + pose/placement math
  capture_bundle.proto              source of truth
  roomstudio_schemas/
    capture_bundle_pb2.py            generated; regen with ./tools/gen_proto.sh
    pose_math.py                     quaternion ops; one place to change
    placement_math.py                depth backprojection, single-view fits, ray triangulation
  tests/                              invariant tests for the proto, poses, and placement math

packages/api-core/                shared logic consumed by both API services
  roomstudio_api_core/
    scene.py                         Scene model, SceneStatus, state machine
    scene_read_repo.py               SceneReadRepository ABC + Firestore/in-memory read-only impls
    upload_session_repo.py           UploadSessionRepository ABC + Firestore/in-memory impls + gcs_mint_resumable_uri
  tests/                              unit tests for the scene model, repos, and manifest validation

tools/                            local scripts (run from repo root)
  gen_proto.sh                      regenerate Python and Swift (ios/RoomStudioCapture/RoomStudioCapture/Generated/)
  build_test_bundle.py              synthesize a bundle from test_data/photos
  inspect_bundle.py                 verify a bundle parses + smoke-checks

services/
  api-public/                     client-facing API (--allow-unauthenticated, Firebase JWT verify)
  api-internal/                   internal API (--no-allow-unauthenticated, Cloud Run IAM)
  perception-obj/                 SAM 3 + SAM 3D Objects + placement/fusion (deployed pre-placement)
  perception-geom/                VGGT for the photo-upload path (deployed, photo-path only)

web/                              Next.js static-export web app (decision 0050); Spark splat viewer
                                  contained in src/components/SplatViewer.tsx (decision 0053)

infra/                            Cloud Build configs, deploy scripts
docs/decisions/                   short notes on dead-ends — see "When to write a decision note"
test_data/photos/                 9 synthetic rendered room views, for synthesis testing
outputs/                          gitignored; generated artifacts
```

## What works right now

- The capture-bundle contract is defined, generated, and tested. `python tools/build_test_bundle.py && python tools/inspect_bundle.py outputs/test_bundle/bundle.pb` runs clean end-to-end.
- The old photo-upload HTTP surface is gone from `perception-obj`: `/segment`, `/segment-raw`, `/objects` and their dead helpers were removed on the review-findings-cleanup branch (`e5b9711`). `/process` (the iOS path) is the only perception entrypoint. `perception-geom` (VGGT) remains deployed but orphaned pending the photo-upload decision.
- The schema + math suites are green. Don't break them; run `python -m pytest` from the repo root for the count.
- **`services/api` two-service split deployed** (see `docs/decisions/0016`): `services/api-public/` (`--allow-unauthenticated`, Firebase JWT in-app verification, hosts `/upload_session`) + `services/api-internal/` (`--no-allow-unauthenticated`, Cloud Run IAM, hosts `/ingest/eventarc`; the legacy `/ingest` route was removed in the cleanup pass) + `packages/api-core/` (shared `UploadSessionRepository` + `gcs_mint_resumable_uri`). Both services live on Cloud Run `asia-southeast1`; see revision details on line below.
- `perception-obj` `/process` receiver: accepts Cloud Tasks HTTP POST (OIDC-verified), claims scenes atomically in Firestore with lease-TTL crash recovery, runs SAM 3 + SAM 3D Objects, writes outputs to GCS, updates Scene state, fires FCM on terminal transitions. System is functional end-to-end locally. Dockerfile, cloudbuild config, and deploy script env vars are all patched (see `docs/decisions/0005`, `0006`). Models are lazy-loaded on first `/process` call: `/health` returns 200 immediately for the startup probe; `/ready` reports per-model load state. DINOv2 weights (~1.13 GB) are pre-cached in the image at `TORCH_HOME=/opt/torch_hub`, eliminating the cold-start runtime fetch. Startup probe targets `httpGet /health`. 165 tests passing across both services.
- **Stuck-scene lease-semantics fix shipped and verified** (revision `perception-obj-00024-89b`, 2026-05-25). The bug from `docs/decisions/0011` and `0012` is fixed: `/process` reclaims stale leases atomically, `EnvironmentalError` eagerly releases the lease, SIGTERM resets held scenes to `queued`, and `holder_id` is checked defensively on all three lease-mutating paths. Verified production trace for scene `561c68ae`: `action=claim` → OOM → `action=release_error` → `action=reclaim_stale` → OOM → `action=release_error` → `action=reclaim_stale` → OOM → `action=release_error` (final, `is_final_attempt`) → scene `failed`. Note: the trace exercised the eager-release optimization path; the load-bearing expiration-check path (stale-but-not-cleared leases) is covered by unit tests only, not this production trace.
- **Smoke tool pass 3 scoping locked** (see `docs/decisions/0017`): manifest derivation, upload sequencing, and PUT semantics are pinned against the current `/upload_session` contract. Recon from a prior Code session covers the handler, request/response schemas, GCS minting, and Firestore session repo.
- **Smoke tool pass 4 scoping locked** (see `docs/decisions/0019`): polling contract pinned against a new `GET /scenes/by-bundle/{bundle_id}` endpoint on api-public. Recon from prior Code session covers the Scene document lifecycle, field population at ingest, idempotency under at-least-once Eventarc delivery, and the existing `SceneRepository` patterns in api-internal.
- **Smoke tool pass 5 scoping locked** (see `docs/decisions/0020`): four-mode CLI shape, `--cleanup` semantics, `--verbose`/`--json` output, and exit-code 2 (misconfig vs. system-under-test failure) pinned.
- **`tools/upload_test_bundle.py` written**: 1008-line substitute iOS client. Four modes (happy-path, skip-blob, duplicate-event, auth-rejection), two-phase upload sequencing per decision 0017, polling per decision 0019, failure semantics per decision 0020. `--cleanup`, `--verbose`, `--json` (NDJSON), exit codes 0–3. 27 unit tests in `tools/test_upload_test_bundle.py`. Verification against a deployed stack pending runbook execution.
- **`packages/api-core/roomstudio_api_core/test_fixtures/capture_bundle.py` added**: `build_capture_bundle()` + `TestBundleArtifacts`. Generates valid `CaptureBundle` protos for all three tiers with configurable per-frame blob kinds. 22 new invariant tests.
- **Deploy runbook preconditions landed**: `infra/eventarc_setup.sh` corrected — `INGESTER_SERVICE` changed from `roomstudio-api` to `api-internal`, `INGESTER_SA` changed to `api-internal-runtime@roomstudio.iam.gserviceaccount.com` (both were stale post-split). `services/api/` confirmed local-only (only gitignored `__pycache__/`) and removed. `upload_sessions.created_at` verified correct (`datetime.now(tz=timezone.utc)`) — TTL semantics match intent, no code change needed.
- **`infra/RUNBOOK.md` written**: 8-phase deploy runbook (Preflight + Phases 1–8) for the two-service iOS upload path (decisions 0014–0020). `eventarc_setup.sh` gains `--lifecycle-only`, `--ttl-only`, `--trigger-only` flags for per-phase invocation.
- **`GET /scenes/by-bundle/{bundle_id}` on api-public built** (board item 1, decision 0019). `SceneReadRepository` + `InMemorySceneReadRepository` + `FirestoreSceneReadRepository` in `packages/api-core/`. Scene model moved to `packages/api-core/roomstudio_api_core/scene.py`; `services/api-internal/scene.py` is now a re-export shim. `SceneRepository` in api-internal extends `SceneReadRepository`; `FirestoreSceneRepository` extends `FirestoreSceneReadRepository` (no duplication of read logic). 242 tests across all packages, all green (25 schema + 48 api-core + 40 api-public + 94 api-internal + 35 smoke tool). Runbook Phase 0 preflight gate now satisfied.
- **Phase 1 deploy infrastructure is live.** GCS lifecycle rule on `gs://roomstudio-captures` (delete after age=1, prefix `captures/`) is configured. Firestore TTL policy is set on `upload_sessions.created_at` (state `ACTIVE`). Automatic single-field index on `scenes.bundle_id` is confirmed unexempt. `infra/RUNBOOK.md` Phase 0 preflight passes; v2 PR hardening (all 20 findings) landed.
- **Firebase Auth is configured for the project and usable from the smoke tool.** Firebase added to the GCP project (previously GCP-native only). Web app `roomstudio-smoke-test` registered (`appId 1:502805861152:web:095d7e3b331e0e0ddcbd45`); anonymous sign-in provider enabled; `firebase.googleapis.com` and Identity Toolkit enabled. `tools/upload_test_bundle.py` can obtain valid `idToken` credentials against the real project. ADC quota project set to `roomstudio`.
- **Protobuf gencode/runtime mismatch fixed in both service images** (see `docs/decisions/0021`). Root cause: `pip install packages/api-core/` in Layer 3 triggered proto-plus's `<7.0.0dev` constraint and downgraded protobuf from 7.35.0 back to 6.33.6, undoing the Layer 2 force-reinstall from decision 0005. Fix: api-core install moved to Layer 2 (before the force-reinstall); schemas install remains last. Container-import smoke added to both `infra/cloudbuild/api-internal.yaml` and `infra/cloudbuild/api-public.yaml` (build → smoke → push).
- **Two-service iOS upload path deployed to `asia-southeast1`:** `api-public` (revision `api-public-00010-cag`, 100% traffic, Firebase verifier wired) and `api-internal` (revision `api-internal-00017-puw`, 100% traffic, Cloud Run IAM gated). `/health` 200 on both. Redeployed 2026-07-21 with the merged web-app stack — see the board-item-1 deploy bullet at the end of this section.
- **Eventarc trigger `captures-bundle-pb-finalized` live**, destination `api-internal/ingest/eventarc`. App-side path filtering working (bundle.pb → 200, non-bundle.pb → 200 with structured ignore-log, per decision 0023).
- **Decision 0021 verified end-to-end:** layer-swap fix landed cleanly, CI import smoke ran in Cloud Build, no protobuf downgrade recurrence.
- **Phase 0 verifier check is now a hard gate** enforcing decision 0016's trust boundary: positive grep that `FirebaseTokenVerifier` is wired into api-public (`server.py`), negative greps that it's absent from `services/api-internal/` and `packages/api-core/`. Exits non-zero with a labeled message on any failure. (v2 PR #9, commit d073785.)
- **Phase 0h is now a liveness-only gate** consistent with perception-obj's scale-to-zero lazy-load design (decision 0024). `gcloud describe` halts on anything but `Ready True`; `curl /ready` confirms reachability and accepts any HTTP response (200/503/500 all confirm the service is invokable). Broken-model failures are caught downstream by Phase 7 + the container-failed-to-start decision-tree branch from Cluster D.
- **v2 PR RUNBOOK hardening landed** (board item 3): all 20 findings closed. Deploy scripts now conditionally pass `--no-traffic` (only when the service already exists) and tag each revision `candidate` for reliable pre-flip smoke URLs (findings #5/#11/#10). Phase 0a IAM check uses effective-permission tests; Phase 0b Firebase setup and ADC quota-project fully specified (#1/#2/#3). Test collection is invocation-independent via pytest rootdir + conftest path setup (#20). 242 tests green from any CWD.
- **Service modules renamed** `server.py` → `services/api-internal/ingest_server.py` and `services/api-public/public_server.py` (resolves a test-collection module-name collision, #20). Dockerfile CMDs updated to the new entrypoints. Post-rename redeploy complete (revisions `api-internal-00007-nic` and `api-public-00004-nah`); both services boot the renamed entrypoints.
- **Phases 0–7 executed against production** (2026-05-29). Full upload→ingest→reconstruction path exercised end-to-end: bundle upload, Eventarc delivery, Cloud Tasks enqueue, SAM 3D model load (94.6 s from cold), reconstruction pipeline reached. skip-blob and auth-rejection modes confirmed green. happy-path and duplicate-event reach reconstruction but not `ready` (synthetic fixture pixels are non-decodable placeholders — by design; see "What does NOT work").
- **Ingest validation gate shipped and live-verified end-to-end** (board item 1). Validates image decodability pre-GPU at the `/ingest/eventarc` handler; fast-fails bundles with non-decodable image blobs to a new `failed_invalid` Scene state. Serving revisions: `api-internal-00009-bej` (gate) and `api-public-00006-quw` (reader rebuild required by the cross-service enum contract — see `docs/decisions/0027`). Phase 7 is now 4/4 green: happy-path and duplicate-event both reach `failed_invalid` end-to-end (~3–10 s including Eventarc delivery latency; perception-obj never wakes on either, confirming pre-GPU fast-fail). Note: `api-internal-runtime` holds `storage.objectViewer` on `gs://roomstudio-captures` at **bucket scope**, not project scope — a project-level IAM check will not show this binding.
- **iOS capture app P1 complete** (HEAD `def4d50`, 2026-05-30). `ios/RoomStudioCapture/` Xcode project, iOS 16, SPM-only (SwiftProtobuf). `capture_bundle.pb.swift` generated by `./tools/gen_proto.sh` and committed as source. ARWorldTrackingConfiguration, gravity-aligned, `.sceneDepth` when available. Keyframe accumulation by pose delta (10 cm / 5°); `.limited`/`.notAvailable` tracking states skipped per decision 0028. Per keyframe: JPEG to temp dir (`frames/NNNNNN.jpg`), device-monotonic µs timestamp, `RSPose` + `RSIntrinsics` from ARCamera. Gravity field is a zero-vector stub — formula deferred to P1→P2 gate. Minimal SwiftUI: start/stop + frame counter + tracking-state badge (headless by design — no camera preview). On-device verified: pose-delta filter correct both directions, JPEG writes confirmed end-to-end (88 accepted / 88 written / 0 failures / 88 on-disk), all ARKit tracking states surface correctly. `PoseTests` 4/4 green (simd-only; quaternion unit-norm + component-order). Write observability (`WriteStats` + `do/catch` + `logWriteSummary`) kept as production code for P2.
- **`schema_version = "1"` enforcement gate closed and deployed** (see `docs/decisions/0031`). The ingest handler validates `schema_version == "1"` and rejects non-matching bundles with a `failed_invalid` Scene, structured log, and HTTP 200 (not a bare 400). A check already existed; this fixed the accepted value (`"1.0.0"` → `"1"`) and the rejection mechanism (bare 400 → `failed_invalid` + 200). Deployed: `api-internal-00013-qeg`, `api-public-00006-quw`.
- **iOS P2 chunk B complete** (see `docs/decisions/0032`, `0033`). Full `CaptureBundle` proto assembly: tier dispatch (ARKIT_ONLY/LIDAR_ARKIT), all fields populated including `schema_version = "1"`, `bundle_id` lowercased. Depth intrinsics corrected to scaled-RGB path per decision 0032 (the old `capturedDepthData` reference was wrong and would not have compiled). B-2 bundle_id casing closed. B-3 ARKIT_ONLY verified on-device (iPhone 16e): schema `"1"`, lowercased bundle_id, real-device provenance, clean quaternion norms, sane RGB intrinsics, gravity zero-stub as expected pre-chunk-C. LIDAR_ARKIT capture + inspect deferred pending hardware (decision 0033).
- **iOS P2 chunk C committed** (commit `ea70df5`, see `docs/decisions/0030`, `0034`). `PoseExtractor.gravityInCameraFrame` implements R^T·world-down via `simd_quatf.inverse.act(simd_float3(0,-1,0))`. Both Gate-1 conditions met: hand-derived PoseTests at 1e-5 (green) + on-device sign check (floor/ceiling/landscape-horizon/roll, all clean). `#if DEBUG` gravity HUD in CaptureManager/ContentView; release builds unaffected. `ARCamera.transform` is a fixed landscapeRight frame — portrait reads ≈ (±1,0,0) and is correct (not a sign error); see decision 0034. P2 status: chunk A deployed, B code-complete (LiDAR depth residual deferred per decision 0033), C committed — P2 is mergeable.
- **iOS P3 complete — Firebase anon auth + `/upload_session`, live-contract-verified** (2026-05-31). Firebase iOS SDK 11.15.0 (Core+Auth) via SPM; offline-safe anonymous auth (cached UID, sign-in only when `currentUser` nil, never churns the UID — 0036); `UploadSessionClient` POSTs `/upload_session` with the 0038 retry/backoff status→action map; per-bundle session record persisted as an `NSFileProtectionComplete` file in Application Support, not Keychain (0037); Firebase-before-`configure()` ordering trap fixed (0039). Verified against live `api-public-00006-quw` (asia-southeast1) with a real anon idToken: happy-path 200, response mapped by `relative_path` not order, idempotent re-mint (same `session_uri`s for the same path-set); leading-slash manifest path → structured 400 (`invalid_manifest`), confirming 0038's fatal set unchanged; invalid-token 401 against the live `FirebaseTokenVerifier`. Integration tests run only when `RUN_INTEGRATION_TESTS=1` is set, using the `RoomStudioCapture-Integration` scheme — the only scheme in this project; there is no separate default scheme. Tests are run manually via `xcodebuild … -scheme RoomStudioCapture-Integration`; there is no automated CI gate (a manual-only workflow exists — see the iOS test policy's CI status, decision 0099). iOS test count and posture are tracked in the "iOS test policy" section (single source of truth).
- **iOS P4 ARKIT_ONLY complete — background blob upload, `bundle.pb` last, finalize hardware-verified** (main `10c5d2e`, 2026-06-03; decisions 0040/0041/0044). Single-shot whole-blob PUT over background `URLSession` (no chunking; `Content-Range: bytes 0-(N-1)/N`, no client `Content-Type` per F2); `bundle.pb` enqueued only after every non-`bundle.pb` blob is `uploaded`, the gate persisted in the 0037 record. On-device verified on a non-LiDAR iPhone17,5 (iOS 26.5): Gate 1 (upload-start handoff) PASS, Gate 4 (24h smoke soak, `failed_invalid` end-to-end) PASS, and **Gate 2 (finalize under suspension, alive process) PASS** — bundle `2fa51f70-…`: capture prefix + all frames + `bundle.pb` confirmed in GCS *during the locked interval*, verified from the Mac with no unlock (first hardware proof finalize completes while the app is non-active; the historical Anomaly-A failure mode is positively contradicted for the alive-process path). The Q1c POST-stall fix (two-window background-assertion architecture) is merged (`d9d8354`) and re-confirmed live. Under suspension GCS is authoritative over logs (Anomaly B: os_log buffers/coalesces/duplicates while suspended — operator tooling, not an app bug). ARKIT_ONLY tier only; LiDAR tiers have zero execution on a non-LiDAR device (see "Next on the board").
- **iOS P5 relaunch-recovery cluster code-complete** (decisions 0044/0045; (c) `7143de0`, (b) `b793307`, (a) `27672d0`, test-pin `5da5784`). Three coupled units: (c) idempotency hardening — `bundlePbEnqueueInFlight` in-process latch + `getAllTasks` cross-process reconciliation + context-preserve fix in `enqueuePhasOneBlobs`; (b) `onFatalBlobError` reclassified into TERMINAL/DEFERRED-INTERRUPTED/DEFERRED-TRANSIENT with `.failed` + `failureReason` persisted on terminal paths, sibling-task cancellation, and bounded cross-launch retry (N=10 per-launch, reset on progress); (a) launch-time rehydration via `rehydrateAllUnfinishedBundles()` triggered from `.task` in `RoomStudioCaptureApp`, with pre-checks for missing blob files before any enqueue. **Hardware gate pending:** the OS-kill route (background URLSession relaunch, `.task` fires without entering foreground) is unverified on-device — code-complete ≠ Gate-2b closed. See "What does NOT work" and decision 0045.
- **iOS P5(a) scene-status poll client + status UI complete** (commit `dbe3188`). `ScenePoller` (@MainActor singleton, ObservableObject): lenient `SceneStatus` decode (unknown wire → `.unknown(raw)`, never throws — 0027); inner 0038 retry policy (401-refresh-once, 5xx bounded jittered backoff → `transientFail`); outer elapsed-gated cadence (2 s < 30 s / 10 s < 5 min / 30 s cap — deliberately left untuned until the perception envelope fix changes the latency profile; rationale recorded at the constants in ScenePoller); no hard give-up while foregrounded (FCM `ready`/`failed` is broken — polling is the sole completion channel). `ScenePollState` covers idle / polling(latest, since, sceneCreatedAt, longRunning, connectionTrouble — sceneCreatedAt added by the status-surface honesty pass, see that bullet) / succeeded / failedTerminal / recoverable / pollError. `SceneStatusView` renders all states premium-clean: live elapsed, long-running flip at 10 min + manual "Check now" (since replaced by pull-to-refresh — see the ios-upload-robustness bullet), connection-trouble note, success, hard-terminal failure, recoverable humanised to file count. A-nudge kick wired in `onBundleComplete` (one `Task { await MainActor.run { ScenePoller.shared.notifyBundleComplete } }` line); `SceneStatusView.onAppear` is the independent start path that reads `.complete` records from `UploadSessionStore`. 29 new `ScenePollTests`, all injected seams, no real clock/network. Suite: 191 tests, 0 failures (was 162; now 187 after the cleanup pass — see iOS test policy).
- **#17 Pub/Sub no-redelivery verified live** (2026-07-15). Uploaded a non-`bundle.pb` blob to `gs://roomstudio-captures`; confirmed a single Eventarc delivery, a single `200`, and no redelivery over several minutes (subscription `minimumBackoff=10s`). Found and fixed a real bug in the process: `services/api-internal/ingest_server.py` and `services/api-public/public_server.py` never called `logging.basicConfig()`, so every `logger.info(...)` call in both services was silently dropped in production (root logger default level `WARNING`) — the `eventarc_ignored` structured log decision 0023 relies on had never once fired in 30 days of history despite the code path executing correctly. Fixed by adding the same `logging.basicConfig(level=logging.INFO, ...)` call already used in `perception-obj/server.py`; both services redeployed (`api-internal-00015-quq`, `api-public-00008-qut`) and the fix re-verified live (the log now appears). 351 tests green (post-cleanup-merge count).
- **Review-findings cleanup pass complete on branch `review-findings-cleanup`** (17 commits, `8707325` → `d42ec90`, 2026-07-17). 198 of 207 findings from the (tracked, now retired) tasks.md resolved. Its 9 survivors were reconciled item-by-item against the code on 2026-08-08 and the file was retired: 5 had in fact shipped, 3 were better recorded in CLAUDE.md/0084/0087, and the 1 genuinely-unrecorded item (client-ack scene cleanup) moved to the residue list below. Recoverable with `git show <retirement-commit>~1:tasks.md`. Highlights: pre-VGGT schemas deleted; `device_id` required end-to-end (new `DeviceIdentity.swift` Keychain UUID, backend `device_id_missing` validation, `DeviceIdSource`/hardware_id fallback removed); `fcm_token` threaded Scene → ClaimResult → notifiers (perception-obj no longer sends the device UUID as an FCM token); legacy `POST /ingest` removed + FAILED-redelivery duplicate-Scene bug fixed (FAILED → QUEUED retry with existing scene_id); perception-obj model loads moved off the event loop (`asyncio.to_thread`); lease-holder guard on `release_ready`/`release_failed` + per-object soft-fail + bounded `release_failed` retry (decision 0048); per-thread GCS client cache in ingest; api-public handlers un-async'd (event-loop unblock) + prod-gated sys.path shim; Cloud Tasks `dispatch_deadline` 930s ≥ Cloud Run 900s timeout; iOS `remintProvider` wired in production, stranded re-mint blobs fatal-routed, `UploadSessionRecord` migration shims removed (strict decode), scene-status poll-start deadlock fixed, phase-codename comments replaced throughout (decision 0049 for the re-mint failure semantics). Suites: 351 Python, 187 iOS, all green.
- **Object placement + scene fusion built in perception-obj** (merged to `main` and deployed 2026-07-21, decision 0052). Each SAM 3D object now gets a world transform in the ARKit frame: rotation from SAM 3D's layout output (`rotation`/`translation`/`scale` — previously discarded, only `"gs"` was read), scale+translation from the LiDAR depth cloud via `placement_math.fit_single_view` (single-view-aware: silhouette-true transverse extents + near-face anchoring; the generic PCA/centroid fit and iterated NN-ICP were evaluated and rejected — measured findings in 0052) + translation-only NN polish. ARKIT_ONLY frames emit per-object view rays; `fusion.py` clusters observations into one entry per physical object (per-frame-uniqueness + behind-camera triangulation guards) and fuses transforms. **Manifest is now v2**: top-level `manifest_version: 2` + fused scene-level `objects[]` (what the viewer renders) alongside `frames[]` (provenance). Every failure path yields explicit `placed: false` + reason — never a guessed transform. Synthetic-fixture accuracy: scale 0.2%, translation ~3 cm, robust to 5mm noise + 3% bleed; pinned as regression tests at achieved-accuracy tolerances.
- **Web read contract on api-public** (decision 0054): `GET /scenes` (newest-first list for the caller — the by-bundle-only read surface left a web client unable to discover scenes), `GET /scenes/{scene_id}/assets` (manifest verbatim + `asset_urls` map of V4-signed HTTPS splat URLs, TTL 1h, IAM-signBlob signing; 409 `scene_not_ready` until ready), and CORS gated on the new `CORS_ALLOWED_ORIGINS` env var (set in `infra/api-public.env.yaml`). Closes gap F4's web half. Shared `_scene_to_client_dict`/`_verify_bearer` helpers keep the three authed routes' shapes from drifting.
- **Web app scaffolded, redesigned product-true, and interactively verified** (`web/`, board item 6; decisions 0050/0053/0055). Next 16 static export (TS, App Router, Tailwind 4): typed API layer with three modes (`mock` fixtures covering all six SceneStatus values / `live-local` against a local uvicorn api-public / `live` Firebase anon); rooms-centric routes `/` (thesis-led landing with cursor-as-light ambient hero), `/rooms`, `/room?bundle=` (query-param routing — static export can't prerender unknown segments; the 3D space embeds inline when ready, with an object inventory and 10s polling while in flight), `/new` (scan instructions), `/viewer` (dev workbench, hidden from nav in live mode). Visual language now per decision 0057 (Good Guest — see the rehaul bullet below; this sentence's 0056 predecessor is superseded); all motion still through the single spring in `web/src/components/ui/spring.tsx` (`motion` lib). The room page is a full-bleed immersive stage with floating chrome (the narration card + disabled composer are the conversational interface's designated home). Status copy narrates analysis, not pipeline states. `SplatViewer.tsx` is the ONLY module importing three.js/Spark (renderer-agnostic `PositionedSplat` input); `Wordmark.tsx` is the single point of change for the placeholder name. Concrete CSP in `web/firebase.json` (connect-src allows api-public + storage.googleapis.com + Firebase auth; worker/child blob: for Spark). Verified in-browser across all routes and status fixtures, including the **assembled room** rendering inline on the room page (fused objects with correct transforms, no coordinate flip — ARKit and three.js share handedness/up-axis) and a live-local smoke (real CORS preflight + Bearer + empty-list 200). 17 vitest tests; lint + build green. `tools/make_synthetic_splat.py` generates INRIA-layout 3DGS PLY fixtures + a v2 manifest into gitignored `web/public/dev-fixtures/`.
- **Full Python suite: 460 tests green** (was 351; new coverage spans placement math incl. the single-view fit evaluation, the placement service layer, fusion incl. two regression guards, list_by_user, and the three new api-public surfaces). Dev toolchain this session: Node 22 via homebrew (keg-only, `/opt/homebrew/opt/node@22/bin`); local packages reinstalled **editable** in `.venv` (site-packages copies were stale); `uvicorn` added to `.venv`.
- **Web app visually rehauled to the "Good Guest" product system** (merged to `main` 2026-07-21; commits `606ae73`…`eee61be`, 2026-07-20; source design file `docs/product/good-guest-product-system.html`; decision 0057 supersedes 0056's visual language). Warm light-first: parchment/cream surfaces, warm-brown ink, rust primary actions, muted gold strictly as light-semantic indicators; Source Serif 4 = the guest's voice (italic) + display, Instrument Sans = UI, IBM Plex Mono = eyebrows + machine data (all next/font, self-hosted at build — CSP untouched). Surfaces: `/` = one-claim hero with the live demo room, plus a returning-visitor greeting grounded in the newest room's real state (localStorage has-rooms hint kills the pitch flash; `?pitch=1` dev escape in non-live modes); `/rooms` = the House (sibling cards on hatch stages, derived titles "the July 12 room", state as treatment + words); `/new` = the Bridge (declared-placeholder QR; a real listening panel that polls `GET /scenes` and walks into the wait when a new scan lands); `/room` = immersive, nav stands down: the Wait (arrival narration from real status + elapsed, breathing hatch, never a percentage), hold rule + objects-first reveal (once per room per browser via `lib/seen.ts`; assembly choreographed inside SplatViewer — largest first, each named as it lands; reduced motion collapses to the finished room), stage-1 layout (guest line grounded in real counts via `lib/voice.ts`, ledger inventory panel, disabled composer that says the guest hasn't arrived), and §8 failure treatments (partial = real missing-file count + the true iOS-resume claim; terminal = dark ink panel + rescan CTA → the bridge). StatusBadge deleted (state reads as treatment + words). 24 vitest tests (voice + seen libs covered), lint + static-export build green; also fixed a pre-existing `react-hooks/set-state-in-effect` lint error on HEAD (NewRoomSheet). SplatViewer stays the only three.js module (re-lit warm; new `frameless`/`reveal` props; `PositionedSplat` contract unchanged); Wordmark stays the one-file name seam (`❖ ROOMSTUDIO` explicit-placeholder treatment).
- **Board item 1 executed: `web-app` merged to `main` and the full backend stack deployed + live-verified (2026-07-21).** Merge was a fast-forward (`84b86d9` → `f422636`; 460 Python tests green on the merged tree). Serving revisions: `api-internal-00017-puw`, `api-public-00010-cag`, `perception-obj-00025-tf6` (image `20260721-003900` — placement/fusion + manifest v2), all `/health` 200 at 100% traffic. RUNBOOK executed Phases 0–8a: preflight green (note: the operator account cannot `describe` Google-managed service agents — PERMISSION_DENIED is expected there; the live trigger + both IAM grants prove the delivery chain), lifecycle+TTL re-applied, candidates smoked pre-flip (401 `invalid_token` / platform 403), Eventarc trigger idempotently re-applied. Phase 7 all four smoke modes green first-attempt (happy-path + duplicate-event → `failed_invalid` ~3–5 s pre-GPU; skip-blob → `failed_incomplete` with the dropped path in `missing_paths`; auth-rejection → 401 `missing_token`); TTL belt-and-braces EXISTS; `--use-hardware-id-fallback` probe confirmed the `device_id_missing` rejection with the exact validation message on the Scene doc (read via ADC — the client API excludes `last_error` by design). **Web read contract live-verified end-to-end** with a manufactured ready scene: `GET /scenes` lists it, `GET /scenes/{id}/assets` returns the manifest verbatim + V4-signed URLs that fetch 200 at exact byte lengths (functionally proving the two new IAM grants: `serviceAccountTokenCreator` on `api-public-runtime` on itself + `objectViewer` on the outputs bucket), non-owner 403, artifacts cleaned. CORS confirmed live (allowed origin echoed on preflight; disallowed origin gets no CORS headers). Three deploy-path traps found and fixed in the same session (commits `5c2e0c7`, `68d7deb`, `38a18df`): `.gcloudignore` now excludes `web/` (gcloud reads only the top-level `.gitignore`, so `node_modules`/`.next` were adding ~1.3 GiB to every build context); the perception-obj Dockerfile now COPYs `placement.py`/`fusion.py` and runs a build-time `import placement, fusion` smoke (both are deferred imports — the missing COPYs passed `/health` and all probes and would have crashed the first real capture); `deploy_perception.sh` now forces `update-traffic --to-latest` after deploy (the service's traffic spec was pinned to revision name `00024-89b` from a 2026-05 rollback, so the new revision validated, was silently Retired at zero traffic, and gcloud printed "serving 100 percent" for the old code — caught and cleared same-session).
- **First real capture executed end-to-end (2026-07-21, scene `25a14caf`).** A real ARKIT_ONLY scan (iPhone17,5, 126 keyframes, 127 blobs) ran the whole chain: fresh install → capture → mint → background upload (127/127 in ~1 min) → **first real bundle ever through the ingest validation gate** → Cloud Tasks → GPU claim. **Decision 0052's result-keys check passed on real data**: every real SAM 3D reconstruct carries `rotation`/`translation`/`scale` (+ `gs`), the fields placement reads. ~20+ object observations reconstructed across frames 0–9 before memory saturation; per-object soft-fail (cleanup pass) contained every OOM exactly as designed. Real cost numbers now exist: ~3.5 min GPU cold start (inside the request budget), ~20 s/object, ~70–130 s/frame. **The capture is preserved at `outputs/real-capture-25a14caf/`** (126 frames + bundle.pb, gitignored, 62 MB) as the regression fixture for the envelope fix — the captures-bucket lifecycle rule would otherwise sweep it 2026-07-22.
- **RP-8 walk defect classes 1–5 + the envelope kink BUILT, DEPLOYED, and re-driven on all four walk rooms (2026-08-07/08; branch `roomplan-placement-quality`, commits `e29fc91`/`43b817b`/`36906a5`/`1e3a94b`; decisions 0081/0082; revision `perception-obj-00038-ses`, image `20260807-202022`, candidate→smoke→flip — the image also carries `ab0a060` + `5e3807e`, closing RP-8 residue (1)).** **Class 1 probe-first (the 0077-P1 pattern, offline on the recorded spike): BOTH charter candidates REFUTED with measurements.** No appearance-scorer variant (production NCC, NCC@256, gradient-magnitude NCC, LiDAR depth-MAP agreement, blends) across any view set (live SAM-mask views, box-footprint evidence, three extra angular-diverse census-quality views over all 722 frames) separates ANY spike box's axis mapping — margins 0.005–0.03 with wrong winners on the known-wrong cases; extra views of a dead instrument stay dead. Mechanism: a truncated splat scored at the BOX center misaligns its visible-region content from the photo under EVERY rotation, so crop-space comparison is structurally noise-bound. **The discovered winner (shipped): a world-space cloud-alignment instrument** — each candidate rotation is translation-fitted to the observation's OWN LiDAR cloud (production's `refine_similarity_nn`, translation mode, 2 passes) and scored `exp(−rms/0.05)`; translation freedom is load-bearing (the fixed-at-box-center control misses the bed) — plus the layout prior's **sign-agnostic up-AXIS filter** (45°; the layout's up AXIS measured trustworthy 6/6, its SIGN wrong on 1/6, so the sign is never trusted). The margin gate keeps its value (0.10) and meaning (refuse coin-flips) but now gates the ASSIGNMENT — the DOF the instrument measures; the 180° sign leaf ships the fixed (+,+) convention (5/5 on the corrected truth table; the bed's sign twins are pinned cloud-degenerate <0.02 so a future sign instrument is a good pin failure), and the appearance `facing_flag` clause is unchanged. `RefinementContext` gains `get_depth` (captures-bucket sourced like `get_rgb`; a swept capture degrades to the up-filtered extent default — still strictly better than the pre-0081 tolerance-gated default). Truth table corrected mid-probe by two-view dispute renders: `obj_007`'s real mapping is the 90°-swap, five independent instruments against the first walk reading. **Classes 2–5 as fusion post-passes (0082)**: cross-label 3D duplicate gate (volume containment ≥0.5 under confusable label groups + same-label pairs; box > measured-contact > score priority; two boxes never dedup), wall back-face anchoring for wall-class free placements (+ planar normal alignment ≤60°) and floor-class wall declip (>8 cm penetration pushed back, ≤0.35 m), door-GEOMETRY opening demotion (a storage-ish label sitting ON a RoomPlan door/window surface demotes like the label rule — the five "cabinet is actually a door" misses), box-top support snap (speaker/lamp/monitor/tv rest ON measured box tops; splat-top supports deliberately v2). `clock` joins the wall class map. **Shell v3 co-planarizes adjacent RoomPlan wall segments** (≤4° / ≤8 cm offset / ≤15 cm gap, union-find chained) onto the group's area-weighted mean plane at RENDER time, with `measured_polygon` + `provenance.coplanarized_with` beside it (the 0069 honesty invariant) — the walk's "B-C-D-E should be a straight line". **Live re-drives (all four captures had been lifecycle-swept and were restored first — the spike from `outputs/roomplan-spike-bundle/` after the converter fix below, and the three RP-6/7 rooms PULLED OFF the 16 Pro's app container into gitignored `outputs/device-pull/` (458 MB) and re-uploaded under the operator uid — the completed-capture disk-accumulation gap paying off as a feature):** spike `a7e073ae` 25 objects / 10 placed / **4 axis-resolved** in ONE warm round (RP-8: 25/13/0 — the three no-longer-"placed" are two mislabeled-door cabinets + the window splat, now honest openings; shell v3 13 walls with 7 co-planarized in 3 groups), `a71d125f` 18/10/**2** (RP-8: 18/14/0; the 4 drops are exactly the operator's duplicate/door complaints — desk↔nightstand, monitor↔tv, mirror×2, cabinet-is-a-door), `09684dde` 21/11/**3** (RP-8: 21/16/0; monitor+tv triple collapsed to one, cabinet↔table dup suppressed, both artworks + the clock now flat on the wall), `b667f891` budget-starved (see the residue bullet — its 53-item long tail exceeds one request, so the 0082 passes never run there). Suites: perception 543→**580** (5 real-data axis pins on 293 KB committed fixtures + 25 synthetic walk-pass units + widened box tests), root 492+6, re-enqueue 18; ruff clean on touched files. Fixtures for the consolidated walk re-staged with regenerated `?labels=1` sidecars.
- **Pre-launch gap (c) client half closed — re-mint sends real per-blob sizes** (branch `ios-remint-real-sizes`, commit `ec91df6`, 2026-07-21). `BlobUploadManager.onSessionExpired` reads each manifest path's real on-disk size via `record.outputDir` (the `enqueuePhasOneBlobs`/`enqueueReput` pattern) and sends it as `expected_size_bytes`, replacing the deliberate placeholder 0; the full path-set is still sent (idempotency key unchanged), and both placeholder-describing comments are rewritten. A missing/unreadable/zero-size file routes to `onFatalBlobError("blob_unreadable_at_remint_manifest")` before the mint call — never a silent 0; the staleness pre-pass remains as TOCTOU defence-in-depth (a dedicated test pins it). No server change (0035 contract untouched). iOS suite 190 (+3), all green post-rebase in one run, the 4 live integration tests passing against the post-deploy stack (`api-public-00010-cag`).
- **Conversation stage 1 built, deployed, and live-verified (decisions 0058/0059; revision `api-public-00012-ziz`, 2026-07-21).** The room-page composer is alive. Server (commits `31f39ff`…`e83ef68`): `scene_facts.py` — the model's ENTIRE world (inventory with confidence tiers from `frames_observed`/`cluster_spread_m`, framed center-to-center distances at honest 0.1 m rounding, relative-only vertical relations, provenance, per-scene limits; pure + deterministic, cached by `(scene_id, FACTS_VERSION)` with a loader so hits skip the GCS fetch); `guest_prompt.py` — PROMPT_VERSION 1 charter + five exemplars, pinned-hash test, observe-only telemetry (foreign-measurement detector with facts∪user-window allowlist, invitation heuristic → `flags` + structured log); `conversation_repo.py` — turns exist only completed; accept = dedupe → UTC-day quota → 150 s reservation, persist = sequential index + counters + holder-guarded lease clear (InMemory impl is the semantics oracle, Firestore mirrors it); `public_server.py` grows `GET /scenes/{id}/conversation` + `POST …/conversation/messages` — the service's FIRST async route (SSE `delta`/`done`/`error` in our vocabulary + `: ping` comments; disconnect shield = independent turn task + held-open request). Model call: `claude-sonnet-5`, zero tools, thinking disabled, 60 s wall clock, cache breakpoints charter/facts/newest-message (see 0059). Web: `GuestEvent` seam (SSE parsing only in `LiveApiClient`; typed pre-stream errors thrown before the iterable exists), pure composer reducer (idle/submitting/streaming/confirming/reoffer/rested/blocked; 27 vitest cases), `Conversation` card + stubs + live composer with ~200 ms delta batching (sentence-boundary under reduced motion), mock parity incl. `!error`/`!budget`/`!slow`/`!inflight`, degrade-to-settled on any conversation GET failure. Verified: 561 offline Python + 50 web tests; live-local e2e with the real key (happy turn, mid-stream-kill shield, 409 turn_in_flight, 429 with `resets_at`, dedupe replay at 0.02 s); voice evals 6/6 live at PROMPT_VERSION 1; deployed checks on the candidate pre-flip — streamed turn, **shield on Cloud Run** (client killed at 2.5 s → the complete turn in Firestore; 0058's recorded abort-on-disconnect fallback stays unneeded on first evidence), cache read 2268 tokens with `input_tokens=2` on a later turn. Ops: `anthropic-api-key` in Secret Manager (operator-created; secret-scoped `secretAccessor` on `api-public-runtime`), Cloud Run `--timeout 120`, `GUEST_MODEL`/`GUEST_DAILY_TURNS` in `infra/api-public.env.yaml`; workspace spend cap set (2026-07-23).
- **iOS upload robustness: three documented no-device gaps closed + a cold-launch auth race fixed** (branch `ios-upload-robustness`, commits `6eb9d5d`/`6bc91f9`/`fa610f8`/`ec57285`, 2026-07-21; suite 190 → 212, green post-rebase in one run incl. the 4 live tests against `api-public-00012-ziz`). (1) **Blob PUTs honor GCS `Retry-After`** (`6eb9d5d`): 408/429 now retry in-process on the shared 0041 schedule (same as 5xx; exhaustion still DEFERRED-TRANSIENT under the cross-launch budget; no-context reclassified DEFERRED-INTERRUPTED — no budget burn for relaunch artifacts); `BlobUploadDelegate` threads the raw header through `handleTaskCompletion`; `parseRetryAfter` handles both RFC 9110 forms against the injected clock; a stated wait overrides the local backoff in full up to `maxRetryAfterHoldSec` (60 s), beyond which the blob defers cross-launch — never retry earlier than the server asked; 503-with-Retry-After honored by the same path. (Distinct from decision 0038's `/upload_session` item, still gated on pre-launch gap (b).) (2) **Upload-level terminal failure is visible** (`6bc91f9`): `UploadFailureMonitor` mirrors the `.complete` architecture — kick from `onFatalBlobError` + independent `.task` scan of the store for `.failed`; `UploadFailureView` banner in ContentView shows honest copy + `failureReason` verbatim in mono; dismissal is session-local BY DESIGN (the record persists until P5 terminal-state cleanup; the failure resurfaces on relaunch). (3) **"Check now" → pull-to-refresh** (`fa610f8`): ScrollView + GeometryReader minHeight preserves centering, `.refreshable` reuses `checkNow()` (unit-pinned), gesture enabled only while `.polling`; the long-running affordance is a quiet pull-down hint. (4) **Cold-launch auth race fixed** (`ec57285`): `signInIfNeeded` gained a single-flight guard (two callers overlapping the sign-in suspension would have double-signed-in — 0036's UID churn), ScenePoller's default token path joins the launch sign-in, and a token-less tick is transient (connection-trouble sub-state) not fatal; 401-after-refresh and refresh-path token throws stay fatal per 0038, both test-pinned. All UI states exercised in the simulator with seeded records (failure banner found by the cold-launch scan; live `.polling` against production; the cold-launch repro that previously died at "Could not reach server" polls from the first tick).
- **perception-obj envelope fix shipped — and the first real capture completed end-to-end (branch `perception-envelope-fix`, revision `perception-obj-00026-449`, 2026-07-21; decisions 0060–0063).** The three envelope breaks are closed inside `services/perception-obj/`: (1) **frame sampling** (`sampling.py`) — deterministic pose-diverse FPS over translation + view-direction (0062), default 12 via `PERCEPTION_MAX_FRAMES`; manifest v2 gains additive `frames_total`/`frames_sampled`/`sampling{}`; (2) **budget honesty** (`budget.py` + a request-entry deadline in `server.py`, `PROCESS_REQUEST_BUDGET_SECONDS`=900 mirroring the Cloud Run timeout) — frame- and object-level admission from observed-max cost estimates with a 60 s reserve; on refusal the run ships what's banked; budget-stopped frames are never cached to GCS; zero complete frames → `EnvironmentalError` (retry runs warm against per-object splat caches); (3) **GPU memory lifecycle** (0061) — the OOM retry moved OUTSIDE the except block (the in-handler retry was suffocating under the first attempt's traceback-pinned tensors; allocator stats in the production OOMs proved reference-pin, not fragmentation — the brief's accumulator suspicion was disproven), eager result-drop after layout+PLY extraction, `torch.no_grad` around reconstruct, `empty_cache` between frames, per-frame `vram` log lines. `tools/reenqueue_scene.py` is the out-of-band cure for stranded scenes (0060; guards test-pinned; needs `google-cloud-tasks`, now in `.venv`). Dockerfile COPYs + build-time import smoke extended to the new modules. **Live-verified on the stranded scene `25a14caf` (126 real keyframes): ONE request, `ready` with ~58 s of budget to spare** — 12 frames sampled `[0,18,28,42,53,67,83,99,105,110,116,124]`, 4 processed (2 whole-frame cache hits from the July zombie + 2 fresh at 277/303 s), clean `budget_stop` at the reserve boundary, fusion 21 obs → 19 objects (2 placed: bed + curtain, `layout_triangulated`), manifest + splats in the outputs bucket, allocated VRAM flat at the 16.4 GiB model baseline after every frame (peaks 21.6–21.9 GiB released). The room renders in the web dev workbench (`/viewer` reading the staged real-scene fixture in gitignored `web/public/dev-fixtures/`) — the first real assembled room. Suites: perception-obj 135→172, +13 tool tests (note: root `pyproject` testpaths deliberately exclude `services/perception-obj/tests` and `tools/test_reenqueue_scene.py` — run those per-directory; current totals 432+6-skipped root, 192 perception (post-0065), 13 re-enqueue = 637 passing offline).
- **api-internal redeployed — the bundle_id cross-check gate is now serving** (`api-internal-00019-des`, 2026-07-21). Candidate `/health` 200 → Phase 4b platform-403 intact → traffic flip (100% latest) → post-flip happy-path smoke exit 0 (`failed_invalid` in 5.4 s, artifacts cleaned) — proving the gate doesn't false-positive on matching ids. Ran same-day as the deploys, so it is NOT the Phase 8c soak (still due ≥2026-07-22).
- **iOS status-surface honesty pass — all three findings from the first real-user walk closed** (branch `ios-status-surface`, commits `7cd34f1`/`9fe276a`/`8408cd6`, 2026-07-21; suite 212 → 224, green in one run incl. the 4 live tests against `api-public-00012-ziz`). (1) **The elapsed clock is server-anchored** (`7cd34f1`): `.polling` carries `sceneCreatedAt` parsed from the poll payload's `created_at` (Python isoformat `+00:00`/microseconds and `Z` forms both handled; fraction stripped — whole-second display), the view renders the clock only once the anchor is known (before the scene doc exists there is nothing honest to count from), it ticks on a 1 s TimelineView independent of poll cadence, clamps clock skew at zero, and gains an hour field past 60 min; the longRunning flip anchors to the same server date (a relaunch at min 44 flips immediately), with poll-start fallback only when `created_at` is unparseable; poll CADENCE stays anchored to the client poll session by design (a relaunch restarting the cadence ladder costs a handful of requests). (2) **The mint badge says what `.ready` means** (`9fe276a`): "Upload authorized — uploading in background" (blue, not success-green), and the badge stands down once a superseding surface activates (scene-status panel after completion, failure banner after terminal upload failure) — it previously lingered forever as "Session ready ✓". (3) **The queued state narrates waiting** (same commit): state-aware subline via `statusDetail` — queued says "Waiting for processing to begin…"; processing carries the verified come-back-later claim. Simulator-walked against a scripted local mock serving the real wire form: queued → processing transition, 1:05 → 1:09 → 1:12 ticking between 10 s polls, relaunch at 5 min shows 5:08 (not 0:00), relaunch at 44 min shows the long-running state + 44:08 immediately. Task item 4 (cadence retune) resolved as leave-with-note (`8408cd6`): values sanity-checked against the first real capture; final tuning deferred until the envelope fix changes the latency profile.
- **SAM 3D layout conventions fixed — 0063's probe closed and verified in production (decision 0065; branch `perception-layout-convention-fix`, revision `perception-obj-00027-n8c`, 2026-07-22).** Five agreeing instruments (Meta-source reading, 576-variant brute force over the recorded real observations incl. conjugation + all 24 proper bases, per-object physical checks 6/6, offline splat renders, a yaw-sensitive relative-geometry check) settled it: the layout quaternion is wxyz but parameterizes camera→local — `extract_layout` now CONJUGATES (pytorch3d row-vector semantics in Meta's own `make_scene`; 0063's xyzw candidate was a duality twin of the truth minus the conjugation, 1/6 on physical checks); the layout camera frame is the pytorch3d camera convention (`_SAM3D_CAM_TO_ARKIT_CAM` = `diag(−1,1,−1)` — 0052's CV guess was wrong, and the probe's first identity conclusion was the 180°-about-camera-Y twin that every axis-LINE instrument is blind to: the first re-drive rendered beds/lamp/chair face-down until the first real-user look caught it; the sign-sensitive layout-TRANSLATION test — B·t must point at the triangulated center, dot 0.94–1.00 vs negative for identity — settled the basis, and signed + translation regression pins now make that error class loud); the exported splat frame needs no alignment (0063's confound dead: `make_scene` composes raw `get_xyz` = what `save_ply` writes; `_fix_gaussian_alignment` is a default-off video helper). Second discovery: SAM 3D's canonical object frame is per-reconstruction ARBITRARY (same object from two frames → world rotations 90–180° apart under EVERY convention), with three consequences shipped together: fusion no longer averages rotations/raw scales across observations (a cluster ships the best member's rotation+scale, strictly frame-paired with the splat it renders; positions/metric extents still fuse); the fixed-axis gravity metric is replaced by `min_axis_to_vertical_deg` (both fused paths carry it as of `aa55e91` — the serving revision predates that one additive field); and a layout sidecar (`{splat}.layout.json`, RAW model fields, converted at read time so it can never go stale against a convention change) keeps rotations alive across budget-split attempts — per-object cache hits previously dropped them entirely. Real-data regression pins in three sign-sensitivity tiers (axis-line at achieved accuracy; signed semantic-up; translation-direction with the identity twin pinned as failing) + discriminators proving 0052's chain and 0063's candidate fail the bed by >45°. Production-verified in two rounds: the first re-drive (identity basis) landed the line metrics but rendered Z-class objects face-down — caught by the operator's own eyes in /viewer, root-caused via the translation test, corrected basis redeployed (`perception-obj-00028-hzq`), and re-driven CHEAPLY (only the 12 objects.json caches deleted, operator-approved; sidecars re-converted at read time, zero SAM 3D re-runs — 2 requests to 12/12). Final numbers: 22 fused / 5 placed, upright-class min-axis median 4.1° across 21 observations (was 96.9° shipped), face-color sign tests pass (bed light-face-up +0.94, lamp upright, curtain/door standing), room visually confirmed corrected in /viewer (lamp upright, bed face-up; curtain-position/two-bed-cluster residue is position-class, see below). Suites: perception-obj 192 (was 172; root 432+6-skipped and re-enqueue 13 unchanged → 637 passing offline).
- **Decision 0051 built on both sides — iOS anonymous→linked Sign in with Apple + the web provider flow** (branch `ios-0051-signin`, commits `43d3bc0` iOS / `e839d45` web, 2026-07-22; E2E gates pending Developer Program enrollment — see What-does-NOT-work; implementation conventions in decision 0064). iOS: `AuthManager.linkAppleAccount` links the Apple credential to the EXISTING user via `link(with:)` with the UID asserted unchanged (0036/0051 invariant; a mismatch refuses + `logger.fault` rather than adopting a new identity); a conflict (Apple ID already owning a different account) surfaces as an explicit switch/keep choice with the cost stated — `switchToExistingAccount` is the app's only deliberate UID-change path; there is deliberately NO iOS sign-out (launch-time `signInIfNeeded` would re-mint a fresh anon UID — the web is where sign-out lives). `SignInWithApple.swift` carries the pure nonce/SHA-256/error-classification helpers; `SignInSheet` is the "why sign in" moment with the full UID in mono as the before/after identity proof; SIWA entitlement wired into both app configs. Web: the `lib/firebase.ts` seam is replaced — anonymous minting is gone; `signInWithApple` runs the popup flow with the never-create guard (`isNewUser` → delete + `AppleIdNotLinkedError`: the web reads identity, never creates it); signed-out is the typed `no_local_token` state rendered as invitations on /rooms, /room, /new, and the account menu (all four verified in-browser in live mode); shared `AppleSignInButton` preloads the firebase chunk so Safari's transient-activation window survives the click-time import. Suites: iOS 224 on this branch's base (baseline 212 + 12 `AuthLinkingTests`, incl. the 4 live tests against `api-public-00012-ziz`; ios-status-surface's +12 landed on main separately — post-merge total will differ), web 58 (+8 firebase-seam pins), lint + static-export build green, no CSP changes.
- **Board 6(a) shipped — Firebase Hosting stood up, PREVIEW CHANNEL ONLY (branch `web-hosting-preview`, 2026-07-22).** firebase-tools 15.24.0 pinned as a `web/` devDependency (`npx firebase …` from `web/`; operator credential minted by `npx firebase login`, lives in `~/.config/configstore/firebase-tools.json`). Proper web-app registration **roomstudio-web** (appId `1:502805861152:web:f556f8bb331bea1bdcbd45`) replaced the smoke-test appId in `.env.example` and the `firebase.ts` docstring. Committed `web/.env.production` makes a bare `next build` THE hosting artifact — live mode against deployed api-public with the new registration (values are public identifiers; `.env.local` still overrides for dev; `next dev` never reads it). `npm run deploy:preview` = build + `hosting:channel:deploy preview --expires 30d`; deliberately NO production-channel script (a public site is the board-4 first-non-developer-user trigger). Hosting CSP grew BOTH popup-flow preconditions — `frame-src roomstudio.firebaseapp.com` (the half recorded in the 0051 bullet) and `script-src https://apis.google.com` (found live: the SDK's gapi iframe-channel loader; `script-src-elem` violation observed on the deployed origin, the flow died before any network or popup call) — plus an explicit cache policy (`no-cache` HTML / `immutable` `/_next/static/**`; Firebase's 3600 s default served hour-stale HTML + stale CSP after redeploys, observed live). Preview channel `preview` live at **https://roomstudio--preview-cydkerk6.web.app** (expires 2026-08-21; the URL is stable across redeploys of the same channel — renewal = redeploy; domain added to Firebase Auth authorized domains). Verified on the preview URL: clean console + zero CSP violations (explicit `securitypolicyviolation` listener) on `/`, `/rooms`, `/room?bundle=`, `/new`; all four signed-out invitations render (rooms / room / new / account menu; signed-out surfaces never touch the network — typed `no_local_token`); production channel `roomstudio.web.app` confirmed untouched (Site Not Found); sign-in attempt runs the whole page-side flow — gapi loads, the hidden auth iframe to `roomstudio.firebaseapp.com` initializes (proving the authorized domain), popup launch fired at the real handler URL (the popup window itself was blocked by the automation pane — environment artifact, not product) — and the Apple provider gate is independently proven closed (`createAuthUri` → `OPERATION_NOT_ALLOWED : Code flow is not enabled for Apple.`): the sign-in leg is recorded as enrollment-pending, exactly the expected boundary. CORS live-proven for `roomstudio.web.app` (ACAO echo); the generated preview origin is NOT in `CORS_ALLOWED_ORIGINS` (proven: preflight 400, no ACAO) — Gate-B precondition: add `https://roomstudio--preview-cydkerk6.web.app` via env-only `gcloud run services update` and mirror `infra/api-public.env.yaml` in the same commit.
- **Room shell BUILT end-to-end — decision 0066's full chain, merged to `main` 2026-07-23** (branch `room-shell`, fast-forward `40c0498` → `dcac271`, six commits; **V1 verify-first probe PASSED** — Spark composites splats with `depthWrite:true` single-sided textured meshes correctly both ways, alphaTest floor cutouts render, back-view culling gives the dollhouse cutaway free; 0066's re-open clause is moot; probe code reverted uncommitted). Schemas `0374e35`: additive `repeated PlaneAnchor plane_anchors = 12`, `schema_version` stays `"1"`, Python+Swift regenerated, fixture builder grows `plane_anchor_count` with self-tests proving plane-carrying bundles pass `validate_bundle` unchanged. iOS `50b00d3`: plane detection on every tier, final anchor set snapshotted at `stopCapture()` before pause, pure `PlaneAnchorExtractor` with hand-derived pins, stop-time count log (by alignment/classification) as the V3 walk's observability. Shell modules `062e91a`: `shell_geometry` (lowest-cluster floor, union-find wall merge, detected-extent-only heights, interior-fronting winding), `shell_texture` (ortho texel grid, SAM-mask exclusion, incidence/distance-weighted luminance-median blend, floor alpha from polygon union clipped at walls, observed/inpainted fractions), `shell_inpaint` (big-lama TorchScript via `torch.jit.load` — zero new deps, Apache-2.0, sha256-pinned; validated through the actual module: 1.65 s warm 512 px CPU tile, byte-identical reruns). `/shell` stage `e2f0a84`: OIDC audience `…/shell`; no lease, no Firestore (pinned by an AST-level test); absent-vs-unavailable exactly per contract (swept bundle → `capture_expired`, no anchors → `no_geometry_source`, missing manifest → noop); byte-deterministic shell.json; fire-and-forget enqueue after `release_ready` with 0060-safe timestamped task names; Dockerfile bakes LaMa weights sha-pinned + extends the import smoke; `deploy_perception.sh` gains queue env vars + idempotent shell-IAM grants; `reenqueue_scene.py --shell`. api-public `caad469`: assets response gains the verbatim `shell` sibling, texture URIs join the signing walk, a shell fetch error degrades to null rather than 502ing the room. Web `dcac271`: `assembleScene` → `{splats, shell, unrenderable}`; SplatViewer renders shell quads (textures via `fetch()`+ImageBitmap — no CSP change; grid stands down when a floor exists); reveal extends floor → walls → objects; RoomStage holds a bounded grace for an absent shell; `arrivalLine` is shell-aware (charter/server line untouched). Verified in-browser both ways: textured mock room with objects composited on it; the no-shell real-scene fixture on the grid unchanged. Merged-tree suites: 445+6-skipped root / 259 perception / 18 re-enqueue / 67 web vitest + lint + static-export build; iOS 245 incl. the 4 live tests (see iOS test policy).
- **ARKIT_ONLY placement-quality DESIGN LOCKED — decision 0067 + build brief, merged 2026-07-23** (branch `placement-quality-design`, docs-only `d30d78d`, merge `943255b`). The 0066-style design session for the operator-ranked placement failures, with measured diagnoses replacing two guesses (recorded in 0067): the bed split is STRUCTURALLY forced — frame 28 carries a nested duplicate "bed" detection (intersection-over-smaller 1.000) that the frame-uniqueness merge guard reads as two physical objects at ANY threshold (not a `FUSION_CLUSTER_DIST_M` miss); the curtain's ~0.5 m depth error is centroid-ray non-correspondence on an object subtending 0.74–1.3 rad (cluster RMS 0.287 m at the 0.3 gate vs the compact lamp's 0.007 m) — triangulation demoted to initializer, not broken. Five of the six named levers adopted, VIO-calibrated monocular depth deferred with named re-open triggers; chunks A–C verifiable today by offline probes + warm re-drive of `25a14caf`; chunk D consumes 0066's plane anchors via a shared `room_planes.py` — **now EXTRACTED by the 0069 surfaces build (shipped 2026-07-23/24), which landed first; chunk D builds contact priors onto it rather than extracting.** Full locks in the layout-convention-residue bullet + board 1(b).
- **Room shell DEPLOYED and live-verified end-to-end (2026-07-23) — 0066's deploy chain closed.** Serving: `perception-obj-00030-8ls` (image `20260723-160737` with the shell stage + sha-pinned LaMa; `00029-czr` + an env-only calibration revision) and `api-public-00014-ben` (candidate→smoke→flip; verifier/CORS/health green pre-flip). api-internal needed no redeploy (nothing landed since `00019-des`). **First plane-carrying capture ran the whole chain in one pass** (bundle `9fbe29b6`, scene `f3d70236`, iPhone17,5: 184 keyframes, 24 plane anchors — 6 horizontal / 18 vertical / 10 classified): upload ~90 s, `ready` in ONE `/process` request (12 sampled frames, 18 fused objects, 3 placed), the fire-and-forget `/shell` task ran unprompted, shell.json (`status: ready`, floor 3.85×4.45 m + 7 walls) + 5 baked textures (floor 31% observed / 69% LaMa-inpainted) landed in the outputs bucket. Assets response carries the shell verbatim; signed texture URLs fetch byte-exact; the room renders in the dev workbench (floor texture + wall quads + cutaway). **Degrade path live-proven on `25a14caf`, both legs**: `capture_expired` (bundle swept), then — after re-uploading the preserved bundle.pb, which also live-verified ingest's READY-idempotency skip — `no_geometry_source`, left as that scene's permanent shell.json. **V3 SHELL_\* calibration done against the real anchors** (commit `634038b`; env-only revision applied same day): `SHELL_WALL_MERGE_GAP_M` 0.35→1.0, `SHELL_WALL_NORMAL_TOL_DEG` 12→15 — measured: the main wall was two same-plane patches (0.3°, coplanar 8 cm) split by a 0.81 m occlusion gap, the door pair coplanar 6 cm at 14.0°; under the new knobs exactly those two pairs merge (9→7 walls, main wall 3.51×2.49 m), every other pair fails coplanarity by 0.2–3.2 m. Floor selection needed no tuning. The capture is preserved at `outputs/real-capture-f3d70236/` (90 MB, gitignored) — the first plane-carrying regression fixture, which 0067 chunk D needs. Preview channel redeployed with main's shell-aware web (production channel confirmed untouched), then redeployed again same-day with the dev-fixtures exclusion (`2312620` — see the privacy bullet in What-does-NOT-work).
- **ARKIT_ONLY placement quality chunks A–C BUILT — 0067 executed, merged 2026-07-23** (branch `placement-quality-build`, 9 commits `fe50c16`…`d5b9b5d`, merge `3f26fcc`; OFFLINE-verified — the warm re-drive of `25a14caf` + operator /viewer walk is the live gate, next perception deploy). Chunk A: `reproject.py` two-tier instrument (tier-1 crop-aware soft-IoU silhouette; tier-2 deterministic numpy z-buffer point-splat render + masked NCC vs the RGB crop), `fit_silhouette` (bounded Hooke–Jeeves over scale+translation, rotation fixed per 0065, scale bounded ×1.5), `mirrored_twin` sign-flag, schemas primitives in `placement_math.py`. Chunks B–C in `fusion.py`: mutual-singleton same-frame containment dedup (pre-clustering), relaxed shared-frame merge + footprint join, silhouette fit as the ≥2-view position/scale authority (`position_source: "silhouette_fit"`, must beat init to ship), margin-gated in-plane resolution, manifest fields `reprojection_score`/`position_source`/`constraints_applied`/`in_plane_resolved`/`sign_flag`/`extent_m_sorted`/`deduped_observations`; `PLACEMENT_REFINE=0` is a test-pinned bit-parity rollback lever; `RefinementContext` is the IO seam, wired into `/process` with Dockerfile COPY + import smoke. Real-data regression pins on the recorded `25a14caf` observations (12 frames' masks/objects committed as ~390 KB fixtures; big PLYs by absolute path with skipif). On-branch quality pass: exact SAT/integral-image rasterization (`MaskEvidence`; 42 s→0.5 s per fit, bit-identical), evidence memoization + strided RGB + mid-pass budget valve (whole-scene refined fusion 22.9 s→4.8 s, tier-2 wired in production at the design's 128 px), a pre-existing mock.patch thread race in concurrent handler tests fixed at root, and main's stale `rotation_world_from_layout` docstring corrected (claimed identity; actual `diag(−1,1,−1)`). Probe verdicts — V2 pass with two measured amendments, V1 sign-flag pass, V1 in-plane gate FAIL (fork re-opened), V3 pass — are decision 0068's subject. Merged-tree suites: 473+6-skipped root / 302 perception / 18 re-enqueue / 67 web (iOS untouched by this Python-only branch).
- **Shell surfaces v2 SHIPPED — 0069 built end-to-end, deployed, and V3-walked (branch `shell-surfaces`, 6 commits `dda583c`…`58af9e5`, merged ff to `main`; deployed 2026-07-23/24: `perception-obj-00032-km5` image `20260723-235521`, `api-public-00016-qer`; decision 0070 records the build verdicts; supersedes 0066's surfacing layer).** The photographic bake left serving. **shell.json v2** (`SHELL_VERSION` 2) carries per-plane **parametric materials** — measured weighted-median albedo in a high-lightness band; confidence-gated family from ONE structured-output vision call per plane over ≤4 rectified ~2.5 mm/px single-frame evidence crops (`SHELL_MATERIAL_MODEL=claude-sonnet-5`, key mounted from Secret Manager; family→roughness lookup; plank direction via axial gradient consensus) — plus **envelope closure**: wall→floor, wall→wall seams at the V1-calibrated `SHELL_JOIN_MAX_GAP_M=0.5`, common top, floor snapped outward then bounded by exact Sutherland–Hodgman half-plane clipping, and the joint-participation fragment filter (`f3d70236`'s four floating quads resolve: 2 drop, 2 keep with recorded justification). Every wall ships `measured_quad` + per-edge provenance (`{state, extension_m}`) beside the rendered quad; the floor `measured_polygon` + per-segment states beside the rendered polygon — closure never mutates measured geometry (test-pinned on real data). `room_planes.py` extracted as THE anchor-interpretation module (0067 chunk D refactors onto it; wall merge preserves door/window members as `openings` with normalized `rect_uv`). shell_texture/shell_inpaint/LaMa **deleted** (image slims ~200 MB; Dockerfile smoke covers the new modules + asserts `anthropic` imports); api-public passes the shell **verbatim and signs nothing shell-borne**; web renders parametric surfaces (warm light rig + `MeshStandardMaterial` from albedo/roughness — Spark splat shaders ignore three.js lights, so objects are unaffected — triangulated floor polygon, openings as inset patches; no CSP change). **THE LOAD-BEARING FALLBACK RULE** test-pinned: below the 0.6 conf gate / "other" / call failure / no crops / no key → family null → clean matte in the measured albedo; no-key leg exercised offline (families null, albedos byte-identical, shell still ships). **V2 adjudication PASSED** on the reference room (floor #c8c1b7 stone, main wall #aab9c3 painted — both read true to the operator). **V3 walk green on the live stack** (`f3d70236` re-driven): v2 shell.json reproduces both probes, **0 texture blobs**, no legacy keys, floor bounded 17→7 vtx with measured preserved, wall_06 closure-extended below its measured bottom, wall_04's 2 door openings normalized in [0,1], assets response verbatim with zero shell-borne signed URLs, `/viewer` renders the parametric room clean-console, `25a14caf` degrades v2 `no_geometry_source`. Suites at merge: 473+6-skipped root / 359 perception / 18 re-enqueue / 68 web, lint + typecheck + static-export build clean.
- **ARKIT_ONLY single-view placement from contact priors BUILT — 0067 chunk D, merged ff to `main` 2026-07-24** (branch `placement-chunk-d`, commits `724b7a5` schemas / `78e4d2e` perception; OFFLINE-verified — the warm re-drive of a plane-carrying capture + operator /viewer walk is the live gate; reaches production on the next perception deploy, NOT yet on the deployed revision). The single-frame objects that can't triangulate (15 of 18 fused on `f3d70236`) now place against the MEASURED room: floor-standing classes drop their bottom onto the detected floor (closed-form 1-D depth solve — bottom-height is linear in depth, no iteration), wall-mounted classes intersect the mask-centroid ray with the nearest detected wall (normal snapped to the wall only within an alignment threshold, never flipping a badly-oriented object). Pure geometry in `placement_math.py` (`solve_floor_contact`, `solve_wall_contact`, `minimal_rotation`); policy in the NEW `contact_priors.py` — a conservative env-overridable floor/wall/none class map (ambiguous classes like `table lamp`/`speaker`/`plant` → none, unplaced on one view) and `RoomPlanes` read through the shared `room_planes.py` (NO re-implementation — 0069 extracted it). Wired into `fusion.py` for unplaced single-member ray clusters; `_refine_fused_object`'s in-plane/sign-flag/score/extents tail extracted into a shared `_finalize_placed_object`. **THE EVIDENCE RULE holds:** a proposed transform ships only if it reprojects onto the object's own SAM mask at tier-1 ≥ `PLACEMENT_SINGLE_VIEW_MIN_TIER1` — a guessed transform is never emitted; because the contact solve reprojects-by-construction, an independent floor-XZ-bounds sanity gate is the geometric check tier-1 can't provide (conservative — rejects to unplaced, never a wrong placement; relax `PLACEMENT_FLOOR_HIT_MARGIN_M` if the live walk shows valid placements rejected). Additive manifest fields carry through (`position_source` = `single_view_floor_contact`/`single_view_wall_contact`, `constraints_applied`, `reprojection_score`, `extent_m_sorted`). `PLACEMENT_REFINE=0` and bundles with no plane anchors reproduce today's behaviour exactly (pinned — the degrade lock). Verified offline through the production fusion path: floor + wall recovery of synthetic objects against `f3d70236`'s REAL measured planes (floor case also end-to-end through a real `f3d70236` camera pose) — position to <1 cm, scale to <2%, bottom on the measured floor to <3 mm, wall normal aligned to dot > 0.99; the evidence gate rejects a mismatched mask; determinism pinned. Suites: schemas 102 (+8 primitive ground-truth tests), perception-obj 371 (+12 real-plane tests), root 481 + 6-skipped, re-enqueue 18; ruff/lint clean. Chunk D's own live gate (warm re-drive of `f3d70236` — its 15/18 single-frame objects are exactly the target population — + operator /viewer walk) is owed alongside the chunks A–C placement gate.
- **ARKIT_ONLY placement sanity gate BUILT + DEPLOYED; gate + chunk D live-verified on `f3d70236` via warm re-drive** (revision `perception-obj-00033-zfg`, image `20260724-041547`, 2026-07-24; gate commit `d939d24` + `tests/test_placement_sanity_gate_real_data.py`, 16 pins; chunk D `78e4d2e`). The room-sanity gate in `fusion.py` demotes any triangulated / silhouette / `depth_fit` placement to unplaced-with-explicit-reason when it lands `outside_room` (beyond the floor rect + `PLACEMENT_ROOM_MARGIN_M=0.5`, below floor, or above wall top), is `implausible_scale` (`extent_m_sorted[0]` outside [`PLACEMENT_MIN_EXTENT_M=0.08`, `PLACEMENT_MAX_EXTENT_M=5.0`]), or is `represented_as_shell_opening` (door/window — the shell already renders these as openings). Chunk-D measured-surface contacts are EXEMPT (self-gated on a real surface — a door on its actual wall is evidence-grounded; a mid-room triangulated door is a guess). Inert without measured planes (degrade lock); `PLACEMENT_REFINE=0` bypasses the whole gate. **Live re-drive of `f3d70236`** (`tools/reenqueue_scene.py <scene> --force`, one round, 12/12 warm frames, `budget_stopped:false`): manifest 24 objects / **7 placed** — the gate demoted exactly the four operator failures (`mirror→outside_room`, `artwork→implausible_scale`, two `door→represented_as_shell_opening`); chunk D placed 5 single-view objects on measured surfaces (2 chairs + desk `single_view_floor_contact`, curtain + door `single_view_wall_contact`) plus bed + curtain via multi-view `silhouette_fit`, no two-bed split; the remaining 17 unplaced are mostly `insufficient_observations` (single-frame, no floor/wall prior class) — a coverage limit, not a bug. Shell re-driven (v2, ready, 5 walls + floor; geometry unchanged — confirmed empirically that shell geometry derives from the fixed plane anchors, not the processed frames, so more frames don't change walls; `wall_03` albedo remains the decision-0070 person-contamination). **Offline + live-re-drive verified; the operator `/viewer` walk (the human judgment gate) is still pending — and now DEFERRED under the board-item-7 Pro-only pivot (possibly moot).** perception-obj suite 387 green.
- **iOS app design BUILT end-to-end to the Good Guest system (branch `ios-app-design`, 13 commits, 2026-07-24; decision 0072).** The whole capture-app surface, translated from the design session's 11-screen spec (`docs/product/ios-capture-screens.html`) to SwiftUI and screenshot-verified on the iPhone 17 Pro simulator: a `DesignSystem/` foundation (`RSColor`/`RSFont`/`RSHaptics`/`RSSound`/`RSComponents`/`Wordmark`/backgrounds — gold=light-semantic-only enforced structurally; fonts on the spec's system fallbacks — New York / SF Pro / mono — behind a one-file bundling seam), and every screen: §1 home + cold-start, §2 guidance sheet, §3 live-capture overlay + gold-ink mesh PLACEHOLDER, §4 got-the-room + review, §5 analyzing/queued/long-running/connection-trouble, §6 the doorway, §7 recoverable + terminal failure + relaunch banner, §8 profile/why-sign-in/account-conflict, §9 thin history + QR bridge, §12 LiDAR-only unsupported-device screen. `RootFlowView` is the navigation coordinator binding every screen to the real `CaptureManager`/`ScenePoller`/`AuthManager`/upload stack — KEY behavioral change from the old `ContentView`: upload begins on review "Send it home", not automatically on `stopCapture`. Development pattern: each screen built as a standalone previewable view, verified via a temp app-entry swap + `simctl` screenshot, then reverted; every commit kept the `project.pbxproj` entitlements workaround and the design exports out. **ACTIVATED as the app root 2026-07-25** (`191f91f`): the LiDAR-device gate that deferred the swap stopped being a blocker once the product went Pro-only (0071) — `UnsupportedDeviceView` on non-LiDAR hardware is now the intended behaviour, not a regression. `ContentView` is retained, unreferenced, as the rollback path. Activation also restored the relaunch recovery the old root provided (`restoreUnfinishedBundle`). **Nine independent review passes** have run over this branch (`bedbb6f`…`9b6e467`); each found real defects in the one before, several of them self-inflicted by the previous fix, so the passes are audited as critically as the original work. The last pass's three HIGH findings — a wait screen stranded on "Sending your room" forever after a background trip, a home screen that both advertised a bundle as in-flight and reported it failed (and, once the banner was dismissed, claimed "sending" permanently for a terminally failed upload), and a deferral signal erased by any sibling blob's progress across ~127 concurrent PUTs — are closed (`26897de`), as is the launch-restore defect that activation itself introduced (`9b6e467`, decision 0073). Decisions that used to live inside SwiftUI bodies are now pure functions pinned by tests (`WaitFlowState` routing, `BundleRestore.pick` launch restore, `ReviewView.cardText` copy-vs-actions precedence) rather than reviewable only by eye — every one of them was extracted after a review found it wrong.
- **Both Pro-device hardware gates GREEN — the first real capture-to-doorway walk (board 2's gate) AND LIDAR_ARKIT tier verification (board 3) ran on a real iPhone 16 Pro (2026-07-26; decision 0074 for the migration finding).** iPhone 16 Pro (iPhone17,1, iOS 26.5.2) paired, built, installed with personal-team signing (entitlements-drop workaround, kept uncommitted). The LiDAR gate admitted the flow on its first-ever run on LiDAR hardware, and the Good Guest flow ran TWO real captures end-to-end (home → guidance "LiDAR READY · PRO CAPTURE" → capture → got-the-room → review "N frames · LiDAR" → Send it home → wait → doorway → Done). Every deliberate disruption path passed on hardware: upload entirely under lock (mint POST through ingest with the phone locked — the two-window background assertion held at 385-blob scale), upload-completes-while-locked → unlock catch-up (the 26897de strand fix, live), background trip across the queued→processing transition, force-quit + relaunch mid-processing (`BundleRestore.pick` chose the live bundle out of a 38-record backup-migrated store), relaunch-after-terminal → re-entry row → doorway direct, doorway-Done clearing home. **Gate B, first LIDAR_ARKIT executions ever:** tier dispatch correct on hardware; capture #1 (scene `247003de`, bundle `893663fd`) 293 keyframes/70.5 s with 292/293 depth (one legal mid-walk `sceneDepth` dropout, frame 266), capture #2 (scene `13bae607`, bundle `b4b15b1f`) 128/128; depth 256×192 packed float32 at exact contract byte sizes, room-scale meters (0.5–4.1 m mid-walk), confidence {0,1,2}; decision-0032 depth intrinsics byte-exact on hardware (fx 181.5 = 1361.6×256/1920); 26 plane anchors incl. first-ever ceiling/seat classes. Backend firsts, all green: the ingest gate passed both bundles (RGB-only validator correctly skips depth/confidence); **decision 0052's `depth_fit` placed 16/23 objects on capture #1 — every placed object via depth_fit, ZERO `insufficient_observations`** (the 16e baseline left 15–17 of ~20 unplaced), doors/windows demoted `represented_as_shell_opening`; shell v2 fired unprompted (floor + 14 walls, materials confidence-gated); the 2-round warm re-drive recipe reached 12/12 sampled frames on LiDAR data. Fixtures preserved: `outputs/real-capture-247003de/` (202 MB, full — THE LIDAR_ARKIT regression fixture) and `outputs/real-capture-13bae607/` (bundle.pb only). **Serial-mint defect found, fixed, and deployed same-session** (commit `d337e10`): capture #1's 878-path manifest took two full ~70–80 s serial mints across 3 POSTs (client 60 s timeout; the 0015 TOCTOU race exercised live, benignly, same user); `_mint_all` now runs a bounded pool (`UPLOAD_SESSION_MINT_CONCURRENCY`=16, order-preserving, abort-without-store), deployed **`api-public-00018-xay`** (candidate → smoke → flip) and live-verified: capture #2's 385-path mint was ONE 200 POST (28.9 s incl. the revision's cold start). Suites: root 486+6-skipped (+5 mint pins), iOS 288/288 incl. the 4 live tests against the new revision.
- **First-LiDAR-rooms adjudication complete (2026-07-27/28; brief `docs/briefs/lidar-first-rooms-adjudication.md` — the board-7 design session's opening exhibit; decision 0075).** Both scenes staged in the dev workbench via the new `/viewer?fixture=<dir>` param (`e6972be`; fixtures gitignored under `web/public/dev-fixtures/scene-*`); production fusion + shell decisions replicated offline bit-for-bit (footprint join 0.5998 joined / 0.3144 refused on the same physical bed; the 14- and 10-wall shells reproduced wall-for-wall from the preserved bundles under the serving knobs). Measured: the bed×3/desk×3/rug×3 "under-merge" mostly DISSOLVES on mask-overlay identification — label-space collapse (stool + folding table both "desk"; three distinct textiles all "rug"; a folded mattress as the third "bed") with ONE true phantom, the f173 edge-truncated bed view, structurally refused by the footprint instrument (0.31 vs the 0.5 line even at its own fitted center) — depth_fit anchors to the VISIBLE REGION (not specifically the near face; displacements measured toward, away, and lateral), scattering partial-view centers 0.5–1.2 m on a 2.0 m object. The envelope is the LiDAR tier's headline win: rectangular to ≤1.1° on BOTH rooms, and the envelope-intersection floor plan (4.20×3.29 m, opposite sides to 3–6 cm) was **operator-confirmed against the real room**. The rendered damage is admission + closure: 10-of-14 / 6-of-10 "walls" are furniture planes (bed side rails ARKit-classified `seat` in both rooms) inflated to full-height slabs (+2.48 m worst case), and the floor S-H-clips against every one of them, compounding to **12% / 38% of the measured floor shipped**. New measured defect classes: cross-label triple-detection (the 0067 dedup is same-label-only — one ~0.3 m frame shipped 3× as artwork/painting/mirror at one position), specular mirror depth (nn_rms 0.196 m, 28× normal), textile scale collapse (a meter-scale throw ships at 0.34 m extent with 8 mm one-directional-NN rms — collapse-into-cloud degeneracy), and object-blind sampling starvation (12/293 frames used on #1; #2 budget-stopped at 3/12; capture quality itself measured GOOD). Analysis artifacts + replication scripts in gitignored `outputs/lidar-adjudication/`.
- **Decision-0074 phantom-room fix BUILT + simulator-verified against production (branch `ios-0074-phantom-room`, commits `052e30e` fix / `3ae6c62` sweep, 2026-07-28; hardware verify on the 16 Pro pending — it carries the live repro state).** A by-bundle poll 403 is now the distinct `ScenePollState.notOwned` terminal — ownership, not auth: api-public 403s only AFTER the token verifies, so it is definitive for the identity, and 0038's 401-refresh-once path is untouched (re-pinned: 401→refresh→403 refreshes exactly once, then classifies notOwned; 5xx/network stay transient; 400/422 stay `.pollError`). Routing follows the house pattern — two new pure table-pinned decisions (`WaitFlowState.standsDownAutomatically`: true exactly for notOwned; `foreignBundleToAcknowledge`: the poller's target, fallback the flight's) drive an automatic stand-down in RootFlowView: acknowledge in `DismissedBundles` (the doorway-Done semantics) + clear the flight, navigate home only from the wait (never yanks a live capture). Cosmetic sibling shipped: `CaptureStorageSweeper.sweepDeadRecords()` (run before the dir pass) reclaims record files that fail strict decode AND have no surviving capture dir — conventional location and the JSON's own `outputDir` both checked via the read-only `UploadSessionStore.undecodableRecords()`; decodable records are untouchable (all test-pinned). Simulator walk ran the REAL wire: seeded the 16e's actual `9fbe29b6` record on a fresh install → phantom row rendered → tap → live production 403 → acknowledge + stand-down in 1.8 s (log: `not owned … standing down`) → cold relaunch clean, `RootFlow.dismissedBundleIds` persisted. Suite 288 → 302, one run, incl. the 4 live tests against `api-public-00018-xay`.
- **RoomPlan co-run spike EXECUTED — board 3 fully closed; the board-7 design session's last de-risk input is in (2026-07-28; branch `roomplan-spike`, commit `46c2910`; decision 0076; four instrumented runs on the 16 Pro, operator-walked).** Headline: **co-run does NOT strip sceneDepth on iOS 26.5** — the documented caveat is refuted on hardware. Mechanism (measured twice): RoomPlan's native configuration IS production's configuration field-for-field (gravity alignment, h+v plane detection, `.sceneDepth`, 1920×1440@60) plus one private `sceneReconstruction=24` (whose mesh never surfaces as ARMeshAnchors); there is no config conflict to manage. Full-scan evidence: depth on 9752/9765 frames, all 722 keyframes; `ARSession.delegate` never touched (the prophylactic interposer never engaged) — the production keyframe pattern ran verbatim at 59–61 fps with 0 write failures; ONE continuous world frame across scan start/re-run/stop (boundary pose jumps ≤0.10 m), so bundle poses and CapturedRoom compose directly. CapturedRoom on the reference bedroom: 13 walls ALL matched to wall/door/window-classified anchor planes within 1–25 cm — the same session's two `seat`-classified bed-rail anchors (the current pipeline's phantom-wall culprits) were REJECTED by RoomPlan, killing the furniture-plane defect by construction, measured; floor is one 10-corner 14.98 m² polygon, oriented 4.26×3.97 m (long side within 6 cm of the anchor-envelope truth; the +0.7 m short side is a REAL entry alcove the envelope method truncated — operator-confirmed); 9 objects, all upright-by-construction (pure-yaw transforms), rich sub-attributes (cabinet/shelf, swivel/dining chair), **operator walk 9/9 on position, extent, AND facing — including a genuinely-angled chair (yaw follows reality, not a snap)**; one label miss (wardrobe→"refrigerator", self-flagged low confidence). Census: full 9-object count live by ~27 s of scanning; `didUpdate` = full room, `didAdd/didChange/didRemove` = DELTA rooms (changed entities only); boxes refine all scan long. Hard constraints measured: retaining just 10 ARFrames stalls the camera feed in ~1 s (Δ88 MB — pool starvation, not memory growth), kills VIO, and RoomPlan self-aborts after exactly 10 s of tracking failure (`didEndWith error: worldTrackingFailure`, partial room still returned; reproduced 2×); RoomPlan NEVER resets tracking — the host app owns tracking hygiene (`.resetTracking` at scan start is load-bearing). Co-run memory: ~+370 MB while scanning + ~905 MB transient at RoomBuilder (builds in 1.7 s). Artifacts in gitignored `outputs/roomplan-spike/` (4 runs; the probe run's CapturedRoom JSONs + parametric/mesh USDZ + plane anchors + full instrument streams + 722-keyframe RGB/depth archive = the first LIDAR_ROOMPLAN prototype data, one shared frame); the throwaway spike app was added in `46c2910` and removed from the tree once the production co-run shipped — recover it from that commit if the runs ever need repeating. Production 7-day resign refreshed same-session (expires 2026-08-04).
- **Board-7 RoomPlan integration design DONE (2026-07-28; decision 0077 + build brief).** Seven questions locked, three operator forks resolved, and two verify-first probes run in-session against recorded real data (gitignored `outputs/roomplan-design/`): P1 — box-frame candidate scoring on 247003de's real bed: correct facing wins with tier-2 margin 0.15 at a box-quality center, upside-down rejected by 0.37, the box-frame candidate BEATS the shipped layout rotation in its own source frame; the same scorer FAILS at the shipped fused center (prefers upside-down — position precedes rotation) and a 1.4 m close view zeroes out (skip degenerate views). Collateral: the shipped bed is ~90° yaw-wrong (long splat axis along the rail NORMAL, |dot| 0.947) and 0.79 m displaced — the operator's orientation verdict quantified on one object, invisible to every shipped metric. P2 — a ~60-line numpy parser over Apple's Codable JSON reproduces every 0076 fact (13 walls up_y=+1.0000, 3.05 m top + four 1.95 m door segments, floor 14.98 m²/10 corners, 9 pure-yaw objects incl. the 40.8° chair, full door/window parenting) — the wire contract validated with no coreModel and no USDZ. Also measured: the RoomPlan bed box is 1.85×2.16 m where the shipped fit said 0.92 m wide (visible-region truncation halved it) — box extents own measurement truth for covered categories.
- **RoomPlan wire foundation BUILT — 0077 chunks RP-0/1/2 (branch `roomplan-wire-foundation`, commits `e5fb9a5` RP-1 / `fec81a6` RP-0 / `6f10fa7` RP-2, 2026-07-28; offline-verified, no deploys; merged to `main` 2026-08-05 via `roomplan-server-chunks`).** RP-1: `RoomPlanModel` carries the CapturedRoom JSON by reference — `json_gcs_path = 4`; the `RoomPlanSummary`/`RoomPlanObject` proto mirror deleted with zero wire history, field 3 + name `summary` reserved and descriptor-pinned; additive only (`schema_version` stays "1", `plane_anchors` untouched on every tier); BOTH languages regenerated via `./tools/gen_proto.sh`; the api-core fixture builder ships `roomplan/room.json` + `roomplan_version` on the ROOMPLAN tier with self-tests proving RoomPlan-carrying bundles pass `validate_bundle` unchanged (the 0066 precedent), and validate_bundle check 6 now covers `json_gcs_path` (relative-only, direct pin). RP-0: `tools/convert_roomplan_spike.py` converts the probe run into THE first LIDAR_ROOMPLAN fixture at gitignored `outputs/roomplan-spike-bundle/` (bundle_id `c1d97365-92b0-5fd6-91b3-dcd1691562ab`, stable uuid5 of the run id; re-run with `--user-id <uid>` before the RP-8 upload): 722 frames with poses/intrinsics/gravity VERBATIM (float32-exact against keyframes.ndjson; the tool fails loudly if the recording's 3-decimal rounding ever breaches the 1e-3 quat contract — worst achieved 6.9e-4), room.json byte-verbatim, 33 anchors converted (spike `none(…)` classifications → `""` matching the production extractor), APFS-clone blob copies; gates green: `inspect_bundle.py` clean (0 warnings; gravity-vs-pose 0.0025; depth intrinsics scaled-RGB exact), 6-frame spot-check float32-exact + sha256-identical blobs, production `validate_bundle` PASS incl. the bundle_id cross-check. RP-2: `roomplan_room.py` in perception-obj — THE CapturedRoom-JSON interpretation module (do not parse room.json elsewhere) + adapters emitting `ShellPlaneGeom` so chunk D and the room-sanity gate consume RoomPlan planes UNCHANGED; the P2 pin set reproduced exactly on the committed 203 KB verbatim fixture (`tests/fixtures/roomplan_spike/`): 13 walls up_y to 2e-7, 9×3.05 m + 4×1.95 m wall heights above the −1.418 floor, families ≤2.65°, floor 14.98 m²/10 corners, 9 pure-yaw boxes incl. the dining chair 40.8° off the room grid, parenting complete (doors→walls 0/2, windows→0/1, openings→10/11); chunk-D consumption proven through the PRODUCTION fusion path (floor contact ≤1 cm + bottom-on-floor ≤3 mm, wall contact normal dot ≥0.99 — the anchor-plane suite's achieved tolerances) and `_position_outside_room` gating on the RoomPlan floor rect + wall top; `coreModel` opaque (garbage-substitution pin), unsupported Codable `version` = the drift pin, `try_parse_captured_room` never raises (degrade path, never `failed_invalid`); Dockerfile COPY + import smoke extended. **Two wire facts MEASURED that the design didn't record, both now pinned:** RoomPlan does NOT orient wall normals toward the interior (2 of the probe's 13 walls point away — the adapter re-orients every normal to the interior reference, pinned exactly-2-flips; RP-3 must consume the ADAPTER's oriented walls, never raw local +Z), and `polygonCorners` is EMPTY on 12/13 walls + every door/window/opening (rect-from-dimensions is the DOMINANT real path; only wall_00 carries an explicit 6-corner polygon). Brief-shorthand decode (now test-pinned): "3.05 top / four 1.95 segments" = wall HEIGHTS above the floor (absolute world tops 1.631/0.532, floor y −1.418); "the 40.8° chair" = the dining chair's yaw off the wall-family grid (84.3° absolute vs the 43.5° family heading), not an absolute yaw. Suites at exit: root 490+6-skipped (was 486+6), schemas 103, perception-obj 425 (was 387), re-enqueue 18, iOS 302/302 incl. the 4 live tests vs `api-public-00018-xay` (run after the Swift regen).
- **RoomPlan server chunks BUILT — 0077 chunks RP-3/4/5 (branch `roomplan-server-chunks`, commits `4642ce5` RP-3 / `0658da8` RP-4 / `9eb594b` RP-5 + housekeeping, 2026-07-28; offline-verified against the spike fixture + both preserved LiDAR captures, no deploys; cut from main at `a0135d0` — contains the wire-foundation work — merged ff to `main` 2026-08-05).** RP-3: shell.json **v3** — method `"roomplan"` (CapturedRoom geometry VERBATIM: interior-wound wall polygons + per-surface confidence + parented openings normalized like 0069, floor polygon verbatim; geometry reads the outputs-cached `roomplan/room.json` first, so a swept capture costs materials only — every plane then ships the honest empty observation) and method `"anchor_envelope"` (the LIDAR_ARKIT / roomplan-absent degrade: `shell_envelope.py` implements the adjudication's validated envelope derivation — candidates by HEIGHT-REACH to the common top, two perpendicular families, extreme-offset planes, seam-intersected floor rectangle; furniture planes are internal evidence, never rendered — closing 0075's slab-wall + floor-collapse classes with zero merge-knob changes). **Decision 0078 records the measured amendment:** the brief's literal "classification OR height-reach" rule selects WRONG envelopes on both real rooms (ARKit labels furniture faces "wall"; door leaves carry openings) — height-reach AND not-seat reproduces the adjudicated sets exactly (247003de → walls 02/05/09/12 under serving knobs, 13.81 m², sides pinned at the operator-confirmed 4.20×3.29 ± 3–7 cm; 13bae607 → 01/03/06/08, 13.50 m²). ARKIT_ONLY keeps the v2 closure path byte-for-byte; roomplan parse failure → structured log + `anchor_envelope`, never a failure. RP-4: `box_placement.py` — the box is the skeleton (association by projected-footprint overlap ≥0.5 + the 0077 label-family map, greedy deterministic, associated observations CONSUMED — one object per box by construction; unmatched boxes ship `placed:false/no_appearance` honest inventory; box-duplicate suppression recorded); position/extent/upright/yaw from the box, `extent_m_sorted` = BOX dims; the splat's arbitrary canonical frame resolved by extent-consistent right-handed axis mappings (4 sign candidates per assignment — P1's up±/long± set) scored at the BOX CENTER across non-degenerate views (in-frame skip: f164 measured 0.0 vs f129's 0.63); ship at `PLACEMENT_AXIS_MARGIN`=0.10 (P1's achieved), below it extent-best default + `splat_axis_resolved:false`, partner preference ≥0.03 → `facing_flag` flag-only. P1 pins at achieved values through PRODUCTION code paths (winner up+long+ 0.6716, margin 0.0999; facing tier-2 0.153; upside-down tier-2 rejection 0.368; beats shipped layout 0.5681; the pinned NEGATIVE — shipped-center prefers upside-down — from the committed f129/f164 masks + absolute-path splat/RGB). The three long-tail gates (census-keyed): cross-label near-identity dedup (intersection-over-LARGER cliques, nested-parent protected; the committed f242 triple collapses), mirror depth-trust (`nn_rms_m`>0.05 → demote to ray/wall-contact path; 0.196 demotes, 0.0066 stays, both pinned), textile silhouette-span (<0.5 → `scale_suspect`, flag-only). RP-5: `census_sampling.py` box-visibility set-cover (gain = on-frame area × in-frame fraction over uncovered boxes; cover bar = scoreable-view quality) + FPS residue seeded with the cover set — spike gate: 9/9 boxes covered in 7 greedy picks (pinned indices `[10,61,142,171,398,568,613]`), empty census == 0062 selection exactly; two-pass /process (`_run_census_two_pass` + the per-object body extracted verbatim into `_reconstruct_one_object`, shared by both paths): pass 1 segments + writes masks.npz, pass 2 reconstructs per the deterministic plan (box best views → weakest-first second views → long tail by score) under untouched budget admission — SAM 3D drops to per-object (6 reconstructions for 8 masks in the pin; policy-skips cache with additive `skipped_reason`; budget-cut frames never cache; the retry-aware plan counts cached views so a warm retry reconstructs only what's missing). Manifest additions all additive: scene-level `roomplan{}` (only on roomplan-claiming bundles), `sampling.census{}`, per-object `roomplan_box{}`/`sam_label`/`splat_axis_resolved`/`facing_flag`/`box_fit_residual`. **Degrade locks pinned:** no-roomplan bundles byte-identical to today (fusion json-identical, manifest key-identical, 0062 policy verbatim), `PLACEMENT_REFINE=0` legacy-identical. Suites at exit: root 490+6-skipped, schemas 103, perception-obj **541** (was 425), re-enqueue 18 (iOS untouched — Python-only branch). NOT built here: the brief's RP-3 viewer half (web polygon-wall rendering + staged-fixture browser walk) and everything RP-6+.

- **RP-6 BUILT + hardware-gated on the 16 Pro (2026-07-28 build / 2026-08-05 gates; branch `roomplan-rp6-corun`, commits `78e3412` + `7929e65` + docs; merged to `main` 2026-08-05, `357b8bf`; decision 0079 — renumbered from the session's 0078 after a parallel-session number collision).** Co-run per 0076 in production CaptureManager: production config first with `.resetTracking`, RoomCaptureSession on the same session, copy-out untouched; stop = stop(pauseARSession:false) → anchors → pause → RoomBuilder → `roomplan/room.json` (+usdz, CAFUFA) as phase-1 blobs; tier LIDAR_ROOMPLAN iff a built room with ≥1 wall/floor ships (every decision a pure table-pinned function in `RoomPlanWire`); review census line; 10 s self-abort ends capture gracefully (RootFlow routes on the isRunning flip); 15 s didEndWith timeout ships LIDAR_ARKIT. **Gates:** G1 PASS — capture `12fe9da3`/scene `09684dde` `ready`; room.json (81.9 KB) + usdz in GCS; bundle.pb tier 3 with room_plan verbatim (`ios26.5.2;CapturedRoom.v2;beautifyObjects`); 247/249 frames with depth. G2 PASS — palm-cover abort: app self-ended (operator-observed), PARTIAL room built and shipped ROOMPLAN (4 walls/1 floor/11 objects/2 doors), `ea40c579`/scene `b667f891` `ready`. G4/0074-verify PASS all three checks — 34 June-era dead records swept (store 38→4), TWO phantoms (`9fbe29b6`, `42bba2b9` — the pre-dump found two foreign rooms, not one) each stood down exactly once and persisted, third cold launch clean. G3 = measured envelope finding (see What-does-NOT-work). Two live findings fixed same-session: same-turn co-run attach loses sceneDepth (0/268 frames on the first capture — 0079's frame-observed re-assert guard cures it, conditions 0076 Q1) and the mint-timeout client half. Suite 313 (was 302; +9 RP-6 pins, +2 envelope pins), one run, incl. the 4 live tests.
- **RP-3 viewer half BUILT — the web renders shell.json v3, both methods (branch `shell-v3-viewer`, commit `806ebfc`, 2026-08-05; offline + browser-walked, no deploys; RP-8's operator walk is UNBLOCKED viewer-side).** Method "roomplan": CapturedRoom polygon walls verbatim — the rect-from-dimensions dominant path AND explicit outlines (the spike's 6-corner wall_00 renders its notch) — with parented door/window/opening inset patches, floor polygon verbatim, per-surface confidence carried on `ShellPlane` for treatments; method "anchor_envelope": the LIDAR_ARKIT degrade rectangle (the 247003de fixture reproduces the adjudicated 02/05/09/12 envelope under serving knobs, 13.8 m²). v2 + unavailable docs render byte-identically (test-pinned + walked); the reveal plays over polygon walls (new mock `!v3` bundle trigger walks /room offline without disturbing the one-scene-per-status list pin). The load-bearing geometry fact, mirrored from the server convention: v3 winding normalization can rotate a wall polygon's start vertex, so the opening UV frame derives from the polygon's bounding rect in-plane (`web/src/lib/shell3d.ts`, pure math — SplatViewer stays the only three.js module) — reproducing v2's corner-0 math bit-for-bit while staying correct on rotated-start walls. Fixtures: `tools/make_shell_v3_fixtures.py` stages four dirs into gitignored `web/public/dev-fixtures/` through the merged perception code verbatim (the test_shell_v3 paths; regeneration verified byte-identical): spike roomplan with measured albedos (family null — no key offline), its capture-expired neutral variant, 247003de anchor-envelope, v2-shaped unavailable. Suites: web vitest 87 (was 68), lint + tsc + static-export build green.
- **RP-7 BUILT + hardware-gated — the Good Guest live floor plan replaces the gold-ink mesh placeholder (branch `roomplan-rp7-floorplan`, commit `d996b8e`, merged to `main` 2026-08-06, `0fe4674`).** `Capture/FloorPlan.swift` (snapshot extraction + pure math + feed) / `Capture/FloorPlanVoice.swift` (language + coverage tables) / `Scan/FloorPlanView.swift` (canvas + live wrapper + spoken line), wired through CaptureManager/LiveCaptureView/ReviewView/RootFlowView; the fake placeholder sketch is DELETED from the live screen (review falls back to `RoomSketch` only when no room shipped); the floor plan CONSUMES RoomPlan delegate data and never touches session config (0079's guard untouched). Behavior per 0077 choice 3: walls stroke in from the `didUpdate` stream, doors/windows as backdrop-cut gaps (no swing arcs — RP-2's normal-orientation lesson), furniture as labeled rounded rects, camera cone as the scan's light; `didAdd` deltas → deduped "— noted." guest moments + the surfaceCovered haptic; `didProvide` → Good Guest guidance copy; §3 coverage ticks wired from the live census + measured corner adjacencies (the task-#13 comment in RootFlowView closed); the same canvas reused static at review under `builtCensus`'s ships-only rule. Architecture: `FloorPlanSnapshot` extracts on the delegate delivery thread (copy-out preserved); `FloorPlanFeed` is a separate ObservableObject so ~20 Hz camera poses re-render only the plan subtree; every language/coverage decision is a table-pinned pure function. CONFIDENCE GATES NAMING — the spike's low-confidence wardrobe/"refrigerator" renders unlabeled and speaks the hedge, pinned end-to-end. Fixture pins decode the committed spike room.json through Apple's CapturedRoom Codable path in simulator tests and reproduce the recorded numbers (grid 43.680°, floor 14.9815 m²/10 corners, bed 1.85×2.16, the −84.3° chair; two pins corrected to measured reality — a real 5.2 cm sliver opening, worst wall 2.54° off the weighted grid). Design-system compliance: mesh-ink gold is the light-semantic use RSColor itself names, camera cone brightest (light being cast now), labels machine-data cream mono via the `RSFont` seam with Dynamic Type, no new hex values; AX5 verified by screenshot (plan + review; honest-fit label drop). One copy surface added beyond the brief's four: the census-stable "enough" line completing §3's triad — operator read owed. **Live hardware gate green (2026-08-06):** bundle `223aca8e`/scene `a71d125f` — floor plan appeared and refined during a real scan, boxes matched the furniture, "— noted." moments + census-stable line fired, review showed the real plan; server-side: 1159-path mint in ONE POST (fresh datapoint: the mint envelope holds to ~1.2k paths), all blobs incl. room.json in GCS in ~2 min, tier LIDAR_ROOMPLAN, `ready` on attempt 0, doorway reached. Suite 313 → 352 (+18 `FloorPlanMathTests`, +13 `FloorPlanVoiceTests`, +8 `FloorPlanFixtureTests` on a committed 203 KB spike-room fixture); screenshots preserved at gitignored `outputs/rp7-screenshots/`. NOT exercised: the guidance relay live (RoomPlan issued no instruction — sparse by nature; table-pinned + seeded-screenshot only), the 10 s abort view-freeze on hardware (code path unchanged from RP-6's gate), review `thinCoverage`/verdict deliberately unwired (census→quality-claim is an operator copy decision), `didChange`/`didRemove` deliberately unconsumed (documented), wall-endpoint jitter on long scans unwatched.
- **RP-8 EXECUTED — the RoomPlan pipeline is deployed, live-verified end-to-end, and operator-scored (2026-08-06; decision 0080; board 7's execution closes).** Serving: `perception-obj-00036-xer` (image `20260806-012927` — RP-2..5: roomplan_room, shell v3 + envelope, box placement, census two-pass; digest-verified against the build push) and `api-public-00023-puz` (mint fix below + `UPLOAD_SESSION_MINT_CONCURRENCY=64`, yaml mirrored `2bf4339`), both candidate→smoke→flip. **The long-walk mint envelope is CLOSED: 2,170 paths, ONE POST, 14.0 s vs the 120 s ceiling** — but it took TWO live fixes beyond the env bump: the 64-thread mint OOM-killed the 512 MiB service at 101 s because EVERY mint call resolved ADC credentials and built a fresh AuthorizedSession (fixed at root with api-internal's per-thread-cache idiom, `5d84463`, +2 pins → root 492+6), and the spike converter's stable uuid5 bundle_id 400'd at the live mint's canonical-UUIDv4 check (fixed `22732b4` — deterministic v4-layout ids; the spike bundle_id is now `c1d97365-92b0-4fd6-…`). The spike bundle (722 frames, LIDAR_ROOMPLAN, 517 MB) uploaded under the operator's REAL uid via an operator-approved tokenCreator grant + custom-token mint (memory note; grant flagged for board 4's IAM audit), and ran the **first live census two-pass**: census set-cover live-matched the pinned 7 greedy picks + 5 FPS residue verbatim; plan 6 box-best + 4 second + 24 long-tail with 4 policy-skips; honest `budget_stop` at 16/34 with the per-object estimate updated 30→**61.9 s observed** (~2× the offline guess); VRAM lifecycle held under two-pass (flat 16.4 GiB baseline, 21.9 GiB peaks released; one big-object OOM contained per 0061). Round 2 ran warm (899.8 s budget, `roomplan source=cached`) and completed the plan: **25 objects / 13 placed + v3 roomplan shell (13 walls) in TWO requests total.** Warm re-drives took the three earlier LiDAR scenes to coverage on the new code (`a71d125f` 18/14 and `09684dde` 21/16 saturated; `b667f891` 33/14 at the driver's 4-round cap) — and exposed a real defect: **the shell redelivery fast-path was version-blind**, so July v2 shells nooped every v3 `--shell` re-drive until the blobs were hand-deleted; fixed properly in `ab0a060` (noop only at current version; ARKIT v2 regen accepted as deterministic byte-identical waste; perception suite 542) — NOT yet serving. All four rooms staged as `/viewer?fixture=` fixtures with a synthesized per-axis stretch A/B twin (`PositionedSplat.scale` union, `165940d`), `?reveal=1` assembly replay (`7b517e7`), and `?labels=1` in-scene badge labels from a `_walk_labels` sidecar shared with the 2D key maps (`5674e6e`) — walk-tooling lessons in memory. **The operator walk RAN; verdicts verbatim in `outputs/rp8-walk/` + decision 0080.** Broadly held: locations/footprints/envelopes/openings (the 0075 baseline's 0.79 m displacement class did not recur; "this room is really 4 plain walls, that is correct"; G2's abort partial "looks okay"). Ranked defect classes (the tier's quality list): splat-axis defaults inside boxes (ALL box placements shipped `splat_axis_resolved=false` — live margins 0.002–0.089 vs the 0.10 gate, ~1 scoreable view/box; tables render lying on their sides, beds 90/180° off), cross-label duplicates surviving at partial overlap (desk/nightstand, monitor/tv), wall-mounted splats centered IN walls (artwork/clock/doors half-outside), door-label misses escaping the opening demotion (5×"cabinet is actually a door"), missing on-top-of support relations, splat visible-region truncation, envelope segment kinks, a 30° in-plane window skew (0068's population), and the mirror-reflection product note. **Operator resolutions at the walk: UNIFORM scale ships (stretch only amplified truncation); fork (a) long-tail gates ALWAYS-ON (built `5e3807e`, pins revised, perception 543 — rides the next deploy); fork (b) facing_flag flag-only BLESSED (both flagged chairs read okay); reveal choreography → a design-session follow-up (settle gracefully; walls/floor materialize in place — the long-standing reveal-watch item CLOSES).**
- **iOS release-residue cluster BUILT — terminal-state reaper, strict concurrency, staged device gates (branch `ios-release-residue`, commits `6bd103d`/`dc5a345`/`8869d43`, 2026-08-07; decision 0084; merged to `main` 2026-08-07).** `CaptureReaper` + `CaptureReclaim` close the completed-capture disk-accumulation gap: reclaim is keyed on USER-SEEN OUTCOMES, not upload success — a flight-end table over leaving the wait screen (doorway / processingFailed / uploadFailed reclaim; incompleteUpload retains record+files; notOwned retains per 0074) plus a launch scan over `DismissedBundles`-acknowledged records (`.failed` directly; `.complete` only after ONE confirming GET shows a terminal backend state — offline/403/404 retains, never a guess). Record-first-then-dir deletion self-heals through the existing sweeper; `reclaim()` refuses active-phase records. **The `.recoverable` re-upload coordinator is SERVER-BLOCKED, found by recon before building** (0084): `create_or_get` returns the STORED session URIs for a matching path-set while the `upload_sessions` doc lives (7-day TTL) and a finalized GCS resumable session is single-use, so a client-only re-drive of a swept blob either silently no-ops or 410s into the `remint_returned_stale_uris` fatal — the ingest half already supports revalidation (`FAILED_INCOMPLETE → QUEUED`); only the mint half is missing. The charter's floor shipped instead: both surfaces' copy is now honest and the files are retained. **Strict concurrency: zero warnings project-wide** — the app target runs `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`; CaptureManager's four CVPixelBuffer crossings use a documented `UncheckedSendable` envelope with `extractPackedBytes` nonisolated, plus the adjacent pre-existing set (NetworkConfig, SceneStatus/SceneResponse, WaitFlowState, UploadSessionClient body types, `UploadContext`/`BackgroundTaskHandle` lock boxes, `DismissedBundles`). A surviving variant of the stale-status finding was found by code-reading and fixed: a cross-launch resumed upload completing mid-wait would flash the OLD bundle's doorway over the new capture's wait — `sendItHome` now declares the flight to `ScenePoller.expectBundle` and foreign completions are dropped. DEBUG-only `StagingHooks` make all three terminal failures and both OS-kill shapes deterministic on device through the REAL `handleTaskCompletion` path (flags from a devicectl-writable one-shot JSON; file-based breadcrumbs as the evidence channel because os_log buffers under suspension — the Anomaly-B lesson). Device rebuilt + installed over WiFi pairing without an operator sitting (re-sign expires **2026-08-15 07:11 UTC** — genuinely renewed 2026-08-08 by forcing profile re-issuance and verified via CreationDate; container preserved, 8 historical records = Run 0's real-data population). Suite 352 → **391** green in one run.
- **iOS Live Activity + mint-429 branch + privacy guidance copy BUILT (branch `ios-live-activity`, commits `0b631b0`/`eab75db`/`ece3847`/`237169b`, 2026-08-08; simulator-verified at the time; the device install that was blocked then landed with the walk-findings pass, see below).** The §5 **Live Activity** ships: a real WidgetKit app-extension target added to `project.pbxproj` (+174 lines, nothing existing rewritten — new PBXNativeTarget, an "Embed Foundation Extensions" copy phase on the app target, container proxy + dependency, two `PBXFileSystemSynchronizedRootGroup`s, and a build-file exception set excluding the extension's own Info.plist from its resource phase; the app target gained exactly one setting, `INFOPLIST_KEY_NSSupportsLiveActivities = YES`, in both configs). `RoomStudioShared/` is a member of BOTH targets — the ActivityKit attributes must be one type, and `RSColor`/`RSFont` moved there so the extension cannot fork the palette (they are the only design-system files that move; the rest reach UIApplication/AVFoundation). Two build failures were found by BUILDING rather than by reasoning: the synchronized folder copying Info.plist twice, and `Phase`'s synthesized `Equatable` vanishing once a case gained an associated value. Remote push updates are a named seam (`LiveActivityController.pushTokenSeam`), not half-built. Screenshot-verified on the simulator: Lock Screen sending/ready/failed + Dynamic Island compact and expanded; the gold rule holds and is test-pinned. **The mint 429 branch closes 0038's reserved follow-up** against the quota api-public now enforces (0087): `Retry-After` ≤ 60 s sleeps-and-retries in place (max 2 holds, jittered) so the user sees nothing; > 60 s — the real case, since the quota rolls at UTC midnight — does NOT sleep and surfaces a `sendRateLimited` screen naming the reset time; a stripped header falls back to `resets_at` in the body; neither present still classifies, without inventing a time. `RetryAfter` is now the ONE RFC 9110 implementation (the blob-path tests moved to it unchanged), `SessionFailure` became an enum, and the two exhaustiveness breaks that caused (CaptureReaper, ContentView) were the check working — the reaper RETAINS a rate-limited capture. **Privacy guidance copy** (the capture-side half of 0089's fix): a §2 guidance row asking that people step out, framed as what it costs the ROOM rather than as a privacy warning, verified at default and AX5. Suite 391 → **463** (457 asserting offline + 2 stubs + 4 live against `api-public-00025-xaj`).

- **Launch-hardening pass EXECUTED — all nine 0015/0018 gaps closed or dispositioned, audit run, deployed + live-verified (2026-08-07; branch `launch-hardening`; decisions 0086/0087/0088).** Serving: **`api-public-00025-xaj`** + **`api-internal-00021-yam`** (candidate→smoke→flip; verifier 401 + platform 403 pre-flip). Gaps (a)(b)(c)(F3) shipped in api-core/api-public: atomic bundle_id ownership claim (one Firestore transaction spanning the session record + quota doc; foreign UID → 403; same-UID overlap stays last-write-wins by design), per-UID UTC-day mint quota (`UPLOAD_SESSION_DAILY_MINTS`=50, replays free, 429 + `resets_at` + `Retry-After`; new collection `upload_mint_quotas`), `expected_size_bytes` required/int/≥1/capped with `X-Upload-Content-Length` ALWAYS set (GCS enforces declared size), and semantic manifest validation (`roomstudio_api_core/manifest_validation.py`: client-shape path grammar, exactly-one bundle.pb, dup/count/size caps env-tunable; tier consistency deliberately stays at ingest). F1/F2 resolved won't-build with re-open triggers (0087). F5/F6 shipped (0086): `scenes.expire_at` TTL policy LIVE (terminal-failure scenes only — failed_invalid/failed_incomplete/dispatch-failed stamped now+`SCENES_FAILED_TTL_DAYS`(90d), CLEARED on revival, ready NEVER stamped; conversations cascade holds by construction), narrow outputs lifecycle (`scenes/*/frames/*/masks.npz` @180d — everything else is product or re-drive substrate), backfill applied (30 pre-existing failed scenes → expire 2026-11-05; stuck-scene ref `f077e9ed` untouched). **Post-flip: all four smoke modes green first-attempt** (happy-path `failed_invalid` 5.1s = the real-mint proof for the deployed iOS contract; skip-blob; duplicate-event; auth-rejection), new behaviors live-probed (missing-size 400, unknown-dir 400, cross-UID 403 + owner replay 200), and F6 live-verified on a fresh scene (`expire_at` = updated_at + 90d exactly; that scene `b049d665` is deliberately left as the mechanism's demo). IAM audit findings in 0088 (headline: perception-obj runs as the default compute SA with project Editor). Suites: **555 passed + 6 skipped** (was 492+6). New env knobs mirrored in both `infra/*.env.yaml`; `eventarc_setup.sh` gained `--scenes-ttl-only` / `--outputs-lifecycle-only`; `tools/backfill_scene_expiry.py` added.
- **Perception security + privacy pass — least-privilege runtime SA, person suppression, F6's perception half (branch `perception-security-privacy`, 2026-08-08; decisions 0089/0090/0091; revision `perception-obj-00036-l9l`, image `20260808-020128`, deployed to 100% and re-drive-verified).** **(1) 0088 finding 1 closed:** perception-obj — the service that parses UNTRUSTED user bundles on a GPU — no longer runs as the default compute SA with project `roles/editor`. `perception-obj-runtime@` owns it, granted exactly what the code reaches: objectViewer on captures (READ ONLY — it never writes there), objectAdmin on perception-outputs, `datastore.user`, logWriter+metricWriter, the secret-scoped Anthropic key, `cloudtasks.enqueuer` + actAs on `tasks-invoker@`, and a CUSTOM `perceptionFcmSender` role holding only `cloudmessaging.messages.create` (the predefined FCM admin role also grants topicSubscriptions.*, which fcm.py never uses). The grant list lives in `ensure_obj_runtime_iam()` in `deploy_perception.sh` — idempotent, runs every obj deploy, and IS the documentation; it replaces two scattered blocks that each resolved the SA by describing the LIVE service (so they would have granted the old SA forever). Deploy pins `--service-account` and asserts the served spec after. Collateral fix: `--platform` seeds the flags array so it is never empty — expanding an empty array under `set -u` is an unbound-variable ERROR on bash 3.2, which is what macOS ships, and the geom path would have died on an operator machine. Editor on the default compute SA is deliberately NOT removed (other things may ride it; that verification is its own pass). **(2) 0070's privacy gap closed:** `person` is now a SUPPRESSION-ONLY SAM 3 concept — seen, never shipped. New `privacy.py` owns the vocabulary (`PERCEPTION_SUPPRESSED_CONCEPTS`, default `person`), the partition, and the masks.npz layout (writer + reader side by side); both segmentation paths (legacy `_process_frame` and the census two-pass) partition suppressed detections out before indices are assigned, so `masks` stays index-aligned with objects.json and every per-object splat cache key survives. Suppression rides EXISTING mechanisms: the new `suppressed` union in masks.npz is ORed into the 0066 exclusion mask, and thin person-free evidence degrades through 0069's gates (no crops → family null, too few texels → albedo null → honest neutral). The one addition is at the evidence crops — a tile a person stood in is disqualified FOR THAT FRAME (checked inside `_rectify_crop` at crop resolution, because a crop is what reaches the vision model), falling back to a person-free frame or no crop. **The risk the fix introduces is pinned three ways:** end-to-end through both production paths (a person is segmented, never reaches SAM 3D, and appears nowhere in objects.json or the manifest — scanned as TEXT, since a leak into any field is a leak into api-public's scene_facts); an AST pin that every `segment()` call site takes `segmentation_prompt(...)` and partitions within 12 lines; and a Dockerfile assertion that suppression is ARMED in the image (`privacy suppression armed: ('person',)` in the build log — privacy.py is a deferred import, so a missing COPY would pass every probe and silently ship people again). Degrade locks pinned: masks.npz bytes identical when nothing is suppressed, pre-0089 files read as no-suppression, empty concepts reproduces pre-0089 exactly. **LIVE-VERIFIED on `f3d70236`** (re-driven on the new revision with the three person frames' objects.json+masks.npz deleted, splat caches retained): SAM found the person in all three (`suppressed=1 labels=['person']`), the shell consumed the unions (144131/201285/239244 px), and **`wall_03` — 0070's named plane — went #616161 → #50504f with observed 1.0000 → 0.6603: a THIRD of that wall's measured evidence was a person**; floor #b7b3a9 → #b7b4a9 (0.8872 → 0.8286, predicted 0.8285 offline); wall_04/05/06 byte-identical. **The degrade lock is confirmed in its strongest form:** the re-segmented frames' `masks` stacks are BIT-IDENTICAL to the ones they replaced (0 differing px) with the same labels in the same order, so the partition removed exactly the person and every splat cache hit — SAM 3's determinism here is now measured, not assumed. The offline probe (`outputs/privacy-0089/`, which reproduced every SHIPPED albedo EXACTLY before measuring) had under-read the effect: hand-drawn polygons let neighbouring frames re-cover the same texels. Its still-valid headline: **80.4% of the person's pixels were eligible as surface evidence before the fix** (existing object masks covered only 10–30%). **(3) Gap F6's perception half closed** (0086's recorded follow-up): `release_failed` stamps `expire_at` = now + `SCENES_FAILED_TTL_DAYS` (90d), `release_queued` CLEARS a pending stamp, `release_ready` never stamps. Deliberate DUPLICATE of api-internal's semantics, not an import — perception-obj's image installs packages/schemas only — with a test pinning this side's env var name and default days against api-internal's source text so the two cannot drift. All 31 existing failed-class scenes already carry expire_at (the hardening backfill), so this is future coverage, not a backlog. **(4) Mirror-as-mirror probed and CUT** (decision 0091). Suites: perception-obj **580 → 612**, root 555+6 and re-enqueue 18 unchanged (this lane touched no root package); ruff clean on every touched file except `test_receiver_repo.py`, which retains 2 pre-existing `assert False` (its other 10 errors were cleaned up in passing).
- **Reveal choreography REDESIGNED and BUILT — the room draws itself, then fills, then settles (branch `reveal-choreography`, commit `3c36055`, 2026-08-08; decision 0097; answers the RP-8 watch verdict in 0080).** Reading the shipped reveal found three defects where the operator had named one: pieces eased down 0.40 m on `1-(1-t)^3`, whose velocity is MAXIMAL at t=0 (= "comes down at high speed then slows as a spring"); surfaces had no transition at all (`visible` false→true, a pop, not a fly-in); and a uniform 650 ms per object ran the spike room ~22.8 s. The redesign is four movements on ONE easing curve — **smootherstep, zero velocity AND acceleration at both ends**, so everything begins and arrives at rest (echoing the single-spring rule in `components/ui/spring.tsx`): (1) the measured boundary DRAWS ITSELF — a pen traces the floor polygon's perimeter arc-length-exact with three dots riding the tip, verticals grow at each corner to the measured wall top, a top loop closes the box, all in the paper tone (gold stays light-semantic); (2) surfaces fade up IN PLACE, zero translation, swept around the room in the contour's own rotational direction while the contour dims away beneath them; (3) pieces settle — fade up while easing down **6 cm (was 0.40 m)**, fully present at 70% of the settle, largest first, the leading three introduced BY NAME and the rest arriving as a quickening wave (naming stays a courtesy, not a manifest readout; rooms under six pieces name them all); (4) a 450 ms beat of quiet before the guest speaks. Spike room ~22.8 s → **~11.7 s**, a 13-piece room ~9.4 s, with no single settle shortened — the time comes out of the queue. **The whole score is a pure function** in the new `web/src/lib/reveal.ts` (37 tests pinning ordering, overlap, the wall sweep, naming rules, the no-shell and no-floor degrades, reduced motion, and the settle curve's endpoint derivatives — including a pin on the ease-out-cubic velocity we moved away from, so the operator's finding cannot silently regress); `SplatViewer.tsx` PLAYS it and no longer decides it, and stays the only three.js module. A fourth defect found in-browser is fixed with it: `reveal` was an effect dependency and RoomStage turns it off at the end, so the renderer tore down and **the room the user just watched assemble blinked out and re-faded over 900 ms** — it is now read once at setup via a ref. Compositing invariant held: surfaces are transparent with `depthWrite:false` ONLY during their own ramp and are restored to exactly the 0066-proven configuration, and objects wait for the last wall to finish so no splat ever fades against a half-transparent wall. Reduced motion collapses to the finished room with no contour and no captions. No new dependency, no CSP change. Verified in-browser on `rp6g1-09684dde`, the roomplan spike, the v2 shell fixture, the no-shell grid degrade, and the `/room?bundle=!v3` product flow (clean console on all); suites **124 vitest** (was 87), lint + tsc + static-export build green. NOT verifiable here and owed to the operator's walk: pacing, and whether the contour reads as measurement rather than ornament — the automation browser throttles rAF.

- **Web platform pass — Google sign-in, account deletion, size/clearance facts, capture ceiling (2026-08-08; branch `web-platform`; decisions 0094–0096 + 0098 (the ceiling's number moved from 0097 — a parallel-session collision with the reveal redesign); MERGED, and SERVING since the 2026-08-08 ship-ops deploy — see that bullet for the revisions and the live probes).** (1) **Google is a second web read-provider** (0094): `signInWithGoogle` shares ONE body with `signInWithApple` (`signInAsReader`) so the never-create guard exists in exactly one place; the rule is provider-agnostic because the damage is (a web-born Google account permanently claims that identity, and the later iOS link hits `credential-already-in-use` against an account owning nothing). `AppleSignInButton` → `SignInPanel` (Apple first, then Google); four signed-out surfaces drop "Apple ID" for provider-neutral copy. Recorded asymmetry: iOS links Apple ONLY, so Apple's refusal is transient and Google's is permanent until iOS ships Google linking — the Google copy deliberately does not send the user to the capture app, test-pinned. (2) **`DELETE /account`** (0095): `services/api-public/account_deletion.py` names every per-user collection and prefix by hand (Firestore never cascades) — scenes, upload_sessions, upload_mint_quotas, conversations + its `turns` SUBCOLLECTION, `captures/{bundle_id}/**`, `scenes/{scene_id}/**`, then the Firebase Auth user. Two load-bearing decisions: the bundle-id UNION across scenes ∪ upload_sessions (different TTLs, each knows bundles the other lost), and the ordering GCS → Firestore → identity, because the plan is DERIVED from the Firestore records — deleting them first and failing would strand blobs with nothing pointing at them. Idempotent/resumable; 202 = "call again", never "corrupt"; counts-only body. The token IS the target (no uid parameter exists). Web: `DeleteAccountPanel` in the account menu with copy as pure tested functions in `lib/account.ts`, stating BOTH consequences (everything goes; the phone starts over with a new empty account). `PERCEPTION_OUTPUTS_BUCKET` is now production-required and startup-enforced. (3) **Sizes + clearance floors in `scene_facts`** (0096, FACTS_VERSION 2 / PROMPT_VERSION 2): sizes come only from `roomplan_box.dims` at box confidence high/medium and speak only the LONGEST dimension — splat extents are size-silent because a real meter-scale rug ships `extent_m_sorted` 0.46×0.29×0.005, and the shipped dims triple carries no recoverable axis semantics (descending-sorted in all six real boxes, so the bed's largest is a length and the wardrobe's a height). Clearances are rigorous LOWER BOUNDS (`d − (r_a + r_b)`, circumradius, any-yaw-true, positive-only, rounded DOWN, "at least"), between measured boxes only. Charter rules 3a/3b carry the epistemics and rule 5 stops claiming the walls "haven't arrived". **Voice evals re-run live: 10/10 at PROMPT_VERSION 2** — and they caught a real self-inflicted regression first (both new exemplars ended without an invitation, diluting a pattern all five originals carry). (4) **Per-UID daily capture ceiling** (0097): measured from 30 days of production `/process` logs — 51 requests, 44 >60 s, **32,021 GPU-seconds total**, mean long request 728 s, max 900 s (the budget ceiling binding) → ~1,500 GPU-s per capture at two requests. Default 12/day (`UPLOAD_SESSION_DAILY_CAPTURES`), 429 `capture_limit_reached`, charged once per bundle_id on FIRST claim (re-mints are free) and evaluated BEFORE the mint quota (a refused capture must not burn mint allowance). **Live change this session:** the preview origin joined `CORS_ALLOWED_ORIGINS` via an env-only revision (`api-public-00013-jx7`, same image digest as `00025-xaj`) — preflight from the preview channel now echoes ACAO, disallowed origins still 400. Preview channel redeployed with the new web build; production channel confirmed untouched (Site Not Found). Suites: root **600 passed + 10 skipped** (was 555+6), web vitest **106** (was 87), tsc + eslint + static-export build clean.

- **GATE B PASSED — the web product is live-verified against real captured rooms, end to end (2026-08-08; decisions 0051/0094).** The first browser sign-in to a real roomstudio account, and the first proof of the web surface against real data. Chain: operator enabled the Google provider → the operator's PURE ANONYMOUS UID `cHfMlULde2WO5x4i1kZ014VPPbI2` (measured: no email, no `provider_data` — which is why Gate B was never merely enrollment-blocked) got a verified email set via the Admin SDK, **UID unchanged**, the same transition the iOS link performs → Google sign-in on the preview channel auto-linked to that existing UID under `allowDuplicateEmails: false` and returned `isNewUser:false`, so 0094's never-create guard passed it through → the operator's real rooms listed and one rendered. **Server-side confirmation:** `provider_data` on that UID is now `['google.com']` (sub `109757950928…`, email matched), uid byte-identical before and after, real `last_sign_in_timestamp`. **Read path verified live from the preview ORIGIN** under a real idToken for that UID: `GET /scenes` → 7 real rooms all `ready`, ACAO echoed; `GET /scenes/a7e073ae/assets` → manifest v2 (25 objects / 10 placed) + shell v3 `roomplan` 13 walls + 22 signed splat URLs, one fetching 34 MB at 200. Incidental finding, useful for future browser work: the Browser pane BLOCKS popups (`window.open` returns NULL) with **zero CSP violations** — so the popup leg must be run in an ordinary browser, and the 6(a) CSP work is independently confirmed sound.

- **Release-hygiene lane — legal documents, CI, synthetic photo fixtures, tracker retirement, repo cleanup (2026-08-08; branch `release-hygiene` merged to `main`; decisions 0099/0100/0101).** The unglamorous set nothing else owned. **Legal:** `/privacy` and `/terms` are live routes (static-exported, so App Store review has a URL), reachable from a new `SiteFooter` (stands down on `/room` like SiteNav) and from the account menu. Both are DERIVED FROM THE PIPELINE, not a template, with the source trace named in a comment beside each claim: what a capture contains (proto), that it carries **no location and no audio** (the app requests neither), the anonymous-then-linked identity model, storage in `asia-southeast1`, the two Anthropic calls stated SEPARATELY because they send very different amounts of the room (material inference transmits real image crops; the conversation transmits derived text and never a pixel), the real retention windows (24 h captures / 90 d failed / 7 d sessions / 180 d intermediates), deletion following `account_deletion.py`'s map, and person suppression stated without rounding up ("we do not claim detection is perfect"; pre-0089 rooms may still carry a person's measurement). Terms §7 names the measured failure modes — hedged single dimensions, lower-bound clearances, the wardrobe-as-refrigerator mislabel, mirror depth — and tells the user not to buy furniture or cut material against them. Entity/jurisdiction/contact/address/liability-cap are unfilled `<Blank>` slots, gold-marked in the page and greppable in source; each file carries an OPERATOR REVIEW NOTES block. **CI:** `.github/workflows/{python,web,ios}.yml`. Python is three jobs because the root `conftest`/testpaths exclude perception + re-enqueue AND because perception's `numpy<2` cannot share an environment with schemas' 2.x; deps come from `tools/ci_deps.py` reading each pyproject (the services are flat modules and NOT pip-installable, so hardcoding was the alternative). Web runs vitest + tsc + eslint + the static-export build. **iOS is `workflow_dispatch`-only on purpose (0099): the sole scheme bakes `RUN_INTEGRATION_TESTS=1`, so the 4 live tests would charge the per-UID daily CAPTURE ceiling (12) on every push and lock the operator out of scanning.** It ships the offline subset via `-skip-testing` and has never executed. **Photos:** `tools/make_synthetic_photos.py` renders nine views FROM `build_test_bundle`'s own `synthesize_pose` (imported, so they cannot drift) through the exact recorded intrinsics — the fixture is now internally consistent, which the real photos never were. **Cleanup:** 38 merged branches deleted, 3 stale worktrees removed (`roomstudio-0074` deliberately left — device lane + `rp7-screenshots`), `tasks.md` retired after checking all 9 survivors against the code (5 had shipped, 3 were better recorded elsewhere, 1 carried forward). **Also:** `AccountMenu`'s pre-existing `react-hooks/set-state-in-effect` error fixed at root (every close path routes through `closeMenu()`); `web/AGENTS.md`'s JSX-entity quirk sharpened — it fires after a preceding ELEMENT too, not only a `{expression}`, and is invisible to eslint/tsc/build (caught in-browser as "room.The app keeps"). Verified: root 600+10, perception 612, re-enqueue 18, **web 143** (the merged count — 106 and 124 shared an 87 baseline), lint/tsc/build green, both legal pages walked in-browser clean-console. **Operator checklist executed 2026-08-08** (`7d0b733`/`4208bad`): all 13 real legal slots filled — operator is **Utkarsh Singh, an individual**, contact `23singhutkarsh@gmail.com`, jurisdiction laws of India / courts at Kanpur, liability cap ₹10,000, effective 8 August 2026; the registered-address slots were DELETED rather than filled (GDPR Art. 13(1)(a) and DPDP are satisfied by the email, and the alternative for an unincorporated operator was publishing a home address — Germany's Impressum rule is recorded in both files as the single re-open trigger). Verified in a RENDERED browser DOM, not source, per `web/AGENTS.md`'s JSX-entity quirk. **Still owed: an Indian lawyer must read Terms §9–§11** — now specific rather than general, because Consumer Protection Act 2019 §2(46) can void the §11 cap against a consumer, and while the service is free the cap is the whole clause (the "greater of what you paid… or ₹10,000" has an always-zero first limb). The Firebase **web API key that the web app ships is restricted**: 2 APIs (identitytoolkit, securetoken — down from 27) and 4 EXACT referrers — but a SECOND browser key exists on the project and is still UNRESTRICTED (no referrer restriction, 27 APIs). **Closing that gap will BREAK the live-authed-check path every recent api-public deploy has used** — the restricted `web/.env.production` key is referrer-locked to 4 exact origins, so a server-side Identity Toolkit mint gets 403 (the restriction working as designed), and the unrestricted key is what the check mints with, fetched via `gcloud services api-keys get-key-string`. Whoever closes it must ship a replacement first — a service-account-minted custom token, or a CI-only key — rather than discovering the breakage mid-deploy. It is published nowhere (absent from `out/` and from every tracked file) and is what the smoke tool uses, so this is incomplete hardening rather than an exposure — found 2026-08-08 by the final-deploy session, and worth closing, probed both directions (allowed → 200; attacker site, other `*.web.app`, and no-referrer → 403). That step caught a real defect — Google's referrer wildcards only replace a WHOLE subdomain label, so `roomstudio--*.web.app` matched nothing and read back as correct while preview-channel sign-in was broken; **a brand-new preview channel will 403 until its exact URL is added**. The iOS key is deliberately untouched.
- **Ship operations — the deployed web renders real rooms; account deletion + capture ceiling SERVING; re-sign extended (branch `ship-ops`, 6 commits, 2026-08-08; decisions 0102/0103).** **The deployed-render blocker was a chain of THREE gates, each hiding the next** (0102): `connect-src` blocking Spark's WASM `data:` fetch (`bfa7eb5`), `script-src` blocking WASM COMPILATION under CSP3 (`4c71c38`, `'wasm-unsafe-eval'` — precisely scoped: WASM compiles, `eval("1+1")` still throws EvalError), and the outputs bucket having NO CORS policy at all (`9bcccfe`, section (6) of `eventarc_setup.sh`; grants no access — signed+listed → 206 + ACAO, unsigned → 403, signed-from-unlisted → no ACAO). Evidence on the DEPLOYED origin: Spark's 217 KB WASM at 200, blob worker spawned, a real 34,043,936-byte splat from `a7e073ae` fetched cross-origin over a V4-signed URL and rendered as Gaussians, zero `securitypolicyviolation` events. **Why every prior check missed it: `next dev` applies no hosting headers, and CORS is browser-only — Gate B's "signed URL fetches 34 MB at 200" was curl, which sends no Origin and checks for no ACAO. A server-side 200 was never evidence about a browser.** **`api-public-00030-def` serving** (image `20260808-162053`, candidate→smoke→flip): all four smoke modes green on the final revision; the capture ceiling live-probed (13th capture → 429 `capture_limit_reached` + `resets_at` + `Retry-After: 47417` — ≫60 s, so the iOS branch takes the name-the-reset-time path, not sleep; re-mint of an already-charged bundle → 200, charged once per bundle_id). **The deletion probe (throwaway UID, the operator's account never touched) verified every target in 0095's map seeded-then-empty** — scenes/upload_sessions/mint_quota/conversations 1→0, turns 2→0, captures across 2 bundles →0, outputs →0; `conversation_messages: 2` proves the non-cascading subcollection and `files: 5` proves both halves of the bundle-id union. **It also found two defects invisible to unit tests, which fake exactly the two clients that failed** (0103): the runtime SA could not delete the Firebase Auth user, so the endpoint 500'd AFTER erasing everything (account unrecoverable, login still working) — fixed with a custom `apiPublicIdentityDeleter` role holding exactly `firebaseauth.users.delete`, the 0090 precedent; and a second call 500'd on a perfectly-deleted account against its own documented idempotency — reachable in real use, since an ID token stays valid ~1 h after its user is deleted (`test_is_idempotent` was green throughout because its fake could not raise). **iOS re-sign genuinely renewed**: binding 2026-08-12 08:22 → **2026-08-15 07:11 UTC** (+3 days, the maximum under the 7-day personal-team cap), proven by `CreationDate` read out of `embedded.mobileprovision` inside the exact installed `.app` — the field that distinguishes real re-issuance from the silent profile-reuse that defeated the previous attempt; `project.pbxproj` restored pristine, the workaround never touched a commit. **`api-runtime@` deleted** — it held THREE bindings, not the one 0088 recorded (also project `cloudtasks.enqueuer` and `datastore.user`), all verified unused across four services, the Eventarc trigger and the task queue. Suites: root **602**+10, web 143 (iOS not re-run — no iOS source changed).

- **Server residue lane — CI hermeticity fixed, the mint contract's re-mint path built, the composer's `confirming` state settled (branch `server-residue`, 2026-08-08; decisions 0116/0117; SERVING as of api-public-00032-has, NOT pushed).** (1) **The first CI run this repo ever executed went red, and it found a real defect** (run 31259471685: 12 failed / 590 passed / 10 skipped, all in `test_upload_session.py`, `DefaultCredentialsError` → 500 → `assert 500 == 200`). Cause: `public_server` binds `gcs_mint_resumable_uri` by from-import, so the test's `patch("roomstudio_api_core.upload_session_repo.gcs_mint_resumable_uri", …)` rebound a DIFFERENT name and never took effect — the handler kept calling the real minter, and the suite had been quietly resolving the operator's ambient ADC for months. Fixed by injection (`public_server._mint_uri_fn`, the seam pattern `UploadSessionRepository` already uses); a second copy of the same defect lived in `TestUploadSessionSemanticValidation._post`, whose one valid-manifest case was the only test there that reached minting. Supplying credentials to CI was rejected as the non-fix it is. Pinned by a matched pair (handler returns 200 when `google.auth.default` raises; the unpatched seam still vends the REAL minter — either alone can be satisfied by a change that breaks the other). Also settled from that run: perception **passed on Linux**, so the numpy-1.26 `test_closer_frame_outweighs_far` failure was macOS/Accelerate-specific. Details + the standing rule in the new "Python test policy" section. (2) **`force_remint`** — decision 0084's named un-defer trigger, see the re-upload bullet below. (3) **The composer's `confirming` phase is REACHABLE and stays** (0117): reproduced end-to-end in a real browser with a real pointer click against a harness that streams deltas and then ends the response with no terminal event — `submitting` → `confirming` in ~1 s, the user's words retained, "Making sure that reached the room…", composer stood down, the 4 s `client_msg_id` refetch loop running, and the flip to `reoffer` at the 150 s window. Suites: root **620 passed + 10 skipped** (was 602), verified BOTH with ADC present and with ADC unavailable; re-enqueue 18.
- **iOS walk findings CLOSED — the 20-minute stall diagnosed as OS behaviour, the Live Activity's terminal gap fixed, the recoverable count restored (branch `ios-walk-findings`, commits `74ad61c`/`94094ea`/`a465830`, merged 2026-08-08; decisions 0110/0111; suite 463 → **482**).** **The stall is Apple's documented background-launch rate limiter, not a defect of ours** (0110): a task started while the app is backgrounded waits out a delay that GROWS with each background relaunch, resets to zero when the user foregrounds, and is treated as `isDiscretionary` regardless of the flag. The decisive evidence was already sitting in 0085 as an unread controlled comparison — same run, same device, same minute: 479 blobs whose tasks were created by the FOREGROUND process uploaded with no app process alive at all (25 → 504 while force-quit), while the one task created by the background-relaunched process stalled 20 minutes. Only the creating process's state differs, which eliminates a broken transfer mechanism, an app-must-be-alive theory, and any defect in the enqueue path. No scheduling flag exists to fix it (`isDiscretionary` is already false and documented inert here; `earliestBeginDate` only moves tasks later; `priority` is an HTTP/2 hint), and it is not bad luck that it hits `bundle.pb` — decision 0040 requires that blob enqueued LAST, so it is structurally the task most likely created by a background process and the only one whose absence means the capture does not exist server-side. The fix is therefore honesty: the wait screen is foreground-only and looking at it is the documented cure, so the Lock Screen was the only surface that could lie — and did. **The Live Activity's terminal gap** (0111): `onBundleComplete` published nothing, and the background path can only emit `.sending`/`.paused`/`.failed(.upload)`, so with the app closed the last word was `.sending(N, N)` forever — "Sending your room 331 of 331" — because `staleAfter` only dims, `terminalDwell` never applied, and `end()`/`noteWaitScreen()` are foreground-only; `reconcileOnLaunch` then adopted the restored bundle's card correctly and had nothing to say. Fixed with `.finalizing` at `enqueueBundlePb` and `.queued` at `onBundleComplete`, plus a no-reopening merge rule so a late `.sending` never lands on a stage past the upload (the old sticky rule guarded terminals only). Verified by driving REAL ActivityKit and screenshotting. **The recoverable count** is carried through the routing table it died in (`ScenePoller → PollSnapshot → WaitScreen → FailureView.Kind`); only the count crosses into routing, the paths stay out, and per 0084 the honesty constraint is pinned as an invariant — "N files need re-uploading" is exactly the promise that must NOT be made while the server half is undeployed. Device build + install worked unattended over WiFi; `-allowProvisioningUpdates` auto-created the second App ID (`…RoomStudioCapture.LiveActivity`), closing the recorded blocker. Effective re-sign clock unchanged at **2026-08-15 07:11 UTC**.
- **The landing hero is the reveal, not a room (branch `landing-hero`, 2026-08-08; decision 0122; BUILT + committed, deployed to the PREVIEW channel only, NOT merged).** `/`'s hero was designed around a live demo room that existed on no deployed origin (`DemoRoom` loaded gitignored `/dev-fixtures/`), so every visitor met the degraded state. It now plays the first two movements of the 0097 choreography — the measured boundary drawing itself, then the surfaces materializing in place — against a real capture's GEOMETRY, with the copy landing in the quiet beat the score already provides. **No choreography code was needed for the cap:** a shell with zero objects already ends the score at the surfaces (`nothingToPlay` wants BOTH empty), pinned against the real fixture so adding objects breaks loudly. Fixture: scene `ce68e24f`'s shell.json v3 verbatim at `web/public/hero/room.json`, **3,557 bytes**, chosen on how its contour traces AND constrained hard by 0089 — `person` was absent from the SAM vocabulary pre-0089, so a pre-0089 shell's zero person-detections prove nothing; that rule disqualified the spike room `a7e073ae` (best contour, 10 corners with the entry alcove, but segmented 2026-08-05 and baked 18:39Z on 08-07, before revision `perception-obj-00036-l9l` at 21:27:53Z) and left `ce68e24f` as the only post-0089 shell with coherent geometry. `HeroRoom` guarantees the copy lands even when the room cannot play at all (fixture 404, renderer failure, reduced motion, or a 6 s ceiling). **Deployed-origin verified** on the preview channel: `/hero/room.json` 200 at exactly 3557 B, WASM + blob worker 200, room rendered, **zero `securitypolicyviolation` events on a clean load**, no page errors, no CSP change needed. `?hero=b` builds the one-piece A/B behind a trivial switch; its splat is gitignored AND hosting-ignored (`next build` copies `public/` into `out/` gitignore and all — caught in this session's build output with the 44 MB bed sitting one deploy from a public origin). Suites: web vitest 143 → **161**, lint + tsc + static-export build green. DemoRoom deleted; the "See one first" CTA went with it.
- **0085 walk: four fixable classes shipped, and rotation's evidence limit MEASURED (branch `placement-quality-3`, 2026-08-08; decision 0104).** Every number below was produced offline through the PRODUCTION fusion path after a trust gate replicated all 14 shipped box objects bit-for-bit (margins, resolutions, view counts, cloud-point counts identical on all three scoreable rooms). **The charter's reframe is refuted by measurement, and usefully:** the 0.10 margin gate does NOT abstain too often — against known truth its sub-gate argmax is right 1 time in 3 (it would fix rp7's bed at 0.0201 and break rp6g1's chair at 0.0738, net zero, while breaking 0081's pins), and 4 of the 5 abstentions already ship correct defaults. **Four attacks on the evidence, all refuted:** a SAM-free box-volume cloud unioned over every frame of the capture COLLAPSES margins (0.2384→0.0012, 0.3383→0.0024) and flips the spike bed to the wrong winner; per-view scoring aggregated across views breaks 2 known answers and fixes 0; appearance on the 180° partner prefers the WRONG answer on both signs the walk proved wrong (−0.0077, −0.0014 = noise) and prefers the partner at +0.1178 on a chair the operator blessed; a truncation-direction prior is not separable (wrong cases +1.79/+1.45 vs ok cases −1.75…+1.86). The mechanism is now measured rather than assumed: **the instrument works because a single-view cloud is a thin surface CORRELATED with the splat's own truncation** — more evidence of the wrong kind destroys the signal. So the walk's box-rotation failures separate into ONE abstention shipping a wrong default and TWO *resolved* boxes with a wrong 180° sign; `facing_flag` stays flag-only for a measured reason (promoting it would break the rp7 chair). **Shipped:** (1) **splat clipping** — measured, 10 of 14 box objects overflow their measured box past 5 cm, worst 0.44 m (the bed the operator saw intersecting the table and the chair, in two rooms). `build_box_object` declares an additive `splat_clip` volume (the box + `PLACEMENT_SPLAT_CLIP_MARGIN_M`=0.10, chosen from a sweep: inert on 8 of 14, trims exactly the four gross overhangs; at 0.0 m a table with a 3 cm overhang loses 60% of its points) and the viewer declines to render outside it — position/rotation/scale untouched, so 0082's "don't falsify a measurement to hide a splat artifact" holds. (2) **support surfaces take the RENDERED top** — the walk's lamps and TV were already resting exactly on measured box tops; the spike speaker sank 3.3 cm into a table whose splat stands 0.033 m proud of its box, the same number to the millimetre, which is why classes 2 and 3 are ONE defect. Contact height is now floored by the measurement and capped by the clip margin, splat-derived surfaces are added where no box covers, a measured box surface always wins, and surfaces are built once so the pass is order-independent. (3) **label scale floor** — the 0.245 m "television" demotes to honest inventory (never a rescale; there is no measurement to rescale to), which also releases the real monitor the dedup had been collapsing into it. Only `tv`/`television`: floors for bed/sofa/door/wardrobe were drafted and CUT when the only evidence they produced was demoting legitimate test fixtures. (4) **two vocabulary gaps** — RoomPlan files a nightstand as `storage` where SAM says `nightstand`, so rp7's nightstand shipped BOTH as an unmatched box and as a free splat (and the lamp and TV on it inherited the disagreement); and a box object is labelled with its RoomPlan CATEGORY, which the cross-label dedup groups did not speak, so `storage` could never collapse against `desk`. Plus `SHELL_MATERIAL_MIN_CONF` 0.6→0.75 (decision 0100's recommendation) as the default, so offline re-drives and production agree. **The Spark clip is browser-proven** on the real spike bed: a near-zero clip volume makes the bed vanish with every other object untouched, the real 0.10 m volume brings it back, clean console. **DEPLOYED and LIVE-VERIFIED: `perception-obj-00037-sd9`** (image `20260808-200124`, digest-checked against the push; runtime SA `perception-obj-runtime@` reasserted; `/health` 200. NOTE the revision is numbered BELOW the existing `00038-ses` — the non-chronological quirk in Conventions; image tag and traffic split are the authorities). Warm re-drive of ALL FOUR walked rooms, one round each except the starved one, 12/12 frames, `budget_stopped=false`: **spike 25/10/4**, **rp7 17/10/2**, **rp6g1 21/11/3**, and **rp6g2 45/16/0 `budget_stopped=true` on BOTH rounds** (the predicted no-change, now confirmed twice — though it does pick up 3 clips, since `build_box_object` computes them even with scoring disabled). Every live value reproduced the offline prediction to four decimals (bed clip 0.3077 vs 0.308, table 0.0224 vs 0.022, speaker snap +0.1810 vs +0.181; rp7 clips 0.1655/0.2768/0.0179; rp6g1 table clip 0.1642). rp7 is where all four mechanisms fire at once: `associated_boxes` now includes `box_05` (the nightstand associates for the FIRST time), the free `desk` splat dedups into it, the 0.245 m tv demotes and releases the real monitor which snaps +0.101 onto the desk, and the lamp snaps −0.062 onto the nightstand box. rp6g1's lamp is the cleanest single demonstration of the class-3 fix — same object, same room, v1 moved it −0.013 m and v2 moves it **+0.078 m**, because v1 aimed at the measured box top and v2 aims at what is rendered. **The material gate raise does NOT retro-apply** (found by checking, not assumed): all four rooms already carry shell.json v3, so `--shell` takes the version-gated redelivery noop and their families stay as baked; 0.75 governs the next BAKE, and forcing a re-bake was declined because 0070 requires reference-room re-adjudication first. **All four `/viewer?fixture=` dirs are RE-STAGED** from the live post-re-drive manifests with fresh `?labels=1` sidecars (spike 13W/10O, rp7 4W/10O, rp6g1 4W/11O, rp6g2 4W/16O), and two were browser-walked clean-console — rp7's unrenderable line now reads `tv (implausible_scale_for_label)`, so the demotion is visible as honest inventory rather than a 24 cm television in the air. Suites: perception-obj 612 → **643**, root 602+10 unchanged, web vitest 143 → **147**, tsc + eslint + ruff clean on touched files.


- **Final deploy — `force_remint` serving, the merged tree CI-green on Linux, the production flip written and pre-verified (branch `final-deploy`, commits `897fdd2`/`32ef1ee`, 2026-08-08).** Serving **`api-public-00032-has`** (image `20260808-224028`, candidate → smoke → flip; all four smoke modes exit 0 post-flip). **`force_remint` live-verified 19/19 on the serving revision** using throwaway anonymous users so the operator's capture ceiling was never touched: replay without the flag returns the SAME URIs; a session consumed by a real finalizing PUT then re-minted WITH the flag returns fresh URIs for every path and the fresh URI accepts a real upload; the response shape stays `{relative_path, session_uri}`; a foreign uid with the flag gets 403 (not a skeleton key); stored URIs survive a refused attempt; and force_remint of an already-claimed bundle succeeds at the capture ceiling, proving no second capture is charged. **Deployed-client compatibility proven LIVE, not assumed** — the request body was transcribed from the deployed Swift (`UploadSessionRequestBody` = exactly manifest + fcm_token; `ManifestBuilder`'s real subdirs) and a full 9-path LIDAR_ROOMPLAN manifest returns 200 with both `fcm_token: null` and a real token; the session's first attempt failed on an INVENTED `frames/*.depth` path, and reading the real builder is what fixed it. **api-internal deliberately NOT redeployed, proven rather than assumed**: exactly one api-core file changed since `00021-yam` was built, api-internal calls exactly two methods from it (`get_user_id`, `get_fcm_token`), neither appears in the diff, both are byte-identical across the trees, it subclasses no ABC whose signature changed, and its 119 tests pass against the new api-core. **Two pre-flight findings fixed** (`897fdd2`): `conftest.py` hand-restated `pyproject.toml`'s testpaths and the copy had DRIFTED, so a run from the repo root collected 630 while a run from anywhere else collected 626 — it now derives the list from `config.getini("testpaths")`; and the numpy question is settled as NOT OUR BUG — macOS/arm64 + Accelerate reproduces the `IndexError`, Linux CI passes on the same numpy, and replaying the failing call's guard logic 50× gives max index 99 into a 100-row mask every time while the real call on those same inputs indexes 100, so the bounds guard is provably correct and the BLAS is miscomputing. **The workflow comment now says explicitly NOT to clamp the indices** — that would convert intermittently corrupt numerics into silently corrupt pixels, and the crash is the better failure mode. Preview channel redeployed and re-verified (hero `room.json` 200 at exactly 3557 B, room renders, zero `securitypolicyviolation`, the `hero/*.ply` + `dev-fixtures/**` 404 lock holding by request); **production channel confirmed untouched** (`roomstudio.web.app` still Site Not Found, live channel still on its 2026-05-27 release). `infra/PRODUCTION_FLIP.md` records the flip with all seven TECHNICAL preconditions verified live rather than listed.
- **The render payload is MEASURED and the P0 is diagnosed — network-bound, decisively (branch `render-payload-p0` merged 2026-08-09; decisions 0123/0124; instrument at `tools/measure_render_payload.py`).** Reference room `a7e073ae` — 10 splats, **275.8 MB, 4,056,000 Gaussians at 68.0 B/Gaussian** (SH degree 0, 17 float32 each). Network: **25.4–56.1 s** across four independent clients (39–87 Mbps aggregate; per-connection capped 6.8–11.2 Mbps; TTFB 0.77–2.63 s). Parse and GPU, through the SHIPPED renderer (Spark 2.1.0 + three 0.185 out of `web/node_modules`, driving SplatViewer's exact `new SplatMesh({url})` + `await Promise.all(initialized)` shape): all 10 splats **0.55 s**, 2.41 s when fetch and parse are timed separately, first frame incl. GPU upload 51–81 ms, steady frame 0.2–0.3 ms, finished-room JS heap 83 MB against a 4,295 MB limit. **Parse is under 1% of the wait — the renderer was never the suspect it looked like, and fewer Gaussians is not the lever for this symptom.** Bounds on the fix, also measured: generic gzip buys only **1.36×** (float32 is high-entropy, so serving the existing PLYs compressed — the cheapest imaginable fix — is worth about a third), quantization to an SPZ-class layout buys **~4.3×** (275.8 → 64.1 MB) at 0.006 mm position error, and **~10% of every payload is downloaded then discarded** (4.1% outside the 0104 `splat_clip` volume, 5.8% below alpha 0.02). Two more hypotheses died with measurements attached, so nobody retests them: signed-URL churn does NOT remount the viewer (the effect key at `SplatViewer.tsx:196` does contain the signature, but the shell-grace refetch completes before mount), and a browser's single HTTP/2 connection to `storage.googleapis.com` is not a throttle — the session set out to confirm it was and measured it **fastest of all four clients** (25.4 s, beating ten separate connections). The `/assets` serial-signing finding is real but measured at **0.9–2.6 s, ~1%** — recorded and deliberately NOT fixed, with its revisit trigger, in 0124. **Coordinator finding on top of the report (2026-08-09):** Spark 2.1.0 already exports `SpzReader`/`SpzWriter`/**`transcodeSpz`** and carries `SPZ = "spz"` in its file-type enum — the shipped renderer reads the compressed format natively and ships a transcoder, so converting the nine existing rooms is a TRANSCODE, not a re-bake, and does not trigger 0070's re-adjudication rule.
- **Conversational redesign DESIGNED — both probes measured, and the coordinator's framing refuted twice (branch `conversational-redesign-design` merged 2026-08-09; decisions 0129 probe 1 / 0130 probe 2 + handback / 0131 the spec / 0132 tools / 0133 scope; 0134 unused).** **Probe 1 — photoreal relocation SURVIVES.** Run on the real spike room (`a7e073ae`, 10 placed objects, 14-plane v3 shell) through the real Spark renderer plus per-face measurement over nine objects across two rooms: a bed translated +1.9 m X / +0.9 m Z reads as a bed in its new place, and spun 180° in place still reads correctly; the unclipped cabinet `obj_000` survives a full turnaround INCLUDING a straight look at the face that stood against the wall. Ghosts / outlines / footprints are NOT needed as the primary language. **Two claims in the coordinator's scoping brief were refuted with measurements** (annotated on the brief so no build session is misled by it): removal does NOT expose unobserved surface — 0069's parametric shell has no memory of what stood there, so removal is visually FREE; and a moved object does NOT turn a class-6 truncated face outward — truncation makes the splat SMALLER than its box and stays hidden. **What actually shears is the 0104 `splat_clip` cross-section**, cutting 30.7% of the spike bed, 28.7% of rp7 storage, 16.5% of the rp7 chair (vs 2.1% / 1.8% on two others) — an independent re-measurement that CONFIRMS 0104's own figures rather than contradicting them (0104 reasons in point fractions: "at 0.0 m a table … loses 60% of its points"). With the clip off at an identical camera the dithered see-through mush vanishes and the bed renders complete with drawers and fabric folds. The mechanism is a splat/box axis MISMATCH, not phantom mass alone: the spike bed's box half-Z is 1.079 m while the splat reaches ±0.83 m — 0.25 m SHORT at each end while overshooting ±X by 0.46 m, so the clip correctly removes real overshoot and then slices body mass on the other axis. That ties the visual defect directly to the unresolved rotation problem (`splat_axis_resolved=false` on every box placement, 0080/0104). Baked light measured second-order (side-face luminance ratios 1.02–2.55, median ~1.4; nothing read wrong at any azimuth). **Probe 2 — R3F reconciles Spark; the escape hatch is not needed.** SparkRenderer inside R3F's loop, correct scene graph from JSX, 8,112,000 tris in 1 call; a state-driven move gives 10→10 constructions / 0 unmounts / same uuid / numSplats preserved; a drop is exactly 1 unmount + 0 constructions and a restore exactly 1 construction; ~35 lines of glue. Three conditions, each found by measuring its failure: memoize args (a fresh literal rebuilt all ten meshes on one move, invisible above the host layer), key on identity (index keys remounted 8 unrelated objects on one removal), `extend(THREE)` in v9. **The two design answers worth carrying:** the spec is a manifest SIBLING holding `measured_transform` beside `proposed_transform`, keyed on `roomplan_box.identifier` (object_ids renumber across re-drives), rendered through the existing `PositionedSplat` contract, with the measurement surviving on screen as the 0097 contour; and **the guest cannot take coordinates** — charter rule 5 means it cannot see walls, facings or footprints, so a `move(x,y,z)` tool would invert the honesty contract. It states INTENT; a server solver owns geometry and refuses when it cannot ground one.
- **Conversational redesign stage 2 BUILT — the guest has hands, and the measurement stays beside every proposal (branch `stage2`, 5 commits, 2026-08-09; decisions 0131/0132/0133 executed, 0135/0136/0137 recorded; NOT deployed, NOT merged).** **The blocker first (0133):** `SplatViewer`'s renderer key contained each splat's position, so changing a transform tore the renderer down and re-downloaded the room — 275.8 MB / 25–56 s before the compressed tier, 47.2 MB after. The key is now STRUCTURE ONLY (which files, which clip volumes, which shell planes, badges, outlines), extracted to `lib/viewerKey.ts` as a pure function with the structure-vs-placement rule written down and pinned; placement rides its own effect. `splats`/`shell`/`labels` are no longer effect dependencies at all — 0133 asked for upstream memoization, but removing the dependency makes correctness not rest on a caller remembering. Two latent bugs went with it: a rotation- or scale-only change had NO code path and failed silently, and `hidden` now renders a removal as a hide per 0130's measurement that unmount/remount is a full re-download. **Server:** `room_geometry.py` (the solver's world — boxes, floor polygon, interior-wound walls with openings — deliberately NOT the guest's), `spec_solver.py` (five closed relations → a transform or a refusal), `design_spec.py` (the document; every entry carries `measured_transform` beside `proposed_transform`, and an entry that cannot is unrepresentable — `from_doc` returns None), `guest_tools.py` (`propose` + `revert`; the schema has no numeric field anywhere, pinned, because a numeric tool field is a coordinate waiting to happen). Routes `GET`/`DELETE /scenes/{id}/design_spec`; new payload-free SSE event `arrangement` (the room changed, refetch — pushing transforms down the conversation stream would make the composer a second source of scene state). Charter → **PROMPT_VERSION 3**: rule 6 rewritten from "eyes, not hands", plus 6a (a refusal is a real answer), 6b (suggest, never act), 2a (placements are verbatim like numbers), 10 (a rearranged room's facts are conditional — say "would"); five exemplars replace the now-false one, all ending on an invitation. Facts are re-derived from the proposed arrangement so a distance is a distance in the room on screen; an empty spec costs exactly what stage 1 cost, pinned. **Web:** `lib/designSpec.ts` overlays the spec onto `assembleScene`'s output as a pure pass — `PositionedSplat` unchanged, so the renderer never learns a proposal exists — joining on the spec key recomputed from the manifest, never on labels (two chairs share a label). The measurement survives on screen as its footprint, drawn in the 0097 contour's paper tone. Chrome states what changed in counts with "back to measured" one control away; the inventory says placed/moved/taken out in words. Mock triggers `!move`/`!remove`/`!revert` make the whole loop walkable offline. **FOUR THINGS WERE MEASURED, three of which changed the design.** (1) The spec key HOLDS: all 9 `roomplan_box.identifier` values in the spike room's live manifest — after RP-8's warm rounds AND the 0104 re-drive — are byte-identical to the capture's own `captured_room_built.json` committed 11 days earlier; 0131 inferred this, it is now confirmed. (2) **The yaw convention is the opposite of what the viewer uses** (0135) — two independent instruments across all four walk rooms, 14 of 15 boxes at exactly 0.0° alignment and 0.000 m flush-edge spread under one sign and 19–27° under the other; `SplatViewer` builds every `splat_clip` with the other sign, so the clip sits ~2θ from the mass it cuts. NOT fixed: it re-renders every existing room and 0104 adjudicated the clip by eye. (3) **The measured rooms fail an ideal constraint set** (0136) — 4 placed boxes leave the floor polygon, 4 pairs already overlap (a chair under a table is physically correct), so a proposal is held to the standard the MEASUREMENT meets, never a better one. (4) "Nearest wall" was the wrong rule: the spike room's nearest wall to the bed is a 0.35 m stub. Solver over all four rooms: **318 solves, 261 grounded, 57 refused, wall relations 21/21, 3.9 ms each.** **Walked in a real browser** (mock mode, real pointer + key events): the sofa moves, the outline draws where it was measured, the chrome and inventory update, "back to measured" restores it, zero console errors — and the decisive test, a tag set on the live canvas BEFORE a transform change survives it, so a placement change never tears the renderer down. (A change that adds or drops the measured OUTLINE does rebuild it — the outline is in the structural key by design; measured 2026-08-13, see the What-does-NOT-work bullet.) That walk also caught the mock promising a footprint it never drew. Suites: root **724**+10, web **204** (was 189); tsc/eslint/static-export build clean. (api-public alone is 346 + 10 skipped WITH `web/public/dev-fixtures` staged, 316 + 40 without — the fixture-backed tests skip when it is absent, so quote which.)
- **Perception names the up-axis, and new captures get the compressed tier (branch `perception-emit` merged 2026-08-09; decision notes OWED — block 0142–0145).** **(1) `roomplan_box.extent_axes_m` = `{up_m, horizontal_m (descending), up_tilt_deg}`**, additive on every box object, placed or not — 0096's named trigger and board 10(a)'s lead item. **The premise was wrong and correcting it made the field better-founded:** 0096 recorded `dims` as "descending-sorted in all six real boxes" and concluded axis semantics were unrecoverable, but `dims` is Apple's local (x, y, z) order with index 1 the vertical extent — the descending triple 0096 saw is `extent_m_sorted`, a DIFFERENT field. Verified independently by the coordinator on the live manifest: 5 of 8 sampled boxes are not descending, and `dims[1]` is a plausible height for every category (bed 0.611, wardrobe 1.912, chairs 0.682/0.957, tables 0.466–0.732). **Two sessions reached this separately** — lane C's 0137 settling the solver's footprint across 31 boxes in four rooms, and this lane's emit work — so the correction is corroborated, not asserted. The up-axis therefore did not need recovering from `i_up` (a splat-extent index anyway); it needed DECLARING. The claim is warranted per box off the transform, not assumed from the format: tilt past `PLACEMENT_BOX_UP_MAX_TILT_DEG` (5°; real data measures exactly 0) and the block is ABSENT rather than carrying a wrong height, with tilt taken off the normalized up column so a 1e-7 column-norm error doesn't read as lean that isn't there. Horizontals ship UNNAMED — RoomPlan doesn't fix which of X/Z is long, so "width"/"depth" would certify more than was measured. EMIT ONLY: no `scene_facts` consumption, no `FACTS_VERSION` bump. **(2) `/compress`** is a third `/process` stage beside `/shell`, so new captures are no longer born slow — chosen over a sidecar on evidence (a sidecar needs its own service, SA, IAM, cloudbuild, smoke and runbook phase for a job measured at 1.2 s/splat; `/shell` already proved a derived-asset stage rides this service without touching a model, and `/compress` reuses its queue and invoker SA for ZERO new IAM). A sweep was rejected as primary because it doesn't fix born-slow for the person who just captured; it survives as the backfill path it already was. **One encoder, not two:** 0126 made the encoder Spark's own `SpzWriter` so it couldn't drift from the browser's decoder, and the same argument applies writer-to-writer — the encode moved to `tools/spz_encode.mjs`, which the operator tool imports; Python owns GCS and the index, Node owns bytes-in/bytes-out, nothing authenticates twice. Every failure ends at "no compressed tier for that splat". Measured: the refactored tool reproduces 0125's numbers against the live bucket (275.8 → 47.2 MB, 5.84×, "10 already current"); a real 34 MB splat at 5.86× in 1.2 s, byte-deterministic; and Spark logs to stdout — found by RUNNING it, and it would have corrupted the JSON the Python caller parses. Suites: perception 646 → **704**, root 632+10 unchanged, `services/api-public/` untouched. **NOT verified: the image has never been built** (AMD64 CUDA, Cloud Build only), so `apt install nodejs`, the npm install and the in-image smoke are unproven, and route registration is unverifiable locally — the same gap `/shell` has.
- **iOS closes 0049 and 0084 — the forced re-mint and the `.recoverable` re-upload coordinator (branch `ios-residue` merged 2026-08-09).** Both were blocked on a server capability that is now SERVING (`force_remint`, 0116, on `api-public-00032-has`). **(1) 0049 item 1:** when a 410 re-mint returns identical URIs, the client mints once more with `force_remint` before the terminal `.failed` — fresh URIs proceed as recovery, still-identical fails with the reason string unchanged, a network failure is DEFERRED-TRANSIENT. Exactly once, not a ladder: the flag's effect is deterministic and each attempt costs a mint-quota unit. The STALENESS path never forces — identical URIs are correct there, and forcing would invalidate live sessions the already-uploaded blobs were written against. The wire shape is pinned as BYTES, not behaviour: an ordinary mint must still post the exact 0035 body the serving revision was verified against, so `force_remint` is `Bool?`/nil and the key is ABSENT — not false, not null — pinned twice (parsed JSON and raw string). **(2) 0084's coordinator** (`Upload/CaptureRecovery.swift`): `missing_paths` → force-re-mint those paths PLUS `bundle.pb` (a subset, not the whole manifest) → re-enqueue as ordinary Phase-1 uploads → the existing Phase-1 gate sends `bundle.pb` last, so decision 0040's ordering is CONSUMED, never re-implemented (driven end-to-end by a test). The real work is record surgery: MERGE fresh URIs rather than replacing the entry list (a subset re-mint would otherwise drop every path it didn't touch), reset only the re-sent paths, and move the phase off `.complete` — without which the blobs land and the finalize is silently refused. **0084's honesty constraint is not relaxed, it is made CONDITIONAL, and the condition is the load-bearing part:** `CaptureRecovery` decides from disk whether the promise can be made — paths must be in this capture's manifest and every named blob plus `bundle.pb` must still exist — and only `.available` says "I can send just those"; otherwise the screen keeps the copy it has always shipped, pinned both directions. Re-send is USER-INITIATED: an automatic one would spend quota on whatever network the phone is on, without the user learning the first attempt fell short, and would repeat every launch. Suite 482 → **523**, verified by the coordinator running it (0 failures, incl. the 4 live tests against `api-public-00032-has`); `project.pbxproj` untouched, so the uncommitted entitlements workaround was not swept in. A screenshot caught what reading could not: `RSPrimaryButtonStyle` ignores `isEnabled`, so an in-flight `.disabled()` was an invisible full-strength rust button swallowing taps — fixed at the call site per `ReviewView:180` rather than by touching the shared style. **NOT verified: anything on hardware**, and the full server round trip is read off 0116 and the ingest source rather than observed (no `failed_incomplete` scene was produced and re-driven; `remintProvider` is stubbed in every test).
- **Ops lane: perception-obj is platform-gated, the tombstone is swept, and builds gain a layer cache (branch `ops-iam-cache` merged 2026-08-10; 0106 carries the flip outcome, 0120 the cache design; 0121 unused).** `allUsers` no longer holds `run.invoker` on perception-obj — `tasks-invoker@` alone does (verified live by the coordinator: policy has exactly one member; an unauthenticated probe gets the Cloud Run platform 403 in ~0.9 s and can no longer boot the L4; Cloud Tasks delivery proven by a live `--shell` noop 200 in 1.75 s). The 0088 propagation-lag lesson re-observed, not a failed command: ~3–4 min where unauth still reached the app. `deploy_perception.sh` obj path is now `--no-allow-unauthenticated` with a post-deploy invoker-grant assert; geom stays parked-open by choice. The tombstoned `api-runtime@` is gone from `tasks-invoker@`'s policy (three live members remain; the default-compute member there is a pre-0090 leftover, deliberately left for 0090's default-compute pass). Cloud Builds now carry `--cache-from :buildcache` + BuildKit inline cache (buildx mode=max rejected with its re-open trigger in 0120); seeded by build `5ab7cb34`, 58 min 39 s = the uncached baseline, traffic verified untouched throughout; **the speedup is deliberately unmeasured — the next real perception build is the measurement.**
- **iOS Google linking BUILT — 0094's named change, riding Apple's exact rails (branch `ios-google-linking` merged 2026-08-10; decisions 0118/0119; suite 523 → **535**, verified by the coordinator on the merged tree incl. the 4 live tests).** Google is a second link provider through the SAME core: `AuthManager.link(credential:)` with the UID asserted unchanged (0036/0051), conflicts as the explicit switch/keep choice, still no iOS sign-out. **The presumed blocker dissolved on measurement:** the iOS OAuth client already existed (auto-created when the Google provider was enabled for Gate B); the local plist was merely STALE — pre-2026-08-08 copies lack `CLIENT_ID`/`REVERSED_CLIENT_ID`, on which the Google button preflight-refuses readably (0119; the SDK otherwise raises an uncatchable NSException, read in its source). Main tree's plist refreshed; the worktree-plist memory note carries the API refresh command. Real project change, committed deliberately: GoogleSignIn-iOS 9.2.0 via SPM (GTMSessionFetcher re-pinned 4.5.0→3.5.0), the redirect scheme in a new committed `RoomStudioCapture-Info.plist` merged via `INFOPLIST_FILE` outside the synchronized folder (the double-copy trap), and a drift pin asserting the committed scheme matches the LIVE client ID — `project.pbxproj` carries zero trace of the entitlements workaround (verified by grep on the diff). Walked to the farthest simulator point: Google's real page rendered "Sign in to continue to roomstudio" (client ID + redirect scheme proven end-to-end), cancel classifies `.canceled` through the shared classifier; the walk also caught the AX5 sheet clipping BOTH provider buttons off-screen (pre-existing for Apple — the exact defect class the iOS test policy warns about), fixed and re-screenshotted. **Reasoned, not observed:** the Firebase link call with a real Google credential, the conflict path with a real second account. **Not verified:** anything on hardware; a COMPLETED link, so the linked-state UI has never rendered. The phone leg is in the operator queue — and on the 16 Pro it will likely hit the CONFLICT path, since the operator's Google account already owns the Gate-B UID.
- **The four-surface deploy EXECUTED — the compressed tier, stage 2, the 0105 gate, and the perception emit are all SERVING (2026-08-09/10; decisions 0106–0109 record the deploy's findings).** Serving: **`api-public-00034-zad`** (image `20260809-225017`), **hosting released 23:31:04**, **`api-internal-00023-mek`** (`20260809-233353`), **`perception-obj-00038-7b5`** (`20260810-002003`; digest-verified against the build push; runtime SA asserted `perception-obj-runtime@`). **Gate 0 first: voice evals 10/10 live at PROMPT_VERSION 3** — with one caveat worth knowing: `test_mutation_gets_the_mover_line` passed on phrasing luck, not contract (it encodes v2's "can't move things yet" and its harness passes no tools — 0107; its negative half, never narrate a move as done, is still right). **The first real-model `propose` call ran on the candidate before the flip and WORKED end-to-end**: `arrangement` fired ahead of the deltas, the spec entry keyed on `roomplan_box.identifier` with `measured_transform` beside `proposed_transform`, solver `against_wall → wall_10`; the second turn is the reassuring one — asked for a desk the room doesn't have, the guest declined the tool, named its real inventory, and held rule 10 unprompted. One language defect found and deliberately NOT hot-fixed: the reply "Done — the bed is BACK against the wall" for a first-time move — the server's own string at `spec_solver.py:422`, quoted verbatim per rule 2a, test-pinned in four places (0108). Per-surface evidence: api-public 4/4 smoke post-flip + `/assets` on a converted scene showing `asset_urls_compressed` 10 / `asset_urls` 22 unnarrowed + a signed `.spz` fetched at exactly its index-claimed 5,809,108 B + CORS both directions; hosting verified in a REAL BROWSER on the production origin (zero console errors, full hero choreography, privacy locks 404 by request, `hero/room.json` 200 at 3557 B) — including the leg 0102 says curl structurally cannot cover: **a signed `.spz` fetched cross-origin at 5,809,108 B in 1.44 s**; api-internal platform-403 intact → 4/4 smoke + the 0105 negative control run LIVE on the ROOMPLAN tier (room.json present → passes; a dropped declared RoomPlan blob → `failed_incomplete` naming `roomplan/room.usdz`); perception-obj — **the build FAILED first, which is the in-image smoke doing its job**: focal's apt nodejs is 10.19.0 and every layer reported success (npm 6 silently ignored `--omit=dev`; Spark declares no engines) until node ran — fixed by taking Node from conda-forge pinned to **22.23.1** (what web/ builds with and what 0126 measured on) in its own prefix, and the smoke now prints node's stderr, whose swallowing cost the diagnosis (`e76ceae`, 0109); re-validated `spz encoder armed: {'bytes': 50, 'gaussians': 2}`. All authed checks used a manufactured ready Scene under a throwaway anonymous uid (board-item-1 precedent), artifacts verified cleaned. Suites after the Dockerfile fix: root 730+10, perception 704, re-enqueue 18.
- **The render payload P0 is BUILT — the compressed tier ships beside the PLY and the reveal waits for bytes (branch `splat-compressed-tier`, 2026-08-09; decisions 0125/0126/0127; converted in the bucket, NOT deployed).** The probe that decided everything came first: Spark 2.1.0's own `transcodeSpz` runs **headless in Node**, so the compressed tier is a **transcode, not a re-bake** — the PLY is the input, nothing re-segments, no perception decision is recomputed, so 0070's re-adjudication rule is not triggered and a pre-0089 scene's person-suppression status is carried across unchanged (no better, no worse; a scene needing suppression still needs a re-drive). Reference room `a7e073ae`: **275.8 MB → 47.2 MB (5.84×)**, Gaussian counts preserved EXACTLY on all ten, encoded in 8.0 s. Both formats are fixed-width per Gaussian (PLY exactly 68.0 B, SPZ 10.9–12.4 B), so the ratio is **structural**, not sample-specific. Round-trip error per Gaussian on real data: **position 0.117 mm mean / 0.21 mm max**, rotation 0.081°, scale 0.005%, alpha 0.001. Renders correctly through the real `SplatViewer`, as does a deliberately **mixed** room (five SPZ + five PLY composited), clean console both ways. **Measured end to end on the live bucket, same client, back to back: 87–93 s → 14–19 s.** Serving (0126): `tools/transcode_scene_splats.mjs` writes the `.spz` beside each `.ply` plus a `scenes/{id}/compressed.json` index — a sibling blob on shell.json's precedent, deliberately NOT a manifest field, because a re-drive rewrites the manifest and would silently erase it; keyed by the PLY's gs:// URI so a re-drive that moves a splat simply misses and falls back. api-public reads it with `fetch_optional` and signs into an additive `asset_urls_compressed`; `asset_urls` **never narrows** (the PLY is always signed, so the fallback is a real URL), and a malformed/unreachable/stale index degrades to PLY rather than 500ing. Signing now runs in a bounded order-preserving pool (`ASSET_SIGN_CONCURRENCY`=8) — **0124's own named trigger firing**. Storage came in at **+17%**, under the ~25% budgeted. Progressive loading (0127, operator's call): the reveal's first two movements need **zero splat bytes**, so they play immediately, and the object wave gates per piece via the pure `revealHoldMs` — when a due piece is missing the whole wave holds, preserving the score's spacing rather than bursting to catch up, because the order IS the narrative. **Measured and it refutes the brief's stated tension:** fetch concurrency does not trade off against time-to-first-object (flat ~5 s at concurrency 1/2/3/4/10 across three runs, because one GCS connection is capped at 7–11 Mbps so the opening file is per-connection limited), so no fetch scheduler was built. Verified in a real browser against a throttled server: at 4 Mbps the floor and walls stand alone with no objects (previously a ~94 s skeleton); at 15 Mbps, 14.25 s in, the instrument read `objT 5220 / hold 9032 / arrived 3 of 10` — `objT` pinned to exactly seq 1's scheduled start — with the bed alone in the finished shell; then the room completed. **All 12 scene dirs converted in the bucket — every one with rendered splats, zero skips: 2,695 MB PLY → 463.8 MB SPZ, 5.81×, +17% storage** (the brief's table listed nine; there are 12, including `f3d70236` and the hero's source `ce68e24f`), ratios 5.62–6.00× across rooms of 3–16 splats. Suites: root **632**+10 (was 620; verified with AND without credentials per 0116), web **179** (was 165), tsc + eslint + static-export build green.
- **Operator-guided queue session — all five steps executed, and the phone leg found more than it was sent for (2026-08-12/13; decisions 0114/0115; commit `3755bad` + the clip-sign production deploy).** (1) **The clip-sign A/B ruled and shipped** — see the What-does-NOT-work bullet; measured wins, flipped, deployed, production verified. (2) **The item-7 second walk ran** (verdicts in gitignored `outputs/item7-walk-2026-08-12/verdicts.md`): every 0104 mechanism landed — spike bed no longer intersects the table/chair (under the measured sign, which is the shipped default as of that same day), rp7 nightstand is ONE object, the sinks are gone, the 0.245 m tv demotion is visible, rp6g1's monitor "dimensions look much better". The walk's NEXT ranked classes are new: a **contact-TILT class** seen on four objects (spike speaker, rp7 lamp, rp6g1 lamp, rp6g1 monitor — right height, but touching at a point because the snap is height-only and the splat's rotation residue tilts the body), and **class-6 truncation in two fresh costumes** (rp7's monitor renders far smaller than the real one; rp6g1's monitor has NO generated base, so it cannot visually rest on anything even at the correct height). rp7's monitor still hovers. Board wording corrected in the process: 0104 placed lamp→nightstand and monitor→DESK; the board had bundled both onto the nightstand. Two operator product suggestions recorded as design inputs: per-object cleanest-frame selection for SAM 3D, and a capture-time camera view with coverage text guides. (3) **The reveal choreography CLOSES, both questions positive** — pacing "reads right" on production, and the contour "reads as measurement". (4) **The social layer is ruled a COMMITMENT** (board 10(b)). (5) **Perception's live gate CLOSED by a real scan** — bundle `3713e40a`/scene `7bbdbaf1`, LIDAR_ROOMPLAN, ready in two rounds (round 1 budget-stopped honestly on the biggest census plan yet seen: 76 detections, 64-item long tail): `extent_axes_m` present on 6/6 box objects with plausible per-category heights, `/process`→`/compress` fired unprompted (compressed.json 12 entries; bed .spz byte-exact 4,333,576 B, 24.1→4.3 MB), the 0105 declared-blob environment passed, shell v3 `roomplan`. A second capture (`8e903997`/`af62270c`) reached ready the next morning, 319.6→54.5 MB compressed. (6) **iOS re-sign genuinely renewed to 2026-08-19 07:15 UTC** (both profiles' CreationDate read off the built .app; the Xcode account had lapsed a THIRD time — probe-by-building caught it; pbxproj restored pristine). (7) **The disruption capture measured force-quit ≠ OS-kill** (0114) and (8) **the identity churn was discovered and the Google link executed as SWITCH** (0115) — see both bullets below.
- **The 16 Pro is signed into the Google account, and 0084's reclaim table is hardware-verified in BOTH directions (2026-08-13; decision 0115).** The switch/keep fork resolved on measurement rather than preference: a Firestore ownership sweep showed the phone's anon UID had **churned twice**, so "keep" preserved two test scans rather than the five rooms the board claimed — and that `cHfMlUL`, the Google account, IS the phone's own original identity (it owns all seven walk rooms, captured 07-26→08-05, before Gate B attached an email to it). Switch was therefore a restore, not an adoption. Executed via SignInSheet's stock conflict alert ("This Google account already has a home" → "Use that account"); Identity Toolkit confirms `cHfMlUL` lastLogin 08:49:04Z with `providers:['google.com']` and the anon uid untouched. Cost as designed: today's two test scans are orphaned. **The payoff was a leaf we could not otherwise test**: switching made 6 retained records owned+acked+terminal at once — the only condition under which the launch-scan POSITIVE reclaim leaf can fire, since the doorway path acks and reclaims in the same instant. One relaunch produced `code=200 status=ready → reclaim` ×6 and `403 → retain` ×6, store 12 → 6, the survivors byte-for-byte the predicted set. The "positive reclaim leaf owed" residue CLOSES.
- **Room quality: contact tilt, the phantom support surface, and the label fork — three defects closed, and view selection refuted (2026-08-13; decisions 0146–0156; MERGED to `main` `d54a0e5` and SERVING as `perception-obj-00043-yiz` since 2026-08-13 — the operator walk RAN and its corrections are in; see the walk bullet in What-does-NOT-work and the ship bullet below).** Every number below came out of an offline replica of the production `RefinementContext` over the four preserved captures — same bundle poses, same per-frame masks/objects from the outputs bucket, production's own `associate_observations`/`build_box_object`/`fuse_scene_objects` — with a **trust gate first: the replica reproduces every shipped view choice and every shipped box object exactly.** **(1) Contact tilt (0147), the 2026-08-12 walk's own top class.** The 0104 support snap is height-only, so an object landed on a measured surface still meets it at whatever tilt its rotation carried — median 5.1° over 26 placed free objects, and the spike speaker at **39.9°**, a soundbar its own capture photo shows standing vertically. Tilt is NOT the dead DOF: the dead one is the splat canonical frame's axis assignment and 180° yaw sign, where the only evidence is appearance or a thin single-view cloud (0081/0104); tilt is rotation about a HORIZONTAL axis with a prior those never had — gravity, for classes whose whole relationship with the room is that they rest on something level. The instrument is the object's own MEASURED LiDAR cloud — metric and world-framed by construction — read for which axis is up and matched to the splat's axis of the same extent rank; the splat's own rendered mass is the degrade path only, bounded at 45° where the cloud path is bounded at 90° (the operator's walk corrected this: reading the splat's mass stood the spike speaker on its edge, because its canonical frame is arbitrary and its extents truncated). The correction is the minimal rotation onto world up, so an object moves by exactly its measured tilt, never further and never sideways. Two independent gates, each verified to refuse alone: class (only classes that rest level — admitting wall/hanging classes as an experiment, the evidence gate alone refuses ten of them on real data) and evidence (the underside must measurably flatten, so a round-bottomed vase whose tilt cannot be read costs nothing). Live effect, read off the DEPLOYED revision's manifests as applied corrections (`level_correction_deg`, not residual tilt — the two differ once the cloud names a different axis than the splat's mass did): **spike speaker 50.66° with flatness 0.0296 → 0.0013 m, spike rug 10.94°, spike cabinet 5.19°, rp6g1 lamp 7.33°, rp6g1 monitor 3.66°, rp7 lamp 3.64°, rp7 tv 3.14°** — every one off the LiDAR cloud except rp7's tv, the single object on the bounded splat-mass path; object and placed counts unchanged on all four rooms. **(2) "The monitor still floats above the desk" (0148) was two defects stacked.** It WAS resting — on the top of the **chair** tucked under the desk, 0.28 m above it: the splat half of the support-surface set has always had a class rule ("a lamp never supports a TV"), the measured half had none, so a chair, a bed and a sofa were all tables. Fixing that left it on the desk's MEASURED top and still 0.206 m above the desk anyone can SEE, because the desk's splat fills 0.42 of its measured height — a tabletop with the legs cut off — and was centred in its box, putting half the deficit at each end (4 of 21 box objects under-fill at 0.31–0.42, and in every one the floor gap equals the top gap to within a centimetre). So an under-filling splat is now seated against the box face that matters: the measured TOP for a category whose top is a surface, the FLOOR otherwise — **one vocabulary (`table,storage`) with one home in `box_placement`, answering both questions**, because two copies would drift exactly the way this defect did. The obvious anchor, the observation's LiDAR cloud, was measured UNUSABLE: it is the visible surface, so it is top-biased on everything (a chair's cloud spans 0.47 m of a 1.08 m box) and would push well-fitting objects up by +0.26 to +0.36 m. Also fixed: the resting object's underside is percentile-clipped like every other extent — from the raw minimum, the 12 mm of stray gaussians measured below the reviewed monitors became 12 mm of hover. **(3) The label fork (0149).** SAM detects rp7's monitor as both `monitor` and `tv` in all three frames that see it; the same-frame cross-label collapse keeps whichever scored higher IN THAT FRAME (monitor at f7, tv at f114 and f385), and the label split then forked three views of one object into a one-view `monitor` and a two-view `tv` — so the pipeline shipped the SMALLEST reconstruction (0.405 m against its own cloud's 0.490 m) while the largest (0.568 m, better tier-1 in its own view and summed across all three) was demoted by the television scale floor and discarded. The refined pass now groups by confusable GROUP using the vocabulary the 3D duplicate gate already owns; nothing is renamed, and the same-frame nested-pair dedup stays PER LABEL (it absorbs on containment of the SMALLER and would swallow a small object genuinely inside a larger one — **the gate's own test caught this**). Measured: rp7 3 views become one object and a demoted duplicate disappears, rp6g1's monitor 1→2 views with its duplicate absorbed rather than demoted, spike and rp6g2 unchanged, placed counts unchanged on all four. Suites: perception-obj 704 → **746**, root 733+10 and re-enqueue 18 unchanged, ruff clean on every touched file. All four rooms staged as `/viewer?fixture=<dir>-fixed` beside the shipped dirs (splats symlinked, `?labels=1` sidecars regenerated); rp7's pair browser-walked clean-console. Walk pack with per-object A/B renders at gitignored `outputs/room-quality/walk/`.
- **Room quality SHIPPED — `perception-obj-00043-yiz` serving, all four rooms re-driven on it, live reproducing offline at the float64 limit (2026-08-13; decisions 0163/0164; branch `lane-c-ship`).** Image `20260813-222442`, digest `sha256:d15ca00d…` verified against the build push, runtime SA `perception-obj-runtime@` read off the served spec (0090). **Candidate → smoke → flip**, which perception-obj could not do until this session: `/health` 200 in 17.3 s cold, `/ready` 503 `not_loaded` (0024's lazy-load), unauthenticated probe 403 (0106's gate intact), rollback target recorded and unused. Live vs the offline prediction: **rp7 17/10 → 16/10, rp6g1 21/11 → 20/11, spike 25/10 unchanged, rp6g2 45/16 unchanged** — every position and rotation identical, the sole residual anywhere being `extent_m_sorted[0]`'s 16th decimal at zero movement (Mac-vs-Linux BLAS), where 0104's standard was four decimals. Per change: rp7's monitor is off the chair's box top and NOT pulled onto the desk either (support `None`, the 0.10 m drop bound refusing, same on rp6g1's 0.123 m), rp7's table seats **+0.2398 m** and rp6g1's **+0.2205 m** to `box_top` while rp6g2's chair and sofa seat **−0.300/−0.305 m** to `box_floor`, and rp7's `tv` duplicate is gone with its survivor at `frames_observed` 3. **The route table is verified live** — the thing 0142 records as unverifiable locally — because every re-drive's success path enqueued `/shell` and `/compress` and both 200'd and correctly nooped. rp6g2 was the only room doing real work: 834.6 s, **51-item long tail**, honest `budget_stopped`, two per-object OOMs contained per 0061. **0120's speedup is now measured**, which that note left open: a source-only perception build is **10 m 23 s** of BUILD step / 11 m 29 s wall clock against the 58 m 39 s uncached baseline — **5.1×**. All four rooms re-staged as `-fixed` fixtures from the LIVE manifests with `?labels=1` sidecars; four browser-walked clean-console. Walk pack + tooling at gitignored `outputs/lane-c-walk/`, report at `outputs/reports/lane-c-ship.md`.
- **A person can correct a facing by saying so, and the room keeps the correction (branch `lane-a-facings`, 2026-08-13; decisions 0157/0158/0159; SERVING since 2026-08-14 as `api-public-00038-qiv` — see the ship bullet below for the voice-eval gate and the live checks).** After five refuted instrument families (0081/0104/0156) the 180° facing sign is settled the only way left: by the person who lives in the room. **It is a SELECTION, not a rotation, and that is what makes it shippable.** Perception enumerates the extent-consistent axis mappings of a splat onto its box and ships one; `_partner_index` names the 180°-about-box-vertical partner, and measured through perception's own enumerator over **1924 partner pairs the partner is `rotY(π) ⊗ q_chosen` to 2.2e-16** (a pure component permutation, `(x,y,z,w) → (z,w,−x,−y)`), with the piece's up axis preserved to better than 1e-4 — a flip, not a somersault. Three invariants, all measured on the four preserved walk rooms, make it the ONE rotation that can ship: the footprint is **exactly** invariant (0.00e+00 across all 21 box placements), so no constraint can newly fail and every refusal is about eligibility; the `splat_clip` volume survives it (clip centre coincides with the object position to 4.8e-5 m, and a box maps onto itself under a half turn, so the browser's parented SDF still cuts the measured box); and it is its own inverse, so a second turn DROPS the entry rather than storing a change equal to no change. Eligibility is `rotation_source == "roomplan_box"`, which on real data coincides exactly with having a box (21/21) — the other 26 placed pieces are `sam3d_layout` (sign pinned by 0065's regression tests) or carry no rotation claim, and neither is ours to overrule. **The epistemics are the real content (0157):** a turn departs from an unresolved DEFAULT, not a measurement — `resolve_axis_mapping` always ships the fixed (+,+) sign, so the 180° bit is unresolved on every box placement by construction whatever `splat_axis_resolved` says (that flag is the assignment, resolved on 9 of 21 at margins 0.10–0.47). So entries carry `departs_from`, computed into `client_dict` and never stored, and three behaviours read it: a turned piece gets **no measured outline** (the footprint is the ground it is standing on), `revert` restores measurements and **REDUCES a corrected entry to a pure turn instead of dropping it** (0133's "one step away" holds exactly — a turn never left the measured room), and rule 10's conditional grammar does not apply (pinned: the facts block is byte-identical before and after, and the box's yaw and dims never change). `facing_flipped` rides independently of `action`, so a piece can be both moved and turned and neither instruction discards the other. **Charter → PROMPT_VERSION 4** (0159): rule 6 gains turning, rule 6c makes the person the authority while rule 5 keeps the guest unable to see a facing ("turning a thing did not give you eyes"), rule 10 excludes turns, four exemplars. `quat_mul` added to `pose_math` — the repo's one home for quaternion ops — rather than a private copy. **Browser-walked in mock mode with real pointer and key events:** the sofa turns, the chrome reads "1 turned round — where you told me it faces" with NO revert control offered, the inventory says "turned round", a move composes ("1 piece moved, 1 turned round", control relabelled "back to the scan"), `!revert` drops the move and keeps the correction, a second turn clears it — zero console errors, and **a tag set on the live canvas survives both turns, so a correction never tears the renderer down.** Suites: api-public **398** + 10 skipped (was 346+10; +52 in the new `test_facing_correction.py`, all driven against the four walk rooms), root **790**+10 (was 733+10), schemas **108** (was 103, +5 pinning `quat_mul`'s composition order), web vitest **212** (was 207); tsc, eslint and the static-export build clean.

- **The guest says which room a number describes, and the pin now covers every instruction it reads (branch `guest-provenance`, 2026-08-14; decisions 0174/0175; revision `api-public-00040-loj`, image `20260814-114714`).** 0173's defect — with an arrangement in place the guest refuses the proposed room's numbers and sources them to the scan — is closed at PROMPT_VERSION 5. **Reproduced first, and it was worse than recorded: n=8 gave 8/8 refusals and 8/8 false provenance** (0173 measured 5/5 and 4/5). The guest was reading its instructions correctly — rule 1 makes THE FACTS its whole knowledge, rule 2 forbids re-deriving, the facts block still says these were *measured from a scan*, and the arrangement block said only "nothing has measured it standing here" — so the block never mentioned that `_proposed_view`'s re-derivation had already happened on its behalf. **A real sample earned its cost immediately: 0173's remedy, written out, closed the original defect completely (0/8) and then called the number "the same figure as before" 3 times in 8** — the measured figure was 1.2 m. A third class appeared in the composed case ("a real measurement of how things stand right now"). All three need the guest to believe it holds the scanned room's figures, so the shipped block says in one sentence that it does not, and the enumeration after it is derived rather than patched. **Measured on the shipped text, n=16: 0 invented provenance, 0 refusals, 16/16 spoke the distance** (literal "would" 12/16; all 16 anchor the number to the arrangement). **0175's structural half:** `PROMPT_SURFACE_SHA256` replaces `STATIC_CHARTER_SHA256` and covers the charter AND the arrangement block's prose, which now lives in `ARRANGEMENT_PREAMBLE`/`ARRANGEMENT_FOOTER`; the renderer holds no prose of its own and a test asserts the block EQUALS constants + the server's descriptions, because pinning constants alone reopens the gap the moment someone adds a line inside the function. PROMPT_VERSION can now move without the charter changing, and 5 is the first such bump. **Verification, in the order it was done:** 0173's own `xfail(strict=True)` extracted verbatim from the parent commit and run against the fix → `[XPASS(strict)]`, the signal it was built to give; live voice evals **19 passed** at v5; the Cloud Build source tarball that produced the image confirmed byte-identical to the working tree; then a REAL two-turn conversation against the candidate before the flip and against the serving revision after, each under a throwaway anonymous uid with a manufactured ready scene (board-item-1 precedent, artifacts verified gone) — candidate: *"In this arrangement, that would be about 2.2 m … I'm saying 'would' because that's the room as it now stands with the sofa moved, not a fresh measurement of it sitting there."* Candidate → smoke → flip with `/health` 200, verifier 401, RUNBOOK Phase 0 greps green and api-internal's platform 403 intact; rollback target `api-public-00038-qiv` recorded and unused. **One correction worth carrying (in 0174):** a third eval leg was written per-sample on an 0/8 probe and the first live run failed it — 0 of 8 is consistent with ~1 in 10, and the move test's two per-sample rules are sound for a different reason, that they read 8/8 *in the other direction* against the pre-fix block. That leg is a rate now. Suites: api-public 326 + 94 skipped, root **718 + 94 skipped** (no dev-fixtures staged), ruff 81 → 79 on `services/api-public/`.
- **The facing correction is SERVING, and the eval suite that certified it was revised before it was run (branch `ship-facings`, 2026-08-14; decisions 0172/0173; revision `api-public-00038-qiv`, image `20260814-014626`, digest `sha256:b1e46933…`).** 0058 gates a PROMPT_VERSION bump on a live eval pass and 0107 adds the harder half — an eval that cannot fail for the right reason certifies nothing — so the suite was revised first, per 0107's own instruction to the next bump. Seven new evals over a harness that rebuilds the system prompt every turn from the spec as it stands (production's `_proposed_view`, with history carrying user and assistant TEXT only, because that is what production persists — two of 0159's four exemplars do not exist inside a single turn). 0107's two deferred items came in with them: rule 10's conditional grammar, and declining a piece the room does not have. **Live: 17 passed, 1 xfailed at PROMPT_VERSION 4** (the xfail is 0173, a pre-existing v3 defect this pass found). **Three assertions were wrong when measured, which is the point of measuring** — the rule-10 move eval passed 2 runs in 5 on replies that REFUSED to answer ("giving you a number would mean inventing one" satisfies a bare `would` while doing none of rule 10's work — 0107's failure reproduced inside the eval written to honour 0107); the invitation ending was asserted per sample where the charter makes it conditional (A/B against the serving v3 charter: 5/8 at v4, 7/8 at v3, noise apart, every miss ending on an honest limit — so it is a rate now, and the verbatim number beside it is checked four times instead of once); and the facing claim is better probed by ASKING which way the piece faces than by watching a correction go by. **Ablation could not break either facing assertion** (0172): strip 6c's "turning a thing did not give you eyes", strip rule 10's turn exclusion, strip rule 5's facing item too, and the voice holds — the properties are over-determined by 6c's remaining clauses, the `turn` tool's description, and rule 5. So these pin BEHAVIOUR, not any one sentence, and nobody should trim charter text on the strength of them passing. Deploy: candidate → smoke → flip, with `/health` 200, verifier 401 `invalid_token`, api-internal's platform 403 intact, and the RUNBOOK Phase 0 verifier greps green BEFORE the flip; rollback target `api-public-00036-duv` recorded and unused. **Real turns ran against the candidate before the flip and the serving revision after**, under a throwaway anonymous uid with a manufactured ready scene (the board-item-1 precedent, artifacts cleaned): the correction applies with `rotation_xyzw [0,1,0,0]` — `rotY(π)` on identity, 0158's `(x,y,z,w) → (z,w,−x,−y)` — position and footprint untouched, `arrangement` fired on the changing turn and only on it; "make the table face the window" is refused without a facing claim and offers the one turn it has; the box-less rug is refused honestly; and a revert after a composed move drops the move, KEEPS the turn ("with the sofa still turned since that's yours to keep or change"), and leaves an entry reduced to `action=turn` carrying `departs_from='unresolved_default'` with the solver's own sentence — 0157's three read-behaviours, live.

- **The app can now tell a lost identity from a first run (branch `uid-churn`, 2026-08-14; decision 0141; instrument only — no behaviour change, NOT deployed anywhere but the branch).** `signInIfNeeded`'s `currentUser != nil` guard is true of a first run and equally true of an install whose credential was discarded, and 0139 measured the second happening twice. `IdentityContinuity.read` classifies the launch from two signals that fail independently — the device UUID (Keychain, same access group as Firebase's item, so reading it back proves the Keychain is answering) and capture records (Application Support, so they outlive a Keychain read failure) — into `continuous` / `firstRun` / `credentialLost` / `keychainUnavailable`. `AuthManager` logs it at `fault` level immediately before minting and **does not change whether it signs in**; the two loss readings are kept distinct because they want different cures, which is also how 0140's open question gets settled by the next occurrence. `DeviceIdentity.existingDeviceId()` is a new non-minting read — a minting probe would fabricate the very signal it checks. The call sits inside the single-flight task, not beside the guard: the store is an actor, and awaiting it beside the guard would put a suspension between the `currentUser` check and the `signInTask` assignment, which is the double-sign-in race that assignment exists to close. Both known churns are pinned as `credentialLost`. Suite 535 → **544**, one run, incl. the 4 live tests.

- **The facing sign is MEASURED, right two times in three, and switched off (branch `lane-e-facing-sign`, 2026-08-14; decisions 0170/0171; MERGED, NOT deployed, NOT applied).** RP-4 discards SAM 3D's layout rotation for box-placed objects and 0081 keeps only its sign-AGNOSTIC up axis, so the one channel that ever saw which side the camera photographed was thrown away one step before the sign was hunted. `box_placement.resolve_facing_sign` reads it back: the rotation distance from the layout to the shipped candidate and to its 180° partner, nearer wins, and the RESIDUAL — the distance to that nearer one — is the gate. **The residual is bimodal, which no refuted scorer ever managed**: over 15 box placements in three preserved walk rooms, eight land at 2.9°–28.9° and seven at 70.0°–177.2° with nothing between 29 and 70, where the five dead instruments all lived inside their own gate's noise at 0.002–0.089 against a 0.10 gate. **Every abstention is explained and none is a near-tie**: on all seven the layout is 3.3°–34.1° from SOME candidate, just never one of the two on offer — three near a different ASSIGNMENT (the cloud's DOF, where the layout has no standing) and four near the same assignment with the opposite UP sign, which is exactly 0081's measured 1-in-6 defect, here 4 in 15. So it abstains precisely where 0081's known failure is present. Driven through `fusion.fuse_scene_objects` over the preserved captures it turns three objects by exactly 180.00° and changes nothing else — position delta 0.00e+00 m, scale delta 0.0e+00, `splat_clip` removed-fraction identical — reproducing lane A's invariance argument (0158) out of production code. **Two of the three turns are exactly the objects the operator reported** (0156's MUST-FLIP rows): rp7's cupboard at residual 2.91°, whose glass-fronted shelves match the photograph after the turn and are a blank white slab before, and rp7's bed at 28.85°. **The third is wrong** — rp6g1's nightstand at 15.41°, whose brass-knobbed drawer faces its source camera in the photograph and in the shipped rotation and faces away after the turn (checked from two viewpoints, after a controlled test of the renderer caught an inverted azimuth in the first attempt). **So it ships recording its preference and applying nothing**, the 0080 lock-6 / 0081 flag-only precedent — not caution but arithmetic: **no number it reports separates the miss from the hits** (2.91 right, 15.41 wrong, 28.85 right — the miss sits between them), and neither separation, assignment margin nor `splat_axis_resolved` orders them either. `PLACEMENT_FACING_SIGN_APPLY=1` is an env-only flip and the operator sitting is prepared at gitignored `outputs/lane-e/WALK.md` (three per-object strips from inside the room and from each splat's own source camera, plus staged `-facing` viewer rooms). Collateral: `rotation_angle_deg` added to `pose_math` — the repo's one home for rotation math — and `splat_up_local` becomes `splat_layout_rotation`, since the up vector was always a projection of a rotation the caller now needs whole. Suites: perception-obj 746 → **769** collected (+13 unit, +10 real-data on a committed 12.5 KB fixture), root 800 → **808** collected, ruff byte-identical to baseline on every touched file.

## What does NOT work / what we're deliberately not doing

- **`scene_facts` still tells a rearranged room's guest that its facts were measured (decision 0174's recorded follow-up; READ FROM THE CODE PATH, never observed in a rendered prompt — sound reasoning about a short path, not an observation).** `derive_scene_facts` runs over the PROPOSED manifest, and its provenance line — "These facts were measured from a scan of N frames of this one room" — survives into a view where some of those numbers were worked out rather than measured. It is corrected downstream, in the arrangement block, which is specific and comes after it; the facts block itself is untouched because changing it is a `FACTS_VERSION` bump, and FACTS_VERSION is a per-process cache key AND half of every persisted turn's reproducibility triple. Worth doing on its own terms. Until then the guest is resolving a contradiction between two of its own blocks rather than reading one consistent story, and the arrangement block's "Worked out is not measured" is load-bearing rather than belt-and-braces.
- **A proposed MOVE still rebuilds the renderer, because the measured outline is in the structural key (measured 2026-08-13).** 0133 took each splat's POSITION out of `rendererKey` so a proposal would not re-download the room, and that half works — a transform change rides the placement effect and the live canvas survives it. But `viewerKey.ts` also puts `outlines` in the key, reasoning that they are "cheap to rebuild and rare to change": the outline object is indeed cheap, but the KEY's effect is global, so the first move of a session adds an outline, changes the key, and tears down every splat mesh with it. Measured both ways: `rendererKey` returns `…|outlines:none` versus `…|outlines:1,0,2/0` for the same splat set, and in a real browser a tag set on the live canvas is LOST exactly when an outline appears and again when a revert removes it, while surviving a facing correction (which adds none). The stage-2 walk's recorded claim that the tag "survives both a move and a full revert" is corrected accordingly. Not fixed here: the honest fix is to build outlines in their own effect so they stop being structure, which is renderer work belonging to whoever owns the stage-2 viewer, and a botched split risks the unmount cost 0130 measured in the other direction. The re-download itself was not re-measured this session — 0130's measurement is what makes it expensive.
- **The sharper, more-revealing frame does NOT reconstruct better — the one GPU experiment 0153/0154 both named has RUN (2026-08-14; decisions 0160/0161/0162).** Every prior view measure was correlational over views the pipeline had already chosen; this is the first test on views that were actually segmented and reconstructed. rp6g1 re-driven at `PERCEPTION_PLAN_VIEWS_PER_BOX=4`, converging in two rounds (`budget_stopped=False`, plan exactly as the offline replica predicted), turning its 3 policy-skipped views into reconstructions — 40 ok / 3 skipped → **43 ok / 0 skipped**, nothing else changed. Those 3 views are **1.7× sharper** (0.368 vs 0.216) and see **2.1× more surface** (0.143 vs 0.067) than the views their boxes already had, and their reconstructions are **worse**: fidelity-at-one-voxel 0.639 vs 0.780, shape error 0.153 vs 0.181. The case chosen in advance as the strongest test in the dataset — rp6g1's bed, where frame 43 sees **0.297** of the object against the incumbents' 0.038 and 0.098 at the sharpest reading anywhere in that capture (0.512) — came back fidelity 0.639 against 0.801/0.740 and shape error 0.161 against 0.038/0.129: worse on both, from much the better photograph. Sharpness picks the better view in **2 of 4** boxes, surface-seen **2 of 4**. **Eleven view-quality measures have now failed.** Honest limits, all three of which a follow-up must respect: n is 3 new reconstructions in one room; the completeness instruments remain unvalidated (0161); and the added views are rank-2+ by footprint overlap, so "sharper" is confounded with "weakest-associated" and this cannot separate them. **No selection score was built, not even behind a flag** — the measurement that would justify one came out against it. The env var is reverted and rp6g1 restored byte-identically to its pre-experiment manifest (20 objects / 11 placed), so the operator's pending walk pack is unaffected.
- **`PERCEPTION_PLAN_VIEWS_PER_BOX` is inert on a warm room, and 4 already saturates the pool (decision 0160).** Driving production's own `_build_reconstruction_plan` over all four rooms' real cached data at budgets 2, 4 AND 8 yields an **empty plan every time** — the granted experiment as literally specified is a measured no-op. `process_receiver.py:1018` admits a cached frame's entries as observations only when `ok` is true, so a policy-skipped view is invisible to the planner forever once its frame caches; the cure is clearing the per-frame `objects.json` (splats and masks retained — the 0065 cheap re-drive). Production had already recorded this unread: lane C's rp6g1 re-drive shipped `plan: {box_best_views: 0, box_second_views: 0, long_tail: 0}`. Association depth is **1–4 views per box** (median 2–3), `policy_skipped` hits 0 at budget 4 in every room, and ~⅓ of the rank-2+ "views" are a second detection in a frame the box was already reconstructed from — same camera, 0.0° apart. **The per-box budget was never the binding constraint; the object-blind sampler is.** Two consequences with teeth: **an OOM-failed view is never retried by any warm re-drive** (same `ok` filter; rp7 carries 8 such, and re-attempting them under this experiment they OOM'd again — fresh confirmation of 0061), which is the exact opposite of the protection 0062 gives a budget cut; and **raising the budget does not widen 0151's baseline** — pairs involving a rank-2+ view sit a median **36.2°** apart against today's **35.5°**, max 82.5 vs 88.3. A wider baseline needs different FRAMES, which is the sampler again, not more views per box.
- **A reconstruction carries most of what it is shown — so the deficit is the input, not the model (decision 0161).** Against 0155's ≥3-frame reference, with the view's own surface taken through production's `placement.observation_world_cloud`: view coverage median 0.087, and the splat reproduces a median **0.777** of that surface at one-voxel tolerance (0.482 exact — the gap is alignment slack, not missing mass). Masking is load-bearing: unmasked, the depth inside a table's box contains the chair tucked under it and reads fidelity 0.279 with a manufactured −0.285 correlation to view coverage, both of which vanish (0.482, −0.081) through the masked cloud. Stated circularity: these placements are `depth_fit`, fitted to this same cloud, so read it as coverage GIVEN a fit — but a fit cannot invent mass, so 0.777 is still a statement about how much is there. This is the measurement that says added surface would survive into the object, which is the case for 0151's union. **It is not a quality instrument either** (named-broken 0.462 vs rest 0.482) — three completeness instruments have now failed to separate the operator's verdicts, and every paired view comparison in this thread rests on instruments that have not earned trust.
- **View selection is dead ground, and the capture is not the constraint — both now MEASURED (decisions 0146/0150).** The room-quality session's opening move was the brief's own hypothesis: `associate_observations` ranks a box's views by footprint-vs-mask overlap, which answers *is this the same object*, while `in_frame_fraction` — the one computed field that speaks to truncation — plays no part in it. **The lever exists and does not work.** In-frame-first ranking changes the pick on 3 of 31 boxes (worst: rp7's desk shipped an in-frame 0.513 view over a 1.000 one, both at overlap 1.000, the tie breaking on frame index alone) — but across the 8 boxes with two reconstructed views it is better on shape agreement in 3/8 and on cross-view silhouette in 5/8. **Seven view features** (in-frame, overlap, front-facing face count, projected-area balance of the two largest visible faces, total projected area, mask pixels, splat points) against those two mapping-independent instruments, paired within each object: all coin flips, best 5/8. The decisive case is spike box_05, whose two candidate reconstructions come from **the same frame** — same camera, same instant, different mask — and differ 3× in shape error and 2.6× in point count, so the variance is SAM 3D's, not the camera's. **And the supply is enormous:** every box in every non-starved room has 22–156 frames that see it at the census cover bar, a view at in-frame 1.000 exists for 20 of 25, and the sampled twelve frames hold **1.6–3.9%** of the good (frame, box) pairs a capture contains — the cover pass reaches every box in 2–3 picks and 75–83% of the frame budget goes to object-blind pose-diverse residue. Supply does not buy quality either: across 20 boxes spanning a 40× range, **r = +0.018** (below-median mean shape error 0.199, above-median 0.177). **Consequence, and it is a design ruling (0150): do NOT build per-object capture-sufficiency feedback, and do not re-open RP-7's camera-preview decision to host it** — the feature would ask users to work harder at something the pipeline already discards, and would promise something reconstruction cannot keep. The named test that would overturn it is one scan: re-capture the reference bedroom deliberately and compare per-object shape agreement; this predicts little or no improvement. **Do not re-attempt view ranking without a completeness instrument that separates good reconstructions from bad ones** — this refutation is partly a statement about the two instruments used.
- **The operator's walk RAN (2026-08-13) and redirected the session; two of my changes were corrections, and the redirect is answered with numbers (decisions 0146/0150/0151/0152).** Verdicts: the four contact-tilt objects and rp7's monitor-on-the-desk landed; two things I shipped were WRONG and are fixed. (i) The speaker was stood on its edge — it rests on its largest face in reality, and its own LiDAR cloud says so unambiguously (thinnest axis 7.0° off vertical, runner-up 83° behind). Levelling now reads which axis is up off the **measured cloud** — metric, world-framed by construction — and stands up the splat's axis of the same extent-rank; the splat's own mass, whose canonical frame is arbitrary and whose extents are truncated, is only the degrade path and is bounded much more tightly for being a heuristic. (ii) rp6g1's monitor was pulled 0.123 m DOWN onto its table, hiding the real defect: the stand holding the screen up was never reconstructed. The snap is now **asymmetric** — lifting out of a surface corrects an impossibility and may travel the full reach; lowering asserts a contact the depth fit did not find, and is bounded at 0.10 m (blessed downward corrections measure 0.06 m, rejected ones 0.12–0.19 m). Both monitors keep their measured height with the honest gap. **The redirect — "no point spot fixing in a capture/frame selection procedure that's not good enough" — is right, one level up from where it was aimed, and is now measured (0152):** occlusion is computed NOWHERE in the pipeline and varies 0.00–0.90 across shipped views, with a far cleaner view available and unused on 4 of 15 box objects (spike box_05 ships at 0.90 occluded with 0.22 available) — but the two "legless table" complaints are NOT selection failures, because 0.37 and 0.50 are the best the whole capture offers across 27 and 49 qualifying frames. The reading that explains all of it is absolute rather than comparative: **no single frame gets a good look at any object** — measured from depth, where porosity cancels (0154; the earlier mask-over-box-hull reading was withdrawn as a porosity artefact), the shipped frame contributes a median **0.48** of the surface the best available frame does, below 0.8 on 11 of 15, and even the best frame in a 722-frame capture is not close to covering an object. Ten candidate view-quality measures have now failed to predict reconstruction quality — **but all ten are GEOMETRIC, and the operator caught the gap (0153): nobody ever measured whether the PICTURE was any good.** Normalised Laplacian variance over each object's own region says the shipped view is a **median 0.50 of the best available sharpness (min 0.09, below half on 7 of 15)** — the spike bed shipped at 0.303 against 3.549 available, 11.7×, confirmed by eye on two crops — and sharpness is the first measure to point the right way at all (r = −0.298, n=15, weak but every other measure sat at zero or wrong-signed). The census sampler selects on pose diversity and box visibility, the association ranks on footprint overlap, and **neither ever opens the image.** The "view selection is dead ground" conclusion is withdrawn to its precise form: where the camera WAS does not predict quality; whether the picture was SHARP was never tested. The sharp frames are already in the capture, so the fix is server-side selection rather than user guidance, and it needs one GPU experiment to confirm. A single view cannot show a whole piece of furniture in a furnished room, so no selection policy over single views has a good option to pick — which makes multi-view consumption (0151) the whole game and the sampler's object-blind budget worth nothing until something can use more than one view. One GPU experiment is named and would settle the selection half. **The capture-guidance question is SETTLED by measurement (0155):** against a reference of voxels seen by ≥3 frames, the shipped frame covers a median **0.18** of an object's surface, the best single frame **0.31 (max 0.50)**, and the best THREE together **0.65**. 0.50 is exactly the geometric ceiling for one viewpoint of a solid object — you see the front-facing half, never the back — so the best-case objects are AT the ceiling and no live guidance can pass it. Selection is a free 1.7× (0.18→0.31, server-side, the frame is already in the capture); multi-view is 2.1× on top of that and is the only thing that reaches a complete object. Guidance is the only one of the three that asks anything of the person holding the phone and has the lowest ceiling; its one surviving case is obstruction (0152), for surface no frame in the capture ever saw. **What multi-view means here, since it is easy to misread (0151): NOT a multi-view reconstruction model** — `SAM3DModel.reconstruct` takes one RGBA image, Meta's documented signature — but unioning the independent single-view outputs the pipeline ALREADY produces and discards (`PERCEPTION_PLAN_VIEWS_PER_BOX`=2). Measured qualifier: those two views sit a **median 33.8° apart, never over 90°, 3 of 8 under 30° and one pair is literally the same frame**, so unioning today's pairs would union two views of the same side. Registration wants overlap and completeness wants baseline, so there is an optimum band rather than a monotone — and "pick a SECOND view that sees a different side" is a selection question in a shape 0146/0152 never tested, one sort key in `_build_reconstruction_plan`.
- **Every reconstruction this project has made fed SAM 3D a GUESSED depth map, on frames carrying measured LiDAR — and it conditions the SHAPE, not only the layout (decision 0180; measured inside the serving image, no GPU run yet).** `Inference.__call__` takes a fourth parameter, `pointmap`, that appears in no documentation; `models/sam3d.py` passes the documented three, and when it is None `InferencePipelinePointMap.compute_pointmap` builds one with a monocular depth model (MoGe) run on the single RGB frame. So the pointmap is not an unused feature — it is used on every run, and ours is estimated. **All four of 0179's open checks are POSITIVE**, read out of `perception-obj:20260813-222442` itself: the parameter is there (upstream `f91db411`, cloned 2026-08-09); `checkpoints/hf/pipeline.yaml` instantiates `InferencePipelinePointMap` with a live `pointmap_transform`; `ss_generator.yaml`'s `EmbedderFuser` lists a **`PointPatchEmbed` over `pointmap`/`rgb_pointmap`** beside the two Dino embedders, and those tokens condition the MM-DiT that emits `shape` as well as rotation/translation/scale — so **0179's layout-only reading is refuted, and check 3 is settled: shape AND layout**; and the fuser loads from `ss_generator.ckpt` with `strict=True`, so the released checkpoint carries trained pointmap weights (its training config even gives the pointmap its own CFG channel). Stage 2 (appearance) gets no pointmap by design. The shape branch sees the pointmap SSI-normalised, so it consumes the *relative* depth relief — which is the truncation axis — while the absolute metres go to the `ScaleShiftInvariant` pose decoder. **The input contract, measured:** `(H, W, 3)` float32 metres, **whole frame** not masked (0179 said masked — wrong; the pipeline crops for itself), NaN for holes (`PointPatchEmbed` substitutes a learned `invalid_xyz_token` for every non-finite point), and the **pytorch3d camera frame** — +X left, +Y up, +Z forward — because a supplied pointmap is used VERBATIM while only the monocular branch gets the convention rotation. From ours: `p_sam3d = (−X, +Y, −Z)` of an ARKit camera-local point, which is 0065's `_SAM3D_CAM_TO_ARKIT_CAM` read backwards. Every resize in the chain is nearest, so supplying at the native 256×192 LiDAR resolution is bit-identical to upsampling ourselves and nothing invents a depth between two surfaces. **What the guess is worth is now MEASURED too, offline, on 42 observations across 8 frames of 3 walk rooms (decision 0181)** — the pinned MoGe (`microsoft/MoGe@a8c37341`, `Ruicheng/moge-vitl`) run as the pipeline runs it, against the LiDAR, through the pipeline's own SSI normalisation. **The frame is independently confirmed by the model itself**: the layout translation is decoded in the point map's frame, and against the median LiDAR point under each object's mask the shipped frame reads **median 3.1° / p90 10.0° / 0% beyond 45°**, where X-not-flipped is 26.7°, Y-flipped 22.9°, and the two Z variants ~140–157° (object behind the camera) — so a mirrored point map is ruled out on real data, not by derivation alone. **Layout: the guess has no usable metric scale and no constant would fix it** — the factor carrying MoGe units to metres measures 0.755–1.630 across the eight frames, a **2.16× spread**, which is the measured form of the defect 0052 and 0065 already worked around by replacing SAM 3D's translation/scale with a LiDAR fit; a supplied point map makes it 1.000 by construction. **Shape: broadly right, bad in the tails** — relief ratio median 1.017, the two maps disagreeing by a median **10% of the object's own depth**, but ranging 0.62×–1.72× and up to 68%, worst on small things (chair 1.72×, nightstand 1.62×, lamp 1.38×, curtain 0.62×) plus the spike bed at **0.677× / 0.209 m** — the bed 0080 called ~90° yaw-wrong and 0129 measured 30.7% sheared, though the same bed from a second view reads 0.931×, so that is a view property. One character difference, not accuracy: MoGe returns a value at every pixel (1.000 valid on all 8 frames) where LiDAR returns 0.89–0.97, so measured depth buys measurement on most of the frame at the cost of the learned invalid token on 3–11%. **Read 0181's own caveat before quoting it: this measures the model's INPUT, not its output** — a 10% conditioning error entails nothing in particular about the reconstruction, and nothing here tests how heavily the sparse-structure generator weights the pointmap tokens against the image. **BUILT, tested, and deliberately unused:** `placement_math.depth_pointmap` (the dense form of the existing back-projection, NaN for holes, pinned against `unproject_depth` on the pixels it keeps) and `placement.sam3d_pointmap` (the same map in SAM 3D's frame); 7 new perception tests + 4 schemas tests. **THE BENCH RAN, AND THE RECONSTRUCTION BARELY MOVES — a measured point map is NOT a fix for class-6 truncation (0181's Outcome, GPU, 2026-08-16).** Six objects in two rooms, control and treatment on the SAME candidate image at 0% traffic, differing only in `PERCEPTION_POINTMAP`; both runs' SAM 3 masks bit-identical to each other AND to production's, so the control reproduces what shipped down to the point counts. **The pre-registered prediction is refuted**: stated before the run, rp7's desk should have moved its e2/e0 shape signature from 0.286 toward its measured box's 0.511; it moved to **0.283** — 1.4% of the predicted magnitude, the wrong way — and across all six the largest move on that axis is 0.011 against gaps of 0.20–0.23. **The one real signal is the spike bed**, picked because it is where the guess was worst: e1/e0 0.597 → **0.650**, closing 20% of its gap to the box, while shedding **half its Gaussians** — one object, one axis, and "half the mass" wants eyes before anyone calls it better. Point counts move −10% to +4.5% elsewhere, so the pointmap IS consumed; the sparse-structure generator simply weights the image far more heavily. **Consequence: lane D's premise is RESTORED** — unioning views remains the live route on truncation, and this one is closed. What stays open is the LAYOUT half, untested here because the bench compared splats rather than placements. The pipeline accepted the tensor without complaint (`measured=0.930` on every object, both scenes `ready`). Nothing was left behind: both bench revisions deleted, all four throwaway scenes and their outputs and captures removed, and production still serving `perception-obj-00041-9kk` on the same image digest it started on. **Collateral defect, unrelated to the outcome:** the Dockerfile's `git clone --depth 1` has no pin, and the BuildKit cache (0120) makes that sharper — the SAM 3D source layer inside the image tagged `20260813-222442` was built **2026-08-09**, so the model code is dated by neither the tag nor the deploy. Pin the clone to `f91db411c50efee93d8db7aeb323885650f6f722`.
- **Room-quality residue: the operator walk RAN; what it leaves (decisions 0148/0149/0152).** (a) CLOSED 2026-08-13 — the work is merged AND serving (`perception-obj-00043-yiz`; see the ship bullet in What-works). (b) **The seating anchor was NOT ruled on** — the operator's answer to the desk question was that BOTH versions have legs not touching the floor, which is the class-6 defect showing through and makes the anchor choice secondary: rp7's desk now has its top at the measured height with its legs stopping 0.47 m above the floor, where before the whole desk floated 0.22 m up with the monitor floating 0.21 m above THAT. The third option is now BUILT as a viewer A/B so the ruling is made against the thing itself — `scene-{rp7,rp6g1}-*-vfill` stretches the vertical axis alone to fill the measured height (rp7 ×2.35 from 0.43 fill, rp6g1 ×2.44 from 0.41), nothing shipped; 0080 ruled out per-axis stretch and 0148 records that this narrow form (one axis, toward a measured height, only under 0.85 fill) is not the thing they judged. The A/B renders now draw the measured box floor and top as reference planes, because the desk sits between both faces before and on the top after, and that is unreadable without them. (c) **The rp7 monitor is still the small reconstruction.** With the fork closed the member reselection still prefers it, because tier 2 (appearance) favours it 0.589 vs 0.426 while tier 1 favours the larger one — a splat containing only what one camera saw reprojects cleanly into that same camera, so appearance self-consistency can reward truncation. The measurement-grounded replacement (does a reconstruction span its own LiDAR cloud) was measured and is WEAK — ratios sit between 0.79 and 1.12 on almost everything and it would change picks nobody has adjudicated, which is the pattern 0104 warns against. (d) rp6g2 is excluded from the shipped-vs-fixed comparison by construction: its manifest was built over four rounds and only 6 of its 12 selected frames ever completed, so a diff there compares inputs, not code. (e) **The 180° facing sign now has FIVE refuted instrument families, not four** (0156): a vision model shown the photo and both hypotheses rendered from the source camera scores **2 right, 2 wrong, 1 unclear** at confidences 0.50–0.65. The informative failure is the cupboard, where the model's own reason is that both renders look alike — SAM 3D invented a back carrying a grid of panels much like its front, so the TEXTURE asymmetry the idea rested on is not there. Named confound: `render_splat` is a crude 256 px point rasteriser and the renders are a sparse dot field, so a proper Gaussian render might move the two chairs — but it cannot rescue the cupboard. The shared reason all five fail is now measured twice over: **a single-view reconstruction's unseen half is fabricated, and every one of these instruments is asking that fabricated half a question.** Re-open only after the union (0151) gives the far side real observed texture, or settle it in conversation (0133's descoped rotation), which after five refutations reads as the correct design rather than a fallback. (f) **class-6 truncation itself is untouched** — rp6g1's monitor still has no generated base, and the desks are still legless; every fix in this session places or orients incomplete reconstructions better rather than completing them. (g) `fusion.py` and `box_placement.py` carry **35 pre-existing `UP045` ruff errors** (`Optional[X]` rather than `X | None`), byte-identical in count before and after this thread — verified by running ruff against both trees. The room-quality work introduced none, and its six new test files are clean. Recorded so a session that runs ruff on those files does not spend time deciding whether it broke something; fixing them is cosmetic and would collide with any lane editing those two files.
- **The anonymous UID churn — MECHANISM NAMED, churn 1's trigger measured, churn 2's trigger still open (decisions 0115, 0138, 0139, 0140, 0141).** Measured on the 16 Pro: three identities minted bundles from this one phone — `cHfMlUL` (6 phone captures 07-26→08-05, plus scene `a7e073ae`, which is the RP-8 Mac-side spike upload and NOT a phone capture), `j9UJyV6s` (5 scenes on 08-08 — 3 ready incl. the hero source `ce68e24f`, 1 `failed_invalid`, 1 `failed_incomplete`), `u4AmDs2Vd` (2 rooms, 08-12/13) — so 0036's "never churns the UID" invariant has been violated at least twice on real hardware, orphaning each period's rooms. **The differential 0138 left unexplained is the SDK deleting its own credential (0139):** `User.signOutIfTokenIsInvalid` fires from twelve call sites — every token refresh, so every `getIDToken()` — and on `userNotFound`/`userDisabled`/`invalidUserToken`/`userTokenExpired` calls `signOutByForce` → `updateCurrentUser(nil, savingToDisk: true)` → `keychainServices.removeData`. `DeviceIdentity`'s item is a different `(service, account)` pair and nothing removes it, so **no whole-partition event is needed**; `signInIfNeeded` then sees nil and mints, and ScenePoller calls it every tick so no relaunch is required. **Churn 1 is dated and attributed:** `cHfMlUL` is the only anonymous uid in the project carrying a `validSince` (2026-08-07 22:26:55 UTC), 93 s after the audit log's `CreateDefaultSupportedIdpConfig` — Gate B — and 8.8 h before the replacement uid. Gate B's own action, setting a verified email on an anonymous user, was replicated on throwaway users: it returns `TOKEN_EXPIRED` on the next refresh (a `displayName` control does not), and `AuthBackend` maps that to `userTokenExpired`. **A revoked token alone does NOT churn** — driven against the real app on a clean simulator, revoke-then-relaunch kept its uid, because `signInIfNeeded` returns early on a loaded user and nothing spends the token; that is why the loss surfaces hours later. **Three leads are dead by measurement (0139):** `GOOGLE_APP_ID` — which is what the Keychain service name derives from — is identical in every build product 07-21→08-12 including both sides of the 08-10 plist refresh; Firebase's accessibility class is the SAME `AfterFirstUnlockThisDeviceOnly` our item uses, not stricter; and the 08-08 API-key restriction hit the web key, not the iOS one. **The re-sign correlation is a sampling artifact, not a cause** — both uids appear ~5 min after a fresh profile, but the app is unlaunchable between re-signs, and the 08-08 09:22 UTC rebuild+reinstall kept its credential. **Churn 2 remains open (0140)**: `j9UJyV6s` exists, is not disabled, and carries no `validSince`, so no server-side revocation explains it; three named survivors with instruments. Two data traps recorded in 0138 so they are not rediscovered as findings: `a7e073ae`'s tool-synthesized `device_id`, and uid `6DiYzw9a` (the smoke tool) sitting precisely between eras 1 and 2. Consequence already banked: the three 08-08 rooms, incl. the hero fixture's source scene, belong to a uid no device is signed in as. **Project-level anonymous-user auto-cleanup is OFF and must stay off** — it would fire this mechanism for every user on a schedule.
- **0074's foreign-record stand-down drains ONE record per launch** (decision 0115). `foreignBundleToAcknowledge` returns a single bundle, so a device with N foreign records needs N force-quit/relaunch/tap cycles, and the pill returning each time reads exactly like acknowledgment failing to persist. It does persist — measured, `acked` 10→16 strictly monotonic, and `ea40c579` flips false→true across one launch. Deliberately NOT fixed yet: quieting the symptom before the churn's cause is known would make the orphaning less visible without making it less real (0115's ordering argument).
- **The Live Activity count freezes when the process is dead, and only remote push can fix it** (decision 0114). Force-quit mid-upload: the daemon finished 554/583 blobs with no process alive, and the Lock Screen held "Sending your room 35 of 582" — the WORD honest, the COUNT stuck at the moment of death, because nothing was alive to hear the remaining callbacks. Not the 0111 gap resurfacing. The cure is the already-named `LiveActivityController.pushTokenSeam`, still unbuilt, now with a hardware measurement attached. **Also settled by the same run: force-quit produces ZERO background relaunches** (positive evidence — our own breadcrumb file is silent for 30+ min), so the `.finalizing`-across-background-relaunch race is OS-KILL-ONLY and cannot be staged by force-quitting. Board item 2's OS-kill gate keeps `StagingHooks`' `exit(0)` route as the only way to reach it, and remains open.
- **`AccountConflictView` has still never rendered.** The designed §8 conflict screen — with real room counts on both options, which is the whole point of it — is documented unreachable at `RootFlowView.swift:31`; `SignInSheet` handles the conflict with two stock alerts that state NO counts. The real switch went through the alert on 2026-08-13, so the shipped path is now hardware-proven and the designed one is dead code with a passing preview. Either wire it or delete it; leaving a screen that only exists in `#Preview` is the trap 0072's review passes kept finding.
- **The 0105 declared-blob gate is SERVING (`api-internal-00023-mek`), with its negative control run live.** A ROOMPLAN bundle with room.json present passes through; a dropped declared RoomPlan blob goes `failed_incomplete` naming the path (`roomplan/room.usdz` in the live probe). The `json_gcs_path` line itself still has no live positive case — the smoke tool's `--drop-blob-kind` takes `rgb|depth|confidence|usdz` — the deploy's negative control used `usdz` — but has no `json` kind, and building one was declined — so that one line rests on its unit pins until the first real ROOMPLAN capture whose room.json fails to land, which is exactly the event the gate exists for.
- **P0 — CLOSED by the operator's own eyes on production (2026-08-10).** The two-minute check ran: signed in on the production origin (the FIRST production sign-in ever — Gate B's remaining leg closes with it), opened a real room, `.spz` loads, and the verdict was "lightning fast" against the former 6–7 minutes. **Both remaining questions ANSWERED 2026-08-12: the 0127 reveal pacing "reads right" on production, and the white-screen reload has NOT recurred since the payload fix — it closes as load-coupled (undiagnosed but moot) and the HAR ask is retired.** **0124's fired trigger is CLOSED** — the placed-only filter shipped on `api-public-00036-duv`, A/B-confirmed 22 → 10. The operator's remaining report — "the visual problems with the room are still present" — is the QUALITY thread, not the payload thread: see board item 7 and the 0135 clip-sign A/B.
- **Stage 2 is DEPLOYED and its language residue is CLOSED (apipub-polish, merged + deployed 2026-08-10; serving `api-public-00036-duv`; outcomes written back into 0107/0108/0124 — 0114/0115 unconsumed, the 0088 precedent).** 0108: a first-time move now reads "the {name} is against the wall" — "back" is revert's word; verified by a live post-flip turn on the original request ("Done — the bed is against the wall…"), with a direct solver pin so the ruling regresses loudly. 0107: the mutation eval now tests the contract the guest HAS — tools attached, real `run_tool` against real geometry, asserting honest grounding or honest refusal; full voice suite 10/10 live at PROMPT_VERSION 3. 0124: `/assets` signs placed objects only, confirmed 22 → 10 by A/B on both revisions with the consumer sweep done first; `asset_urls_compressed` can never carry a key without its PLY fallback (pinned). **Still open:** (1) `unprompted_proposal` has never been observed in real traffic — only real usage answers it; (2) CLOSED 2026-08-10 — the mock strings sync'd to the deployed wording (chip session, `d850b81`; browser-walked: "back" survives only in the revert offer); (3) CLOSED 2026-08-13 — 0133's rotation descope re-opened as a facing CORRECTION rather than a proposal (0157/0158/0159), built on `lane-a-facings`; its voice evals ran and it deployed 2026-08-14 (`api-public-00038-qiv`).
- **The clip-sign A/B is BUILT and MEASURED, and the measurement inverts the story: no recorded clip number ever described what users actually see (decision 0112; branch merged; the operator walk is the one open gate — no default changed).** The A/B instrument's trust gate reproduced 0129's published fractions and the manifest's `removed_fraction` to ±0.01 under the MEASURED sign — so 0129's instrument, 0104's "live values", and perception's own accounting all describe the clip as perception meant it, while **what the browser actually cuts had no recorded number until today, and it is much worse on 7 of 9 objects**: the spike table believed "inert" at 2.2% actually renders with **all four legs amputated at 35.4%**; rp7 storage renders a diagonal sliver at **50.8%** (believed 27.7 — 0129's 28.7 is a transcription slip); rp7 bed 24.0% (believed 1.8), rp6g1 table 34.5% (believed 16.4). Alpha-weighted fractions sit within ~2 points of counts — visible mass, not transparent fluff. **The flip cannot un-trim**: wherever `cut_only_measured` is 0 the measured cut is a strict subset of the shipped cut, and mass past the measured box is cut under BOTH signs on every object — while the shipped sign KEEPS phantom past the measured box on 4 objects (spike bed to 0.195 m: part of the 0.44 m overhang the 0085 walk complained about is still rendering today). The two-errors-compensate mechanism is real on exactly ONE object — the spike bed, whose wrongly-oriented splat happens to fit the wrongly-rotated box, which is why 0104's by-eye adjudication read acceptably while the same error amputated table legs elsewhere. **A second consumer found: the stage-2 measured OUTLINE is also drawn with the shipped sign** — 0135's expectation that solver-outline and clip would visibly disagree on a mis-clipped room is refuted; they agree with each other and both sit 2θ off the true box (`clipSign.ts` now owns the sign for both, toggle `?clipsign=measured`, shipped default test-pinned untouched). **THE WALK RAN 2026-08-12 AND MEASURED WINS — SHIPPED AND DEPLOYED TO PRODUCTION** (commit `3755bad`; verdicts in `outputs/clipsign-ab/WALK.md` + 0112's Outcome section). Tally: 3 clear measured wins (spike table — "doesn't make sense to have a table with no legs" — rp7 storage, rp7 bed), 1 lean (spike bed, "more accurate in its extents"; the operator attributed its residual roughness to rotation/truncation, not the clip), 3 ties (rp7 chair, rp6g1 table, rp6g2), 0 shipped wins — both eyes-call objects landed "no worse", so nothing argued for the shipped sign anywhere. `viewerYawRad` now negates unconditionally; `parseClipSign`/`ClipSign`/`?clipsign`/the `clipSign` prop are DELETED; the pins flip to hold the semantics (local +x at (cos θ, sin θ)) plus a new pin asserting the applied sign differs from raw yaw by exactly 2θ, so a regression to the shipped sign is loud. web vitest 207, tsc, eslint, static-export build green; production origin verified after deploy (zero console errors, hero 200 @ 3557 B, privacy locks 404). 0135's open question closes answered-POSITIVE. Independent corroboration from the same day's item-7 walk: the spike bed stops intersecting the table and chair **only** under the measured sign.
- **0096's stated reason for the longest-dimension-only size rule does not hold (decision 0137), and the rule is unchanged anyway.** `roomplan_box.dims` is (width, height, depth) in the box's yaw frame: across 31 boxes in four rooms the largest lands at index 1 sixteen times, index 0 twelve, index 2 three — not sorted in any order — component order survives into `splat_clip.half_extents_m` to 2e-4, and every box over 1.5 m tall is a wardrobe or a refrigerator. `scene_facts`'s SIZES section says the triple is "descending-sorted in all six real boxes examined" and that "nothing in the manifest distinguishes them"; that is false on 31 boxes. Nothing was changed: it is a charter rule (3b), a bump needs live voice evals, and 0096's SECOND reason (splat extents are not size truth) is untouched. This is the evidence board item 10(a) was waiting for, and 10(a) still wants perception to ship `i_up` explicitly rather than anyone inferring an axis convention — inferring is what produced this note.
- **The hero's A/B is OPEN — the operator's taste call (decision 0122).** Variant (a), geometry only, ships and is deployed to the preview channel. Variant (b) — exactly one piece settling in at the end, named — is built behind `?hero=b` but **cannot be seen on any deployed origin by design**: a real object splat is a possession, so its files are gitignored AND in `firebase.json`'s hosting ignore list. To look at it, stage `web/public/hero/piece.json` + `piece.ply` and run the dev server. Measured while probing: the cheapest splat from that scene that still reads as *furniture* is 18 MB and renders as a pale truncated slab (0080's class-6 defect); the one that reads WELL is the bed at 44 MB, and it does read well — that is a real counter-argument to (a), not a formality. **Also unjudgeable here: pacing.** The automation Browser pane runs **zero rAF frames while idle** (measured: 0 frames in 9 s) and only renders when a screenshot forces one, so no mid-reveal frame can be observed on demand — the deployed-origin screenshot that happens to catch the contour mid-draw was luck. The choreography's shape was verified offline instead, by driving the SAME pure functions from `lib/reveal` over the shipped fixture and rendering a storyboard; whether ~3.4 s of wordless stage before the copy lands is right needs the operator's eyes.

- **The photos history purge is DONE, and the repo has a remote (decision 0101).** Verified 2026-08-08: `git rev-list --objects --all | grep -c HEIC` → 0 and `git log --all -- 'test_data/photos/*.HEIC'` → no commits, so the nine HEIC photographs of a real bedroom — which carried GPS EXIF pinning a home location — are gone from history, not merely from the working tree. `origin` is `github.com/feynma1h/roomstudio` (private) and `main` tracks it. The one branch deliberately NOT pushed is `diag-bundlepb-reason-public` (local-only, parked — the single piece of history not on the remote). Consequence: `.github/workflows/*` have now EXECUTED — python and web are green on Linux for the merged tree. The FIRST run went red and found a real test-hermeticity defect (see the Python test policy section), which is the argument for having landed CI when it did.
- **The numpy/Accelerate test failure is SETTLED, and it is not our bug** (commit `897fdd2`). `test_shell_observation.py::TestMedianSelect::test_closer_frame_outweighs_far` raises `IndexError` under numpy 1.26.4 on macOS/arm64, where numpy links Apple Accelerate — and **passes on Linux CI with the same numpy**. Replaying the failing call's guard logic 50× gives max index 99 into a 100-row mask every time, while the real call on those same inputs indexes 100: the bounds guard is provably correct and the BLAS is miscomputing. **The workflow comment says explicitly NOT to clamp the indices** — that would convert intermittently corrupt numerics into silently corrupt pixels, and the crash is the better failure mode.
- **Material FAMILY instability is diagnosed, and the recommended fix is NOT applied (decision 0100).** Cause: the vision call is non-deterministic (temperature omitted — non-default sampling is rejected on claude-sonnet-5) and its only stability mechanism is the receiver's write-once noop, which a re-bake bypasses by definition; the prompt asks for a self-reported probability; and the gate admits the floor (`>=`). Measured across every preserved real shell (n=7 admitted families): six at 0.85–0.98, one at exactly 0.60, and **the interval (0.60, 0.85) is empty** — the distribution is bimodal and the floor answers are the unstable ones. SHIPPED: admissions are now logged with their confidence and an `AT_GATE_FLOOR` tag (previously only rejections logged, which is why this needed a hand diff of two shell.json files to find). RECOMMENDED but not applied: raise `SHELL_MATERIAL_MIN_CONF` 0.6 → 0.75 — env-only, no code change, drops exactly the unstable answer and keeps all six confident ones. Left to the operator because this session had a no-deploy constraint and 0070 asks for reference-room re-adjudication on material changes. Direction of error is safe: raising the gate can only yield `family = None`, which is the module's own load-bearing fallback (clean matte in the MEASURED albedo, which this gate does not touch).
- **Deferred optimization carried over from the retired `tasks.md`:** a client-ack cleanup path for the `scenes` collection, layered on F6's TTL — the client acks after consuming a terminal result, triggering prompt deletion, with the TTL remaining as the backstop for scenes never acked (app crash, force-quit, uninstall). Explicitly NOT ack-only: client cooperation is not guaranteed. F6's TTL half shipped in 0086, so the precondition is met and this is now a live optimization rather than a blocked one. This was the single item in the 207-entry tracker not recorded anywhere else.
- **Composer mid-stream failure: BOTH `reoffer` AND `confirming` now WATCHED — `confirming` is REACHABLE and stays (decision 0117, 2026-08-08).** The doubt is resolved: reading the dispatch sites found THREE, not one — a mid-stream read throw, a response that ends with no terminal event, and (the decisive one) `fetch()` itself rejecting in `Conversation.tsx:205`, which a user whose connection drops hits on an ordinary day. Reproduced live in a real browser with a REAL pointer click against a harness that streams three deltas and then ends the response with no `done`/`error` (verified on the raw wire): `submitting` → `confirming` in ~1 s ("Making sure that reached the room…", words retained, composer stood down) → the 4 s `client_msg_id` refetch loop → `reoffer` at the 150 s window with the distinct `connection_lost` copy and the draft live in a re-enabled composer. The previous attempt's inconclusiveness WAS the JS-driving artifact it suspected — a real click and real key events drive the machine correctly and nothing needed fixing. The state earns its complexity: the 0058 disconnect shield means a turn can persist server-side after the client stops listening, so "unknown" is the honest position and collapsing it into `reoffer` would tell users their words were lost while the room was answering them. Harness preserved at `confirming_harness.py` in this session's scratchpad. **The prior wording of this bullet is superseded; what remains unchanged:** client wake cadences stay untuned, standing trigger real usage data, not a staged failure.
- **RP-8 residue (the mint-envelope gap itself is CLOSED — see What-works; decision 0080).** (1) CLOSED 2026-08-07: `ab0a060` + `5e3807e` are SERVING (first on `perception-obj-00038-ses`, and carried by every image since — the current serving revision is `perception-obj-00036-l9l`; see the revision-numbering note in Conventions). (2) Classes 1–5 + the envelope kink are BUILT and SERVING (decisions 0081/0082 — see What-works); what REMAINS of the 0080 list: class-6 splat visible-region truncation (model capability, not placement), the window's 30° in-plane skew (0068-population — the cloud instrument is structurally blind to in-plane spin on near-square planar objects, same as every appearance instrument before it), and box-splat overflow, whose 0082 NON-fix was REOPENED by the 0085 walk and is now RESOLVED a third way (decision 0104): the object is still never moved or rescaled — 0082's reasoning holds — but the overshoot is declared as a `splat_clip` volume and simply not rendered, since the box is measurement and mass outside it is known-false. (3) FULLY CLOSED 2026-08-12 — the reveal choreography was REDESIGNED and BUILT (decision 0097) and the operator's half is now answered POSITIVE on both questions: pacing "reads right" on production, and the contour "reads as measurement" in the item-7 walk. No follow-up. (4) **`b667f891` is BUDGET-STARVED, now with a measured cause** (2026-08-07, 7 warm rounds across two drivers): its census plan carries a **53-item long tail** (the spike's is 24) against the 900 s request budget, so every round ends `budget_stopped=true` AND sets `refinement_skipped` mid-pass — the box pass ships 6/11 boxes with `allow_scoring=false` (extent-best defaults, 0 scored) and the 0082 post-passes never run on that scene. Object counts fluctuate round to round (33→52→45) because a budget-cut frame writes no `objects.json`, so fusion only ever sees the frames that fully completed that round. Not a placement-quality defect: the abort-room's clutter exceeds what one request can reconstruct AND refine. Cures if it ever matters: raise `PERCEPTION_MAX_FRAMES`-adjacent budget knobs, or split reconstruction from refinement across requests. **Judge classes 1–5 on the other three rooms.** (4b) **Class 5 — "the smaller cabinet still hides behind a wall" — is NOT what 0080 supposed, measured 2026-08-08 (decision 0104).** It is not the 0.35 m declip bound: the declip pass never engages at all, because the object's centre projects OUTSIDE every wall's rectangle so `_center_in_wall_rect`'s `inside` test fails. Nor is it the room-sanity gate: the cabinet sits INSIDE the measured floor polygon, 2.14 m from the nearest corner. Whoever picks this up starts from those two facts, not from the bound. (5) CLOSED 2026-08-08: the operator's `serviceAccountTokenCreator` on `firebase-adminsdk-fbsvc` is REVOKED per 0088's recommendation (that SA now has zero bindings; impersonation confirmed 403 twice — note `roles/owner` does NOT confer `getAccessToken`, and an interim "revocation didn't work" reading was IAM propagation lag). Re-grant/revoke commands are in `outputs/PURGE-RUNBOOK.md`; a future operator walk that needs a custom-token mint re-grants for the day. (6) The captures-bucket copy of the spike bundle sweeps at age=1d; the durable copy is `outputs/roomplan-spike-bundle/` (regenerable from the spike recording). (7) The **mirror-reflection product note was PROBED and CUT** 2026-08-08 (decision 0091): the depth-trust gate is NOT a mirror detector — across six preserved LiDAR manifests only 2 of 5 mirrors were demoted and 9 demoted objects were fans/curtains/chairs/doors (precision 18%, recall 40%; the pattern re-observed live on the spike re-drive). The reliable identifier is the SAM `mirror` LABEL (5/5). Build cut because only 1 of 5 mirrors is anchored to a MEASURED wall it could inherit a plane from, and "what should a mirror look like" (true planar reflection vs stylized vs the honest baked splat) is a design call — it belongs with the reveal-choreography session, keyed on the label, never on the depth-trust flag.

- **ARKIT_ONLY placement + shell quality investment PARKED — pivot to a Pro-only / LiDAR-first pipeline (operator decision, 2026-07-24).** After the non-LiDAR reference room (`f3d70236`) still read poorly on real output, the operator chose to redirect effort to the premium LiDAR path (decision 0071 direction, Option A) rather than keep raising the non-LiDAR quality ceiling. The hardware block CLEARED 2026-07-26 (iPhone 16 Pro in hand; both hardware gates green — see What-works) and the operator RESOLVED the 0071 fork: **Option A — RoomPlan-only, Pro-only, assembly-first**. The RoomPlan integration DESIGN is done (2026-07-28, decision 0077 + build brief); execution is board item 7. The shipped sanity gate + chunk D stay LIVE (strictly better than before; `PLACEMENT_REFINE=0` rolls back if ever needed) — nothing is rolled back. No further non-LiDAR merge-knob / placement grinding; the operator `/viewer` walk of the non-LiDAR output stays moot under the pivot (the LiDAR rooms are the ones worth walking now).
- **Phantom-room row on backup-migrated devices (decision 0074): fix BUILT + MERGED (2026-07-28), hardware verify pending.** The defect chain (records migrate; blobs, Firebase identity, DeviceIdentity do not; restore adopts a foreign `.complete` record; its poll 403s; connection-trouble with no exit; phantom forever) is closed in code — see the What-works bullet. What remains: the operator's 16 Pro carries the LIVE repro state (the 16e's migrated records + the real 403) — verify opportunistically in the next device session: cold launch the fixed build, watch the phantom stand down once, confirm the next cold launch is clean AND the 34 June-era dead records are gone from the store after first launch (the sweep runs at every launch; the walk verified both mechanisms in the simulator only).
- **First LiDAR rooms ADJUDICATED — operator verdict: the rooms read "very far from reality"; the correct set is minimal (2026-07-27/28; decision 0075; full diagnosis + per-finding RoomPlan table in `docs/briefs/lidar-first-rooms-adjudication.md`).** The prior under-merge suspicion is RESOLVED: mostly label collapse, not fusion error, and the merge knobs MEASURED CORRECT on both rooms (small↔envelope wall pairs fail on coplanar offset 0.44–3.4 m — real plane separations) — **do NOT tune `FUSION_CLUSTER_DIST_M` / `SHELL_WALL_MERGE_*` to chase these symptoms (0075)**. Measured defect ranking for board 7: (1) floor collapse — 12%/38% of the measured floor ships after clipping against furniture-plane "walls"; (2) furniture-plane wall admission + closure inflation (a 0.53 m bed rail renders as a 3.06 m slab; admission is area+verticality only, ignoring ARKit's own seat/none classifications and height-reach); (3) object-blind sampling starvation (pipeline-side; the captures themselves measured good); (4) ORIENTATION reads near-globally wrong to the operator while the tracked metric is green — min-axis median 9.3° certifies only that SOME axis points up; **YAW is unscored by any current instrument**, in-plane unresolved on 13/16, sign checked on 1/16 (the 0065 lesson generalized to a whole instrument family); (5) the one true under-merge phantom (edge truncation structurally defeats footprint containment); (6) cross-label duplication, mirror specular depth, textile scale collapse. Operator hypotheses recorded as design inputs: live capture-coverage feedback (= task #13's real shape; RoomPlan-native) and object-aware frame selection from a furniture census (= `CapturedRoom.objects` convergence; 0062's successor under Option A). 13bae607's warm re-drive: available, adjudged moot for the design session.
- **iOS app design residue (branch `ios-app-design`, decisions 0072/0073; the flow itself is now hardware-proven — see the 2026-07-26 What-works bullet).** `RootFlowView` is the app root as of `191f91f`; two real captures have run capture-to-doorway on the iPhone 16 Pro, including the disruption paths the nine review passes could never exercise (background trip, upload under lock, relaunch restore, doorway exit). The LiDAR gate still sends the non-LiDAR 16e to `UnsupportedDeviceView` — intended (0071/0072); **the 16e is not a test target and must not shape decisions** (operator direction 2026-07-25). Still UNEXERCISED on hardware: terminal blob failure, server-side scene failure (`failed`/`failed_invalid`/`failed_incomplete` UI), and the OS-kill relaunch (board 2's Gate 2b). Task #13 CLOSED 2026-08-06 — RP-7's live floor plan + real coverage ticks shipped (see the RP-7 What-works bullet; residue: the guidance relay has never fired live — RoomPlan issued no instruction during the gate scan, sparse by nature; review's `thinCoverage`/verdict stay deliberately unwired pending an operator copy decision). Task #14 CLOSED 2026-08-08 — the §5 Live Activity is BUILT (extension target grafted into `project.pbxproj` with the entitlements workaround preserved; see What-works), simulator-verified only: its Lock Screen / Dynamic Island behaviour has never run on hardware, and the device install that would prove it is blocked (see the Xcode-account bullet). The doorway universal-link handoff, the QR bridge, and push remain enrollment/entitlement-gated (associated-domains / APNs) — UI built, transport stubbed at named seams. Fonts fall back to system faces until the branded `.ttf`s are bundled at the `RSFont` seam. Activation follow-ups (recorded in `RootFlowView`'s docstring): add-more resume-with-progress and the real web-handoff link (relaunch poll-resume is DONE — `restoreUnfinishedBundle`, landed with activation and corrected by 0073). The design SCREENS are still verified by build + screenshot, not unit tests; what the 302-test suite pins is the flow LOGIC beneath them (routing, restore selection, deferral scoping, poller visibility, review-copy precedence) — a green suite says nothing about whether a screen renders correctly, which is why AX5 claims must be re-verified by screenshot every time (three separate passes claimed AX coverage they did not have).
- **0051 E2E verification blocked on Apple Developer Program enrollment** (operator enrolling — chosen 2026-07-22 over reopening the provider choice, see decision 0064; approval typically <48 h). The personal team (`3HU2SP8346`) can provision neither the Sign in with Apple entitlement — **device builds fail at provisioning until the paid team lands** (interim capture rebuilds: locally drop the two `CODE_SIGN_ENTITLEMENTS` lines in `project.pbxproj`, never commit that; personal-team signatures expire after 7 days, so this will come up — the 16 Pro's install is on this clock (last rebuilt 2026-08-06 in the RP-7 session; expires 2026-08-13) or the app stops launching) — nor the web Services ID Gate B needs. Post-enrollment, in order: re-select the team in Xcode (if the team ID changes from `3HU2SP8346`, `DEVELOPMENT_TEAM` appears 3× in the pbxproj); Firebase console → enable the Apple provider (no fields); Gate A on-device link (UID byte-identical before/after in the SignInSheet footer, `auth.link … uid_unchanged=true` in the log); Apple portal → Services ID `com.roomstudio.web` (primary App ID = the capture app, domain `roomstudio.firebaseapp.com`, return URL `https://roomstudio.firebaseapp.com/__/auth/handler`) → set it on the Firebase Apple provider; Gate B browser sign-in with the same Apple ID → GET /scenes lists the real captured room → screenshot. The CSP preconditions are CLOSED by 6(a) (2026-07-22): `web/firebase.json` now carries `frame-src roomstudio.firebaseapp.com` AND the second live-found requirement, `script-src https://apis.google.com` (the popup flow's gapi iframe-channel loader). Gate B runs on the preview URL and additionally needs the preview origin added to api-public `CORS_ALLOWED_ORIGINS` first (env-only revision; mirror the yaml in the same commit).
- **iOS P2 LiDAR depth residual (decision 0033): CLOSED 2026-07-26.** Every deferred check ran green on real hardware, twice (both 16 Pro captures): `sceneDepth` non-nil per frame with legal dropouts, 256×192, one depth blob per frame at exact byte sizes, decision-0032 depth intrinsics byte-exact. See the What-works hardware-gates bullet.
- Scene `f077e9ed-d339-4be8-8dbf-37b952abfec2` is intentionally left in `processing` with an expired lease as a canonical stuck-scene reference. Re-enqueue via `tools/reenqueue_scene.py` if ever needed to verify the expiration-check reclaim path end-to-end.
- **Production is LIVE at https://roomstudio.web.app (2026-08-08) — and the render payload on it is the P0 at the top of this section.** The channel that was "deliberately not deployed" pending board-4 hardening is deployed; those gaps closed (decisions 0086/0087/0088). Verified on that origin 2026-08-08: landing, `/privacy`, `/terms` all 200; `hero/room.json` 200; the `hero/*.ply` and `dev-fixtures/**` 404 locks hold; CSP and CORS proven live (0102). What is NOT yet done there: **a real sign-in on the production origin — Gate B passed on the PREVIEW origin only**, and the two differ in referrer-restriction terms (Google's wildcards only replace a whole subdomain label, which already bit once and read back as correct while preview sign-in was broken). Google sign-in works (0094); Apple stays enrollment-gated.
- **Bridge QR is a declared placeholder.** The QR encodes nothing (no deep-link infra exists) and its caption says so. (The reveal's real-browser watch RAN at RP-8 — decision 0080; the follow-up is a choreography redesign, not a constants retune — see the RP-8 residue bullet.)
- **Conversation stage 1 is live; its recorded residue.** (1) Manifest v2 still carries no per-object extents — sizes, gaps, and clearances stay unspeakable (the charter forbids restating center distances as any of those); the manifest half now exists on `main` (0067's `extent_m_sorted`, merged 2026-07-23; reaches production at the next perception deploy); what remains is `scene_facts.py` consuming it with a `facts_version` bump. (2) The ledger deferral and its vocabulary ban are unchanged — no pin/keep/"put into words" anywhere; the "in this room" inventory panel stays as-is. (3) The reoffer/confirming composer states are pinned by reducer tests and mock triggers but haven't been watched against a real mid-stream server failure in a browser. (4) Client wake cadences (blocked 5 s, confirm 4 s, rested 15 s ticks) are untuned placeholder values. (5) Voice evals are live-gated (`RUN_VOICE_EVALS=1` + key; NOT in the offline suite); re-run on PROMPT_VERSION bump OR `GUEST_MODEL` change — the env yaml notes this at the `GUEST_MODEL` line. (6) Real users still can't reach it: 6(a) now serves a preview channel (production deliberately closed until board-4 hardening), and sign-in stays enrollment-gated (6(b)); CORS allows localhost + the two production hosting domains — the preview origin joins at Gate B.
- **Envelope fix residue (fix itself shipped — see What-works).** (1) Cold-start coverage is thin by design (the 2026-07-22 re-drive confirmed the pattern: request 1 spent its wall clock on boot + model load and banked 2 frames; two WARM re-drives then took the scene to 12/12 sampled frames — the multi-round `reenqueue --force` loop is the working coverage recipe); sampling/budget knobs (`PERCEPTION_MAX_FRAMES`, priors, reserve) are one-capture-calibrated placeholders. (2) Large single objects (big masks: doors, beds) can transiently exceed L4's 22 GiB even at the flat 16.4 GiB baseline — both reconstruct attempts OOM, per-object soft-fail contains it, those objects are simply absent from that frame. A capacity fact of L4 + these models, not a lifecycle bug (0061). (3) Stranded scenes from the pre-fix era don't self-heal — `tools/reenqueue_scene.py` is the cure (0060); scene `f077e9ed` remains the deliberate stuck-scene reference.
- **Layout-convention residue (fix shipped, basis-corrected, and production-verified 2026-07-23 — see What-works; decision 0065).** (1) `min_axis_to_vertical_deg` stays sign-blind BY DESIGN; the sign dimension is now guarded by the SIGNED and TRANSLATION-DIRECTION regression pins instead (the identity-twin episode is the case study: line metrics alone crowned a basis that flipped every Z-class object). (2) ARKIT_ONLY POSITION quality is the new frontier, ranked by the operator's real-room walk: the curtain's triangulated center sits ~0.5 m into the room and interpenetrates the bed (measured: centroid rays on an object subtending 0.74–1.3 rad don't correspond to any fixed 3D point — cluster RMS 0.287 m grazing the 0.3 gate, vs 0.007 m on the compact lamp); the one physical bed splits into two clusters ~1.2 m apart (measured root cause, correcting the earlier `FUSION_CLUSTER_DIST_M` guess: frame 28 carries a NESTED duplicate "bed" detection — intersection-over-smaller 1.000 — and fusion's frame-uniqueness merge guard reads it as two objects, structurally forcing the fork at ANY threshold); no room shell for pre-plane captures (`25a14caf` predates plane anchors) amplifies both. **BUILT — chunks A–C merged 2026-07-23** (decision 0067; offline-verified only, warm re-drive + /viewer walk = the live gate): pixel-footprint correspondence (mutual-singleton dedup — the brief's raw-containment contract was amended by a measured door counterexample, see 0068), multi-view silhouette fitting as the ≥2-view position/scale authority (V3 passed: the curtain's fitted center leaves the bed volume, tier-1 0.306→0.430), the two-tier instrument in production; **chunk D (single-view contact priors via the shared `room_planes.py`) BUILT + merged 2026-07-24** (branch `placement-chunk-d`; offline-verified, live gate owed — see What-works). VIO-calibrated monocular depth: DEFERRED with named re-open triggers (0067). Bed verdict: the phantom is eliminated, honest residual 5+1 (0068). (2b) One residual ROTATION failure mode, found by the operator post-fix: IN-PLANE rotation noise — the rendered curtain's plane is vertical but the fabric is turned ~90° within it (pleats sideways; measured: normal horizontal −0.01, in-plane axis plumb +1.00, pleat banding horizontal in the front-view raster). Near-square planar objects are ~90°-ambiguous to the model, and NO current instrument scores in-plane orientation (min-axis satisfied, sign tests inapplicable; provably not a convention twin — doors/beds stand correct). Both candidate fixes are BUILT (chunks A + C, merged 2026-07-23) — but chunk C's V1 gate FAILED on the real curtain: the instrument ranks the shipped-wrong candidate last-of-4 (sees the right direction) with winner margin ≈0.0007 vs the 0.03 gate, so `in_plane_resolved: false` and the layout rotation stands; per 0067's re-open clause the instrument fork RE-OPENS for near-square planar objects (0068; the failing margin is test-pinned so a better instrument = a good pin failure). The sign-flag half passed cleanly (bed true 0.474 vs mirrored twin 0.379). (3) Splats without sidecars degrade to `rotation_source: "none"` on cache hit (none remain for `25a14caf`). (4) 17 of 22 fused objects are `insufficient_observations` — single-frame objects can't triangulate (`PERCEPTION_MAX_FRAMES=12` placeholder), not conventions; 0067 chunk D (BUILT + merged 2026-07-24, offline-verified) makes the floor/wall-class ones placeable from ONE view via measured contact priors — but ONLY on captures WITH plane anchors (`25a14caf` has none, so its 17 stay unplaced until re-captured; `f3d70236` is the target), with `PERCEPTION_MAX_FRAMES` as the complementary coverage lever. (5) The LiDAR `depth_fit` path now consumes a correct rotation — 0063's don't-trust warning lifted in principle; path stays hardware-parked (board item 3).
- **Known fusion limits (v1, documented in 0052):** two same-label objects closer than 0.4 m (`FUSION_CLUSTER_DIST_M`) can merge into one (0067's footprint correspondence is now BUILT on `main` — merged 2026-07-23, `PLACEMENT_REFINE`-gated — but the DEPLOYED revision still runs the bare heuristic until the next perception deploy). The room shell now RENDERS in production for plane-carrying captures (deployed 2026-07-23 — see What-works); pre-plane captures — including `25a14caf` — carry an explicit `unavailable`/`no_geometry_source` shell.json (objects on the grid, no silent absence). The room-shell build brief is deleted (its live-E2E deletion trigger met).
- **Room shell residue (0066 deployed 2026-07-23; surfacing layer superseded by 0069 — see What-works).** (1) CLOSED at RP-8: the shell reveal was watched at real speed (decision 0080) — the follow-up is a choreography redesign, recorded in the RP-8 residue bullet. (2) RESOLVED by 0069: the four floating unclassified fragments no longer render as floating quads — the joint-participation fragment filter drops those with no floor contact and no seam with structure (`f3d70236`: 2 drop, 2 keep with recorded justification), `SHELL_MIN_WALL_AREA_M2` untouched. (3) NEW iOS finding from the E2E: during a fresh capture's upload, `SceneStatusView` kept showing the PRIOR scene's "Room ready" state — the panel doesn't stand down when a new bundle starts uploading (the new scene's poll ran fine; the surface misleads). Next iOS pass. (4) MOOTED by 0069: low-observed-fraction planes no longer render as bare quads from a failed bake — parametric materials give any observed plane a measured-color clean matte, and an unobserved plane (albedo null) renders the honest neutral treatment. No bake coverage needed.
- **PRIVACY GAP — people contaminate shell surface observation (sharpened by the 0069 adjudication).** "Person" is not a SAM-detected class, so anyone in the room during capture is observed as if they were the surface behind them. 0069 (shipped 2026-07-23/24) removed baked textures from serving — person PIXELS no longer ship, which closed the original form of this gap (a family member was visible in `f3d70236`'s v1 floor texture). But adjudication of the v2 shell found the gap MUTATED, not mooted: `f3d70236`'s `wall_03` measured albedo (#899fbf) is the operator's mother lying in front of that wall patch, and a person's image can reach the ≤4 evidence crops sent to the material-inference vision call. The family gate held there (below the confidence bar → null), but albedo is measurement, not inference — it ships. **CLOSED 2026-08-08 by decision 0089** (person is now a suppression-only SAM 3 concept; live-verified on this very plane — a third of `wall_03`'s measured evidence was the person, and it is gone from the shipped albedo — see the What-works bullet). **Residue:** (a) a scene only gains suppression when its per-frame `objects.json`+`masks.npz` are deleted and re-segmented — cached frames keep their pre-0089 masks, so every OTHER existing scene still carries whatever it measured (a re-drive is the cure, and the splat caches survive it); (b) the crop-rejection half has never been exercised by real data (on `f3d70236` the contaminated frames were never a crop tile's best-observing frame) — unit-pinned only; (c) capture-time guidance ("ask people to step out") is still unbuilt and is the complementary fix. Related closed finding: `web/public/dev-fixtures/` (real-room artifacts) was shipping into every static export — the standing preview channel publicly served the July room splats from 6(a) until 2026-07-23; fixed by `2312620` (firebase.json ignore + channel redeploy, fixture URLs now 404).
- **Shell-surfaces v1 residue (0069 shipped 2026-07-23/24; decision 0070).** (1) The closure knobs (`SHELL_JOIN_MAX_GAP_M=0.5`, `SHELL_FLOOR_DROP_MAX_M=2.0`, `SHELL_FLOOR_CONTACT_TOL_M=0.15`, `SHELL_STRUCTURAL_MIN_AREA_M2=1.0`, `SHELL_COMMON_TOP_ENABLED`) are one-capture-calibrated against `f3d70236` like every SHELL_* knob — a room whose real corners need > 0.5 m closure re-opens the join gate's default. (2) The floor's **rectangular-stone pattern is real and detectable** (tile pitch + grid orientation via the plank-direction instrument class over the rectified crops — the operator flagged it on adjudication) — the first candidate for `material.params` growth; deliberately NOT in v1 (0069 excludes pattern treatment: a wrong pattern breaks recognition worse than a clean matte). (3) CLOSED 2026-08-08: `wall_03`'s albedo is no longer measured off a person (decision 0089). NEW in its place, found by that re-drive: **material FAMILY is unstable at the confidence gate** — `wall_06`'s evidence was byte-identical across two bakes (18329 texels, 5 frames, same albedo) yet its family moved None → "tile", and the floor moved "stone" → "tile", both returning confidence exactly 0.6 = `SHELL_MATERIAL_MIN_CONF`. A re-baked shell can therefore ship a different family than the one the operator adjudicated; worth its own look before families are trusted as stable product data. (4) `secondary_hex` is always null (two-tone separation deferred as noisy). (5) The material vision call runs at most once per plane per scene lifetime (write-once noop); a `SHELL_MATERIAL_MODEL` swap must re-adjudicate on the reference room first (env-yaml note). (6) CLOSED at RP-8: reveal watched at real speed (0080); follow-up is the choreography redesign in the RP-8 residue bullet.
- **Pre-launch gaps: CLOSED or dispositioned 2026-08-07 (launch-hardening pass; decisions 0086/0087/0088 supersede the old gap list here — see the What-works bullet for what shipped).** What REMAINS open from that pass, each with its owner: (1) CLOSED 2026-08-08 (decision 0089's session): perception-side `failed` scenes now stamp `expire_at`. (2) The deployed iOS build treats a mint 429 as terminal (`unexpectedStatus`, no retry) — 0038's reserved Retry-After branch is the iOS follow-up; the 50/day default cap makes real-user encounters unlikely. (3) IAM audit findings (0088): the perception-obj default-compute-SA finding is CLOSED 2026-08-08 (decision 0090 — dedicated `perception-obj-runtime` SA deployed and re-drive-verified); what remains here is that `roles/editor` on the default compute SA was deliberately NOT removed (other workloads may ride it — its own verification pass), the stale pre-split `api-runtime@` SA + its captures objectViewer binding should be deleted (operator one-liner); the operator's `serviceAccountTokenCreator` on `firebase-adminsdk-fbsvc` is FLAGGED with a revoke/re-grant-per-walk recommendation — operator decision pending. (4) F1/F2 resolved WON'T-BUILD with named re-open triggers (0087): F1 expires_at (no client consumer wants URI expiry; re-opens if the captures lifecycle window changes or a second platform needs the staleness constant), F2 content-type hardcode (captures are backend-only + swept at 1d; re-opens if capture blobs ever get a browser consumer). (F4 remains closed per 0054 — nothing to do.)
- **happy-path and duplicate-event smoke modes terminate at `failed_invalid`, not `ready`.** The ingest validation gate fast-fails the synthetic fixture's non-decodable placeholder images pre-GPU (~3–10 s including Eventarc latency). This is the verified, expected terminal state for smoke runs. Reaching `ready` requires real image data from an iOS bundle.
- **Phase 8c soak: RUN AND GREEN (2026-07-22 00:57 local / 19:27Z 07-21).** Operator-run happy-path against production: exit 0, `failed_invalid` in 10.5 s, cleanup clean. Covers the serving stack (`api-public-00012-ziz`, `api-internal-00019-des`, `perception-obj-00026-449`); the cold-steady-state intent (lifecycle age=1d + Firestore TTL having fired since the original deploy, scale-to-zero cold start) was met — with the honest note that the two newest revisions were hours old at run time. RUNBOOK Phases 0-8 are now fully executed for the 2026-07-21 deploy cycle; iOS real uploads are unblocked.
- **iOS P3 401 recovery-success leaf unverified.** The live 401 test used a garbage token — exercises refresh→retry→propagate (give-up branch), not the expired→refresh→valid→200 case 0038 actually cites. Mechanism runs; the "refresh yields a working token" leaf is untested. Low risk. The path-violation 400 is confirmed for a leading slash only (`..` not separately confirmed; client emits neither).
- **`validate_bundle` bundle_id cross-check: CLOSED in code, not yet serving.** Built on branch `ingest-bundle-id-crosscheck` (commit `0533c44`, merged to `main` 2026-07-21): `validate_bundle(bundle, expected_bundle_id=…)` runs the proto-vs-URI bundle_id cross-check as check 2 (right after schema_version); any divergence → `failed_invalid` Scene with `bundle_id_mismatch` (both ids in `last_error`) + structured log + HTTP 200, per decision 0031's rejection pattern. Comparison is exact and **case-sensitive** — every downstream join on bundle_id (Firestore `scenes.bundle_id` queries, `upload_sessions` doc IDs, `captures/{bundle_id}/` GCS paths) is byte-wise, so a case-differing id breaks those joins like any other mismatch and is rejected, not normalized (rationale in `_check_bundle_id`'s docstring); an empty proto bundle_id is rejected by the same check. Passing `expected_bundle_id=None` skips the check — only for callers with no upload URI (the api-core fixture self-tests call `validate_bundle(b)` and were deliberately left untouched). **SERVING as of 2026-07-21** (`api-internal-00019-des`, envelope session's operator-approved redeploy; post-flip happy-path smoke green — see What-works).
- **Staleness re-mint missing-file stall: CLOSED by P5(b)+(a).** `onFatalBlobError("blob_file_missing_at_staleness_remint")` now routes to the real terminal handler (sets `uploadPhase = .failed`, cancels sibling URLSession tasks, no record update without explicit save). The staleness pre-pass (Phase 1 of `onSessionExpired`'s `!loopGuardEnabled` branch) confirms all non-`bundle.pb` files exist before resetting any blob status or starting any upload task; a missing-file abort starts zero sibling uploads and leaves the store untouched. `bundle.pb` is never finalized against the hole. Gap was: `onFatalBlobError` was an unbuilt stub — now built by P5(b).
- **Completed-capture disk accumulation: CLOSED by decision 0084** (2026-08-07 — see the release-residue What-works bullet). `CaptureReaper` reclaims record + directory once the user has SEEN the outcome, never on mere upload success. Residual by design: a record the user never acknowledged and never reopened persists — that is the retention the recoverable path depends on, not a leak.
- **No automatic re-upload for `.recoverable`: the SERVER HALF IS BUILT (decision 0116); the iOS client half is the remaining work.** 0084 recorded this as server-blocked and named the un-defer trigger — "a mint contract change that vends fresh URIs for consumed sessions". That change landed 2026-08-08 (branch `server-residue`, NOT deployed): `POST /captures/{id}/upload_session` accepts an additive `force_remint`, which suppresses the path-set replay and mints genuinely fresh URIs for the same paths. Ownership is still checked first (403), it charges mint quota like any real mint, and it charges no second capture for a bundle already claimed. Measuring first corrected the old problem statement: a caller could ALREADY get fresh URIs by sending a different path-set (a subset falls through the replay branch), but that accident misses the case that matters — the captures bucket sweeps at age=1d, so a capture retried the next day has ALL blobs gone and the recovery manifest is the FULL path-set, exactly the one that replays dead URIs. The ingest half was already ready (`FAILED_INCOMPLETE → QUEUED` with the same scene_id), and `validate_manifest`'s exactly-one-`bundle.pb` rule means a recovery manifest necessarily carries the blob whose finalize event re-triggers ingest. 0084's reaper already RETAINS record + files on `incompleteUpload`, so the coordinator's inputs are on disk. **Remaining: the iOS coordinator** (watch `pollState == .recoverable(missingPaths:)`, re-mint those paths with `force_remint`, re-upload with `bundle.pb` last) **and a deploy** — the deployed api-public does not yet accept the field.
- **`bundle.pb` relaunch-recovery: code-complete, OS-kill gate unverified (Gate 2b).** Root cause (decision 0044): recovery path was unreachable on relaunch — wired only to `ContentView.onChange(of: capture.bundlePath)`, not a bare reopen. **Fixed in code by the P5 cluster** ((c) `7143de0`, (b) `b793307`, (a) `27672d0`): `rehydrateAllUnfinishedBundles()` fires on every launch from `.task` in `RoomStudioCaptureApp`; the OS-kill secondary route (`contexts[bundleId] == nil` → `onFatalBlobError`) now routes to the real terminal handler which sets `uploadPhase = .failed` and cancels siblings. **NOT VERIFIED ON HARDWARE:** the OS-kill route (background URLSession relaunch, `.task` fires without entering foreground) has not been staged on-device. The AppDelegate fallback is deferred pending the hardware gate — see decision 0045 Fork A ordering constraint. `diag-bundlepb-reason-public` (`dc552ab`) is the parked tool for reading the redacted `reason=` on the OS-kill route — do not delete. Code-complete ≠ Gate-2b closed; the hardware gate is the close.

## Python test policy

**The suites must pass with NO cloud credentials available.** This is a hard
property, not an aspiration, and it was violated silently until the first CI
run this repo ever executed (run 31259471685, 2026-08-08) failed 12 tests in
`services/api-public/tests/test_upload_session.py` with
`DefaultCredentialsError` → the endpoint 500s → `assert 500 == 200`. They had
been green locally for months only because the operator's machine carries
ambient ADC from `gcloud auth application-default login`. Every count recorded
in this file before that date is therefore "passed **with ADC present**" — the
root suite's 602 included 12 tests that were quietly resolving real Google
credentials and taking ~4 s each to do it.

Cause and fix (2026-08-08): `public_server.py` binds `gcs_mint_resumable_uri`
by from-import, so the test's `patch("roomstudio_api_core.upload_session_repo.
gcs_mint_resumable_uri", …)` rebound a different name and never took effect —
the handler kept calling the real minter. Fixed by injection, the same seam
pattern `UploadSessionRepository` already uses: `public_server._mint_uri_fn`
(None → the production minter; tests patch the global). **Do not "fix" a
credential failure in CI by supplying credentials** — that turns the tests
green while leaving them non-hermetic, which is the actual defect.

Current: **root 620 passed + 10 skipped**, verified BOTH with ADC present and
with ADC made unavailable (`GOOGLE_APPLICATION_CREDENTIALS` unset,
`CLOUDSDK_CONFIG` → empty dir, `GCE_METADATA_HOST` → unroutable). The
credential-free run is also 25× faster (1.3 s vs 33 s), which is itself the
tell that the old suite was doing real auth work. Two pins hold the property
(`TestUploadSessionNeedsNoCredentials`): one asserts the handler returns 200
when `google.auth.default` raises, the other asserts the unpatched seam still
vends the REAL minter — the matched pair, because either alone can be
satisfied by a change that breaks the other (defaulting the seam to a fake
would turn CI green and ship an api-public handing clients fabricated URIs).

**The local `.venv` is not the perception container.** `services/perception-obj/pyproject.toml`
declares `numpy<2`; the shared `.venv` carries **2.4.4**, and perception's 746
tests pass on it. So a green local perception run says less about production
than it looks like, and CLAUDE.md's "cannot share an environment" is about the
DECLARED constraints (which is why CI splits the jobs), not about what actually
runs — they do share one here. This also explains why the numpy-1.26
Accelerate failure below does not reproduce locally: nothing local is on 1.26.
A worktree has no `.venv` of its own; lanes use the main tree's interpreter by
absolute path, which still imports the worktree's own modules.

**A worktree's `packages/` edits are INVISIBLE to the shared `.venv`**, which
carries the MAIN tree installed editable — so `import roomstudio_schemas` in a
worktree resolves to `/Users/aubrey/projects/roomstudio/packages/schemas/...`,
and a lane that adds a function there gets `ImportError` from its own service
code while its own `packages/schemas` tests pass (that suite's conftest inserts
the local path). Prefix the run with
`PYTHONPATH=<worktree>/packages/schemas`, which does win over the editable
install — measured 2026-08-16. Same class as the roomlib trap below, and it
bites `services/*` and root runs rather than `packages/*` ones.

**`outputs/room-quality/roomlib.py` hardcodes the MAIN tree at `sys.path[0]`**
(`REPO = Path("/Users/aubrey/projects/roomstudio")`). A worktree session that
imports it loads MAIN's perception modules, not its own — so a trust gate can
silently certify shipped code against itself, and test collection in a worktree
depends on the main tree's state. Measured by lane E, 2026-08-14. Repoint REPO
before trusting any number from it.

**Suite counts depend on whether `web/public/dev-fixtures` is staged**, and two
lanes have now reported numbers that read like regressions and were not: root/
api-public is 790+18 with it and 715+93 without; perception is 746+0 with and
739+7 without. The fixture-backed tests skip silently. Always say which
invocation a count came from.

Other Python jobs at that CI run: perception-obj **passed on Linux** — the
`test_shell_observation.py::TestMedianSelect::test_closer_frame_outweighs_far`
failure recorded under numpy 1.26.4 did NOT reproduce, so it was macOS/
Accelerate-specific as the workflow comment's second hypothesis predicted.
re-enqueue (18) and ruff (non-gating) passed.

## iOS test policy

The iOS suite is **544 tests total** (was 535; the uid-churn investigation added 9 — `IdentityContinuityTests`, the launch continuity table. Before that, 523 → 535 from the Google-linking pass and 482 → 523 from the ios-residue pass. Before that, 463 → 482; the walk-findings pass added 19 — Live Activity narration, failure copy, and the recoverable count. Before that, 391 → 463 from the Live Activity / 429 / guidance pass, which added 76 and relocated 4. Before that, 352 → 391 from the release-residue pass; the release-residue pass added 39 — `CaptureReclaimTests` 15, `CaptureReaperTests` 12, `StagingHooksTests` 7, `ScenePollExpectationTests` 5. Before that, 302 → 352 from RP-6/RP-7; RP-6 added 11 — 9 co-run/wire pins + 2 envelope-edge pins — and RP-7 added 39 — `FloorPlanMathTests` 18, `FloorPlanVoiceTests` 13, `FloorPlanFixtureTests` 8. Before that, 288 → 302 from the 0074 phantom-room pass), run manually via `xcodebuild … -scheme RoomStudioCapture-Integration` — the only scheme in this project (no separate default scheme, no CI gate). That scheme bakes `RUN_INTEGRATION_TESTS=1`, so the 4 `UploadSessionClientTests` **execute live on every run**; they are NOT skipped in practice. They last ran live 2026-08-14 (the uid-churn run, 544/544) against `api-public-00032-has`, the whole suite in 84.6 s.

**What the suite does and does not cover.** It pins flow LOGIC — routing tables, restore selection, deferral scoping, poller visibility — deliberately extracted into pure functions so they are reviewable as tables instead of by reading SwiftUI. It does NOT cover rendering: a green suite is compatible with a screen whose only exit is clipped off-frame at accessibility sizes. **AX5 layout claims must be re-verified by screenshot, never by reading** (`xcrun simctl ui <udid> content_size accessibility-extra-extra-extra-large`, then a temporary app-entry swap to the screen under test); three separate review passes claimed AX coverage they did not have, and the two screens that actually failed — `AccountConflictView` and `QRBridgeView` — were both found by screenshot after being read as fine.

**Posture: fail-closed-live, not fail-open.** Each integration test calls `XCTSkipIf(!RUN_INTEGRATION_TESTS)` — the fail-open default — but because the sole scheme always sets the flag, that skip path is never taken here. With the flag set they hit the live `/upload_session` contract and go **red if the backend is unreachable**. Running the suite therefore requires a reachable backend; an offline run will fail those 4 (expected, not a regression).

**Honest count:** report as "538 asserting offline unit tests + 2 boilerplate stubs (`testExample`/`testPerformanceExample`) + 4 live integration tests (require a reachable backend)", total 544 — not a bare total: the 2 stubs assert nothing and the 4 integration tests carry an external dependency the unit tests don't.

**Parallel-worktree note:** `GoogleService-Info.plist` is gitignored, so a fresh git worktree lacks it and the 4 live tests fail with a Firebase configure error that mimics a backend failure. Copy it from the main tree (`ios/RoomStudioCapture/RoomStudioCapture/`) before running the suite in a worktree; a rebuild picks it into the app bundle.

**Concurrent sessions MUST use separate worktrees, not the same tree.** Two iOS sessions ran in the main tree on 2026-07-25 and nearly produced a false green: one stashed two files to unblock a branch checkout while the other was mid-edit on them, so for the length of a merge the tree held the pre-edit content. Any build, test run, or `git add` in that window would have silently captured stale source, and BOTH sessions would have reported a green suite for a tree neither of them had. Nothing warns you — the suite passes, the diff looks right, and the commit that lands is whatever happened to be on disk. `git worktree` is the fix (one is already in use for `placement-quality-build`); the plist note above is its one gotcha.

**Contract/CI note:** the `/upload_session` contract is frozen (decision 0035), so the live integration tests assert against a stable shape. (The earlier fail-open rationale — that fail-closed-without-CI trains operators to ignore red — is superseded: the sole-scheme decision already made these intentionally fail-closed-live.)

**CI status (2026-08-08, decision 0099):** `.github/workflows/ios.yml` exists but is **`workflow_dispatch`-only and has never executed**. It is not a gate and is not on a push trigger, deliberately: the sole scheme bakes `RUN_INTEGRATION_TESTS=1`, so an automatic run would charge the per-UID daily CAPTURE ceiling (12, decision 0098) on every push and could lock the operator out of scanning their own rooms. The workflow runs the offline subset via `-skip-testing:RoomStudioCaptureTests/UploadSessionClientTests` (no source change; the device build stays pinned to what is on the phone) and carries a commented-out plist-restore step naming the secret it needs. **The real unblock is a CI-only backend project or a CI service account with its own quota** — that removes the objection entirely and would let the live tests run on every push. Python and web CI ARE push-triggered; only iOS is held back.

## Conventions

**NEVER enable anonymous-user auto-cleanup in Firebase Auth.** It is off
(verified 2026-08-14: the project config carries no auto-delete block) and it
must stay off. Decision 0139 measured the mechanism it would trigger — the SDK
deletes its own Keychain credential when the server reports a user gone, the
app then silently mints a new anonymous uid, and every room captured under the
old one is orphaned. Auto-cleanup would fire that for every user on a schedule.
It is a single checkbox in the console.

**Cloud Run revision numbers are NOT chronological on `perception-obj`.** The service has produced two revisions numbered 00036 (`-xer`, RP-8; `-l9l`, the 0089/0090 security+privacy deploy), one numbered 00038 (`-ses`, the 0081/0082 wave), and `perception-obj-00037-sd9` (image `20260808-200124`, the 0104 walk-classes deploy), which served 100% for two days despite a LOWER number than 00038. Do NOT read a lower revision number as a rollback — check the image tag and the traffic split (`gcloud run services describe perception-obj --region asia-southeast1`), which are the authorities. Serving now: **`perception-obj-00043-yiz`**, image `20260813-222442`.

**A perception build that suddenly takes forever: check `PIP_EXTRA_INDEX_URL` first (decision 0182).** `pypi.ngc.nvidia.com` — first in Meta's index line, carried verbatim into our Dockerfile — went NXDOMAIN, and an extra index is consulted for EVERY package, so pip paid five DNS retries with backoff per dependency: **764 retry warnings** in one build, with `pip install -e '.[dev]'` at 25 m 38 s and still resolving when it was cancelled. Dropped 2026-08-16 (dropped, not repointed at `pypi.nvidia.com` — every recent build already resolved from PyPI proper, so removing a dead index changes nothing while adding a live one could change which wheels are selected). **The layer cache is what hid it** — every deploy since 0120 rode the cache, and the first miss fell straight into it. Two adjacent facts from the same session: the cache missed BROADLY (apt, the clone and `mamba env create` all ran) for an undetermined reason — inline cache does not re-export entries a build itself imported, which is the first place to look — and **the base image is NOT a suspect** — `condaforge/mambaforge:24.7.1-0` has not moved, verified by `rootfs.diff_ids[0]`, and an apparent difference in *compressed* layer digests is an artefact of Artifact Registry re-compressing on push. Compare diff_ids, never compressed digests, across registries. One more reading trap, which cost a wrong conclusion before the timestamps caught it: in BuildKit's plain progress output the number after a step id is **seconds since the BUILD started**, not seconds into that step.

**perception-obj deploys candidate → smoke → flip like every other service.** `./infra/deploy_perception.sh obj --candidate` holds the new revision at 0% with a tagged URL and prints the smoke and flip commands; without the flag it still goes straight to 100%. The smoke needs an identity token (the service is platform-gated, 0106). Prefer the flag: a revision that fails at run time burns a full 900 s GPU request before anyone finds out, and route registration — the `/shell` and `/compress` stages — cannot be checked locally at all (0142).

- Python 3.11+. Ruff for lint/format (config at `pyproject.toml`).
- Frame of reference: ARKit-native everywhere on the wire. Convert at the boundary, not in transit.
- Coordinates: right-handed, +Y up, meters.
- Quaternions: `(x, y, z, w)`, unit norm within 1e-3.
- GCS paths in the bundle are RELATIVE to the bundle prefix, not full `gs://` URIs.
- File creation: every long-lived file gets a docstring explaining what it's for and who reads it. No "test code anyway" justifications for shortcuts.
- Tests pin invariants, not implementation. They should still pass after a refactor.

**Standards the repo is held to.** No AI attribution anywhere — commit subject,
body, trailer, or file content. No intermediate-dev references, no placeholder
or WIP markers, no comments that narrate a session's process. No claim in a
doc, comment, or decision note that contradicts the code or another doc — when
you find a mismatch, verify against the live system and fix the wrong one
rather than making the text self-consistent. The repo must read as good first
decisions, not as a record of fixing earlier bad ones: the decision lives in
the code and CLAUDE.md, the story of how it changed lives in `docs/decisions/`.

## Tooling conventions

Default model for routine work: **Sonnet 5**. Switch to **Opus 5** for hard reasoning (coordinate-frame conversions, perception-pipeline architecture, anything where a wrong answer propagates). Haiku 4.5 is not in use yet. The escalation rule is the durable part; the names are bindings — update them together with `.claude/WORKFLOW.md` when the model family moves.

Default tool for code work: **Claude Code**. Default for strategy / architecture decisions: **Claude Chat**. See `.claude/WORKFLOW.md` for the full rubric and prompt templates.

**A whole thread — a quality push, an investigation, a migration — is briefed as a CHARTER, not a task list**, per `.claude/WORKFLOW.md`. Its five parts exist because a task-scoped brief produces a session that stops at the first adjacent defect: an outcome with a self-checkable acceptance test, autonomy grants stated POSITIVELY (the part most briefs omit, and the reason sessions stall), named stopping conditions, the batched-judgment protocol when the acceptance test is the operator's eyes, and a scope boundary that is not a file list. A charter loosens scope, never rigour.

**A lane that walks the room page will 404 on `dev-fixtures`** — it is deliberately absent from worktrees (3.9 GB). The cheap fix is `tools/make_synthetic_splat.py` (~14 MB of synthetic rooms, no real capture), and the rule is DELETE THEM AFTERWARDS so nothing real or bulky can reach a build.

**Starting a session is the orchestrator's job to make one paste long.** Whenever
new work should run in its own session, the coordinator delivers a
copy-pasteable prompt AND does the pre-session setup first — provision the
worktree, symlink `web/public/dev-fixtures` from the main tree, run
`npm install` in `web/`, copy the gitignored `GoogleService-Info.plist` for iOS
lanes — so the session's first act is work, not environment repair. Setup traps
that are already known: `outputs/room-quality/stage_fixed_fixtures.py` writes to
an ABSOLUTE main-tree path, so a worktree lane stages fixtures outside itself;
a worktree has no `.venv`, so Python runs via the main tree's absolute
interpreter path (verified to still import the worktree's own modules, not
main's); and **only symlink `dev-fixtures` into a lane that actually views
rooms.** That directory is **3.9 GB of real captured homes**, `next build`
copies `public/` into `out/`, and any lane whose acceptance includes a green
static-export build should not have it present — the deploy is protected by
`firebase.json`'s `dev-fixtures/**` ignore, but the build is not, and 0122
already caught a real room's splat one deploy from a public origin.

**A session's ready report goes to `outputs/reports/<lane>.md`** — gitignored,
like every other artifact under `outputs/` (walk verdicts, operator-queue logs,
adjudication output). Not pasted into a conversation, where it dies with the
session, and not committed, which would put process chatter into history.

**The report is transport; the finding is content.** Any finding another
session needs — a cross-lane handback, a convention correction, a measured
fact that changes someone else's assumptions — must be written to its DURABLE
home by the session that found it: a decision note, or CLAUDE.md. Never left
sitting in a report for a coordinator to relay. Decision 0130's handback was
written down, was correct, and still never reached the session it was for,
because a human bus was the only path between them.

## Git conventions

This repo is tracked with git. The remote is `origin` =
github.com/feynma1h/roomstudio (private), and `main` tracks `origin/main`. The
one branch deliberately not pushed is `diag-bundlepb-reason-public`.

**Claude Code's role with git:**

- Commit as part of normal work. One commit per logical unit (a feature, a fix, a refactor — not "end of session"). If a session produces multiple distinct changes, that's multiple commits.
- Write descriptive commit messages: what changed and why, not just what. Subject line under 72 chars; add a body if the why isn't obvious from the subject.
- Run `git status` and `git diff` before committing, and surface anything unexpected (e.g. a file changed that wasn't part of the task). Don't `git add -A` blindly.
- Run the relevant test suite after every code change. Show the full test output before proposing a commit. "Relevant" means: tests for the package or service that changed, plus any tests for packages that depend on it. If unsure which tests are relevant, run them all. Never commit untested code. If tests fail, fix and rerun until green; do not commit red, do not commit "mostly green," do not commit with an explanation of why a failure is fine. If a test is genuinely wrong, fix the test in the same commit and explain in the message.
- Do NOT put AI attribution in a commit. No `Co-Authored-By` trailer, no generator footer, no tool name in the subject or body. The history is part of the product surface, and it is pushed. Write in the project's voice, as the author.
- Do NOT push to remotes. The user pushes manually after reviewing.
- Do NOT rewrite history (`git rebase`, `git commit --amend` on already-committed work, `git reset --hard` on commits you didn't make this session) without asking.
- Do NOT delete branches or force-anything.

**Session-end housekeeping commits:** the CLAUDE.md updates and any decision-note additions from session-end housekeeping should land in their own commit, separate from the code changes that prompted them. Message convention: `docs: session housekeeping — <one-line summary of what changed>`.

**What's gitignored:** see `.gitignore`. Notably: `outputs/`, virtualenvs, `.env`, `.claude/cache/` and `.claude/projects/` (Code's session state). Note that `CLAUDE.md`, `.claude/WORKFLOW.md`, and everything under `docs/decisions/` ARE tracked — they're project documentation, not local state.

## When to write a decision note

`docs/decisions/` holds the *why* behind decisions that aren't obvious from the code. One file per decision, filename `NNNN-short-slug.md` (zero-padded). Template at `docs/decisions/0000-template.md`.

The criteria for "is this worth a note?" live in the session-end housekeeping section below.

## Next on the board


**READ THIS FIRST. The numbering below is historical and cross-referenced throughout this file; it is not priority order.** As of 2026-08-13 the operator's queue is DONE except external paper (see the queue paragraph below) — the next build session is unblocked on everything it was waiting for: the clip sign is ruled, the walk verdicts are in, the social layer is ruled a commitment, and perception's live gate closed on a real scan. Item **8** — the render-payload P0 — outranks everything else for BUILD, App Store collateral included: people cannot see their rooms yet. Item **9** — conversational redesign — is the product's own definition and runs in PARALLEL as a design session, because design costs no code and touches none of item 8's files. Item **10** holds the backlog an audit of all 103 re-open triggers turned up — fired conditions nothing was scheduling — and it carries one operator RULING that must not be allowed to drift the way item 9 did: **is the social layer a commitment or a direction?** Items 9 and 10 are the standing proof that this section decays in a particular way: nothing was decided about them, they simply stopped being scheduled and became sub-clauses. **When this section gets stale, the project's drifting — and drift here looks like a core feature quietly turning into someone else's dependent clause. Keep it current.**

**ALL FOUR 2026-08-10 LANES MERGED, plus the 0108 mock-string chip; every worktree removed and every branch deleted. As of 2026-08-13 the repo is ONE clean tree at `main`, in sync with origin, with NO sessions in flight.** | lane | worktree · branch | decisions | scope | |---|---|---|---| | clip A/B | REPORTED + MERGED 2026-08-10 (0112; 0113 unused) | done | walk DONE 2026-08-12: measured won, flip shipped (`3755bad`) and deployed to production. | | api-public polish | REPORTED + MERGED + DEPLOYED 2026-08-10 (`api-public-00036-duv`; 0114/0115 later consumed by the phone session) | done | outcomes in 0107/0108/0124. | | iOS Google linking | REPORTED + MERGED 2026-08-10 (0118/0119; suite 535) | done | phone leg + web copy retirement remain — see the What-works bullet and the operator queue. | | ops | REPORTED + MERGED 2026-08-10 (0120; 0121 unused) | done | perception-obj platform-gated, tombstone swept, build cache seeded — all live-verified. | **Held deliberately:** per-room deletion (collides with the api-public lane; product-shaped), 0062 frame coverage (GPU-cost-entangled; the item-7 walk and the next real scan reshape it), the social-layer ruling (operator's). The two lane-B notes are written (0142 `/compress` as a stage, 0143 `extent_axes_m` as a declaration). **THE OPERATOR QUEUE — EXECUTED 2026-08-12/13 (all five steps; session record in gitignored `outputs/operator-queue-2026-08-12.md`).** Closed: the clip-sign walk (measured wins, shipped, deployed to production), the item-7 second walk, the reveal's two questions, the social-layer ruling, and the whole phone session — real scan (perception's live gate), the disruption capture (0114), the identity switch and the reclaim leaf (0115). **WHAT REMAINS ON THE OPERATOR, and it is now only paper plus one investigation:** (1) **Apple Developer enrollment is STUCK, not merely pending** — filed 2026-07-22, still unapproved three weeks on against a typical <48 h; worth contacting Apple Developer Support or checking for an unseen identity-verification hold, because it gates Gate A, APNs, TestFlight, submission AND the 7-day re-sign treadmill — and per 0115 it may also be gating a defect that destroys user identity on every device build. (2) App Store collateral: **nothing started** — icon, screenshots, support URL, age rating, privacy nutrition labels (which must name the material-inference vision call per 0089). (3) The Indian lawyer on Terms §9–§11: **not engaged**. (4) The 0115 churn investigation — no longer an operator item at all: its decisive first step ran and killed the entitlements/access-group hypothesis (0138), and the leads that remain are source-level rather than device-level. Parked with named triggers, NO action unless one fires: the material re-bake (0070 wants reference-room re-adjudication first), the second unrestricted Firebase browser key. **Re-sign clock: 2026-08-19 07:15 UTC.** **The ROOM QUALITY session RAN (2026-08-13, decisions 0146–0156); its walk RAN, its two resulting corrections are in, and it is MERGED to `main` and SERVING as `perception-obj-00043-yiz`.** Three defects are closed and committed — contact tilt, the phantom support surface (rp7's monitor was resting on the chair tucked under its desk), and the label fork that split one monitor into two objects — see the What-works bullet for the measurements and the What-does-NOT-work pair for what was refuted. **The walk RAN, the branch is MERGED, and lane C has now SHIPPED it — `perception-obj-00043-yiz`, all four rooms re-driven on it, live reproducing offline at the float64 limit (0163/0164).** What remains is one operator sitting, against the deployed rooms: the pack is at gitignored `outputs/lane-c-walk/WALK.md`, built from LIVE manifests, and carries exactly ONE decision that is genuinely theirs — which face of its measured box an under-filling splat is seated against (0148) — now with the third option built as a `-vfill` viewer variant so the narrow form can be judged rather than imagined. **Two things the session settled that change what comes next.** The capture-side half of the brief is answered NEGATIVE and with numbers: the capture already contains 22–156 good views of every piece of furniture, the pipeline uses one or two, and supply does not predict quality (r = +0.018 across a 40× range) — so per-object capture-sufficiency feedback is NOT built (0150), and the deliberate re-scan is worth running as a TEST of that prediction rather than as an expected improvement. View selection is refuted across seven features and two instruments (0146). **The one line of attack left on class-6 is PROBED and priced (0151):** two reconstructions of one object do not already align (centres 0.11–0.78 m apart, frames 10–52°, RMS 0.07–0.42 m), trimmed ICP appears to close all of it, and the scale-drift + mutual-coverage check says it honestly closes 2 of 6 — the bed pairs inflate one cloud 70–90% and end at 5–22% mutual coverage. **RMS is not the acceptance criterion here**, and the next session to reach for this will otherwise reach for it. Worth knowing why it matters: a union of registered reconstructions has honest proportions, and 0081's finding is that extent consistency is misleading *under truncation* — so this is the only route that could reach the rotation ceiling without re-running an instrument already measured dead. **HANDED OFF 2026-08-13 as four lanes — prompts at gitignored `outputs/handoffs/room-quality-next.md`, decision blocks assigned per lane inside it.** A = the facings, scoped as a PRODUCT decision (0133's descoped conversational rotation) rather than a sixth instrument, since five families are now refuted — **DONE 2026-08-13 (0157/0158/0159), MERGED, and SHIPPED 2026-08-14 by the ship-facings lane (0172/0173) — evals green live at PROMPT_VERSION 4, serving as `api-public-00038-qiv`**; B = the selection experiment — **DONE 2026-08-14 (0160/0161/0162), the GPU spent (~17.5 min, two rounds on rp6g1), env reverted and the room restored byte-identically; NOT merged.** Answer is NEGATIVE: a 1.7× sharper view seeing 2.1× more surface reconstructed WORSE, so no selection score was built even behind a flag — see the three What-does-NOT-work bullets. **What D inherits from it:** the budget lever does not widen D's baseline (36.2° vs 35.5°, max 82.5 vs 88.3 — a wider baseline needs different frames, not more views per box), and D's case is strengthened from the other side by 0161's fidelity 0.777 — added surface does survive into the object, so a union has headroom that single-view selection does not; C = deploy — **DONE 2026-08-13**, serving, re-driven, fixtures re-staged, pack built; D = the multi-view union, **unblocked and confirmed as the live route on class-6.** The rival that appeared mid-August is now measured dead: 0180 found that SAM 3D's undocumented `pointmap` conditions SHAPE and that every reconstruction we have made fed it a monocular guess of a frame whose LiDAR we captured — but the bench in 0181 fed it the real depth and the reconstruction's proportions moved by at most 0.011 against gaps of 0.20–0.23. Read 0181's Outcome before starting D: it also removes a confound from 0151's registration numbers, which were produced by two independent depth guesses; **E = the facing-sign probe — DONE 2026-08-14 (0170/0171), MERGED, not deployed.** The sign WAS unmeasured rather than unmeasurable: the layout rotation reads it, bimodally, and gets both of the operator's reported failures right and a third object wrong. It ships flag-only; what it owes is one operator sitting (`outputs/lane-e/WALK.md`) and, if they say yes, an env-only `PLACEMENT_FACING_SIGN_APPLY=1` on the next perception deploy. **It also corrects the premise this lane was launched on:** 0169's room-geometry table cannot grade a sign instrument at all. RoomPlan's box local **+Z is the object's front** — 23 of 25 wall-backed boxes across the four rooms present −Z to the wall, p ≈ 1e-5, the two exceptions being chairs at desks whose +Z faces the desk — so every box carries a measured facing DIRECTION; but a direction is not a label, because deciding whether a rotation is right needs the splat-local direction of the splat's own front and the room has no opinion about that. The table grew from 12 rows to 21 and from 5 labels to 5 labels (0170). The session's offline harness lives at gitignored `outputs/room-quality/` — `roomlib.py` is a replica of the production `RefinementContext` over all four preserved captures, **trust-gated to reproduce every shipped view choice and box object exactly — and note what that gate does NOT cover: lane C found the replica handed fusion no room planes, silently killing the single-view contact-prior path and mispredicting three free objects (0163). Fixed; re-read any probe conclusion about free objects.** Plus 18 probes and the walk/fixture builders; read the report's last section before rebuilding any of it. **What remains, in order:** (a) the operator's sitting on the shipped rooms — the deploy is done; (b) **class-6 truncation itself, still untouched by anyone** — every fix so far places or orients incomplete reconstructions better rather than completing them, and the two attacks on its cause are now both measured dead, which leaves consuming MORE THAN ONE VIEW per object (SAM 3D reconstructs from one and the pipeline discards the rest — the 96–98% finding becomes the headline the moment anything can use it) and decision 0052's standing trigger, a model exposing calibrated metric scale or pose; (c) the sampler spends 75–83% of its frame budget on object-blind pose-diverse residue, which is where the same finding points even though better single-view selection is refuted.

**ALL THREE PARALLEL LANES MERGED 2026-08-09** (`stage2` → 0135–0137, `perception-emit`, `ios-residue`; worktrees removed, branches deleted). Merged-tree verification: root **724 passed + 10 skipped**, perception **704**, web **204**, iOS **523**, tsc clean, zero conflict markers. **What the lanes left owed, now written:** lane B's two notes are 0142 (`/compress` as a third `/process` stage rather than a sidecar) and 0143 (`extent_axes_m` declared per box, horizontals deliberately unnamed). The `dims` correction is lane C's **0137**, reached independently — there is no third note on it.

**Decision numbers.** Taken through 0159, plus 0139–0141, 0163/0164, 0169–0171, 0172/0173, and 0174/0175. Free: 0113, 0121, 0128, 0134, 0144, 0145, 0183+. **Reserved: D 0165–0168 (its premise now depends on 0180 — read that first), operator-sittings 0176–0178.** 0179 records that SAM 3D accepts a LiDAR pointmap we do not pass; the sam3d-pointmap lane took **0180–0182** in full (what the pointmap conditions; how wrong the guess is; why perception-obj could not be rebuilt). Lane A took 0157–0159 in full (the epistemics, the mechanism, the voice), lane C 0163–0164, the uid-churn investigation 0139–0141, the coordinator 0169 (the room-geometry ground truth; lane E has not started), the ship-facings lane 0172–0173, and the guest-provenance lane 0174–0175 (the fix; the pin over the whole instruction surface) (what a voice eval can certify; the rule-10 defect it found); all merged. **Reserved, blocks inside each charter at `outputs/handoffs/`: B selection 0160–0162, D multi-view union 0165–0168, and E facing-sign 0170–0171.** Reserved to live sessions: **0139–0141** the uid-churn investigation. Put a session's number block INSIDE the prompt body — a block written in a chat heading once reached nobody and two lanes claimed the same numbers. The room-quality session was handed **0139–0145** in its prompt and **0146–0151** in its handoff file and in this line; it took 0146–0150, because 0142/0143 were already written and 0139–0141 belong to another live session. When a prompt and this file disagree about a block, this file and the handoff win — a prompt is written once and this ledger is maintained.

**The lesson from running three at once, recorded because it nearly cost a collision:** the coordinator wrote each lane's decision block in a chat heading rather than inside the prompt text, so NO session received one and lane C reasonably took the next free numbers — which were lane A's. Only the fact that A and B had not yet written a note prevented a real clash. **Put the block inside the prompt body, every time.** Second lesson, same session: decision 0130's cross-lane handback never reached the session it was written for — the coordinator is the only relay, and re-derivation was luck.

**1 — Post-conventions placement thread: merge the fix branch, then ARKIT_ONLY position quality + LiDAR variant.** The 0063 convention probe CLOSED 2026-07-23 (decision 0065; see What-works): conventions fixed incl. the basis correction, deployed (`perception-obj-00028-hzq`, carries all branch code), production-verified (upright median 4.1°, sign tests pass, 12/12 frames, room visually corrected). Remaining on this thread: (a) DONE — `perception-layout-convention-fix` is merged (confirmed in `main`'s history 2026-07-23; every branch except the parked `diag-bundlepb-reason-public` is merged); (b) **ARKIT_ONLY placement quality — chunks A–C BUILT + merged 2026-07-23, now SERVING** (branch `placement-quality-build`, merge `3f26fcc`; probe verdicts in decision 0068): the code shipped with the 0069 shell deploy (`perception-obj-00032-km5`, 2026-07-24 — that revision built off main, so it carries chunks A–C too), the placement LIVE gate is now PARTIALLY MET: the whole `PLACEMENT_REFINE` pipeline (chunks A–D + the new room-sanity gate) was live-verified via a warm `/process` re-drive of `f3d70236` on `perception-obj-00033-zfg` (2026-07-24 — 24 objects / 7 placed, the gate demoted exactly the four operator failures; see What-works), so only the operator `/viewer` walk remains — and it is now DEFERRED under the board-item-7 Pro-only pivot (possibly moot). `PLACEMENT_REFINE=0` is the rollback lever. The re-opened in-plane instrument fork for near-square planar objects (0068) still stands (not a deploy blocker). **Chunk D (single-view contact priors) BUILT + merged 2026-07-24** (branch `placement-chunk-d`, commits `724b7a5`/`78e4d2e`; built onto the shared `room_planes.py` 0069 extracted — no anchor-interpretation duplication; offline-verified against `f3d70236`'s real planes, see What-works). Its live gate RAN 2026-07-24: the warm `/process` re-drive of `f3d70236` on `perception-obj-00033-zfg` placed 5 single-view objects on measured surfaces (chunk D) and the total placed count jumped ~2 → 7; only the operator `/viewer` walk confirming furniture-on-floor / wall-objects-on-walls remains, now DEFERRED under the board-item-7 pivot. **Sampling-starvation insight (operator-flagged 2026-07-24):** the thin walls + missing furniture in real outputs are pipeline-side, not capture-side — only 5 of `f3d70236`'s 184 frames completed with masks, and single-view objects can't triangulate; the cures are warm re-drives (complete more frames cheaply), `PERCEPTION_MAX_FRAMES` (placeholder=12), and chunk D's contact priors (place single-view objects from ONE view). VIO-calibrated monocular depth deferred with named re-open triggers; (c) **LiDAR variant of the placement event — parked on Pro hardware** (board item 3 runbook applies first); 0063's don't-trust warning lifted in principle, zero real executions; (d) opportunistic: coverage knobs stay one-capture-calibrated (17/22 single-frame objects can't triangulate — `PERCEPTION_MAX_FRAMES` is one lever; 0067 chunk D's contact priors are the complementary one). (Phase 8c soak: RUN AND GREEN 2026-07-22 pre-dating this session's deploy — the new revision `00027-n8c` has since served the full re-drive successfully, which is its own functional soak; the RUNBOOK 8c one-liner can re-run on the next quiet day if desired. The former ops decision — deleting the pre-split `api` Cloud Run service — was operator-approved and executed 2026-07-21; Cloud Run runs exactly api-internal + api-public + perception-obj, plus the deliberately parked perception-geom.)

**2 — iOS P5 — OS-kill hardware gate** (decisions 0029, 0044, 0045). Core poll + status UI shipped (P5(a), commit `dbe3188`). Remaining: **OS-kill hardware gate (cluster close item):** stage on-device: all blobs uploaded, `bundle.pb` PUT enqueued while alive, force-quit, reopen — confirm `bundle.pb` reaches GCS with no user interaction (GCS-authoritative, verified from Mac during locked interval); also verify `.task` fires on background OS-relaunch. `diag-bundlepb-reason-public` (`dc552ab`) is the parked tool for reading the redacted `reason=`. AppDelegate fallback (decision 0045 Fork A) deferred pending this gate. After hardware gate: FCM `ready`/`failed` — backend threading is done (cleanup pass: Scene.fcm_token → ClaimResult → notifiers), so what remains is iOS-side FCM registration and passing the real `fcm_token` at `/upload_session`. Cold-start poll recovery is DONE (ios-upload-robustness, 2026-07-21): the cold-launch auth race is fixed (`ec57285`), simulator-verified. NOTE post-activation: the `SceneStatusView` scan that used to restart polling across launches is no longer in the app — `ContentView` is unreferenced, so that recovery now lives in `RootFlowView.restoreUnfinishedBundle` + `BundleRestore` (decision 0073). The three status-surface honesty findings are CLOSED (branch `ios-status-surface`, 2026-07-21) — no open follow-up on this surface. **Separately, the iOS app DESIGN is built AND ACTIVATED as the app root** (decisions 0072/0073 — the Good Guest capture app: `DesignSystem/` foundation + every screen + the `RootFlowView` navigation coordinator; nine review passes; 288 tests). **The capture-to-doorway walk RAN 2026-07-26 on the 16 Pro — PASS** (two real captures; upload-under-lock, 26897de catch-up, background trip, relaunch restore, doorway exits all green — see What-works). Remaining on this surface, in order: the OS-kill hardware gate + the three terminal-failure UI screens — both staged as of 2026-08-07 behind DEBUG `StagingHooks`; the sitting ran and its verdicts, including the 0045 Fork A decision table, are decision 0085; the 0074 HARDWARE VERIFY is DONE (RP-6 Gate 4, 2026-08-05). Then the Live Activity's hardware verification (task #14 BUILT 2026-08-08, simulator-only — folds into the same sitting; task #13 shipped as RP-7) — then the remaining activation follow-ups in `RootFlowView`'s docstring (add-more resume, web-handoff link). Terminal-failure UI (`failed`/`failed_invalid`/`failed_incomplete`, blob-failure banner) has still never rendered on hardware — exercise opportunistically when a real failure occurs or stage one deliberately.

**3 — LiDAR-tier hardware verification: DONE 2026-07-26** (item number kept — it is cross-referenced throughout this file). The `[LIDAR-UNVERIFIED]` set ran green twice on the iPhone 16 Pro: LIDAR_ARKIT tier dispatch, `captureDepth()`/`sceneDepth`, one depth blob per frame at 256×192 exact bytes, decision-0032 depth intrinsics, and — the raised-stakes half — decision 0052's `depth_fit` consuming real rasters in production (16/23 placed on capture #1). Decision 0033's residual is closed. The **RoomPlan co-run spike is DONE** (2026-07-28, decision 0076 — see What-works; the `sceneDepth`-strip caveat is refuted, the ARFrame-retention issue is quantified at 10 frames = pipeline death, and the co-run architecture is proven clean). Item 3 is fully closed. The **LIDAR_ROOMPLAN tier** itself remains unbuilt — its shape comes out of the board-7 design session, which now has every input.

**4 — Launch-hardening pass: EXECUTED 2026-08-07 (decisions 0086/0087/0088).** All
nine 0015/0018 gaps closed or dispositioned + the audit run — see the What-works
bullet and the "Pre-launch gaps" residue bullet in What-does-NOT-work. Residue on
this thread, in order: (a) operator decision on the flagged `firebase-adminsdk-fbsvc`
tokenCreator grant (0088 recommends revoke + re-grant-per-walk; commands in the
note); (b) delete the stale pre-split `api-runtime@` SA + its captures binding
(operator one-liner, 0088); (c) DONE 2026-08-08 — both perception-cycle
follow-ups shipped on `perception-obj-00036-l9l` (`release_failed` expire_at
stamping per 0086; the dedicated `perception-obj-runtime` SA per 0088/0090);
(d) iOS 429 Retry-After branch (0038's reserved follow-up) — BUILT in the
Live Activity pass and covering BOTH 429 codes, `rate_limited` and 0098's
`capture_limit_reached`, but it is not on the phone (device install blocked —
see the Xcode-account bullet). The abuse-surface gate for the first
non-developer user is now CLOSED server-side, and the person-observation
privacy gap (0070) is CLOSED in the pipeline by decision 0089 — what remains
on that trigger is re-driving existing scenes so they actually gain
suppression (cached frames keep pre-0089 masks) and the complementary
capture-time guidance, whose capture-side copy shipped with the Live Activity
pass.

**5 — DONE 2026-08-08: the purge ran and the repo has a remote.** `git filter-repo` removed the nine HEIC blobs (verified: zero HEIC objects in history, no commits touching them), `tools/remap_doc_shas.py` remapped the doc SHA citations, and `origin` is `github.com/feynma1h/roomstudio` (private). The slot is kept numbered because it is cross-referenced elsewhere in this file; nothing remains on it. `diag-bundlepb-reason-public` stays deliberately local-only.

**6 — Web app: next increment** (decisions 0050, 0052–0057; Good Guest rehaul
live on branch `web-app`, verified against fixtures)

In rough order once board item 1's deploy lands: (a) DONE 2026-07-22 —
standing preview channel live (see What-works; per-PR channel automation
is an optional later add); (b) **Gate B PASSED 2026-08-08** (see What-works) —
0051 BUILT on both sides, 0094 added Google, and the web half is now
live-verified against the operator's real rooms. Remaining on this thread:
enrollment-gated **Gate A** (on-device Apple link, UID unchanged), which is
the iOS half and still waits on Developer Program enrollment; (c) first real assembled room in the PRODUCT
viewer — the dev-workbench half landed 2026-07-21 (scene `25a14caf`
renders at `/viewer` from the staged real-scene fixture in gitignored
`web/public/dev-fixtures/`); the `/room` product flow still needs (b),
and richer scenes need the 0063 convention fix + a warm re-drive for
coverage;
(d) **conversation fast-follows** (stage 1 SHIPPED 2026-07-21, decisions
0058/0059, revision `api-public-00012-ziz`): per-object extents into the
manifest (v2.x, after board item 1's verification event) → unlocks gap/
clearance speech with a `facts_version` bump; a real-browser watch of the
reveal AND the streaming composer at real speed (the dev preview pane
throttles both); tune the client wake cadences against real usage;
(e) interactive surface — per-object selection, the reactive scene, the Design
Specification contract, and conversation stage 2 (mutation) are now **board item
9**, with the dependency chain and the two verify-first probes written out. They
were a dependent clause here and drifted; do not re-bury them under 6;
(f) **room shell — 0066 geometry + 0069
parametric surfaces both SHIPPED and V3-walked** (2026-07-23/24; decisions
0066/0069/0070; see What-works for the full deploy + V3 chain). Remaining
on this surface: nothing — the reveal choreography redesign the RP-8 watch
asked for (0080) is BUILT (decision 0097), pending only the operator's
walk. The person-observation privacy gap is board 4's,
sharpened — 0069 removed baked person pixels but a person can still
contaminate a plane's measured albedo and reach the material-inference
evidence crops (decision 0070; see the privacy bullet).

**7 — RoomPlan tier: the 0085 walk's fixable classes are BUILT; a SECOND operator walk is the open gate.** The consolidated walk RAN (2026-08-08, verdicts in gitignored `outputs/consolidated-walk/verdicts.md`, decision 0085) and its ranked classes were attacked in decision 0104 — see the What-works bullet for what shipped and, just as important, for the four measured refutations that close the rotation thread for now. **THE SECOND WALK RAN 2026-08-12** (verdicts in gitignored `outputs/item7-walk-2026-08-12/verdicts.md`) — every 0104 mechanism confirmed landed, and the reveal's two questions closed positive. **What it leaves, ranked by the operator's own eyes:** (a) **the contact-TILT class, NEW and the top item** — four objects across two rooms (spike speaker, rp7 lamp, rp6g1 lamp, rp6g1 monitor) now sit at the right height but touch at a single point, tilted, because the 0104 support snap is height-only and the splat's residual rotation tilts the body; this is the same rotation ceiling wearing a contact costume, so it is NOT a fifth attack on the rotation DOF but a question about whether contact should level an object it cannot orient; (b) **class-6 truncation in two fresh costumes** — rp7's monitor renders far smaller than the real object, and rp6g1's monitor has NO generated base, so it cannot visually rest on any surface at any height (a floating monitor whose position is correct); (c) rp7's monitor still hovers; (d) the window's in-plane skew, class 5 (re-scoped, see the residue bullet), and `b667f891`'s starvation, all unchanged. **Still NOT to be re-reported as new:** rp7's bed facing and the fronts-to-wall 180° signs — three instrument families are measured dead on that DOF (0104). **Operator product suggestions from this walk — promoted 2026-08-13 to the room-quality session's two starting defects, and both ANSWERED NEGATIVE by it (decisions 0146/0150; mechanisms as stated in `docs/briefs/next-work-directions.md` §1a, whose reading of the code was correct and whose implied fix was not):** per-object *cleanest-frame* selection for SAM 3D ("if we use some frame that doesn't really capture the object, we can't expect the render to look how it actually looks" — adjacent to RP-5's census box-best-view scoring but aimed at reconstruction quality rather than coverage, and pointed straight at class-6), and capture-time per-object sufficiency feedback (touches §3, where RP-7's floor plan deliberately replaced any camera preview — re-opening that is a design argument to make explicitly, and the floor plan may host the same signal). Both were first recorded as "design inputs" in **0075 (2026-07-28)** as live capture-coverage feedback and object-aware frame selection, restated after this walk, and acted on only now — the clearest instance of the drift the outcome-scoping reframe exists to stop. Standing: the `facts_version`-gated scene_facts consumption of box extents. **Re-sign clock now 2026-08-19 07:15 UTC.**

**8 — DONE 2026-08-09/10: the four-surface deploy executed** (revisions in the What-works deploy bullet; the build-gate failure and fix in 0109). The slot keeps its number — it is cross-referenced throughout this file. **Follow-ons, each small and named:** (a) the operator's two-minute production browser leg + the HAR + the white-screen wording (the only legs nobody else can run — see the P0 bullet); (b) CLOSED 2026-08-10 — 0124's filter shipped on `api-public-00036-duv` (22 → 10, A/B-confirmed); (c) CLOSED 2026-08-10 — perception-obj platform-gated (unauth probes can no longer boot the L4) and the tombstone swept; (d) CLOSED 2026-08-10 — layer cache landed and seeded (0120; speedup measured on the next real build); (e) CLOSED 2026-08-10 — 0108 wording + 0107 eval reshape shipped and live-verified (see the stage-2 bullet). **Perception's live gate = the operator's next real scan**, chosen on cost: it proves `extent_axes_m` and the `/process`→`/compress` enqueue end-to-end free, where re-uploading the 494 MB spike bundle would cost ~1,800 GPU-s and a re-grant of the revoked tokenCreator.

**9 — Conversational redesign (stage 2): MERGED and DEPLOYED — serving since `api-public-00034-zad`, with the language residue closed on `00036-duv`; see the four-surface deploy and stage-2 bullets.** Decisions 0129–0133 executed; 0135/0136/0137 record what building it measured. See the What-works bullet for the whole shape. **What remains, in order:** (a) CLOSED — the v3 evals ran at the four-surface deploy (0107's outcome) and the v4 ones at the facing ship (0172); both green live, both after revising the suite. (b) CLOSED — real turns with the real model have run at every deploy since, `propose` and `turn` included. (c) CLOSED — deployed; the `arrangement` SSE event and both spec routes are live and were exercised end to end on 2026-08-14. (d) **The operator's eyes**, which are the standard (0080/0085): whether a proposed move reads as a proposal rather than as the room being wrong, and whether the contour footprint reads as measurement — 0129 answered the first question with my eyes on nine objects in two already-walked rooms and explicitly flagged that as thin. (e) `0135`'s clip-yaw A/B, which is small, possibly a real quality win on every room, and deliberately left as an operator call. (f) CLOSED 2026-08-14 — rule 10's conditional grammar works with an arrangement in place, and the pin now covers the arrangement block too (0174/0175; see the What-works bullet). The residue it leaves is one composed-case sample in seven and the `scene_facts` provenance line, both in What-does-NOT-work. **Deliberately NOT built, each with its trigger:** free rotation in proposals — the trigger 0133 wrote (splat axis resolution) will not fire, and the one that did is that five instrument families are dead, so rotation returned on `lane-a-facings` as a facing CORRECTION rather than a proposal: one half turn, no angle, no direction (0157/0158/0159). Also out: per-object selection and R3F (0133: not prerequisites), the catalog, DAG versioning, and the ledger with its vocabulary ban.

**10 — The fired-trigger backlog: things nobody decided to defer.** Produced by a 2026-08-09 audit of all 103 re-open triggers in `docs/decisions/`, the founding vision's durable list, and the briefs — prompted by item 9, which drifted the same way. **The audit's own method is the point: a trigger whose condition has fired but which nothing schedules is indistinguishable from an oversight.** Two candidates were already closed by the design session before this was written (0133 keeps the ledger deferred WITH its vocabulary ban as a deliberate recorded choice; 0131 carries the reasoning trace 0055 lists as durable) — those are resolved, not pending.

**(a) Fired triggers, nothing scheduled.** **`i_up` — the up-axis extent (0096) is the best value-to-cost item on this board.** VERIFIED 2026-08-09: perception already resolves the up-axis during axis assignment (`box_placement.py:481–500`) but the manifest emits only a bare `dims` triple (line 693) with no axis semantics, descending-sorted — which is precisely why the guest may speak only a LONGEST dimension. One additive field retires that restriction and unlocks height, width, footprint and area, and it is also 0133's named re-open trigger for the furniture catalog. Then: frame coverage (0062 — the trigger says real rooms under-covering at 12 frames means raise the default or make it adaptive; `b667f891`'s 53-item tail and 17-of-22 `insufficient_observations` ARE that evidence, and the board currently only calls the knobs "one-capture-calibrated", which reads as posture rather than a fired trigger); the re-mint fatal (0049 item 1 — `force_remint` is serving, so convert `remint_returned_stale_uris` from fatal into ONE forced re-mint; this is SMALLER and SEPARATE from the `.recoverable` coordinator that is already tracked, and easy to assume is covered by it); and 0088's own instruction to record finding 4's outcome in the note — **CLOSED 2026-08-09**: both of that note's fired triggers are now written back into it (finding 4 revoked; remediation 1 shipped as 0090), each verified live rather than restated — the SA has zero bindings and perception-obj runs as `perception-obj-runtime@`.

**(b) RULED 2026-08-12: the social layer is a COMMITMENT.** The operator ruled the whole layer — sharing, comparison, evolution over time — a commitment, explicitly declining the split option (some pieces commitment, others direction). So it is NOT in the direction-not-commitments list with shopping and the taste graph; it is load-bearing product, one of the three layers the thesis names, and **it needs its own design session** — nothing about its surfaces, its privacy model, or what "comparison" means between two people's rooms has been designed. Treat its continued absence from this board as drift, which is exactly what the audit that raised it was built to catch. Alongside it: the spatial relationship graph, named as unbuilt, which 0056 said stage 1 would "make earn its place" — plausibly item 9's substrate, and that build session should absorb it rather than leaving it homeless.

**(c) Seams built and left empty.** `RSSound.swift` is wired at three call sites with a documented three-cue design and says plainly "the cue files are not yet in the bundle" — the app is silent, and 0055 lists sound as first-class durable. The web has no sound at all, and the founding reveal opens on ambient room tone. Branded fonts (already in the iOS residue) and the product name are the same class: a one-file swap waiting on an asset or a decision.

**(d) Not recorded, which is its own risk.** **Alerting and monitoring appear ZERO times in this file.** The deferral may well be accepted, but that acceptance is not written down anywhere, and an unrecorded accepted deferral is indistinguishable from an oversight to the next session. 0048 named "the launch-hardening pass standing up alerting" as its trigger; that pass ran without it. Record the acceptance or schedule the work — either is fine, silence is not.

**(e) Product gaps found by looking, not by trigger.** No per-room deletion — account deletion is all-or-nothing, and for a product whose thesis is that rooms are identity that is conspicuous (0086 already anticipates it as the trigger for whole-scene GC). And iOS Google linking (0094): not technically fired, but live — the operator's own rooms are split across two identities right now, iOS links Apple ONLY, and Apple is enrollment-gated, so a Google-signed-in web user has no path to unify.

**Negative results from the same audit, recorded so nobody re-checks them:** 0122's hero-fixture swap has NOT fired — a warm re-drive does not re-segment, so `a7e073ae` still carries pre-0089 masks and stays ineligible (this was nearly recorded as fired). 0053's Spark-vs-WebGPU watch condition is measured NEGATIVE — 0123 profiled a real captured room and Spark is not the bottleneck (parse 0.55 s, under 1%). 0015/0018's gaps are closed, and 0073's hardware walk is done.


## Session-end housekeeping (Claude Code: do this before ending any session)

Before the session ends — when the user indicates they're wrapping up, when tests are green and the task is done, or when the conversation is winding down — propose the following without being asked:

**1. CLAUDE.md updates.** Show a proposed diff for:
- **"What works right now"** — add anything that started working this session.
- **"What does NOT work / what we're deliberately not doing"** — add anything broken, half-done, or explicitly deferred. Be honest. Half-done is half-done, not "in progress."
- **"Next on the board"** — replace, don't append. This section reflects the present, not history.

Show the diff and ask for approval before writing. Don't write speculatively.

**2. Decision note candidates.** Identify whether anything this session is worth a `docs/decisions/NNNN-slug.md` note. The bar:

- A path was explored and rejected — write one.
- A non-obvious convention was chosen — write one.
- A workaround was applied that looks wrong without context — write one.
- Routine implementation that's self-evident from the code — skip.
- Strategy already captured in CLAUDE.md — skip.

If the bar isn't met, say so explicitly ("nothing this session rises to a decision note") and move on. Don't write notes just to seem thorough. If the bar IS met, draft using `docs/decisions/0000-template.md`, pick the next number, propose, wait for approval.

**3. If neither applies**, say so and end the session cleanly. Not every session changes the state.

The user can short-circuit this with "skip housekeeping" if the session was trivial (e.g. a one-line fix). Otherwise, run through it.

## How history is preserved

CLAUDE.md is the present. Anything historical — abandoned approaches, the reasoning behind a current convention, "we tried X and it didn't work" — goes in `docs/decisions/`, not here. If you find yourself wanting to add "we used to do Y" to CLAUDE.md, that's a signal to write a decision note instead and keep CLAUDE.md focused on what's true now.
