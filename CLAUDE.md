# roomstudio (working name — no product name has been chosen)

A spatial intelligence product that helps people discover the best version of their home: AI-powered room analysis, conversational redesign, and an immersive 3D representation of *their own* space.

This file is the always-current state of the project. Both Claude Code (reads it automatically) and Claude Chat (you upload it) consume it at the start of every session. If something in here is wrong, fix it before doing anything else.

## What we're building

**The thesis: every home contains a version of itself that its owner has never seen. This product makes that version visible, understandable, and achievable — one conversation at a time.** Every feature decision filters through this. The full founding vision lives at `docs/product/initial-idea-draft.md` (verbatim, with what's superseded vs durable mapped in decision 0055) — read it before making product-surface decisions.

This is NOT an "upload → generate a 3D scene" showcase. The 3D reconstruction is the *medium*; the product is helping people make AI-based decisions about improving their room. Three product layers frame everything: the **AI layer** (understands space structurally — object relationships, traffic flow, light, proportion — with algorithmic spatial analysis before any LLM is invoked, and reasoning traces on every design decision), the **emotional layer** (feels personal, not algorithmic — the experience bar is Linear/Vercel/Figma-tier premium consumer software; conversation is the primary post-reveal interface; the cinematic reveal is the defining moment; design language is Apple-grade restraint per decision 0056 — neutral chrome, content carries the color, one sans, mono only for machine data), and the **social layer** (rooms are identity — sharing, comparison, evolution over time). Direction, not yet commitments: room health scoring, taste graph, lighting simulation, budget-aware shopping, DAG version history. Deliberately out (per the founding draft, still sound): AR overlay, social feed, photorealistic image generation, floor plans, voice input; desktop-first.

**Naming:** "RoomMind" (the draft's name) was discarded; nothing has been chosen. "roomstudio" is a stand-in used for the repo, GCP project, and wordmark — the web wordmark is isolated in `web/src/components/Wordmark.tsx` for a one-file swap when the name lands.

Three technical surfaces today:

- **iOS capture app** (Swift + ARKit + RoomPlan) — capture-only, no viewer. The app's only job is producing a high-quality capture bundle and uploading it. Users come to the web for everything else.
- **Backend perception pipeline** (FastAPI on Cloud Run, `asia-southeast1`) — ingests bundles, runs SAM 3 segmentation + SAM 3D Objects reconstruction, places objects in the room's gravity-aligned metric frame using ARKit data (decision 0052), renders the room shell — walls/floor as textured quads from measured ARKit planes (decision 0066; BUILT, deploy pending). This is the modern substrate for the draft's perception + spatial-reasoning layers; the spatial relationship graph and design-generation layers above it are unbuilt.
- **Web app** (Next.js, static export + web splat rendering — WebGL2 via Spark, decision 0053 — hosted on Firebase Hosting) — the product surface: today rooms + viewer; next analysis, conversation, and redesign. Capture path is one screen: "Open the iOS app." Auth: same Firebase identity as iOS — requires upgrading iOS's anonymous auth to a real sign-in linked to the existing anonymous credential (see "Next on the board"); anonymous UIDs don't carry across devices.

Photo-upload (Android, no-iPhone users) is a deferred concern. Until the iOS path is solid we don't build the web-fallback capture.

## Capture bundle — the central contract

Everything between iOS and the backend flows through `packages/schemas/capture_bundle.proto`. The bundle is metadata; pixel data (frames, depth) lives in GCS by reference.

Frame of reference is **ARKit-native** end-to-end: right-handed, +Y up, camera looks down -Z in its local frame. The iOS client does NOT transform; the backend converts to downstream model frames (e.g. SAM 3D's per-object frame) when it has to.

Pose is **position + unit quaternion (x, y, z, w)**, not a 4×4 matrix. ARKit-native, ARCore-native, glTF-native. 7 floats instead of 16. The proto file's docstring carries the full reasoning.

Quaternion math is centralized in `packages/schemas/roomstudio_schemas/pose_math.py`. Any Python that touches a Pose imports from there. Do not re-implement.

## Repo layout

```
packages/schemas/                 capture bundle proto + generated Python + pose/placement math
  capture_bundle.proto              source of truth
  roomstudio_schemas/
    capture_bundle_pb2.py            generated; regen with ./tools/gen_proto.sh
    pose_math.py                     quaternion ops; one place to change
    placement_math.py                depth backprojection, single-view fits, ray triangulation
  tests/                              61 invariant tests, all green

packages/api-core/                shared logic consumed by both API services
  roomstudio_api_core/
    scene.py                         Scene model, SceneStatus, state machine
    scene_read_repo.py               SceneReadRepository ABC + Firestore/in-memory read-only impls
    upload_session_repo.py           UploadSessionRepository ABC + Firestore/in-memory impls + gcs_mint_resumable_uri
  tests/                              53 direct unit tests, all green

tools/                            local scripts (run from repo root)
  gen_proto.sh                      regenerate Python and Swift (ios/RoomStudioCapture/RoomStudioCapture/Generated/)
  build_test_bundle.py              synthesize a bundle from test_data/photos
  inspect_bundle.py                 verify a bundle parses + smoke-checks

services/
  api-public/                     client-facing API (--allow-unauthenticated, Firebase JWT verify)
  api-internal/                   internal API (--no-allow-unauthenticated, Cloud Run IAM)
  perception-obj/                 SAM 3 + SAM 3D Objects + placement/fusion (deployed pre-placement)
  perception-geom/                VGGT for the photo-upload path (deployed, photo-path only)

web/                              Next.js static-export web app (decision 0050); Spark splat viewer
                                  contained in src/components/SplatViewer.tsx (decision 0053)

infra/                            Cloud Build configs, deploy scripts
docs/decisions/                   short notes on dead-ends — see "When to write a decision note"
test_data/photos/                 9 HEIC photos of a real room, for synthesis testing
outputs/                          gitignored; generated artifacts
```

## What works right now

- The capture-bundle contract is defined, generated, and tested. `python tools/build_test_bundle.py && python tools/inspect_bundle.py outputs/test_bundle/bundle.pb` runs clean end-to-end.
- The old photo-upload HTTP surface is gone from `perception-obj`: `/segment`, `/segment-raw`, `/objects` and their dead helpers were removed on the review-findings-cleanup branch (`67b2f26`). `/process` (the iOS path) is the only perception entrypoint. `perception-geom` (VGGT) remains deployed but orphaned pending the photo-upload decision.
- 25 schema + math tests, all passing. Don't break them.
- **`services/api` two-service split deployed** (see `docs/decisions/0016`): `services/api-public/` (`--allow-unauthenticated`, Firebase JWT in-app verification, hosts `/upload_session`) + `services/api-internal/` (`--no-allow-unauthenticated`, Cloud Run IAM, hosts `/ingest/eventarc`; the legacy `/ingest` route was removed in the cleanup pass) + `packages/api-core/` (shared `UploadSessionRepository` + `gcs_mint_resumable_uri`). Both services live on Cloud Run `asia-southeast1`; see revision details on line below.
- `perception-obj` `/process` receiver: accepts Cloud Tasks HTTP POST (OIDC-verified), claims scenes atomically in Firestore with lease-TTL crash recovery, runs SAM 3 + SAM 3D Objects, writes outputs to GCS, updates Scene state, fires FCM on terminal transitions. System is functional end-to-end locally. Dockerfile, cloudbuild config, and deploy script env vars are all patched (see `docs/decisions/0005`, `0006`). Models are lazy-loaded on first `/process` call: `/health` returns 200 immediately for the startup probe; `/ready` reports per-model load state. DINOv2 weights (~1.13 GB) are pre-cached in the image at `TORCH_HOME=/opt/torch_hub`, eliminating the cold-start runtime fetch. Startup probe targets `httpGet /health`. 165 tests passing across both services.
- **Stuck-scene lease-semantics fix shipped and verified** (revision `perception-obj-00024-89b`, 2026-05-25). The bug from `docs/decisions/0011` and `0012` is fixed: `/process` reclaims stale leases atomically, `EnvironmentalError` eagerly releases the lease, SIGTERM resets held scenes to `queued`, and `holder_id` is checked defensively on all three lease-mutating paths. Verified production trace for scene `561c68ae`: `action=claim` → OOM → `action=release_error` → `action=reclaim_stale` → OOM → `action=release_error` → `action=reclaim_stale` → OOM → `action=release_error` (final, `is_final_attempt`) → scene `failed`. Note: the trace exercised the eager-release optimization path; the load-bearing expiration-check path (stale-but-not-cleared leases) is covered by unit tests only, not this production trace.
- **Smoke tool pass 3 scoping locked** (see `docs/decisions/0017`): manifest derivation, upload sequencing, and PUT semantics are pinned against the current `/upload_session` contract. Recon from a prior Code session covers the handler, request/response schemas, GCS minting, and Firestore session repo.
- **Smoke tool pass 4 scoping locked** (see `docs/decisions/0019`): polling contract pinned against a new `GET /scenes/by-bundle/{bundle_id}` endpoint on api-public. Recon from prior Code session covers the Scene document lifecycle, field population at ingest, idempotency under at-least-once Eventarc delivery, and the existing `SceneRepository` patterns in api-internal.
- **Smoke tool pass 5 scoping locked** (see `docs/decisions/0020`): four-mode CLI shape, `--cleanup` semantics, `--verbose`/`--json` output, and exit-code 2 (misconfig vs. system-under-test failure) pinned.
- **`tools/upload_test_bundle.py` written**: 1008-line substitute iOS client. Four modes (happy-path, skip-blob, duplicate-event, auth-rejection), two-phase upload sequencing per decision 0017, polling per decision 0019, failure semantics per decision 0020. `--cleanup`, `--verbose`, `--json` (NDJSON), exit codes 0–3. 27 unit tests in `tools/test_upload_test_bundle.py`. Verification against a deployed stack pending runbook execution.
- **`packages/api-core/roomstudio_api_core/test_fixtures/capture_bundle.py` added**: `build_capture_bundle()` + `TestBundleArtifacts`. Generates valid `CaptureBundle` protos for all three tiers with configurable per-frame blob kinds. 22 new invariant tests.
- **Deploy runbook preconditions landed**: `infra/eventarc_setup.sh` corrected — `INGESTER_SERVICE` changed from `roomstudio-api` to `api-internal`, `INGESTER_SA` changed to `api-internal-runtime@roomstudio.iam.gserviceaccount.com` (both were stale post-split). `services/api/` confirmed local-only (only gitignored `__pycache__/`) and removed. `upload_sessions.created_at` verified correct (`datetime.now(tz=timezone.utc)`) — TTL semantics match intent, no code change needed.
- **`infra/RUNBOOK.md` written**: 8-phase deploy runbook (Preflight + Phases 1–8) for the two-service iOS upload path (decisions 0014–0020). `eventarc_setup.sh` gains `--lifecycle-only`, `--ttl-only`, `--trigger-only` flags for per-phase invocation.
- **`GET /scenes/by-bundle/{bundle_id}` on api-public built** (board item 1, decision 0019). `SceneReadRepository` + `InMemorySceneReadRepository` + `FirestoreSceneReadRepository` in `packages/api-core/`. Scene model moved to `packages/api-core/roomstudio_api_core/scene.py`; `services/api-internal/scene.py` is now a re-export shim. `SceneRepository` in api-internal extends `SceneReadRepository`; `FirestoreSceneRepository` extends `FirestoreSceneReadRepository` (no duplication of read logic). 242 tests across all packages, all green (25 schema + 48 api-core + 40 api-public + 94 api-internal + 35 smoke tool). Runbook Phase 0 preflight gate now satisfied.
- **Phase 1 deploy infrastructure is live.** GCS lifecycle rule on `gs://roomstudio-captures` (delete after age=1, prefix `captures/`) is configured. Firestore TTL policy is set on `upload_sessions.created_at` (state `ACTIVE`). Automatic single-field index on `scenes.bundle_id` is confirmed unexempt. `infra/RUNBOOK.md` Phase 0 preflight passes; v2 PR hardening (all 20 findings) landed.
- **Firebase Auth is configured for the project and usable from the smoke tool.** Firebase added to the GCP project (previously GCP-native only). Web app `roomstudio-smoke-test` registered (`appId 1:502805861152:web:095d7e3b331e0e0ddcbd45`); anonymous sign-in provider enabled; `firebase.googleapis.com` and Identity Toolkit enabled. `tools/upload_test_bundle.py` can obtain valid `idToken` credentials against the real project. ADC quota project set to `roomstudio`.
- **Protobuf gencode/runtime mismatch fixed in both service images** (see `docs/decisions/0021`). Root cause: `pip install packages/api-core/` in Layer 3 triggered proto-plus's `<7.0.0dev` constraint and downgraded protobuf from 7.35.0 back to 6.33.6, undoing the Layer 2 force-reinstall from decision 0005. Fix: api-core install moved to Layer 2 (before the force-reinstall); schemas install remains last. Container-import smoke added to both `infra/cloudbuild/api-internal.yaml` and `infra/cloudbuild/api-public.yaml` (build → smoke → push).
- **Two-service iOS upload path deployed to `asia-southeast1`:** `api-public` (revision `api-public-00010-cag`, 100% traffic, Firebase verifier wired) and `api-internal` (revision `api-internal-00017-puw`, 100% traffic, Cloud Run IAM gated). `/health` 200 on both. Redeployed 2026-07-21 with the merged web-app stack — see the board-item-1 deploy bullet at the end of this section.
- **Eventarc trigger `captures-bundle-pb-finalized` live**, destination `api-internal/ingest/eventarc`. App-side path filtering working (bundle.pb → 200, non-bundle.pb → 200 with structured ignore-log, per decision 0023).
- **Decision 0021 verified end-to-end:** layer-swap fix landed cleanly, CI import smoke ran in Cloud Build, no protobuf downgrade recurrence.
- **Phase 0 verifier check is now a hard gate** enforcing decision 0016's trust boundary: positive grep that `FirebaseTokenVerifier` is wired into api-public (`server.py`), negative greps that it's absent from `services/api-internal/` and `packages/api-core/`. Exits non-zero with a labeled message on any failure. (v2 PR #9, commit d166ec0.)
- **Phase 0h is now a liveness-only gate** consistent with perception-obj's scale-to-zero lazy-load design (decision 0024). `gcloud describe` halts on anything but `Ready True`; `curl /ready` confirms reachability and accepts any HTTP response (200/503/500 all confirm the service is invokable). Broken-model failures are caught downstream by Phase 7 + the container-failed-to-start decision-tree branch from Cluster D.
- **v2 PR RUNBOOK hardening landed** (board item 3): all 20 findings closed. Deploy scripts now conditionally pass `--no-traffic` (only when the service already exists) and tag each revision `candidate` for reliable pre-flip smoke URLs (findings #5/#11/#10). Phase 0a IAM check uses effective-permission tests; Phase 0b Firebase setup and ADC quota-project fully specified (#1/#2/#3). Test collection is invocation-independent via pytest rootdir + conftest path setup (#20). 242 tests green from any CWD.
- **Service modules renamed** `server.py` → `services/api-internal/ingest_server.py` and `services/api-public/public_server.py` (resolves a test-collection module-name collision, #20). Dockerfile CMDs updated to the new entrypoints. Post-rename redeploy complete (revisions `api-internal-00007-nic` and `api-public-00004-nah`); both services boot the renamed entrypoints.
- **Phases 0–7 executed against production** (2026-05-29). Full upload→ingest→reconstruction path exercised end-to-end: bundle upload, Eventarc delivery, Cloud Tasks enqueue, SAM 3D model load (94.6 s from cold), reconstruction pipeline reached. skip-blob and auth-rejection modes confirmed green. happy-path and duplicate-event reach reconstruction but not `ready` (synthetic fixture pixels are non-decodable placeholders — by design; see "What does NOT work").
- **Ingest validation gate shipped and live-verified end-to-end** (board item 1). Validates image decodability pre-GPU at the `/ingest/eventarc` handler; fast-fails bundles with non-decodable image blobs to a new `failed_invalid` Scene state. Serving revisions: `api-internal-00009-bej` (gate) and `api-public-00006-quw` (reader rebuild required by the cross-service enum contract — see `docs/decisions/0027`). Phase 7 is now 4/4 green: happy-path and duplicate-event both reach `failed_invalid` end-to-end (~3–10 s including Eventarc delivery latency; perception-obj never wakes on either, confirming pre-GPU fast-fail). Note: `api-internal-runtime` holds `storage.objectViewer` on `gs://roomstudio-captures` at **bucket scope**, not project scope — a project-level IAM check will not show this binding.
- **iOS capture app P1 complete** (HEAD `9094ed1`, 2026-05-30). `ios/RoomStudioCapture/` Xcode project, iOS 16, SPM-only (SwiftProtobuf). `capture_bundle.pb.swift` generated by `./tools/gen_proto.sh` and committed as source. ARWorldTrackingConfiguration, gravity-aligned, `.sceneDepth` when available. Keyframe accumulation by pose delta (10 cm / 5°); `.limited`/`.notAvailable` tracking states skipped per decision 0028. Per keyframe: JPEG to temp dir (`frames/NNNNNN.jpg`), device-monotonic µs timestamp, `RSPose` + `RSIntrinsics` from ARCamera. Gravity field is a zero-vector stub — formula deferred to P1→P2 gate. Minimal SwiftUI: start/stop + frame counter + tracking-state badge (headless by design — no camera preview). On-device verified: pose-delta filter correct both directions, JPEG writes confirmed end-to-end (88 accepted / 88 written / 0 failures / 88 on-disk), all ARKit tracking states surface correctly. `PoseTests` 4/4 green (simd-only; quaternion unit-norm + component-order). Write observability (`WriteStats` + `do/catch` + `logWriteSummary`) kept as production code for P2.
- **`schema_version = "1"` enforcement gate closed and deployed** (see `docs/decisions/0031`). The ingest handler validates `schema_version == "1"` and rejects non-matching bundles with a `failed_invalid` Scene, structured log, and HTTP 200 (not a bare 400). A check already existed; this fixed the accepted value (`"1.0.0"` → `"1"`) and the rejection mechanism (bare 400 → `failed_invalid` + 200). Deployed: `api-internal-00013-qeg`, `api-public-00006-quw`.
- **iOS P2 chunk B complete** (see `docs/decisions/0032`, `0033`). Full `CaptureBundle` proto assembly: tier dispatch (ARKIT_ONLY/LIDAR_ARKIT), all fields populated including `schema_version = "1"`, `bundle_id` lowercased. Depth intrinsics corrected to scaled-RGB path per decision 0032 (the old `capturedDepthData` reference was wrong and would not have compiled). B-2 bundle_id casing closed. B-3 ARKIT_ONLY verified on-device (iPhone 16e): schema `"1"`, lowercased bundle_id, real-device provenance, clean quaternion norms, sane RGB intrinsics, gravity zero-stub as expected pre-chunk-C. LIDAR_ARKIT capture + inspect deferred pending hardware (decision 0033).
- **iOS P2 chunk C committed** (commit `a90418d`, see `docs/decisions/0030`, `0034`). `PoseExtractor.gravityInCameraFrame` implements R^T·world-down via `simd_quatf.inverse.act(simd_float3(0,-1,0))`. Both Gate-1 conditions met: hand-derived PoseTests at 1e-5 (green) + on-device sign check (floor/ceiling/landscape-horizon/roll, all clean). `#if DEBUG` gravity HUD in CaptureManager/ContentView; release builds unaffected. `ARCamera.transform` is a fixed landscapeRight frame — portrait reads ≈ (±1,0,0) and is correct (not a sign error); see decision 0034. P2 status: chunk A deployed, B code-complete (LiDAR depth residual deferred per decision 0033), C committed — P2 is mergeable.
- **iOS P3 complete — Firebase anon auth + `/upload_session`, live-contract-verified** (2026-05-31). Firebase iOS SDK 11.15.0 (Core+Auth) via SPM; offline-safe anonymous auth (cached UID, sign-in only when `currentUser` nil, never churns the UID — 0036); `UploadSessionClient` POSTs `/upload_session` with the 0038 retry/backoff status→action map; per-bundle session record persisted as an `NSFileProtectionComplete` file in Application Support, not Keychain (0037); Firebase-before-`configure()` ordering trap fixed (0039). Verified against live `api-public-00006-quw` (asia-southeast1) with a real anon idToken: happy-path 200, response mapped by `relative_path` not order, idempotent re-mint (same `session_uri`s for the same path-set); leading-slash manifest path → structured 400 (`invalid_manifest`), confirming 0038's fatal set unchanged; invalid-token 401 against the live `FirebaseTokenVerifier`. Integration tests run only when `RUN_INTEGRATION_TESTS=1` is set, using the `RoomStudioCapture-Integration` scheme — the only scheme in this project; there is no separate default scheme. Tests are run manually via `xcodebuild … -scheme RoomStudioCapture-Integration`; there is no automated CI gate at present. iOS test count and posture are tracked in the "iOS test policy" section (single source of truth).
- **iOS P4 ARKIT_ONLY complete — background blob upload, `bundle.pb` last, finalize hardware-verified** (main `0c0b39b`, 2026-06-03; decisions 0040/0041/0044). Single-shot whole-blob PUT over background `URLSession` (no chunking; `Content-Range: bytes 0-(N-1)/N`, no client `Content-Type` per F2); `bundle.pb` enqueued only after every non-`bundle.pb` blob is `uploaded`, the gate persisted in the 0037 record. On-device verified on a non-LiDAR iPhone17,5 (iOS 26.5): Gate 1 (upload-start handoff) PASS, Gate 4 (24h smoke soak, `failed_invalid` end-to-end) PASS, and **Gate 2 (finalize under suspension, alive process) PASS** — bundle `2fa51f70-…`: capture prefix + all frames + `bundle.pb` confirmed in GCS *during the locked interval*, verified from the Mac with no unlock (first hardware proof finalize completes while the app is non-active; the historical Anomaly-A failure mode is positively contradicted for the alive-process path). The Q1c POST-stall fix (two-window background-assertion architecture) is merged (`2586fc1`) and re-confirmed live. Under suspension GCS is authoritative over logs (Anomaly B: os_log buffers/coalesces/duplicates while suspended — operator tooling, not an app bug). ARKIT_ONLY tier only; LiDAR tiers have zero execution on a non-LiDAR device (see "Next on the board").
- **iOS P5 relaunch-recovery cluster code-complete** (decisions 0044/0045; (c) `5bb07c9`, (b) `cc7aba5`, (a) `658bc4a`, test-pin `bd8b86f`). Three coupled units: (c) idempotency hardening — `bundlePbEnqueueInFlight` in-process latch + `getAllTasks` cross-process reconciliation + context-preserve fix in `enqueuePhasOneBlobs`; (b) `onFatalBlobError` reclassified into TERMINAL/DEFERRED-INTERRUPTED/DEFERRED-TRANSIENT with `.failed` + `failureReason` persisted on terminal paths, sibling-task cancellation, and bounded cross-launch retry (N=10 per-launch, reset on progress); (a) launch-time rehydration via `rehydrateAllUnfinishedBundles()` triggered from `.task` in `RoomStudioCaptureApp`, with pre-checks for missing blob files before any enqueue. **Hardware gate pending:** the OS-kill route (background URLSession relaunch, `.task` fires without entering foreground) is unverified on-device — code-complete ≠ Gate-2b closed. See "What does NOT work" and decision 0045.
- **iOS P5(a) scene-status poll client + status UI complete** (commit `4556e44`). `ScenePoller` (@MainActor singleton, ObservableObject): lenient `SceneStatus` decode (unknown wire → `.unknown(raw)`, never throws — 0027); inner 0038 retry policy (401-refresh-once, 5xx bounded jittered backoff → `transientFail`); outer elapsed-gated cadence (2 s < 30 s / 10 s < 5 min / 30 s cap — deliberately left untuned until the perception envelope fix changes the latency profile; rationale recorded at the constants in ScenePoller); no hard give-up while foregrounded (FCM `ready`/`failed` is broken — polling is the sole completion channel). `ScenePollState` covers idle / polling(latest, since, sceneCreatedAt, longRunning, connectionTrouble — sceneCreatedAt added by the status-surface honesty pass, see that bullet) / succeeded / failedTerminal / recoverable / pollError. `SceneStatusView` renders all states premium-clean: live elapsed, long-running flip at 10 min + manual "Check now" (since replaced by pull-to-refresh — see the ios-upload-robustness bullet), connection-trouble note, success, hard-terminal failure, recoverable humanised to file count. A-nudge kick wired in `onBundleComplete` (one `Task { await MainActor.run { ScenePoller.shared.notifyBundleComplete } }` line); `SceneStatusView.onAppear` is the independent start path that reads `.complete` records from `UploadSessionStore`. 29 new `ScenePollTests`, all injected seams, no real clock/network. Suite: 191 tests, 0 failures (was 162; now 187 after the cleanup pass — see iOS test policy).
- **#17 Pub/Sub no-redelivery verified live** (2026-07-15). Uploaded a non-`bundle.pb` blob to `gs://roomstudio-captures`; confirmed a single Eventarc delivery, a single `200`, and no redelivery over several minutes (subscription `minimumBackoff=10s`). Found and fixed a real bug in the process: `services/api-internal/ingest_server.py` and `services/api-public/public_server.py` never called `logging.basicConfig()`, so every `logger.info(...)` call in both services was silently dropped in production (root logger default level `WARNING`) — the `eventarc_ignored` structured log decision 0023 relies on had never once fired in 30 days of history despite the code path executing correctly. Fixed by adding the same `logging.basicConfig(level=logging.INFO, ...)` call already used in `perception-obj/server.py`; both services redeployed (`api-internal-00015-quq`, `api-public-00008-qut`) and the fix re-verified live (the log now appears). 351 tests green (post-cleanup-merge count).
- **Review-findings cleanup pass complete on branch `review-findings-cleanup`** (17 commits, `41c8e59` → `f289b5d`, 2026-07-17). 198 of 207 findings from the untracked tasks.md resolved; the 9 remaining are deferred features/launch gaps recorded there. Highlights: pre-VGGT schemas deleted; `device_id` required end-to-end (new `DeviceIdentity.swift` Keychain UUID, backend `device_id_missing` validation, `DeviceIdSource`/hardware_id fallback removed); `fcm_token` threaded Scene → ClaimResult → notifiers (perception-obj no longer sends the device UUID as an FCM token); legacy `POST /ingest` removed + FAILED-redelivery duplicate-Scene bug fixed (FAILED → QUEUED retry with existing scene_id); perception-obj model loads moved off the event loop (`asyncio.to_thread`); lease-holder guard on `release_ready`/`release_failed` + per-object soft-fail + bounded `release_failed` retry (decision 0048); per-thread GCS client cache in ingest; api-public handlers un-async'd (event-loop unblock) + prod-gated sys.path shim; Cloud Tasks `dispatch_deadline` 930s ≥ Cloud Run 900s timeout; iOS `remintProvider` wired in production, stranded re-mint blobs fatal-routed, `UploadSessionRecord` migration shims removed (strict decode), scene-status poll-start deadlock fixed, phase-codename comments replaced throughout (decision 0049 for the re-mint failure semantics). Suites: 351 Python, 187 iOS, all green.
- **Object placement + scene fusion built in perception-obj** (merged to `main` and deployed 2026-07-21, decision 0052). Each SAM 3D object now gets a world transform in the ARKit frame: rotation from SAM 3D's layout output (`rotation`/`translation`/`scale` — previously discarded, only `"gs"` was read), scale+translation from the LiDAR depth cloud via `placement_math.fit_single_view` (single-view-aware: silhouette-true transverse extents + near-face anchoring; the generic PCA/centroid fit and iterated NN-ICP were evaluated and rejected — measured findings in 0052) + translation-only NN polish. ARKIT_ONLY frames emit per-object view rays; `fusion.py` clusters observations into one entry per physical object (per-frame-uniqueness + behind-camera triangulation guards) and fuses transforms. **Manifest is now v2**: top-level `manifest_version: 2` + fused scene-level `objects[]` (what the viewer renders) alongside `frames[]` (provenance). Every failure path yields explicit `placed: false` + reason — never a guessed transform. Synthetic-fixture accuracy: scale 0.2%, translation ~3 cm, robust to 5mm noise + 3% bleed; pinned as regression tests at achieved-accuracy tolerances.
- **Web read contract on api-public** (decision 0054): `GET /scenes` (newest-first list for the caller — the by-bundle-only read surface left a web client unable to discover scenes), `GET /scenes/{scene_id}/assets` (manifest verbatim + `asset_urls` map of V4-signed HTTPS splat URLs, TTL 1h, IAM-signBlob signing; 409 `scene_not_ready` until ready), and CORS gated on the new `CORS_ALLOWED_ORIGINS` env var (set in `infra/api-public.env.yaml`). Closes gap F4's web half. Shared `_scene_to_client_dict`/`_verify_bearer` helpers keep the three authed routes' shapes from drifting.
- **Web app scaffolded, redesigned product-true, and interactively verified** (`web/`, board item 6; decisions 0050/0053/0055). Next 16 static export (TS, App Router, Tailwind 4): typed API layer with three modes (`mock` fixtures covering all six SceneStatus values / `live-local` against a local uvicorn api-public / `live` Firebase anon); rooms-centric routes `/` (thesis-led landing with cursor-as-light ambient hero), `/rooms`, `/room?bundle=` (query-param routing — static export can't prerender unknown segments; the 3D space embeds inline when ready, with an object inventory and 10s polling while in flight), `/new` (scan instructions), `/viewer` (dev workbench, hidden from nav in live mode). Visual language now per decision 0057 (Good Guest — see the rehaul bullet below; this sentence's 0056 predecessor is superseded); all motion still through the single spring in `web/src/components/ui/spring.tsx` (`motion` lib). The room page is a full-bleed immersive stage with floating chrome (the narration card + disabled composer are the conversational interface's designated home). Status copy narrates analysis, not pipeline states. `SplatViewer.tsx` is the ONLY module importing three.js/Spark (renderer-agnostic `PositionedSplat` input); `Wordmark.tsx` is the single point of change for the placeholder name. Concrete CSP in `web/firebase.json` (connect-src allows api-public + storage.googleapis.com + Firebase auth; worker/child blob: for Spark). Verified in-browser across all routes and status fixtures, including the **assembled room** rendering inline on the room page (fused objects with correct transforms, no coordinate flip — ARKit and three.js share handedness/up-axis) and a live-local smoke (real CORS preflight + Bearer + empty-list 200). 17 vitest tests; lint + build green. `tools/make_synthetic_splat.py` generates INRIA-layout 3DGS PLY fixtures + a v2 manifest into gitignored `web/public/dev-fixtures/`.
- **Full Python suite: 460 tests green** (was 351; new coverage spans placement math incl. the single-view fit evaluation, the placement service layer, fusion incl. two regression guards, list_by_user, and the three new api-public surfaces). Dev toolchain this session: Node 22 via homebrew (keg-only, `/opt/homebrew/opt/node@22/bin`); local packages reinstalled **editable** in `.venv` (site-packages copies were stale); `uvicorn` added to `.venv`.
- **Web app visually rehauled to the "Good Guest" product system** (merged to `main` 2026-07-21; commits `d20b7c3`…`22bef7a`, 2026-07-20; source design file `Good Guest - Product System.dc.html`, untracked at repo root; decision 0057 supersedes 0056's visual language). Warm light-first: parchment/cream surfaces, warm-brown ink, rust primary actions, muted gold strictly as light-semantic indicators; Source Serif 4 = the guest's voice (italic) + display, Instrument Sans = UI, IBM Plex Mono = eyebrows + machine data (all next/font, self-hosted at build — CSP untouched). Surfaces: `/` = one-claim hero with the live demo room, plus a returning-visitor greeting grounded in the newest room's real state (localStorage has-rooms hint kills the pitch flash; `?pitch=1` dev escape in non-live modes); `/rooms` = the House (sibling cards on hatch stages, derived titles "the July 12 room", state as treatment + words); `/new` = the Bridge (declared-placeholder QR; a real listening panel that polls `GET /scenes` and walks into the wait when a new scan lands); `/room` = immersive, nav stands down: the Wait (arrival narration from real status + elapsed, breathing hatch, never a percentage), hold rule + objects-first reveal (once per room per browser via `lib/seen.ts`; assembly choreographed inside SplatViewer — largest first, each named as it lands; reduced motion collapses to the finished room), stage-1 layout (guest line grounded in real counts via `lib/voice.ts`, ledger inventory panel, disabled composer that says the guest hasn't arrived), and §8 failure treatments (partial = real missing-file count + the true iOS-resume claim; terminal = dark ink panel + rescan CTA → the bridge). StatusBadge deleted (state reads as treatment + words). 24 vitest tests (voice + seen libs covered), lint + static-export build green; also fixed a pre-existing `react-hooks/set-state-in-effect` lint error on HEAD (NewRoomSheet). SplatViewer stays the only three.js module (re-lit warm; new `frameless`/`reveal` props; `PositionedSplat` contract unchanged); Wordmark stays the one-file name seam (`❖ ROOMSTUDIO` explicit-placeholder treatment).
- **Board item 1 executed: `web-app` merged to `main` and the full backend stack deployed + live-verified (2026-07-21).** Merge was a fast-forward (`5dfa046` → `3c06fb2`; 460 Python tests green on the merged tree). Serving revisions: `api-internal-00017-puw`, `api-public-00010-cag`, `perception-obj-00025-tf6` (image `20260721-003900` — placement/fusion + manifest v2), all `/health` 200 at 100% traffic. RUNBOOK executed Phases 0–8a: preflight green (note: the operator account cannot `describe` Google-managed service agents — PERMISSION_DENIED is expected there; the live trigger + both IAM grants prove the delivery chain), lifecycle+TTL re-applied, candidates smoked pre-flip (401 `invalid_token` / platform 403), Eventarc trigger idempotently re-applied. Phase 7 all four smoke modes green first-attempt (happy-path + duplicate-event → `failed_invalid` ~3–5 s pre-GPU; skip-blob → `failed_incomplete` with the dropped path in `missing_paths`; auth-rejection → 401 `missing_token`); TTL belt-and-braces EXISTS; `--use-hardware-id-fallback` probe confirmed the `device_id_missing` rejection with the exact validation message on the Scene doc (read via ADC — the client API excludes `last_error` by design). **Web read contract live-verified end-to-end** with a manufactured ready scene: `GET /scenes` lists it, `GET /scenes/{id}/assets` returns the manifest verbatim + V4-signed URLs that fetch 200 at exact byte lengths (functionally proving the two new IAM grants: `serviceAccountTokenCreator` on `api-public-runtime` on itself + `objectViewer` on the outputs bucket), non-owner 403, artifacts cleaned. CORS confirmed live (allowed origin echoed on preflight; disallowed origin gets no CORS headers). Three deploy-path traps found and fixed in the same session (commits `7f6a74b`, `88a0de2`, `b3484bc`): `.gcloudignore` now excludes `web/` (gcloud reads only the top-level `.gitignore`, so `node_modules`/`.next` were adding ~1.3 GiB to every build context); the perception-obj Dockerfile now COPYs `placement.py`/`fusion.py` and runs a build-time `import placement, fusion` smoke (both are deferred imports — the missing COPYs passed `/health` and all probes and would have crashed the first real capture); `deploy_perception.sh` now forces `update-traffic --to-latest` after deploy (the service's traffic spec was pinned to revision name `00024-89b` from a 2026-05 rollback, so the new revision validated, was silently Retired at zero traffic, and gcloud printed "serving 100 percent" for the old code — caught and cleared same-session).
- **First real capture executed end-to-end (2026-07-21, scene `25a14caf`).** A real ARKIT_ONLY scan (iPhone17,5, 126 keyframes, 127 blobs) ran the whole chain: fresh install → capture → mint → background upload (127/127 in ~1 min) → **first real bundle ever through the ingest validation gate** → Cloud Tasks → GPU claim. **Decision 0052's result-keys check passed on real data**: every real SAM 3D reconstruct carries `rotation`/`translation`/`scale` (+ `gs`), the fields placement reads. ~20+ object observations reconstructed across frames 0–9 before memory saturation; per-object soft-fail (cleanup pass) contained every OOM exactly as designed. Real cost numbers now exist: ~3.5 min GPU cold start (inside the request budget), ~20 s/object, ~70–130 s/frame. **The capture is preserved at `outputs/real-capture-25a14caf/`** (126 frames + bundle.pb, gitignored, 62 MB) as the regression fixture for the envelope fix — the captures-bucket lifecycle rule would otherwise sweep it 2026-07-22.
- **Pre-launch gap (c) client half closed — re-mint sends real per-blob sizes** (branch `ios-remint-real-sizes`, commit `1b4d85c`, 2026-07-21). `BlobUploadManager.onSessionExpired` reads each manifest path's real on-disk size via `record.outputDir` (the `enqueuePhasOneBlobs`/`enqueueReput` pattern) and sends it as `expected_size_bytes`, replacing the deliberate placeholder 0; the full path-set is still sent (idempotency key unchanged), and both placeholder-describing comments are rewritten. A missing/unreadable/zero-size file routes to `onFatalBlobError("blob_unreadable_at_remint_manifest")` before the mint call — never a silent 0; the staleness pre-pass remains as TOCTOU defence-in-depth (a dedicated test pins it). No server change (0035 contract untouched). iOS suite 190 (+3), all green post-rebase in one run, the 4 live integration tests passing against the post-deploy stack (`api-public-00010-cag`).
- **Conversation stage 1 built, deployed, and live-verified (decisions 0058/0059; revision `api-public-00012-ziz`, 2026-07-21).** The room-page composer is alive. Server (commits `fc68e2d`…`697e6f9`): `scene_facts.py` — the model's ENTIRE world (inventory with confidence tiers from `frames_observed`/`cluster_spread_m`, framed center-to-center distances at honest 0.1 m rounding, relative-only vertical relations, provenance, per-scene limits; pure + deterministic, cached by `(scene_id, FACTS_VERSION)` with a loader so hits skip the GCS fetch); `guest_prompt.py` — PROMPT_VERSION 1 charter + five exemplars, pinned-hash test, observe-only telemetry (foreign-measurement detector with facts∪user-window allowlist, invitation heuristic → `flags` + structured log); `conversation_repo.py` — turns exist only completed; accept = dedupe → UTC-day quota → 150 s reservation, persist = sequential index + counters + holder-guarded lease clear (InMemory impl is the semantics oracle, Firestore mirrors it); `public_server.py` grows `GET /scenes/{id}/conversation` + `POST …/conversation/messages` — the service's FIRST async route (SSE `delta`/`done`/`error` in our vocabulary + `: ping` comments; disconnect shield = independent turn task + held-open request). Model call: `claude-sonnet-5`, zero tools, thinking disabled, 60 s wall clock, cache breakpoints charter/facts/newest-message (see 0059). Web: `GuestEvent` seam (SSE parsing only in `LiveApiClient`; typed pre-stream errors thrown before the iterable exists), pure composer reducer (idle/submitting/streaming/confirming/reoffer/rested/blocked; 27 vitest cases), `Conversation` card + stubs + live composer with ~200 ms delta batching (sentence-boundary under reduced motion), mock parity incl. `!error`/`!budget`/`!slow`/`!inflight`, degrade-to-settled on any conversation GET failure. Verified: 561 offline Python + 50 web tests; live-local e2e with the real key (happy turn, mid-stream-kill shield, 409 turn_in_flight, 429 with `resets_at`, dedupe replay at 0.02 s); voice evals 6/6 live at PROMPT_VERSION 1; deployed checks on the candidate pre-flip — streamed turn, **shield on Cloud Run** (client killed at 2.5 s → the complete turn in Firestore; 0058's recorded abort-on-disconnect fallback stays unneeded on first evidence), cache read 2268 tokens with `input_tokens=2` on a later turn. Ops: `anthropic-api-key` in Secret Manager (operator-created; secret-scoped `secretAccessor` on `api-public-runtime`), Cloud Run `--timeout 120`, `GUEST_MODEL`/`GUEST_DAILY_TURNS` in `infra/api-public.env.yaml`; workspace spend cap set (2026-07-23).
- **iOS upload robustness: three documented no-device gaps closed + a cold-launch auth race fixed** (branch `ios-upload-robustness`, commits `2e064a6`/`faea541`/`a29f9ec`/`c3a7396`, 2026-07-21; suite 190 → 212, green post-rebase in one run incl. the 4 live tests against `api-public-00012-ziz`). (1) **Blob PUTs honor GCS `Retry-After`** (`2e064a6`): 408/429 now retry in-process on the shared 0041 schedule (same as 5xx; exhaustion still DEFERRED-TRANSIENT under the cross-launch budget; no-context reclassified DEFERRED-INTERRUPTED — no budget burn for relaunch artifacts); `BlobUploadDelegate` threads the raw header through `handleTaskCompletion`; `parseRetryAfter` handles both RFC 9110 forms against the injected clock; a stated wait overrides the local backoff in full up to `maxRetryAfterHoldSec` (60 s), beyond which the blob defers cross-launch — never retry earlier than the server asked; 503-with-Retry-After honored by the same path. (Distinct from decision 0038's `/upload_session` item, still gated on pre-launch gap (b).) (2) **Upload-level terminal failure is visible** (`faea541`): `UploadFailureMonitor` mirrors the `.complete` architecture — kick from `onFatalBlobError` + independent `.task` scan of the store for `.failed`; `UploadFailureView` banner in ContentView shows honest copy + `failureReason` verbatim in mono; dismissal is session-local BY DESIGN (the record persists until P5 terminal-state cleanup; the failure resurfaces on relaunch). (3) **"Check now" → pull-to-refresh** (`a29f9ec`): ScrollView + GeometryReader minHeight preserves centering, `.refreshable` reuses `checkNow()` (unit-pinned), gesture enabled only while `.polling`; the long-running affordance is a quiet pull-down hint. (4) **Cold-launch auth race fixed** (`c3a7396`): `signInIfNeeded` gained a single-flight guard (two callers overlapping the sign-in suspension would have double-signed-in — 0036's UID churn), ScenePoller's default token path joins the launch sign-in, and a token-less tick is transient (connection-trouble sub-state) not fatal; 401-after-refresh and refresh-path token throws stay fatal per 0038, both test-pinned. All UI states exercised in the simulator with seeded records (failure banner found by the cold-launch scan; live `.polling` against production; the cold-launch repro that previously died at "Could not reach server" polls from the first tick).
- **perception-obj envelope fix shipped — and the first real capture completed end-to-end (branch `perception-envelope-fix`, revision `perception-obj-00026-449`, 2026-07-21; decisions 0060–0063).** The three envelope breaks are closed inside `services/perception-obj/`: (1) **frame sampling** (`sampling.py`) — deterministic pose-diverse FPS over translation + view-direction (0062), default 12 via `PERCEPTION_MAX_FRAMES`; manifest v2 gains additive `frames_total`/`frames_sampled`/`sampling{}`; (2) **budget honesty** (`budget.py` + a request-entry deadline in `server.py`, `PROCESS_REQUEST_BUDGET_SECONDS`=900 mirroring the Cloud Run timeout) — frame- and object-level admission from observed-max cost estimates with a 60 s reserve; on refusal the run ships what's banked; budget-stopped frames are never cached to GCS; zero complete frames → `EnvironmentalError` (retry runs warm against per-object splat caches); (3) **GPU memory lifecycle** (0061) — the OOM retry moved OUTSIDE the except block (the in-handler retry was suffocating under the first attempt's traceback-pinned tensors; allocator stats in the production OOMs proved reference-pin, not fragmentation — the brief's accumulator suspicion was disproven), eager result-drop after layout+PLY extraction, `torch.no_grad` around reconstruct, `empty_cache` between frames, per-frame `vram` log lines. `tools/reenqueue_scene.py` is the out-of-band cure for stranded scenes (0060; guards test-pinned; needs `google-cloud-tasks`, now in `.venv`). Dockerfile COPYs + build-time import smoke extended to the new modules. **Live-verified on the stranded scene `25a14caf` (126 real keyframes): ONE request, `ready` with ~58 s of budget to spare** — 12 frames sampled `[0,18,28,42,53,67,83,99,105,110,116,124]`, 4 processed (2 whole-frame cache hits from the July zombie + 2 fresh at 277/303 s), clean `budget_stop` at the reserve boundary, fusion 21 obs → 19 objects (2 placed: bed + curtain, `layout_triangulated`), manifest + splats in the outputs bucket, allocated VRAM flat at the 16.4 GiB model baseline after every frame (peaks 21.6–21.9 GiB released). The room renders in the web dev workbench (`/viewer` reading the staged real-scene fixture in gitignored `web/public/dev-fixtures/`) — the first real assembled room. Suites: perception-obj 135→172, +13 tool tests (note: root `pyproject` testpaths deliberately exclude `services/perception-obj/tests` and `tools/test_reenqueue_scene.py` — run those per-directory; current totals 432+6-skipped root, 192 perception (post-0065), 13 re-enqueue = 637 passing offline).
- **api-internal redeployed — the bundle_id cross-check gate is now serving** (`api-internal-00019-des`, 2026-07-21). Candidate `/health` 200 → Phase 4b platform-403 intact → traffic flip (100% latest) → post-flip happy-path smoke exit 0 (`failed_invalid` in 5.4 s, artifacts cleaned) — proving the gate doesn't false-positive on matching ids. Ran same-day as the deploys, so it is NOT the Phase 8c soak (still due ≥2026-07-22).
- **iOS status-surface honesty pass — all three findings from the first real-user walk closed** (branch `ios-status-surface`, commits `b21300b`/`9ed6281`/`21022c7`, 2026-07-21; suite 212 → 224, green in one run incl. the 4 live tests against `api-public-00012-ziz`). (1) **The elapsed clock is server-anchored** (`b21300b`): `.polling` carries `sceneCreatedAt` parsed from the poll payload's `created_at` (Python isoformat `+00:00`/microseconds and `Z` forms both handled; fraction stripped — whole-second display), the view renders the clock only once the anchor is known (before the scene doc exists there is nothing honest to count from), it ticks on a 1 s TimelineView independent of poll cadence, clamps clock skew at zero, and gains an hour field past 60 min; the longRunning flip anchors to the same server date (a relaunch at min 44 flips immediately), with poll-start fallback only when `created_at` is unparseable; poll CADENCE stays anchored to the client poll session by design (a relaunch restarting the cadence ladder costs a handful of requests). (2) **The mint badge says what `.ready` means** (`9ed6281`): "Upload authorized — uploading in background" (blue, not success-green), and the badge stands down once a superseding surface activates (scene-status panel after completion, failure banner after terminal upload failure) — it previously lingered forever as "Session ready ✓". (3) **The queued state narrates waiting** (same commit): state-aware subline via `statusDetail` — queued says "Waiting for processing to begin…"; processing carries the verified come-back-later claim. Simulator-walked against a scripted local mock serving the real wire form: queued → processing transition, 1:05 → 1:09 → 1:12 ticking between 10 s polls, relaunch at 5 min shows 5:08 (not 0:00), relaunch at 44 min shows the long-running state + 44:08 immediately. Task item 4 (cadence retune) resolved as leave-with-note (`21022c7`): values sanity-checked against the first real capture; final tuning deferred until the envelope fix changes the latency profile.
- **SAM 3D layout conventions fixed — 0063's probe closed and verified in production (decision 0065; branch `perception-layout-convention-fix`, revision `perception-obj-00027-n8c`, 2026-07-22).** Five agreeing instruments (Meta-source reading, 576-variant brute force over the recorded real observations incl. conjugation + all 24 proper bases, per-object physical checks 6/6, offline splat renders, a yaw-sensitive relative-geometry check) settled it: the layout quaternion is wxyz but parameterizes camera→local — `extract_layout` now CONJUGATES (pytorch3d row-vector semantics in Meta's own `make_scene`; 0063's xyzw candidate was a duality twin of the truth minus the conjugation, 1/6 on physical checks); the layout camera frame is the pytorch3d camera convention (`_SAM3D_CAM_TO_ARKIT_CAM` = `diag(−1,1,−1)` — 0052's CV guess was wrong, and the probe's first identity conclusion was the 180°-about-camera-Y twin that every axis-LINE instrument is blind to: the first re-drive rendered beds/lamp/chair face-down until the first real-user look caught it; the sign-sensitive layout-TRANSLATION test — B·t must point at the triangulated center, dot 0.94–1.00 vs negative for identity — settled the basis, and signed + translation regression pins now make that error class loud); the exported splat frame needs no alignment (0063's confound dead: `make_scene` composes raw `get_xyz` = what `save_ply` writes; `_fix_gaussian_alignment` is a default-off video helper). Second discovery: SAM 3D's canonical object frame is per-reconstruction ARBITRARY (same object from two frames → world rotations 90–180° apart under EVERY convention), with three consequences shipped together: fusion no longer averages rotations/raw scales across observations (a cluster ships the best member's rotation+scale, strictly frame-paired with the splat it renders; positions/metric extents still fuse); the fixed-axis gravity metric is replaced by `min_axis_to_vertical_deg` (both fused paths carry it as of `b673433` — the serving revision predates that one additive field); and a layout sidecar (`{splat}.layout.json`, RAW model fields, converted at read time so it can never go stale against a convention change) keeps rotations alive across budget-split attempts — per-object cache hits previously dropped them entirely. Real-data regression pins in three sign-sensitivity tiers (axis-line at achieved accuracy; signed semantic-up; translation-direction with the identity twin pinned as failing) + discriminators proving 0052's chain and 0063's candidate fail the bed by >45°. Production-verified in two rounds: the first re-drive (identity basis) landed the line metrics but rendered Z-class objects face-down — caught by the operator's own eyes in /viewer, root-caused via the translation test, corrected basis redeployed (`perception-obj-00028-hzq`), and re-driven CHEAPLY (only the 12 objects.json caches deleted, operator-approved; sidecars re-converted at read time, zero SAM 3D re-runs — 2 requests to 12/12). Final numbers: 22 fused / 5 placed, upright-class min-axis median 4.1° across 21 observations (was 96.9° shipped), face-color sign tests pass (bed light-face-up +0.94, lamp upright, curtain/door standing), room visually confirmed corrected in /viewer (lamp upright, bed face-up; curtain-position/two-bed-cluster residue is position-class, see below). Suites: perception-obj 192 (was 172; root 432+6-skipped and re-enqueue 13 unchanged → 637 passing offline).
- **Decision 0051 built on both sides — iOS anonymous→linked Sign in with Apple + the web provider flow** (branch `ios-0051-signin`, commits `32ad80e` iOS / `2f0fab9` web, 2026-07-22; E2E gates pending Developer Program enrollment — see What-does-NOT-work; implementation conventions in decision 0064). iOS: `AuthManager.linkAppleAccount` links the Apple credential to the EXISTING user via `link(with:)` with the UID asserted unchanged (0036/0051 invariant; a mismatch refuses + `logger.fault` rather than adopting a new identity); a conflict (Apple ID already owning a different account) surfaces as an explicit switch/keep choice with the cost stated — `switchToExistingAccount` is the app's only deliberate UID-change path; there is deliberately NO iOS sign-out (launch-time `signInIfNeeded` would re-mint a fresh anon UID — the web is where sign-out lives). `SignInWithApple.swift` carries the pure nonce/SHA-256/error-classification helpers; `SignInSheet` is the "why sign in" moment with the full UID in mono as the before/after identity proof; SIWA entitlement wired into both app configs. Web: the `lib/firebase.ts` seam is replaced — anonymous minting is gone; `signInWithApple` runs the popup flow with the never-create guard (`isNewUser` → delete + `AppleIdNotLinkedError`: the web reads identity, never creates it); signed-out is the typed `no_local_token` state rendered as invitations on /rooms, /room, /new, and the account menu (all four verified in-browser in live mode); shared `AppleSignInButton` preloads the firebase chunk so Safari's transient-activation window survives the click-time import. Suites: iOS 224 on this branch's base (baseline 212 + 12 `AuthLinkingTests`, incl. the 4 live tests against `api-public-00012-ziz`; ios-status-surface's +12 landed on main separately — post-merge total will differ), web 58 (+8 firebase-seam pins), lint + static-export build green, no CSP changes.
- **Board 6(a) shipped — Firebase Hosting stood up, PREVIEW CHANNEL ONLY (branch `web-hosting-preview`, 2026-07-22).** firebase-tools 15.24.0 pinned as a `web/` devDependency (`npx firebase …` from `web/`; operator credential minted by `npx firebase login`, lives in `~/.config/configstore/firebase-tools.json`). Proper web-app registration **roomstudio-web** (appId `1:502805861152:web:f556f8bb331bea1bdcbd45`) replaced the smoke-test appId in `.env.example` and the `firebase.ts` docstring. Committed `web/.env.production` makes a bare `next build` THE hosting artifact — live mode against deployed api-public with the new registration (values are public identifiers; `.env.local` still overrides for dev; `next dev` never reads it). `npm run deploy:preview` = build + `hosting:channel:deploy preview --expires 30d`; deliberately NO production-channel script (a public site is the board-4 first-non-developer-user trigger). Hosting CSP grew BOTH popup-flow preconditions — `frame-src roomstudio.firebaseapp.com` (the half recorded in the 0051 bullet) and `script-src https://apis.google.com` (found live: the SDK's gapi iframe-channel loader; `script-src-elem` violation observed on the deployed origin, the flow died before any network or popup call) — plus an explicit cache policy (`no-cache` HTML / `immutable` `/_next/static/**`; Firebase's 3600 s default served hour-stale HTML + stale CSP after redeploys, observed live). Preview channel `preview` live at **https://roomstudio--preview-cydkerk6.web.app** (expires 2026-08-21; the URL is stable across redeploys of the same channel — renewal = redeploy; domain added to Firebase Auth authorized domains). Verified on the preview URL: clean console + zero CSP violations (explicit `securitypolicyviolation` listener) on `/`, `/rooms`, `/room?bundle=`, `/new`; all four signed-out invitations render (rooms / room / new / account menu; signed-out surfaces never touch the network — typed `no_local_token`); production channel `roomstudio.web.app` confirmed untouched (Site Not Found); sign-in attempt runs the whole page-side flow — gapi loads, the hidden auth iframe to `roomstudio.firebaseapp.com` initializes (proving the authorized domain), popup launch fired at the real handler URL (the popup window itself was blocked by the automation pane — environment artifact, not product) — and the Apple provider gate is independently proven closed (`createAuthUri` → `OPERATION_NOT_ALLOWED : Code flow is not enabled for Apple.`): the sign-in leg is recorded as enrollment-pending, exactly the expected boundary. CORS live-proven for `roomstudio.web.app` (ACAO echo); the generated preview origin is NOT in `CORS_ALLOWED_ORIGINS` (proven: preflight 400, no ACAO) — Gate-B precondition: add `https://roomstudio--preview-cydkerk6.web.app` via env-only `gcloud run services update` and mirror `infra/api-public.env.yaml` in the same commit.
- **Room shell BUILT end-to-end — decision 0066's full chain, merged to `main` 2026-07-23** (branch `room-shell`, fast-forward `ac256d1` → `4115089`, six commits; **V1 verify-first probe PASSED** — Spark composites splats with `depthWrite:true` single-sided textured meshes correctly both ways, alphaTest floor cutouts render, back-view culling gives the dollhouse cutaway free; 0066's re-open clause is moot; probe code reverted uncommitted). Schemas `e79352d`: additive `repeated PlaneAnchor plane_anchors = 12`, `schema_version` stays `"1"`, Python+Swift regenerated, fixture builder grows `plane_anchor_count` with self-tests proving plane-carrying bundles pass `validate_bundle` unchanged. iOS `d5f09f3`: plane detection on every tier, final anchor set snapshotted at `stopCapture()` before pause, pure `PlaneAnchorExtractor` with hand-derived pins, stop-time count log (by alignment/classification) as the V3 walk's observability. Shell modules `97758ff`: `shell_geometry` (lowest-cluster floor, union-find wall merge, detected-extent-only heights, interior-fronting winding), `shell_texture` (ortho texel grid, SAM-mask exclusion, incidence/distance-weighted luminance-median blend, floor alpha from polygon union clipped at walls, observed/inpainted fractions), `shell_inpaint` (big-lama TorchScript via `torch.jit.load` — zero new deps, Apache-2.0, sha256-pinned; validated through the actual module: 1.65 s warm 512 px CPU tile, byte-identical reruns). `/shell` stage `48c2f91`: OIDC audience `…/shell`; no lease, no Firestore (pinned by an AST-level test); absent-vs-unavailable exactly per contract (swept bundle → `capture_expired`, no anchors → `no_geometry_source`, missing manifest → noop); byte-deterministic shell.json; fire-and-forget enqueue after `release_ready` with 0060-safe timestamped task names; Dockerfile bakes LaMa weights sha-pinned + extends the import smoke; `deploy_perception.sh` gains queue env vars + idempotent shell-IAM grants; `reenqueue_scene.py --shell`. api-public `66aeb20`: assets response gains the verbatim `shell` sibling, texture URIs join the signing walk, a shell fetch error degrades to null rather than 502ing the room. Web `4115089`: `assembleScene` → `{splats, shell, unrenderable}`; SplatViewer renders shell quads (textures via `fetch()`+ImageBitmap — no CSP change; grid stands down when a floor exists); reveal extends floor → walls → objects; RoomStage holds a bounded grace for an absent shell; `arrivalLine` is shell-aware (charter/server line untouched). Verified in-browser both ways: textured mock room with objects composited on it; the no-shell real-scene fixture on the grid unchanged. Merged-tree suites: 445+6-skipped root / 259 perception / 18 re-enqueue / 67 web vitest + lint + static-export build; iOS 245 incl. the 4 live tests (see iOS test policy).
- **ARKIT_ONLY placement-quality DESIGN LOCKED — decision 0067 + build brief, merged 2026-07-23** (branch `placement-quality-design`, docs-only `c8ab42f`, merge `78d1950`). The 0066-style design session for the operator-ranked placement failures, with measured diagnoses replacing two guesses (recorded in 0067): the bed split is STRUCTURALLY forced — frame 28 carries a nested duplicate "bed" detection (intersection-over-smaller 1.000) that the frame-uniqueness merge guard reads as two physical objects at ANY threshold (not a `FUSION_CLUSTER_DIST_M` miss); the curtain's ~0.5 m depth error is centroid-ray non-correspondence on an object subtending 0.74–1.3 rad (cluster RMS 0.287 m at the 0.3 gate vs the compact lamp's 0.007 m) — triangulation demoted to initializer, not broken. Five of the six named levers adopted, VIO-calibrated monocular depth deferred with named re-open triggers; chunks A–C verifiable today by offline probes + warm re-drive of `25a14caf`; chunk D consumes 0066's plane anchors via a shared `room_planes.py` — **now EXTRACTED by the 0069 surfaces build (shipped 2026-07-23/24), which landed first; chunk D builds contact priors onto it rather than extracting.** Full locks in the layout-convention-residue bullet + board 1(b).
- **Room shell DEPLOYED and live-verified end-to-end (2026-07-23) — 0066's deploy chain closed.** Serving: `perception-obj-00030-8ls` (image `20260723-160737` with the shell stage + sha-pinned LaMa; `00029-czr` + an env-only calibration revision) and `api-public-00014-ben` (candidate→smoke→flip; verifier/CORS/health green pre-flip). api-internal needed no redeploy (nothing landed since `00019-des`). **First plane-carrying capture ran the whole chain in one pass** (bundle `9fbe29b6`, scene `f3d70236`, iPhone17,5: 184 keyframes, 24 plane anchors — 6 horizontal / 18 vertical / 10 classified): upload ~90 s, `ready` in ONE `/process` request (12 sampled frames, 18 fused objects, 3 placed), the fire-and-forget `/shell` task ran unprompted, shell.json (`status: ready`, floor 3.85×4.45 m + 7 walls) + 5 baked textures (floor 31% observed / 69% LaMa-inpainted) landed in the outputs bucket. Assets response carries the shell verbatim; signed texture URLs fetch byte-exact; the room renders in the dev workbench (floor texture + wall quads + cutaway). **Degrade path live-proven on `25a14caf`, both legs**: `capture_expired` (bundle swept), then — after re-uploading the preserved bundle.pb, which also live-verified ingest's READY-idempotency skip — `no_geometry_source`, left as that scene's permanent shell.json. **V3 SHELL_\* calibration done against the real anchors** (commit `e33d98a`; env-only revision applied same day): `SHELL_WALL_MERGE_GAP_M` 0.35→1.0, `SHELL_WALL_NORMAL_TOL_DEG` 12→15 — measured: the main wall was two same-plane patches (0.3°, coplanar 8 cm) split by a 0.81 m occlusion gap, the door pair coplanar 6 cm at 14.0°; under the new knobs exactly those two pairs merge (9→7 walls, main wall 3.51×2.49 m), every other pair fails coplanarity by 0.2–3.2 m. Floor selection needed no tuning. The capture is preserved at `outputs/real-capture-f3d70236/` (90 MB, gitignored) — the first plane-carrying regression fixture, which 0067 chunk D needs. Preview channel redeployed with main's shell-aware web (production channel confirmed untouched), then redeployed again same-day with the dev-fixtures exclusion (`63b5fba` — see the privacy bullet in What-does-NOT-work).
- **ARKIT_ONLY placement quality chunks A–C BUILT — 0067 executed, merged 2026-07-23** (branch `placement-quality-build`, 9 commits `0ded45e`…`53fe58e`, merge `e72a256`; OFFLINE-verified — the warm re-drive of `25a14caf` + operator /viewer walk is the live gate, next perception deploy). Chunk A: `reproject.py` two-tier instrument (tier-1 crop-aware soft-IoU silhouette; tier-2 deterministic numpy z-buffer point-splat render + masked NCC vs the RGB crop), `fit_silhouette` (bounded Hooke–Jeeves over scale+translation, rotation fixed per 0065, scale bounded ×1.5), `mirrored_twin` sign-flag, schemas primitives in `placement_math.py`. Chunks B–C in `fusion.py`: mutual-singleton same-frame containment dedup (pre-clustering), relaxed shared-frame merge + footprint join, silhouette fit as the ≥2-view position/scale authority (`position_source: "silhouette_fit"`, must beat init to ship), margin-gated in-plane resolution, manifest fields `reprojection_score`/`position_source`/`constraints_applied`/`in_plane_resolved`/`sign_flag`/`extent_m_sorted`/`deduped_observations`; `PLACEMENT_REFINE=0` is a test-pinned bit-parity rollback lever; `RefinementContext` is the IO seam, wired into `/process` with Dockerfile COPY + import smoke. Real-data regression pins on the recorded `25a14caf` observations (12 frames' masks/objects committed as ~390 KB fixtures; big PLYs by absolute path with skipif). On-branch quality pass: exact SAT/integral-image rasterization (`MaskEvidence`; 42 s→0.5 s per fit, bit-identical), evidence memoization + strided RGB + mid-pass budget valve (whole-scene refined fusion 22.9 s→4.8 s, tier-2 wired in production at the design's 128 px), a pre-existing mock.patch thread race in concurrent handler tests fixed at root, and main's stale `rotation_world_from_layout` docstring corrected (claimed identity; actual `diag(−1,1,−1)`). Probe verdicts — V2 pass with two measured amendments, V1 sign-flag pass, V1 in-plane gate FAIL (fork re-opened), V3 pass — are decision 0068's subject. Merged-tree suites: 473+6-skipped root / 302 perception / 18 re-enqueue / 67 web (iOS untouched by this Python-only branch).
- **Shell surfaces v2 SHIPPED — 0069 built end-to-end, deployed, and V3-walked (branch `shell-surfaces`, 6 commits `2cec4b7`…`ba068c9`, merged ff to `main`; deployed 2026-07-23/24: `perception-obj-00032-km5` image `20260723-235521`, `api-public-00016-qer`; decision 0070 records the build verdicts; supersedes 0066's surfacing layer).** The photographic bake left serving. **shell.json v2** (`SHELL_VERSION` 2) carries per-plane **parametric materials** — measured weighted-median albedo in a high-lightness band; confidence-gated family from ONE structured-output vision call per plane over ≤4 rectified ~2.5 mm/px single-frame evidence crops (`SHELL_MATERIAL_MODEL=claude-sonnet-5`, key mounted from Secret Manager; family→roughness lookup; plank direction via axial gradient consensus) — plus **envelope closure**: wall→floor, wall→wall seams at the V1-calibrated `SHELL_JOIN_MAX_GAP_M=0.5`, common top, floor snapped outward then bounded by exact Sutherland–Hodgman half-plane clipping, and the joint-participation fragment filter (`f3d70236`'s four floating quads resolve: 2 drop, 2 keep with recorded justification). Every wall ships `measured_quad` + per-edge provenance (`{state, extension_m}`) beside the rendered quad; the floor `measured_polygon` + per-segment states beside the rendered polygon — closure never mutates measured geometry (test-pinned on real data). `room_planes.py` extracted as THE anchor-interpretation module (0067 chunk D refactors onto it; wall merge preserves door/window members as `openings` with normalized `rect_uv`). shell_texture/shell_inpaint/LaMa **deleted** (image slims ~200 MB; Dockerfile smoke covers the new modules + asserts `anthropic` imports); api-public passes the shell **verbatim and signs nothing shell-borne**; web renders parametric surfaces (warm light rig + `MeshStandardMaterial` from albedo/roughness — Spark splat shaders ignore three.js lights, so objects are unaffected — triangulated floor polygon, openings as inset patches; no CSP change). **THE LOAD-BEARING FALLBACK RULE** test-pinned: below the 0.6 conf gate / "other" / call failure / no crops / no key → family null → clean matte in the measured albedo; no-key leg exercised offline (families null, albedos byte-identical, shell still ships). **V2 adjudication PASSED** on the reference room (floor #c8c1b7 stone, main wall #aab9c3 painted — both read true to the operator). **V3 walk green on the live stack** (`f3d70236` re-driven): v2 shell.json reproduces both probes, **0 texture blobs**, no legacy keys, floor bounded 17→7 vtx with measured preserved, wall_06 closure-extended below its measured bottom, wall_04's 2 door openings normalized in [0,1], assets response verbatim with zero shell-borne signed URLs, `/viewer` renders the parametric room clean-console, `25a14caf` degrades v2 `no_geometry_source`. Suites at merge: 473+6-skipped root / 359 perception / 18 re-enqueue / 68 web, lint + typecheck + static-export build clean.
- **ARKIT_ONLY single-view placement from contact priors BUILT — 0067 chunk D, merged ff to `main` 2026-07-24** (branch `placement-chunk-d`, commits `ba78510` schemas / `af77edd` perception; OFFLINE-verified — the warm re-drive of a plane-carrying capture + operator /viewer walk is the live gate; reaches production on the next perception deploy, NOT yet on the deployed revision). The single-frame objects that can't triangulate (15 of 18 fused on `f3d70236`) now place against the MEASURED room: floor-standing classes drop their bottom onto the detected floor (closed-form 1-D depth solve — bottom-height is linear in depth, no iteration), wall-mounted classes intersect the mask-centroid ray with the nearest detected wall (normal snapped to the wall only within an alignment threshold, never flipping a badly-oriented object). Pure geometry in `placement_math.py` (`solve_floor_contact`, `solve_wall_contact`, `minimal_rotation`); policy in the NEW `contact_priors.py` — a conservative env-overridable floor/wall/none class map (ambiguous classes like `table lamp`/`speaker`/`plant` → none, unplaced on one view) and `RoomPlanes` read through the shared `room_planes.py` (NO re-implementation — 0069 extracted it). Wired into `fusion.py` for unplaced single-member ray clusters; `_refine_fused_object`'s in-plane/sign-flag/score/extents tail extracted into a shared `_finalize_placed_object`. **THE EVIDENCE RULE holds:** a proposed transform ships only if it reprojects onto the object's own SAM mask at tier-1 ≥ `PLACEMENT_SINGLE_VIEW_MIN_TIER1` — a guessed transform is never emitted; because the contact solve reprojects-by-construction, an independent floor-XZ-bounds sanity gate is the geometric check tier-1 can't provide (conservative — rejects to unplaced, never a wrong placement; relax `PLACEMENT_FLOOR_HIT_MARGIN_M` if the live walk shows valid placements rejected). Additive manifest fields carry through (`position_source` = `single_view_floor_contact`/`single_view_wall_contact`, `constraints_applied`, `reprojection_score`, `extent_m_sorted`). `PLACEMENT_REFINE=0` and bundles with no plane anchors reproduce today's behaviour exactly (pinned — the degrade lock). Verified offline through the production fusion path: floor + wall recovery of synthetic objects against `f3d70236`'s REAL measured planes (floor case also end-to-end through a real `f3d70236` camera pose) — position to <1 cm, scale to <2%, bottom on the measured floor to <3 mm, wall normal aligned to dot > 0.99; the evidence gate rejects a mismatched mask; determinism pinned. Suites: schemas 102 (+8 primitive ground-truth tests), perception-obj 371 (+12 real-plane tests), root 481 + 6-skipped, re-enqueue 18; ruff/lint clean. Chunk D's own live gate (warm re-drive of `f3d70236` — its 15/18 single-frame objects are exactly the target population — + operator /viewer walk) is owed alongside the chunks A–C placement gate.
- **ARKIT_ONLY placement sanity gate BUILT + DEPLOYED; gate + chunk D live-verified on `f3d70236` via warm re-drive** (revision `perception-obj-00033-zfg`, image `20260724-041547`, 2026-07-24; gate commit `f4fc8be` + `tests/test_placement_sanity_gate_real_data.py`, 16 pins; chunk D `af77edd`). The room-sanity gate in `fusion.py` demotes any triangulated / silhouette / `depth_fit` placement to unplaced-with-explicit-reason when it lands `outside_room` (beyond the floor rect + `PLACEMENT_ROOM_MARGIN_M=0.5`, below floor, or above wall top), is `implausible_scale` (`extent_m_sorted[0]` outside [`PLACEMENT_MIN_EXTENT_M=0.08`, `PLACEMENT_MAX_EXTENT_M=5.0`]), or is `represented_as_shell_opening` (door/window — the shell already renders these as openings). Chunk-D measured-surface contacts are EXEMPT (self-gated on a real surface — a door on its actual wall is evidence-grounded; a mid-room triangulated door is a guess). Inert without measured planes (degrade lock); `PLACEMENT_REFINE=0` bypasses the whole gate. **Live re-drive of `f3d70236`** (`tools/reenqueue_scene.py <scene> --force`, one round, 12/12 warm frames, `budget_stopped:false`): manifest 24 objects / **7 placed** — the gate demoted exactly the four operator failures (`mirror→outside_room`, `artwork→implausible_scale`, two `door→represented_as_shell_opening`); chunk D placed 5 single-view objects on measured surfaces (2 chairs + desk `single_view_floor_contact`, curtain + door `single_view_wall_contact`) plus bed + curtain via multi-view `silhouette_fit`, no two-bed split; the remaining 17 unplaced are mostly `insufficient_observations` (single-frame, no floor/wall prior class) — a coverage limit, not a bug. Shell re-driven (v2, ready, 5 walls + floor; geometry unchanged — confirmed empirically that shell geometry derives from the fixed plane anchors, not the processed frames, so more frames don't change walls; `wall_03` albedo remains the decision-0070 person-contamination). **Offline + live-re-drive verified; the operator `/viewer` walk (the human judgment gate) is still pending — and now DEFERRED under the board-item-7 Pro-only pivot (possibly moot).** perception-obj suite 387 green.
- **iOS app design BUILT end-to-end to the Good Guest system (branch `ios-app-design`, 13 commits, 2026-07-24; decision 0072).** The whole capture-app surface, translated from the design session's `Roomstudio iOS Capture.dc.html` (untracked at repo root) to SwiftUI and screenshot-verified on the iPhone 17 Pro simulator: a `DesignSystem/` foundation (`RSColor`/`RSFont`/`RSHaptics`/`RSSound`/`RSComponents`/`Wordmark`/backgrounds — gold=light-semantic-only enforced structurally; fonts on the spec's system fallbacks — New York / SF Pro / mono — behind a one-file bundling seam), and every screen: §1 home + cold-start, §2 guidance sheet, §3 live-capture overlay + gold-ink mesh PLACEHOLDER, §4 got-the-room + review, §5 analyzing/queued/long-running/connection-trouble, §6 the doorway, §7 recoverable + terminal failure + relaunch banner, §8 profile/why-sign-in/account-conflict, §9 thin history + QR bridge, §12 LiDAR-only unsupported-device screen. `RootFlowView` is the navigation coordinator binding every screen to the real `CaptureManager`/`ScenePoller`/`AuthManager`/upload stack — KEY behavioral change from the old `ContentView`: upload begins on review "Send it home", not automatically on `stopCapture`. Development pattern: each screen built as a standalone previewable view, verified via a temp app-entry swap + `simctl` screenshot, then reverted; every commit kept the `project.pbxproj` entitlements workaround and the untracked design files out. **ACTIVATED as the app root 2026-07-25** (`753f32e`): the LiDAR-device gate that deferred the swap stopped being a blocker once the product went Pro-only (0071) — `UnsupportedDeviceView` on non-LiDAR hardware is now the intended behaviour, not a regression. `ContentView` is retained, unreferenced, as the rollback path. Activation also restored the relaunch recovery the old root provided (`restoreUnfinishedBundle`). **Nine independent review passes** have run over this branch (`60737bd`…`84f0bfe`); each found real defects in the one before, several of them self-inflicted by the previous fix, so the passes are audited as critically as the original work. The last pass's three HIGH findings — a wait screen stranded on "Sending your room" forever after a background trip, a home screen that both advertised a bundle as in-flight and reported it failed (and, once the banner was dismissed, claimed "sending" permanently for a terminally failed upload), and a deferral signal erased by any sibling blob's progress across ~127 concurrent PUTs — are closed (`405131a`), as is the launch-restore defect that activation itself introduced (`84f0bfe`, decision 0073). Decisions that used to live inside SwiftUI bodies are now pure functions pinned by tests (`WaitFlowState` routing, `BundleRestore.pick` launch restore, `ReviewView.cardText` copy-vs-actions precedence) rather than reviewable only by eye — every one of them was extracted after a review found it wrong.
- **Both Pro-device hardware gates GREEN — the first real capture-to-doorway walk (board 2's gate) AND LIDAR_ARKIT tier verification (board 3) ran on a real iPhone 16 Pro (2026-07-26; decision 0074 for the migration finding).** iPhone 16 Pro (iPhone17,1, iOS 26.5.2) paired, built, installed with personal-team signing (entitlements-drop workaround, kept uncommitted). The LiDAR gate admitted the flow on its first-ever run on LiDAR hardware, and the Good Guest flow ran TWO real captures end-to-end (home → guidance "LiDAR READY · PRO CAPTURE" → capture → got-the-room → review "N frames · LiDAR" → Send it home → wait → doorway → Done). Every deliberate disruption path passed on hardware: upload entirely under lock (mint POST through ingest with the phone locked — the two-window background assertion held at 385-blob scale), upload-completes-while-locked → unlock catch-up (the 405131a strand fix, live), background trip across the queued→processing transition, force-quit + relaunch mid-processing (`BundleRestore.pick` chose the live bundle out of a 38-record backup-migrated store), relaunch-after-terminal → re-entry row → doorway direct, doorway-Done clearing home. **Gate B, first LIDAR_ARKIT executions ever:** tier dispatch correct on hardware; capture #1 (scene `247003de`, bundle `893663fd`) 293 keyframes/70.5 s with 292/293 depth (one legal mid-walk `sceneDepth` dropout, frame 266), capture #2 (scene `13bae607`, bundle `b4b15b1f`) 128/128; depth 256×192 packed float32 at exact contract byte sizes, room-scale meters (0.5–4.1 m mid-walk), confidence {0,1,2}; decision-0032 depth intrinsics byte-exact on hardware (fx 181.5 = 1361.6×256/1920); 26 plane anchors incl. first-ever ceiling/seat classes. Backend firsts, all green: the ingest gate passed both bundles (RGB-only validator correctly skips depth/confidence); **decision 0052's `depth_fit` placed 16/23 objects on capture #1 — every placed object via depth_fit, ZERO `insufficient_observations`** (the 16e baseline left 15–17 of ~20 unplaced), doors/windows demoted `represented_as_shell_opening`; shell v2 fired unprompted (floor + 14 walls, materials confidence-gated); the 2-round warm re-drive recipe reached 12/12 sampled frames on LiDAR data. Fixtures preserved: `outputs/real-capture-247003de/` (202 MB, full — THE LIDAR_ARKIT regression fixture) and `outputs/real-capture-13bae607/` (bundle.pb only). **Serial-mint defect found, fixed, and deployed same-session** (commit `2c821ab`): capture #1's 878-path manifest took two full ~70–80 s serial mints across 3 POSTs (client 60 s timeout; the 0015 TOCTOU race exercised live, benignly, same user); `_mint_all` now runs a bounded pool (`UPLOAD_SESSION_MINT_CONCURRENCY`=16, order-preserving, abort-without-store), deployed **`api-public-00018-xay`** (candidate → smoke → flip) and live-verified: capture #2's 385-path mint was ONE 200 POST (28.9 s incl. the revision's cold start). Suites: root 486+6-skipped (+5 mint pins), iOS 288/288 incl. the 4 live tests against the new revision.
- **First-LiDAR-rooms adjudication complete (2026-07-27/28; brief `docs/briefs/lidar-first-rooms-adjudication.md` — the board-7 design session's opening exhibit; decision 0075).** Both scenes staged in the dev workbench via the new `/viewer?fixture=<dir>` param (`5464c71`; fixtures gitignored under `web/public/dev-fixtures/scene-*`); production fusion + shell decisions replicated offline bit-for-bit (footprint join 0.5998 joined / 0.3144 refused on the same physical bed; the 14- and 10-wall shells reproduced wall-for-wall from the preserved bundles under the serving knobs). Measured: the bed×3/desk×3/rug×3 "under-merge" mostly DISSOLVES on mask-overlay identification — label-space collapse (stool + folding table both "desk"; three distinct textiles all "rug"; a folded mattress as the third "bed") with ONE true phantom, the f173 edge-truncated bed view, structurally refused by the footprint instrument (0.31 vs the 0.5 line even at its own fitted center) — depth_fit anchors to the VISIBLE REGION (not specifically the near face; displacements measured toward, away, and lateral), scattering partial-view centers 0.5–1.2 m on a 2.0 m object. The envelope is the LiDAR tier's headline win: rectangular to ≤1.1° on BOTH rooms, and the envelope-intersection floor plan (4.20×3.29 m, opposite sides to 3–6 cm) was **operator-confirmed against the real room**. The rendered damage is admission + closure: 10-of-14 / 6-of-10 "walls" are furniture planes (bed side rails ARKit-classified `seat` in both rooms) inflated to full-height slabs (+2.48 m worst case), and the floor S-H-clips against every one of them, compounding to **12% / 38% of the measured floor shipped**. New measured defect classes: cross-label triple-detection (the 0067 dedup is same-label-only — one ~0.3 m frame shipped 3× as artwork/painting/mirror at one position), specular mirror depth (nn_rms 0.196 m, 28× normal), textile scale collapse (a meter-scale throw ships at 0.34 m extent with 8 mm one-directional-NN rms — collapse-into-cloud degeneracy), and object-blind sampling starvation (12/293 frames used on #1; #2 budget-stopped at 3/12; capture quality itself measured GOOD). Analysis artifacts + replication scripts in gitignored `outputs/lidar-adjudication/`.
- **RoomPlan co-run spike EXECUTED — board 3 fully closed; the board-7 design session's last de-risk input is in (2026-07-28; branch `roomplan-spike`, commit `db46eb6`; decision 0076; four instrumented runs on the 16 Pro, operator-walked).** Headline: **co-run does NOT strip sceneDepth on iOS 26.5** — the documented caveat is refuted on hardware. Mechanism (measured twice): RoomPlan's native configuration IS production's configuration field-for-field (gravity alignment, h+v plane detection, `.sceneDepth`, 1920×1440@60) plus one private `sceneReconstruction=24` (whose mesh never surfaces as ARMeshAnchors); there is no config conflict to manage. Full-scan evidence: depth on 9752/9765 frames, all 722 keyframes; `ARSession.delegate` never touched (the prophylactic interposer never engaged) — the production keyframe pattern ran verbatim at 59–61 fps with 0 write failures; ONE continuous world frame across scan start/re-run/stop (boundary pose jumps ≤0.10 m), so bundle poses and CapturedRoom compose directly. CapturedRoom on the reference bedroom: 13 walls ALL matched to wall/door/window-classified anchor planes within 1–25 cm — the same session's two `seat`-classified bed-rail anchors (the current pipeline's phantom-wall culprits) were REJECTED by RoomPlan, killing the furniture-plane defect by construction, measured; floor is one 10-corner 14.98 m² polygon, oriented 4.26×3.97 m (long side within 6 cm of the anchor-envelope truth; the +0.7 m short side is a REAL entry alcove the envelope method truncated — operator-confirmed); 9 objects, all upright-by-construction (pure-yaw transforms), rich sub-attributes (cabinet/shelf, swivel/dining chair), **operator walk 9/9 on position, extent, AND facing — including a genuinely-angled chair (yaw follows reality, not a snap)**; one label miss (wardrobe→"refrigerator", self-flagged low confidence). Census: full 9-object count live by ~27 s of scanning; `didUpdate` = full room, `didAdd/didChange/didRemove` = DELTA rooms (changed entities only); boxes refine all scan long. Hard constraints measured: retaining just 10 ARFrames stalls the camera feed in ~1 s (Δ88 MB — pool starvation, not memory growth), kills VIO, and RoomPlan self-aborts after exactly 10 s of tracking failure (`didEndWith error: worldTrackingFailure`, partial room still returned; reproduced 2×); RoomPlan NEVER resets tracking — the host app owns tracking hygiene (`.resetTracking` at scan start is load-bearing). Co-run memory: ~+370 MB while scanning + ~905 MB transient at RoomBuilder (builds in 1.7 s). Artifacts in gitignored `outputs/roomplan-spike/` (4 runs; the probe run's CapturedRoom JSONs + parametric/mesh USDZ + plane anchors + full instrument streams + 722-keyframe RGB/depth archive = the first LIDAR_ROOMPLAN prototype data, one shared frame); spike app committed at `ios/RoomPlanSpike/` (throwaway, own pbxproj — production project untouched). Production 7-day resign refreshed same-session (expires 2026-08-04).
- **Board-7 RoomPlan integration design DONE (2026-07-28; decision 0077 + build brief).** Seven questions locked, three operator forks resolved, and two verify-first probes run in-session against recorded real data (gitignored `outputs/roomplan-design/`): P1 — box-frame candidate scoring on 247003de's real bed: correct facing wins with tier-2 margin 0.15 at a box-quality center, upside-down rejected by 0.37, the box-frame candidate BEATS the shipped layout rotation in its own source frame; the same scorer FAILS at the shipped fused center (prefers upside-down — position precedes rotation) and a 1.4 m close view zeroes out (skip degenerate views). Collateral: the shipped bed is ~90° yaw-wrong (long splat axis along the rail NORMAL, |dot| 0.947) and 0.79 m displaced — the operator's orientation verdict quantified on one object, invisible to every shipped metric. P2 — a ~60-line numpy parser over Apple's Codable JSON reproduces every 0076 fact (13 walls up_y=+1.0000, 3.05 m top + four 1.95 m door segments, floor 14.98 m²/10 corners, 9 pure-yaw objects incl. the 40.8° chair, full door/window parenting) — the wire contract validated with no coreModel and no USDZ. Also measured: the RoomPlan bed box is 1.85×2.16 m where the shipped fit said 0.92 m wide (visible-region truncation halved it) — box extents own measurement truth for covered categories.

## What does NOT work / what we're deliberately not doing

- **ARKIT_ONLY placement + shell quality investment PARKED — pivot to a Pro-only / LiDAR-first pipeline (operator decision, 2026-07-24).** After the non-LiDAR reference room (`f3d70236`) still read poorly on real output, the operator chose to redirect effort to the premium LiDAR path (decision 0071 direction, Option A) rather than keep raising the non-LiDAR quality ceiling. The hardware block CLEARED 2026-07-26 (iPhone 16 Pro in hand; both hardware gates green — see What-works) and the operator RESOLVED the 0071 fork: **Option A — RoomPlan-only, Pro-only, assembly-first**. The RoomPlan integration DESIGN is done (2026-07-28, decision 0077 + build brief); execution is board item 7. The shipped sanity gate + chunk D stay LIVE (strictly better than before; `PLACEMENT_REFINE=0` rolls back if ever needed) — nothing is rolled back. No further non-LiDAR merge-knob / placement grinding; the operator `/viewer` walk of the non-LiDAR output stays moot under the pivot (the LiDAR rooms are the ones worth walking now).
- **Phantom-room row on backup-migrated devices (decision 0074; found live on the 16 Pro's first launch).** iCloud backup migrates `UploadSessionStore` records (38 of the 16e's arrived) but NOT capture blobs (backup-excluded), NOT the Firebase anon identity, NOT the DeviceIdentity UUID (both minted fresh — verified against the new scene docs). Consequence chain, every link verified: `BundleRestore.pick` adopts the old device's newest `.complete` record (the pre-RootFlowView flow never wrote acknowledgments) → home advertises the 16e's July room on EVERY cold launch → polling it 403s (ownership enforced, fresh UID) → the connection-trouble screen claims "your room is safe up there" (false for this identity) and has no acknowledging exit → the phantom recurs forever. Fix direction (own iOS pass): treat a by-bundle poll 403 as terminal-not-ours — acknowledge + reset instead of connection-trouble. Cosmetic sibling: the 34 June-era records predate `uploadPhase`, fail strict decode, and sit as dead files no sweep ever reclaims.
- **First LiDAR rooms ADJUDICATED — operator verdict: the rooms read "very far from reality"; the correct set is minimal (2026-07-27/28; decision 0075; full diagnosis + per-finding RoomPlan table in `docs/briefs/lidar-first-rooms-adjudication.md`).** The prior under-merge suspicion is RESOLVED: mostly label collapse, not fusion error, and the merge knobs MEASURED CORRECT on both rooms (small↔envelope wall pairs fail on coplanar offset 0.44–3.4 m — real plane separations) — **do NOT tune `FUSION_CLUSTER_DIST_M` / `SHELL_WALL_MERGE_*` to chase these symptoms (0075)**. Measured defect ranking for board 7: (1) floor collapse — 12%/38% of the measured floor ships after clipping against furniture-plane "walls"; (2) furniture-plane wall admission + closure inflation (a 0.53 m bed rail renders as a 3.06 m slab; admission is area+verticality only, ignoring ARKit's own seat/none classifications and height-reach); (3) object-blind sampling starvation (pipeline-side; the captures themselves measured good); (4) ORIENTATION reads near-globally wrong to the operator while the tracked metric is green — min-axis median 9.3° certifies only that SOME axis points up; **YAW is unscored by any current instrument**, in-plane unresolved on 13/16, sign checked on 1/16 (the 0065 lesson generalized to a whole instrument family); (5) the one true under-merge phantom (edge truncation structurally defeats footprint containment); (6) cross-label duplication, mirror specular depth, textile scale collapse. Operator hypotheses recorded as design inputs: live capture-coverage feedback (= task #13's real shape; RoomPlan-native) and object-aware frame selection from a furniture census (= `CapturedRoom.objects` convergence; 0062's successor under Option A). 13bae607's warm re-drive: available, adjudged moot for the design session.
- **iOS app design residue (branch `ios-app-design`, decisions 0072/0073; the flow itself is now hardware-proven — see the 2026-07-26 What-works bullet).** `RootFlowView` is the app root as of `753f32e`; two real captures have run capture-to-doorway on the iPhone 16 Pro, including the disruption paths the nine review passes could never exercise (background trip, upload under lock, relaunch restore, doorway exit). The LiDAR gate still sends the non-LiDAR 16e to `UnsupportedDeviceView` — intended (0071/0072); **the 16e is not a test target and must not shape decisions** (operator direction 2026-07-25). Still UNEXERCISED on hardware: terminal blob failure, server-side scene failure (`failed`/`failed_invalid`/`failed_incomplete` UI), and the OS-kill relaunch (board 2's Gate 2b). Two build pieces remain: (1) the live-capture screen renders a gold-ink SwiftUI mesh PLACEHOLDER behind the `LiveMeshHost` seam — the real RoomPlan/ARKit scene-reconstruction mesh + real floor/walls/corners coverage is hardware-only (task #13 — now UNBLOCKED by the 16 Pro, and its shape should follow the board-7 RoomPlan design session); (2) the §5 **Live Activity** (Lock Screen / Dynamic Island upload progress) is unbuilt — it needs a new WidgetKit **extension target**, an Xcode project-structure change beyond synchronized file groups, deliberately not hand-grafted into `project.pbxproj` while the entitlements workaround sits uncommitted there (task #14). The doorway universal-link handoff, the QR bridge, and push remain enrollment/entitlement-gated (associated-domains / APNs) — UI built, transport stubbed at named seams. Fonts fall back to system faces until the branded `.ttf`s are bundled at the `RSFont` seam. Activation follow-ups (recorded in `RootFlowView`'s docstring): add-more resume-with-progress and the real web-handoff link (relaunch poll-resume is DONE — `restoreUnfinishedBundle`, landed with activation and corrected by 0073). The design SCREENS are still verified by build + screenshot, not unit tests; what the 288-test suite pins is the flow LOGIC beneath them (routing, restore selection, deferral scoping, poller visibility, review-copy precedence) — a green suite says nothing about whether a screen renders correctly, which is why AX5 claims must be re-verified by screenshot every time (three separate passes claimed AX coverage they did not have).
- **0051 E2E verification blocked on Apple Developer Program enrollment** (operator enrolling — chosen 2026-07-22 over reopening the provider choice, see decision 0064; approval typically <48 h). The personal team (`3HU2SP8346`) can provision neither the Sign in with Apple entitlement — **device builds fail at provisioning until the paid team lands** (interim capture rebuilds: locally drop the two `CODE_SIGN_ENTITLEMENTS` lines in `project.pbxproj`, never commit that; personal-team signatures expire after 7 days, so this will come up — the 16 Pro's install, built 2026-07-26, is on this clock: rebuild by 2026-08-02 or the app stops launching) — nor the web Services ID Gate B needs. Post-enrollment, in order: re-select the team in Xcode (if the team ID changes from `3HU2SP8346`, `DEVELOPMENT_TEAM` appears 3× in the pbxproj); Firebase console → enable the Apple provider (no fields); Gate A on-device link (UID byte-identical before/after in the SignInSheet footer, `auth.link … uid_unchanged=true` in the log); Apple portal → Services ID `com.roomstudio.web` (primary App ID = the capture app, domain `roomstudio.firebaseapp.com`, return URL `https://roomstudio.firebaseapp.com/__/auth/handler`) → set it on the Firebase Apple provider; Gate B browser sign-in with the same Apple ID → GET /scenes lists the real captured room → screenshot. The CSP preconditions are CLOSED by 6(a) (2026-07-22): `web/firebase.json` now carries `frame-src roomstudio.firebaseapp.com` AND the second live-found requirement, `script-src https://apis.google.com` (the popup flow's gapi iframe-channel loader). Gate B runs on the preview URL and additionally needs the preview origin added to api-public `CORS_ALLOWED_ORIGINS` first (env-only revision; mirror the yaml in the same commit).
- **Sendable warnings in CaptureManager.swift.** `CVImageBuffer`/`CVPixelBuffer` cross the `DispatchQueue.main.async` boundary without `Sendable` conformance. Warnings in Swift 5 language mode; become errors under strict concurrency. Flagged for P1→P2 pre-flight.
- **iOS P2 LiDAR depth residual (decision 0033): CLOSED 2026-07-26.** Every deferred check ran green on real hardware, twice (both 16 Pro captures): `sceneDepth` non-nil per frame with legal dropouts, 256×192, one depth blob per frame at exact byte sizes, decision-0032 depth intrinsics byte-exact. See the What-works hardware-gates bullet.
- Scene `f077e9ed-d339-4be8-8dbf-37b952abfec2` is intentionally left in `processing` with an expired lease as a canonical stuck-scene reference. Re-enqueue via `tools/reenqueue_scene.py` if ever needed to verify the expiration-check reclaim path end-to-end.
- **`test_data/photos/` privacy is deferred.** 9 HEIC photos of a real room are tracked by git, used by `tools/build_test_bundle.py` for local synthesis testing. Privacy review (anonymise, replace with synthetic data, or remove from history) is a separate session. Do not act on this until explicitly scoped — it may require a history rewrite.
- **Web app: preview channel only — the production channel is deliberately not deployed.** 6(a) stood hosting up on preview channel `preview` (2026-07-22; see What-works). `roomstudio.web.app` stays undeployed on purpose until launch hardening closes the board-4 abuse-surface gaps (a public site is their first-non-developer-user trigger). 0051 is BUILT on both sides — what remains is enrollment-gated E2E (Gates A/B), so live sign-in on any origin still stops at the Apple provider gate (`OPERATION_NOT_ALLOWED`) until enrollment lands and the provider is configured. The preview origin also isn't in api-public's `CORS_ALLOWED_ORIGINS` yet (Gate-B precondition).
- **Bridge QR is a declared placeholder; reveal timing has had no real-browser watch.** The QR encodes nothing (no deep-link infra exists) and its caption says so. The objects-first assembly (§4 reveal) is verified by code + end state only — the dev preview pane throttles rAF/timers — so nobody has watched it play at real speed yet; do that once in a normal browser and tune the timing constants at the top of `web/src/components/SplatViewer.tsx` if the pacing feels off.
- **Conversation stage 1 is live; its recorded residue.** (1) Manifest v2 still carries no per-object extents — sizes, gaps, and clearances stay unspeakable (the charter forbids restating center distances as any of those); the manifest half now exists on `main` (0067's `extent_m_sorted`, merged 2026-07-23; reaches production at the next perception deploy); what remains is `scene_facts.py` consuming it with a `facts_version` bump. (2) The ledger deferral and its vocabulary ban are unchanged — no pin/keep/"put into words" anywhere; the "in this room" inventory panel stays as-is. (3) The reoffer/confirming composer states are pinned by reducer tests and mock triggers but haven't been watched against a real mid-stream server failure in a browser. (4) Client wake cadences (blocked 5 s, confirm 4 s, rested 15 s ticks) are untuned placeholder values. (5) Voice evals are live-gated (`RUN_VOICE_EVALS=1` + key; NOT in the offline suite); re-run on PROMPT_VERSION bump OR `GUEST_MODEL` change — the env yaml notes this at the `GUEST_MODEL` line. (6) Real users still can't reach it: 6(a) now serves a preview channel (production deliberately closed until board-4 hardening), and sign-in stays enrollment-gated (6(b)); CORS allows localhost + the two production hosting domains — the preview origin joins at Gate B.
- **Envelope fix residue (fix itself shipped — see What-works).** (1) Cold-start coverage is thin by design (the 2026-07-22 re-drive confirmed the pattern: request 1 spent its wall clock on boot + model load and banked 2 frames; two WARM re-drives then took the scene to 12/12 sampled frames — the multi-round `reenqueue --force` loop is the working coverage recipe); sampling/budget knobs (`PERCEPTION_MAX_FRAMES`, priors, reserve) are one-capture-calibrated placeholders. (2) Large single objects (big masks: doors, beds) can transiently exceed L4's 22 GiB even at the flat 16.4 GiB baseline — both reconstruct attempts OOM, per-object soft-fail contains it, those objects are simply absent from that frame. A capacity fact of L4 + these models, not a lifecycle bug (0061). (3) Stranded scenes from the pre-fix era don't self-heal — `tools/reenqueue_scene.py` is the cure (0060); scene `f077e9ed` remains the deliberate stuck-scene reference.
- **Layout-convention residue (fix shipped, basis-corrected, and production-verified 2026-07-23 — see What-works; decision 0065).** (1) `min_axis_to_vertical_deg` stays sign-blind BY DESIGN; the sign dimension is now guarded by the SIGNED and TRANSLATION-DIRECTION regression pins instead (the identity-twin episode is the case study: line metrics alone crowned a basis that flipped every Z-class object). (2) ARKIT_ONLY POSITION quality is the new frontier, ranked by the operator's real-room walk: the curtain's triangulated center sits ~0.5 m into the room and interpenetrates the bed (measured: centroid rays on an object subtending 0.74–1.3 rad don't correspond to any fixed 3D point — cluster RMS 0.287 m grazing the 0.3 gate, vs 0.007 m on the compact lamp); the one physical bed splits into two clusters ~1.2 m apart (measured root cause, correcting the earlier `FUSION_CLUSTER_DIST_M` guess: frame 28 carries a NESTED duplicate "bed" detection — intersection-over-smaller 1.000 — and fusion's frame-uniqueness merge guard reads it as two objects, structurally forcing the fork at ANY threshold); no room shell for pre-plane captures (`25a14caf` predates plane anchors) amplifies both. **BUILT — chunks A–C merged 2026-07-23** (decision 0067 + `docs/briefs/placement-quality-build-brief.md`; offline-verified only, warm re-drive + /viewer walk = the live gate): pixel-footprint correspondence (mutual-singleton dedup — the brief's raw-containment contract was amended by a measured door counterexample, see 0068), multi-view silhouette fitting as the ≥2-view position/scale authority (V3 passed: the curtain's fitted center leaves the bed volume, tier-1 0.306→0.430), the two-tier instrument in production; **chunk D (single-view contact priors via the shared `room_planes.py`) BUILT + merged 2026-07-24** (branch `placement-chunk-d`; offline-verified, live gate owed — see What-works). VIO-calibrated monocular depth: DEFERRED with named re-open triggers (0067). Bed verdict: the phantom is eliminated, honest residual 5+1 (0068). (2b) One residual ROTATION failure mode, found by the operator post-fix: IN-PLANE rotation noise — the rendered curtain's plane is vertical but the fabric is turned ~90° within it (pleats sideways; measured: normal horizontal −0.01, in-plane axis plumb +1.00, pleat banding horizontal in the front-view raster). Near-square planar objects are ~90°-ambiguous to the model, and NO current instrument scores in-plane orientation (min-axis satisfied, sign tests inapplicable; provably not a convention twin — doors/beds stand correct). Both candidate fixes are BUILT (chunks A + C, merged 2026-07-23) — but chunk C's V1 gate FAILED on the real curtain: the instrument ranks the shipped-wrong candidate last-of-4 (sees the right direction) with winner margin ≈0.0007 vs the 0.03 gate, so `in_plane_resolved: false` and the layout rotation stands; per 0067's re-open clause the instrument fork RE-OPENS for near-square planar objects (0068; the failing margin is test-pinned so a better instrument = a good pin failure). The sign-flag half passed cleanly (bed true 0.474 vs mirrored twin 0.379). (3) Splats without sidecars degrade to `rotation_source: "none"` on cache hit (none remain for `25a14caf`). (4) 17 of 22 fused objects are `insufficient_observations` — single-frame objects can't triangulate (`PERCEPTION_MAX_FRAMES=12` placeholder), not conventions; 0067 chunk D (BUILT + merged 2026-07-24, offline-verified) makes the floor/wall-class ones placeable from ONE view via measured contact priors — but ONLY on captures WITH plane anchors (`25a14caf` has none, so its 17 stay unplaced until re-captured; `f3d70236` is the target), with `PERCEPTION_MAX_FRAMES` as the complementary coverage lever. (5) The LiDAR `depth_fit` path now consumes a correct rotation — 0063's don't-trust warning lifted in principle; path stays hardware-parked (board item 3).
- **Known fusion limits (v1, documented in 0052):** two same-label objects closer than 0.4 m (`FUSION_CLUSTER_DIST_M`) can merge into one (0067's footprint correspondence is now BUILT on `main` — merged 2026-07-23, `PLACEMENT_REFINE`-gated — but the DEPLOYED revision still runs the bare heuristic until the next perception deploy). The room shell now RENDERS in production for plane-carrying captures (deployed 2026-07-23 — see What-works); pre-plane captures — including `25a14caf` — carry an explicit `unavailable`/`no_geometry_source` shell.json (objects on the grid, no silent absence). The room-shell build brief is deleted (its live-E2E deletion trigger met).
- **Room shell residue (0066 deployed 2026-07-23; surfacing layer superseded by 0069 — see What-works).** (1) Shell reveal pacing (floor → walls → objects) still has no real-browser watch — joins the existing item. (2) RESOLVED by 0069: the four floating unclassified fragments no longer render as floating quads — the joint-participation fragment filter drops those with no floor contact and no seam with structure (`f3d70236`: 2 drop, 2 keep with recorded justification), `SHELL_MIN_WALL_AREA_M2` untouched. (3) NEW iOS finding from the E2E: during a fresh capture's upload, `SceneStatusView` kept showing the PRIOR scene's "Room ready" state — the panel doesn't stand down when a new bundle starts uploading (the new scene's poll ran fine; the surface misleads). Next iOS pass. (4) MOOTED by 0069: low-observed-fraction planes no longer render as bare quads from a failed bake — parametric materials give any observed plane a measured-color clean matte, and an unobserved plane (albedo null) renders the honest neutral treatment. No bake coverage needed.
- **PRIVACY GAP — people contaminate shell surface observation (sharpened by the 0069 adjudication).** "Person" is not a SAM-detected class, so anyone in the room during capture is observed as if they were the surface behind them. 0069 (shipped 2026-07-23/24) removed baked textures from serving — person PIXELS no longer ship, which closed the original form of this gap (a family member was visible in `f3d70236`'s v1 floor texture). But adjudication of the v2 shell found the gap MUTATED, not mooted: `f3d70236`'s `wall_03` measured albedo (#899fbf) is the operator's mother lying in front of that wall patch, and a person's image can reach the ≤4 evidence crops sent to the material-inference vision call. The family gate held there (below the confidence bar → null), but albedo is measurement, not inference — it ships. Person detection/masking (or capture-time guidance) stays the launch-hardening fix before any non-developer user, now with a concrete manifestation (decision 0070). Related closed finding: `web/public/dev-fixtures/` (real-room artifacts) was shipping into every static export — the standing preview channel publicly served the July room splats from 6(a) until 2026-07-23; fixed by `63b5fba` (firebase.json ignore + channel redeploy, fixture URLs now 404).
- **Shell-surfaces v1 residue (0069 shipped 2026-07-23/24; decision 0070).** (1) The closure knobs (`SHELL_JOIN_MAX_GAP_M=0.5`, `SHELL_FLOOR_DROP_MAX_M=2.0`, `SHELL_FLOOR_CONTACT_TOL_M=0.15`, `SHELL_STRUCTURAL_MIN_AREA_M2=1.0`, `SHELL_COMMON_TOP_ENABLED`) are one-capture-calibrated against `f3d70236` like every SHELL_* knob — a room whose real corners need > 0.5 m closure re-opens the join gate's default. (2) The floor's **rectangular-stone pattern is real and detectable** (tile pitch + grid orientation via the plank-direction instrument class over the rectified crops — the operator flagged it on adjudication) — the first candidate for `material.params` growth; deliberately NOT in v1 (0069 excludes pattern treatment: a wrong pattern breaks recognition worse than a clean matte). (3) `wall_03`'s shipped albedo is measured off a person (see the privacy bullet); its family gate held. (4) `secondary_hex` is always null (two-tone separation deferred as noisy). (5) The material vision call runs at most once per plane per scene lifetime (write-once noop); a `SHELL_MATERIAL_MODEL` swap must re-adjudicate on the reference room first (env-yaml note). (6) Reveal pacing (floor → walls → objects, now parametric surfaces) still has no real-browser watch — joins the existing reveal-pacing item.
- **Pre-launch gaps (nine gaps + one audit item, categorized).** See `docs/decisions/0015` and `0018` for full list and un-defer triggers. Abuse-surface (original 0015 set, trigger: first non-developer user): (a) TOCTOU race on `bundle_id` ownership in `/upload_session`; (b) no per-UID rate limit on `/upload_session` (the conversation endpoint ships its own local budget bounds per 0058; per-UID velocity limiting across expensive endpoints remains this gap, unchanged trigger); (c) `expected_size_bytes` optional server-side, defaults to 0 — **client half CLOSED** (2026-07-21, `1b4d85c`): the re-mint path reads real sizes off disk via `record.outputDir` and fatals (`blob_unreadable_at_remint_manifest`, pre-mint) on a missing/unreadable/zero-size file, and the two placeholder comments are updated; every client mint path (initial via `ManifestBuilder`, re-mint via `onSessionExpired`) now sends honest sizes. What remains is server-side only: the field is optional and unvalidated at `/upload_session` — requiring/enforcing it is a 0035 contract change that lands with the launch-hardening pass like the rest of this list. Contract-shape (from pass 3 + pass 4 recon, trigger: iOS development or web app build begins): (F1) `expires_at` not surfaced in `/upload_session` response — **on review, this likely doesn't need building as originally conceived**: the only consumer of a client-side mint timestamp (`BlobUploadManager`'s 12h staleness guard) measures time-since-mint, not URI expiry — a different quantity — and the 410-triggered re-mint path already handles genuine URI death; if anything from the server is worth surfacing here, it's the GCS object lifecycle window (currently a hardcoded 12h client-side mirror of a server config), not the resumable URI's own ~7-day expiry; (F2) `X-Upload-Content-Type` hardcoded to `application/octet-stream` server-side; (F3) no semantic manifest validation (unknown extensions, tier/path consistency) — server does independently enforce relative-path **format** (leading slash → structured 400, live-verified 2026-05-31); semantic checks remain client-owned and unverified; (F4) **web half CLOSED** (decision 0054): `GET /scenes/{scene_id}/assets` now serves the manifest + V4-signed splat URLs; `/scenes/by-bundle` still returns raw `gs://` in `result_uri`, which is fine — iOS treats it as an opaque readiness token and the web client uses the assets endpoint instead; nothing remains to build here unless a client someday needs to fetch `result_uri` directly. Production-hygiene (trigger: launch): (F5) no lifecycle rule on `gs://roomstudio-perception-outputs/scenes/`; (F6) no TTL on Firestore `scenes` collection — this isn't just a storage-hygiene gap: `receiver_repo.py`'s `claim()` currently treats a `NOT_FOUND` result (no scene document for a task's scene_id) as an unusual, essentially manual-deletion-only case, since nothing auto-deletes scenes today. Once F6 ships, the exact same `NOT_FOUND` response becomes routine, expected steady-state traffic — any Cloud Tasks redelivery arriving after its scene has aged out and been TTL-swept hits this path normally, not exceptionally. **When building F6**, update the docstring at `receiver_repo.py:91-96` (the `claim()` not-found case), which currently describes the pre-TTL "manually deleted only" framing and will go stale the moment TTL exists. **Also when building F6:** ship TTL-only first — a client-ack cleanup path (client acks after successfully fetching/consuming a ready/terminal result, triggering prompt deletion, with TTL remaining as the mandatory backstop since client cooperation isn't guaranteed) is a possible later optimization layered on top, not a substitute for TTL and not scoped or designed yet — do not build ack-only. **F6 also inherits (once conversation stage 1 is built):** Firestore never cascades deletes — scene TTL alone would orphan decision 0058's `conversations` docs and their `turns` subcollections; plan per-collection-group TTL on `turns.created_at` + `conversations`, or explicit recursive delete in the sweep. Audit item (during launch hardening): verify perception-obj runtime SA identity and storage IAM — the grant half is now folded into `deploy_perception.sh`'s idempotent shell-IAM grants (`48c2f91`); the verify half remains. All nine gaps close in the same launch-hardening pass.
- **happy-path and duplicate-event smoke modes terminate at `failed_invalid`, not `ready`.** The ingest validation gate fast-fails the synthetic fixture's non-decodable placeholder images pre-GPU (~3–10 s including Eventarc latency). This is the verified, expected terminal state for smoke runs. Reaching `ready` requires real image data from an iOS bundle.
- **Phase 8c soak: RUN AND GREEN (2026-07-22 00:57 local / 19:27Z 07-21).** Operator-run happy-path against production: exit 0, `failed_invalid` in 10.5 s, cleanup clean. Covers the serving stack (`api-public-00012-ziz`, `api-internal-00019-des`, `perception-obj-00026-449`); the cold-steady-state intent (lifecycle age=1d + Firestore TTL having fired since the original deploy, scale-to-zero cold start) was met — with the honest note that the two newest revisions were hours old at run time. RUNBOOK Phases 0-8 are now fully executed for the 2026-07-21 deploy cycle; iOS real uploads are unblocked.
- **iOS P3 401 recovery-success leaf unverified.** The live 401 test used a garbage token — exercises refresh→retry→propagate (give-up branch), not the expired→refresh→valid→200 case 0038 actually cites. Mechanism runs; the "refresh yields a working token" leaf is untested. Low risk. The path-violation 400 is confirmed for a leading slash only (`..` not separately confirmed; client emits neither).
- **`validate_bundle` bundle_id cross-check: CLOSED in code, not yet serving.** Built on branch `ingest-bundle-id-crosscheck` (commit `c30e317`, merged to `main` 2026-07-21): `validate_bundle(bundle, expected_bundle_id=…)` runs the proto-vs-URI bundle_id cross-check as check 2 (right after schema_version); any divergence → `failed_invalid` Scene with `bundle_id_mismatch` (both ids in `last_error`) + structured log + HTTP 200, per decision 0031's rejection pattern. Comparison is exact and **case-sensitive** — every downstream join on bundle_id (Firestore `scenes.bundle_id` queries, `upload_sessions` doc IDs, `captures/{bundle_id}/` GCS paths) is byte-wise, so a case-differing id breaks those joins like any other mismatch and is rejected, not normalized (rationale in `_check_bundle_id`'s docstring); an empty proto bundle_id is rejected by the same check. Passing `expected_bundle_id=None` skips the check — only for callers with no upload URI (the api-core fixture self-tests call `validate_bundle(b)` and were deliberately left untouched). **SERVING as of 2026-07-21** (`api-internal-00019-des`, envelope session's operator-approved redeploy; post-flip happy-path smoke green — see What-works).
- **Staleness re-mint missing-file stall: CLOSED by P5(b)+(a).** `onFatalBlobError("blob_file_missing_at_staleness_remint")` now routes to the real terminal handler (sets `uploadPhase = .failed`, cancels sibling URLSession tasks, no record update without explicit save). The staleness pre-pass (Phase 1 of `onSessionExpired`'s `!loopGuardEnabled` branch) confirms all non-`bundle.pb` files exist before resetting any blob status or starting any upload task; a missing-file abort starts zero sibling uploads and leaves the store untouched. `bundle.pb` is never finalized against the hole. Gap was: `onFatalBlobError` was an unbuilt stub — now built by P5(b).
- **KNOWN GAP — completed-capture disk accumulation.** As of decision 0043, blob files live in Application Support (durable, CAFUFA), no longer tmp (which auto-purged). `CaptureStorageSweeper` reclaims ONLY abandoned captures (no `UploadSessionRecord`). Completed bundles keep their record — `onBundleComplete` is an unbuilt P5 stub that does not delete it — so the sweep keeps their directory. Each completed capture leaves one ~50–300 MB directory in Application Support that is never reclaimed until P5 ships. Bound: flat accumulation, one dir per completed capture; not runaway. Pre-launch, no real captures yet. **Closure trigger:** P5's `onBundleComplete`, when built, must delete the store record AND the session dir on successful upload. Do NOT pre-empt it by teaching the sweep to delete completed-bundle dirs — distinguishing "completed, safe to delete" from "completed, may need retry after failed_incomplete" is a P5 design decision tied to terminal-state handling, not a sweep heuristic.
- **KNOWN GAP — no automatic re-upload for `.recoverable` (failed_incomplete).** Nothing watches `pollState == .recoverable(missingPaths:)` and re-drives `BlobUploadManager` with the missing paths — this coordinator doesn't exist. `SceneStatusView.recoverableView` currently tells the user "Re-upload will be handled automatically," which is not true today; **fixing that copy is deferred** until either the coordinator ships or the copy is made honest about the gap, whichever comes first. **Dependency with the disk-accumulation gap above:** `onBundleComplete` fires the moment `bundle.pb`'s PUT succeeds — client-side, before the backend's async validation can determine `failed_incomplete`. If `onBundleComplete`'s eventual cleanup deletes the local session directory at that point, the files a re-upload coordinator needs may already be gone by the time `.recoverable` is observed. Whichever of these two gaps is built first must account for this, or the recoverable UI's promise stays false even after the coordinator ships. **Closure trigger:** build the coordinator AND defer `onBundleComplete`'s file/record deletion until a genuinely terminal backend state (`ready`/`failed`/`failed_invalid`), not just client-side upload success.
- **`bundle.pb` relaunch-recovery: code-complete, OS-kill gate unverified (Gate 2b).** Root cause (decision 0044): recovery path was unreachable on relaunch — wired only to `ContentView.onChange(of: capture.bundlePath)`, not a bare reopen. **Fixed in code by the P5 cluster** ((c) `5bb07c9`, (b) `cc7aba5`, (a) `658cc4a`): `rehydrateAllUnfinishedBundles()` fires on every launch from `.task` in `RoomStudioCaptureApp`; the OS-kill secondary route (`contexts[bundleId] == nil` → `onFatalBlobError`) now routes to the real terminal handler which sets `uploadPhase = .failed` and cancels siblings. **NOT VERIFIED ON HARDWARE:** the OS-kill route (background URLSession relaunch, `.task` fires without entering foreground) has not been staged on-device. The AppDelegate fallback is deferred pending the hardware gate — see decision 0045 Fork A ordering constraint. `diag-bundlepb-reason-public` (`5bdd12f`) is the parked tool for reading the redacted `reason=` on the OS-kill route — do not delete. Code-complete ≠ Gate-2b closed; the hardware gate is the close.

## iOS test policy

The iOS suite is **288 tests total** (was 245; the ios-app-design review passes added the flow-logic pins — `WaitFlowStateTests` 21, `ScenePollerVisibilityTests` 3, the deferral-scoping pins, `BundleRestoreTests` 10, and `ReviewCopyTests` 5), run manually via `xcodebuild … -scheme RoomStudioCapture-Integration` — the only scheme in this project (no separate default scheme, no CI gate). That scheme bakes `RUN_INTEGRATION_TESTS=1`, so the 4 `UploadSessionClientTests` **execute live on every run**; they are NOT skipped in practice. They last ran live 2026-07-26 (post-deploy contract verification of the parallel-mint revision, 288/288) against the serving stack (`api-public-00018-xay`).

**What the suite does and does not cover.** It pins flow LOGIC — routing tables, restore selection, deferral scoping, poller visibility — deliberately extracted into pure functions so they are reviewable as tables instead of by reading SwiftUI. It does NOT cover rendering: a green suite is compatible with a screen whose only exit is clipped off-frame at accessibility sizes. **AX5 layout claims must be re-verified by screenshot, never by reading** (`xcrun simctl ui <udid> content_size accessibility-extra-extra-extra-large`, then a temporary app-entry swap to the screen under test); three separate review passes claimed AX coverage they did not have, and the two screens that actually failed — `AccountConflictView` and `QRBridgeView` — were both found by screenshot after being read as fine.

**Posture: fail-closed-live, not fail-open.** Each integration test calls `XCTSkipIf(!RUN_INTEGRATION_TESTS)` — the fail-open default — but because the sole scheme always sets the flag, that skip path is never taken here. With the flag set they hit the live `/upload_session` contract and go **red if the backend is unreachable**. Running the suite therefore requires a reachable backend; an offline run will fail those 4 (expected, not a regression).

**Honest count:** report as "282 asserting offline unit tests + 2 boilerplate stubs (`testExample`/`testPerformanceExample`) + 4 live integration tests (require a reachable backend)", total 288 — not a bare total: the 2 stubs assert nothing and the 4 integration tests carry an external dependency the unit tests don't.

**Parallel-worktree note:** `GoogleService-Info.plist` is gitignored, so a fresh git worktree lacks it and the 4 live tests fail with a Firebase configure error that mimics a backend failure. Copy it from the main tree (`ios/RoomStudioCapture/RoomStudioCapture/`) before running the suite in a worktree; a rebuild picks it into the app bundle.

**Concurrent sessions MUST use separate worktrees, not the same tree.** Two iOS sessions ran in the main tree on 2026-07-25 and nearly produced a false green: one stashed two files to unblock a branch checkout while the other was mid-edit on them, so for the length of a merge the tree held the pre-edit content. Any build, test run, or `git add` in that window would have silently captured stale source, and BOTH sessions would have reported a green suite for a tree neither of them had. Nothing warns you — the suite passes, the diff looks right, and the commit that lands is whatever happened to be on disk. `git worktree` is the fix (one is already in use for `placement-quality-build`); the plist note above is its one gotcha.

**Contract/CI note:** the `/upload_session` contract is frozen (decision 0035), so the live integration tests assert against a stable shape. When the contract next changes (launch hardening or any server-side change), or when CI is stood up, the live-integration posture is already in place — CI would set the same flag against a known-up backend. (The earlier fail-open rationale — that fail-closed-without-CI trains operators to ignore red — is superseded: the sole-scheme decision already made these intentionally fail-closed-live.)

## Conventions

- Python 3.11+. Ruff for lint/format (config at `pyproject.toml`).
- Frame of reference: ARKit-native everywhere on the wire. Convert at the boundary, not in transit.
- Coordinates: right-handed, +Y up, meters.
- Quaternions: `(x, y, z, w)`, unit norm within 1e-3.
- GCS paths in the bundle are RELATIVE to the bundle prefix, not full `gs://` URIs.
- File creation: every long-lived file gets a docstring explaining what it's for and who reads it. No "test code anyway" justifications for shortcuts.
- Tests pin invariants, not implementation. They should still pass after a refactor.

## Tooling conventions

Default model for routine work: **Sonnet 4.6**. Switch to **Opus 4.7** for hard reasoning (coordinate-frame conversions, perception-pipeline architecture, anything where a wrong answer propagates). Haiku 4.5 is not in use yet.

Default tool for code work: **Claude Code**. Default for strategy / architecture decisions: **Claude Chat**. See `.claude/WORKFLOW.md` for the full rubric and prompt templates.

## Git conventions

This repo is tracked with git. Local-only as of now; no remote yet.

**Claude Code's role with git:**

- Commit as part of normal work. One commit per logical unit (a feature, a fix, a refactor — not "end of session"). If a session produces multiple distinct changes, that's multiple commits.
- Write descriptive commit messages: what changed and why, not just what. Subject line under 72 chars; add a body if the why isn't obvious from the subject.
- Run `git status` and `git diff` before committing, and surface anything unexpected (e.g. a file changed that wasn't part of the task). Don't `git add -A` blindly.
- Run the relevant test suite after every code change. Show the full test output before proposing a commit. "Relevant" means: tests for the package or service that changed, plus any tests for packages that depend on it. If unsure which tests are relevant, run them all. Never commit untested code. If tests fail, fix and rerun until green; do not commit red, do not commit "mostly green," do not commit with an explanation of why a failure is fine. If a test is genuinely wrong, fix the test in the same commit and explain in the message.
- Do NOT push to remotes. The user pushes manually after reviewing. (As of now there is no remote anyway, but this rule pre-applies for when one is added.)
- Do NOT rewrite history (`git rebase`, `git commit --amend` on already-committed work, `git reset --hard` on commits you didn't make this session) without asking.
- Do NOT delete branches or force-anything.

**Session-end housekeeping commits:** the CLAUDE.md updates and any decision-note additions from session-end housekeeping should land in their own commit, separate from the code changes that prompted them. Message convention: `docs: session housekeeping — <one-line summary of what changed>`.

**What's gitignored:** see `.gitignore`. Notably: `outputs/`, virtualenvs, `.env`, `.claude/cache/` and `.claude/projects/` (Code's session state). Note that `CLAUDE.md`, `.claude/WORKFLOW.md`, and everything under `docs/decisions/` ARE tracked — they're project documentation, not local state.

## When to write a decision note

`docs/decisions/` holds the *why* behind decisions that aren't obvious from the code. One file per decision, filename `NNNN-short-slug.md` (zero-padded). Template at `docs/decisions/0000-template.md`.

The criteria for "is this worth a note?" live in the session-end housekeeping section below.

## Next on the board

When this section gets stale, the project's drifting. Keep it current.

**1 — Post-conventions placement thread: merge the fix branch, then ARKIT_ONLY position quality + LiDAR variant.** The 0063 convention probe CLOSED 2026-07-23 (decision 0065; see What-works): conventions fixed incl. the basis correction, deployed (`perception-obj-00028-hzq`, carries all branch code), production-verified (upright median 4.1°, sign tests pass, 12/12 frames, room visually corrected). Remaining on this thread: (a) DONE — `perception-layout-convention-fix` is merged (confirmed in `main`'s history 2026-07-23; every branch except the parked `diag-bundlepb-reason-public` is merged); (b) **ARKIT_ONLY placement quality — chunks A–C BUILT + merged 2026-07-23, now SERVING** (branch `placement-quality-build`, merge `e72a256`; probe verdicts in decision 0068): the code shipped with the 0069 shell deploy (`perception-obj-00032-km5`, 2026-07-24 — that revision built off main, so it carries chunks A–C too), the placement LIVE gate is now PARTIALLY MET: the whole `PLACEMENT_REFINE` pipeline (chunks A–D + the new room-sanity gate) was live-verified via a warm `/process` re-drive of `f3d70236` on `perception-obj-00033-zfg` (2026-07-24 — 24 objects / 7 placed, the gate demoted exactly the four operator failures; see What-works), so only the operator `/viewer` walk remains — and it is now DEFERRED under the board-item-7 Pro-only pivot (possibly moot). `PLACEMENT_REFINE=0` is the rollback lever. The re-opened in-plane instrument fork for near-square planar objects (0068) still stands (not a deploy blocker). **Chunk D (single-view contact priors) BUILT + merged 2026-07-24** (branch `placement-chunk-d`, commits `ba78510`/`af77edd`; built onto the shared `room_planes.py` 0069 extracted — no anchor-interpretation duplication; offline-verified against `f3d70236`'s real planes, see What-works). Its live gate RAN 2026-07-24: the warm `/process` re-drive of `f3d70236` on `perception-obj-00033-zfg` placed 5 single-view objects on measured surfaces (chunk D) and the total placed count jumped ~2 → 7; only the operator `/viewer` walk confirming furniture-on-floor / wall-objects-on-walls remains, now DEFERRED under the board-item-7 pivot. **Sampling-starvation insight (operator-flagged 2026-07-24):** the thin walls + missing furniture in real outputs are pipeline-side, not capture-side — only 5 of `f3d70236`'s 184 frames completed with masks, and single-view objects can't triangulate; the cures are warm re-drives (complete more frames cheaply), `PERCEPTION_MAX_FRAMES` (placeholder=12), and chunk D's contact priors (place single-view objects from ONE view). VIO-calibrated monocular depth deferred with named re-open triggers; (c) **LiDAR variant of the placement event — parked on Pro hardware** (board item 3 runbook applies first); 0063's don't-trust warning lifted in principle, zero real executions; (d) opportunistic: coverage knobs stay one-capture-calibrated (17/22 single-frame objects can't triangulate — `PERCEPTION_MAX_FRAMES` is one lever; 0067 chunk D's contact priors are the complementary one). (Phase 8c soak: RUN AND GREEN 2026-07-22 pre-dating this session's deploy — the new revision `00027-n8c` has since served the full re-drive successfully, which is its own functional soak; the RUNBOOK 8c one-liner can re-run on the next quiet day if desired. The former ops decision — deleting the pre-split `api` Cloud Run service — was operator-approved and executed 2026-07-21; Cloud Run runs exactly api-internal + api-public + perception-obj, plus the deliberately parked perception-geom.)

**2 — iOS P5 — OS-kill hardware gate** (decisions 0029, 0044, 0045). Core poll + status UI shipped (P5(a), commit `4556e44`). Remaining: **OS-kill hardware gate (cluster close item):** stage on-device: all blobs uploaded, `bundle.pb` PUT enqueued while alive, force-quit, reopen — confirm `bundle.pb` reaches GCS with no user interaction (GCS-authoritative, verified from Mac during locked interval); also verify `.task` fires on background OS-relaunch. `diag-bundlepb-reason-public` (`5bdd12f`) is the parked tool for reading the redacted `reason=`. AppDelegate fallback (decision 0045 Fork A) deferred pending this gate. After hardware gate: FCM `ready`/`failed` — backend threading is done (cleanup pass: Scene.fcm_token → ClaimResult → notifiers), so what remains is iOS-side FCM registration and passing the real `fcm_token` at `/upload_session`. Cold-start poll recovery is DONE (ios-upload-robustness, 2026-07-21): the cold-launch auth race is fixed (`c3a7396`), simulator-verified. NOTE post-activation: the `SceneStatusView` scan that used to restart polling across launches is no longer in the app — `ContentView` is unreferenced, so that recovery now lives in `RootFlowView.restoreUnfinishedBundle` + `BundleRestore` (decision 0073). The three status-surface honesty findings are CLOSED (branch `ios-status-surface`, 2026-07-21) — no open follow-up on this surface. **Separately, the iOS app DESIGN is built AND ACTIVATED as the app root** (decisions 0072/0073 — the Good Guest capture app: `DesignSystem/` foundation + every screen + the `RootFlowView` navigation coordinator; nine review passes; 288 tests). **The capture-to-doorway walk RAN 2026-07-26 on the 16 Pro — PASS** (two real captures; upload-under-lock, 405131a catch-up, background trip, relaunch restore, doorway exits all green — see What-works). Remaining on this surface, in order: the OS-kill hardware gate above (now executable on the 16 Pro), the decision-0074 phantom-room fix (403-as-not-ours acknowledge), the two build pieces — the real mesh render (task #13, unblocked; shape it after the board-7 RoomPlan design session) and the §5 Live Activity widget-extension target (task #14) — then the remaining activation follow-ups in `RootFlowView`'s docstring (add-more resume, web-handoff link). Terminal-failure UI (`failed`/`failed_invalid`/`failed_incomplete`, blob-failure banner) has still never rendered on hardware — exercise opportunistically when a real failure occurs or stage one deliberately.

**3 — LiDAR-tier hardware verification: DONE 2026-07-26** (item number kept — it is cross-referenced throughout this file). The `[LIDAR-UNVERIFIED]` set ran green twice on the iPhone 16 Pro: LIDAR_ARKIT tier dispatch, `captureDepth()`/`sceneDepth`, one depth blob per frame at 256×192 exact bytes, decision-0032 depth intrinsics, and — the raised-stakes half — decision 0052's `depth_fit` consuming real rasters in production (16/23 placed on capture #1). Decision 0033's residual is closed. The **RoomPlan co-run spike is DONE** (2026-07-28, decision 0076 — see What-works; the `sceneDepth`-strip caveat is refuted, the ARFrame-retention issue is quantified at 10 frames = pipeline death, and the co-run architecture is proven clean). Item 3 is fully closed. The **LIDAR_ROOMPLAN tier** itself remains unbuilt — its shape comes out of the board-7 design session, which now has every input.

**4 — Close the nine pre-launch gaps + one audit item (decisions 0015 + 0018)** (before public traffic)

Decision 0018 extended to nine gaps: original three abuse-surface gaps ((c)'s
client half closed 2026-07-21 — re-mint sends real sizes; its server half, field
optional/unvalidated, stays in this pass) + F1/F2/F3
contract-shape gaps (F4's web half closed this session via the assets endpoint,
decision 0054 — see the gap list for what little remains) + F5 (no lifecycle rule on
`gs://roomstudio-perception-outputs/scenes/`)
+ F6 (no TTL on Firestore `scenes` collection — ship TTL-only first; a client-ack cleanup
optimization on top is a later, unscoped possibility, see "What does NOT work" for detail).
Plus one audit item: verify perception-obj
runtime SA identity and storage IAM. Abuse-surface trigger: first non-developer user.
Contract-shape trigger: iOS development or web app build begins (now met — F1/F2/F3
remain open). Production-hygiene and
audit: launch hardening. The remaining gaps close in the same launch-hardening pass.

**5 — test_data/photos/ privacy review** (deferred, low urgency)

9 HEIC photos of a real room are tracked by git and used by
`tools/build_test_bundle.py` for local synthesis testing. Review whether they
should be anonymised, replaced with synthetic data, or removed from history.
Do not act on this until explicitly scoped — it may require a history rewrite.

**6 — Web app: next increment** (decisions 0050, 0052–0057; Good Guest rehaul
live on branch `web-app`, verified against fixtures)

In rough order once board item 1's deploy lands: (a) DONE 2026-07-22 —
standing preview channel live (see What-works; per-PR channel automation
is an optional later add); (b) 0051 BUILT on both sides (merged to main
2026-07-22; conventions in decision 0064) — remaining: enrollment-gated
Gate A (on-device link, UID unchanged) + Gate B on the preview URL (web
E2E: same Apple ID lists the real room; precondition: preview origin into
`CORS_ALLOWED_ORIGINS`); (c) first real assembled room in the PRODUCT
viewer — the dev-workbench half landed 2026-07-21 (scene `25a14caf`
renders at `/viewer` from the staged real-scene fixture in gitignored
`web/public/dev-fixtures/`); the `/room` product flow still needs (b),
and richer scenes need the 0063 convention fix + a warm re-drive for
coverage;
(d) **conversation fast-follows** (stage 1 SHIPPED 2026-07-21, decisions
0058/0059, revision `api-public-00012-ziz`): per-object extents into the
manifest (v2.x, after board item 1's verification event) → unlocks gap/
clearance speech with a `facts_version` bump; a real-browser watch of the
reveal AND the streaming composer at real speed (the dev preview pane
throttles both); tune the client wake cadences against real usage;
(e) interactive surface — per-object selection first
(buildable against fixtures now; this is the R3F/Drei adoption point per 0056,
with Theatre.js arriving for the reveal), then replace/rearrange; conversation
stage 2 (mutation) lands WITH the Design Specification contract + reactive
scene as one workstream; (f) **room shell — 0066 geometry + 0069
parametric surfaces both SHIPPED and V3-walked** (2026-07-23/24; decisions
0066/0069/0070; see What-works for the full deploy + V3 chain). Remaining
on this surface: the reveal-pacing real-browser watch (floor → walls →
objects, now parametric). The person-observation privacy gap is board 4's,
sharpened — 0069 removed baked person pixels but a person can still
contaminate a plane's measured albedo and reach the material-inference
evidence crops (decision 0070; see the privacy bullet).

**7 — RoomPlan integration: DESIGN DONE — EXECUTE (decision 0077 + `docs/briefs/roomplan-integration-build-brief.md`).** The board-7 design session ran 2026-07-28: all seven questions locked (wire / capture app / server shell / placement-fusion / frame selection / tier ladder / verification), three operator forks resolved (envelope-only degrade shell for non-RoomPlan LiDAR bundles; Good Guest live floor plan as task #13's shape; long tail ships v1 with new gates), and the two riskiest claims MEASURED in-session before locking (P1: the two-tier instrument discriminates the real bed's facing with tier-2 margin 0.15 GIVEN a box-quality center — and prefers upside-down at the shipped center, so box centers are load-bearing; P2: Apple's CapturedRoom Codable JSON is self-sufficient server-side — no coreModel, no USDZ). The design in one line: CapturedRoom JSON travels verbatim by reference (`roomplan/room.json`, additive proto, `schema_version` stays "1"); RoomPlan boxes are the object skeleton for covered categories (box carries position/extent/upright/yaw — SAM 3D drops to appearance, its layout rotation leaves the chain); the census drives per-object frame selection + reconstruction (two-pass /process — 39 SAM-3D passes → ~per-object); CapturedRoom is the shell geometry source (closure retires on this tier; 0069 materials retarget verbatim); the long tail keeps depth_fit + contact priors + three new measured gates (label-agnostic dedup, mirror depth-trust, textile silhouette-span). Execution = brief chunks RP-0..RP-8: RP-0 spike→bundle converter, RP-1 schemas, RP-2 roomplan_room.py parser (P2 pins), RP-3 shell v3 + envelope-only degrade, RP-4 box-anchored placement (P1 pins), RP-5 census selection, RP-6 iOS co-run + wire, RP-7 live floor plan, RP-8 deploy + upload the converted spike bundle + operator walk scored BY THE ACCEPTANCE METRIC. RP-0..5 verify OFFLINE against `outputs/roomplan-spike/probe-20260728-143602/` before any iOS work; RP-6 needs the 16 Pro (re-sign by 2026-08-04). 0075 defect-class map: floor collapse, furniture-slab walls, sampling starvation, covered-category orientation, edge-truncation phantom, label collapse — CLOSED by construction; cross-label triples, mirror depth, textile scale — gated; long-tail in-plane fork stays 0068-open.

## Session-end housekeeping (Claude Code: do this before ending any session)

Before the session ends — when the user indicates they're wrapping up, when tests are green and the task is done, or when the conversation is winding down — propose the following without being asked:

**1. CLAUDE.md updates.** Show a proposed diff for:
- **"What works right now"** — add anything that started working this session.
- **"What does NOT work / what we're deliberately not doing"** — add anything broken, half-done, or explicitly deferred. Be honest. Half-done is half-done, not "in progress."
- **"Next on the board"** — replace, don't append. This section reflects the present, not history.

Show the diff and ask for approval before writing. Don't write speculatively.

**2. Decision note candidates.** Identify whether anything this session is worth a `docs/decisions/NNNN-slug.md` note. The bar:

- A path was explored and rejected — write one.
- A non-obvious convention was chosen — write one.
- A workaround was applied that looks wrong without context — write one.
- Routine implementation that's self-evident from the code — skip.
- Strategy already captured in CLAUDE.md — skip.

If the bar isn't met, say so explicitly ("nothing this session rises to a decision note") and move on. Don't write notes just to seem thorough. If the bar IS met, draft using `docs/decisions/0000-template.md`, pick the next number, propose, wait for approval.

**3. If neither applies**, say so and end the session cleanly. Not every session changes the state.

The user can short-circuit this with "skip housekeeping" if the session was trivial (e.g. a one-line fix). Otherwise, run through it.

## How history is preserved

CLAUDE.md is the present. Anything historical — abandoned approaches, the reasoning behind a current convention, "we tried X and it didn't work" — goes in `docs/decisions/`, not here. If you find yourself wanting to add "we used to do Y" to CLAUDE.md, that's a signal to write a decision note instead and keep CLAUDE.md focused on what's true now.
