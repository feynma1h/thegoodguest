/// THROWAWAY SPIKE (board item 3 → board 7 design session input). Ships nothing.
///
/// App entry for the RoomPlan co-run spike. See SpikeController.swift for what
/// this app measures and why. Deliverable is ANSWERS (run artifacts under
/// Documents/runs/, pulled to outputs/roomplan-spike/), not software.

import SwiftUI

@main
struct RoomPlanSpikeApp: App {
    var body: some Scene {
        WindowGroup {
            SpikeView()
        }
    }
}
