~~packages/schemas/room_perception.py – pre-VGGT Layer 1/2 pydantic schema, superseded by capture_bundle.proto/SAM 3 pipeline, not referenced anywhere in the current architecture; candidate for removal.~~
~~packages/schemas/spatial_graph.py – pre-VGGT Layer 1/2 pydantic schema, superseded by capture_bundle.proto/SAM 3 pipeline, not referenced anywhere in the current architecture; candidate for removal.~~
services/api-public/public_server.py – Design + build client-ack cleanup path for scenes collection, layered on top of the (currently unbuilt, gap F6) TTL policy — client acks after successfully fetching/consuming the result (ready/terminal state), triggering prompt deletion; TTL remains as the backstop for scenes never acked (app crash, force-quit, uninstall, never reopened). Do not build ack-only — TTL must stay as the fallback since client cooperation isn't guaranteed. Sequencing: ship TTL-only first (gap F6, launch-hardening pass per CLAUDE.md), treat ack as a later optimization.

~~capture_bundle.proto:6 – Filename mentioned as CaptureBundle.pb.swift when it is actually capture_bundle.pb.swift~~
~~capture_bundle.proto:128 – See comment and decide if any change is warranted~~
~~RoomStudioCaptureApp.swift:3-8 – Comments here need to be polished before the project is deemed finalised~~
~~RoomStudioCaptureApp.swift:47-48 – Comments here need to be polished before the project is deemed finalised~~
~~PoseExtractor.swift:6-8 – Comments here need to be polished before the project is deemed finalised~~
~~CaptureManager.swift:1-11 – Comments here need to be polished before the project is deemed finalised~~
~~CaptureManager.swift:32 – Comments here need to be polished before the project is deemed finalised~~
~~CaptureManager.swift:294-300 – `captureDepth` declared `-> RSDepth?` but has no `return nil` path; decide whether to tighten to non-optional `RSDepth` (and switch the line-270 call site from `flatMap` to `map`) or keep the optional for call-site symmetry / future synchronous-failure handling~~
~~BundleAssembler.swift:6-8 – Comments here need to be polished before the project is deemed finalised~~
~~BundleAssembler.swift:54 – Stale comment: "zero vector stub — chunk C fills formula"; chunk C is committed (decisions 0030/0034), gravity is now a real computed vector. Update or remove.~~
~~BundleAssembler.swift:74-75 – device.deviceID (Keychain UUID) is never populated by the iOS app; every capture uses the hardware_id fallback. ~~
~~BundleAssembler.swift:80 – Misleading doc: 'Returns "hw.machine"' reads as returning the literal key string; it returns the value (e.g. "iPhone15,3"). Reword.~~
~~capture_bundle.proto:108 – Stale: "Raw from `utsname`"; implementation uses sysctlbyname per decision 0028. Update to reference hw.machine/sysctlbyname.~~
~~BundleAssembler.swift:81-82 – After proto:108 is fixed, drop the "the proto comment is stale" clause (it self-stales); keep only the "use sysctlbyname, NOT utsname — diverges on simulator" rationale.~~
~~ManifestBuilder.swift:11 – "(P4)" is a project-phase codename, not a component; meaningless to a future reader. Replace with the actual caller ("UploadCoordinator"), keeping the upload-last/ingest-signal rationale intact.~~
~~UploadSessionClient.swift:118 – Doc comment uses phase codenames ("nil for P3/P4; wired in P5").~~
~~UploadSessionRecord.swift:4 – "P4 reads this" names a dev phase not a component~~
~~UploadSessionRecord.swift:9 – "Int32 proto field number" is wrong; it's an enum value, not a field number~~
~~UploadSessionRecord.swift:11-15 – "gap F1" internal codename~~
~~UploadSessionRecord.swift:18-19 – "Stored so P4 can verify idempotency" — phase codename + incomplete rationale~~
~~UploadSessionRecord.swift:23-24 – "Pre-P4 records on disk" — phase codename in field doc~~
~~UploadSessionRecord.swift:26-28 – "Nil for pre-P4 records" — phase codename in outputDir field description~~
~~UploadSessionRecord.swift:53-54 – "unit (b)"/"unit (c)" — internal P5 sub-unit codenames in doc comment~~
~~UploadSessionRecord.swift:56-57 – "Phase-1 blobs" in uploadingBlobs doc — phase codename~~
~~UploadSessionRecord.swift:58-59 – "Phase-1 blobs uploaded" in uploadingBundlePb doc — phase codename~~
~~UploadSessionRecord.swift:62-63 – "populated by unit (b). Unit (c) never sets this" in failed doc — sub-unit codenames~~
~~UploadSessionRecord.swift:71,214 – `tier` convenience accessor (defined at line 214) appears unused; verify before shipping or remove~~
UploadSessionRecord.swift:73-74 – `clientMintTimestamp` resets to `Date()` (local clock); when F1 closes and server provides `expires_at`, this field and its reset logic should be replaced with the server-provided value
~~UploadSessionRecord.swift:83 – "Old records (pre-P4, no blobStatuses key)" — phase codename~~
~~UploadSessionRecord.swift:86 – "Nil for pre-P4 records" — phase codename in outputDir doc comment~~
~~UploadSessionRecord.swift:92 – "Reserved for unit (b); always nil in unit (c)" — sub-unit codenames~~
~~UploadSessionRecord.swift:98 – "Unit (b) only — always 0 in unit (c)" — sub-unit codenames~~
~~UploadSessionRecord.swift:169-170 – comment overstates the reason; the real constraint is simply that all stored properties must be assigned before the initializer exits, not specifically about closures capturing self~~
~~UploadSessionRecord.swift:88,111,136 – outputDir: URL? is optional only as a pre-P4 migration shim; no real users means no legacy records on disk — make it non-optional now and remove the shim decode path (line 186)~~
~~UploadSessionRecord.swift:177-184 – blobStatuses uses decodeIfPresent as a migration shim for pre-blobStatuses records; no real users means no such records — replace with plain decode~~
~~UploadSessionRecord.swift:188-194 – uploadPhase inference block is a migration shim for records that predate the field; no real users — remove the inference logic and replace with plain decode~~
~~UploadSessionRecord.swift:197 – crossLaunchRetryCount uses decodeIfPresent as a migration shim; no real users — replace with plain decode~~
~~UploadSessionRecord.swift:168-209 – once all four shims above are removed, the custom init(from:) can likely be dropped entirely in favour of Swift's synthesized Codable implementation~~
~~UploadSessionRecord.swift:254-255 – "Phase-1 gate re-closes" uses internal phase codename~~
~~UploadSessionRecord.swift:333 – "Phase-1→Phase-2 gate predicate" MARK label uses phase codenames~~
~~UploadSessionRecord.swift:337 – "load-bearing Phase-1→Phase-2 gate (decision 0040, item 5)"~~
~~UploadSessionRecord.swift:346-348 – "Phase-2 trigger" and "Phase-1 prerequisite" — phase codenames~~
~~UploadSessionStore.swift:10 – "Complete would silently stall finalize until next unlock" should say NSFileProtectionComplete to avoid ambiguity~~
~~UploadSessionStore.swift:15-16 – "P4 upload logic" is a phase codename~~
~~UploadSessionStore.swift:108 – "called by P4 cleanup on success" is a phase codename~~
~~UploadCoordinator.swift:1 – "P3" phase codename in class docstring; replace with functional description~~
~~UploadCoordinator.swift:11 – "P4" phase codename~~
~~UploadCoordinator.swift:109 – "Phase-1" codename in comment~~
~~UploadCoordinator.swift:203 – "P4" codename in comment~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:1 – doc comment uses unexplained internal jargon ("Phase-1", "Phase-2", "P4")~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:19-20 – "Step 2" cross-references a test file MARK section; opaque in the class-level doc comment.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:28-29 – "Unbuilt seams" doc comment is stale; onFatalBlobError is built — only the UI/FCM surface inside it remains a TODO.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:31-33 – "not yet wired" comment is stale; AppDelegate and @UIApplicationDelegateAdaptor are both live.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:35 – decision note references don't belong in a shipped file-level doc comment.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:96-99 – "Part B (M2 fix)" are internal dev labels~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:111-112 – launchToken is declared but never read; dead code.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:134,772 – remintProvider not wired in production; onSessionExpired routes to fatal instead of re-minting.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:298 – "pre-P4 records without the field cannot participate in Phase-1" is a migration comment that becomes stale~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:303 – "gap F2, decision 0040 item 2" is an open-issue tracking reference; drop it in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:308 – "outputDir is now in the persisted record" is a migration note explaining a past refactor; drop it in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:356 – "F2" is an open-issue tracking reference; drop it in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:367 – "0040 item 4" is an internal tracking reference; drop it in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:369 – "Map a completing task" should be "completed task" — the task has already finished by the time this runs.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:386 – "Q2 map" is an internal dev reference; drop it in the final form.~~
ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:434 – Retry-After header not honored on 408/429; pre-launch gap to implement before public traffic.
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:515 – redundant write-back; ctx is unmodified at this point, making contexts[bundleId] = ctx a no-op.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:616-618 – "not yet built" is stale; the relaunch path in UploadCoordinator is already implemented.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:646 – "F2" is an open-issue tracking reference; drop it in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:708 – "F2" is an open-issue tracking reference; drop it in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:731 – loopGuardEnabled check fatals on identical URIs assuming Firestore TTL lag, but a bounded wait-and-retry may be more appropriate; evaluate whether the loop guard design is correct and document the decision — changes would affect all loopGuardEnabled sites.~~
ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:748 – re-mint passes expectedSizeBytes = 0 instead of real file sizes; server should validate this (gap F3, pre-launch).
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:820-821 – save failure allowing an extra retry is noted as "tolerable" inline but could create an unbounded retry loop if saves consistently fail; evaluate whether this is load-bearing or acceptable to ignore, and document the decision clearly.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:847 – "decision 0043 follow-up" is an internal tracking reference; drop it in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:863-870 – save failure on staleness-reset record routes to deferTransientBlobError which could create an unbounded retry loop if saves consistently fail; evaluate whether this is load-bearing or acceptable to ignore, and document the decision clearly.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:878 – malformed sessionUri silently skips the blob via continue; if a fresh re-mint URI is invalid, the blob is permanently stranded — should route to onFatalBlobError instead.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:880-882 – unreadable or zero-size file silently skips the blob via continue; should route to onFatalBlobError instead of permanently stranding the blob.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:905,907-911 – same issue as staleness path; malformed URI or unreadable file silently skips blob via continue on the 410 path — should route to onFatalBlobError instead.~~
ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:927-937 – onBundleComplete is an unbuilt stub; needs to surface completion to UI/FCM, delete the on-device session directory, and remove the UploadSessionRecord.
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:933 – "decision 0045 P8 route A" is an internal planning reference; drop it in the final form (keep the decision note reference if desired, drop "P8 route A").~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:939 – "P5 unit a" is an internal codename; drop it in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:952 – "unit (c)'s guards" is an internal codename; drop it in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:954 – "Fork A = A1, unit a" is an internal planning reference; drop it, keep only the decision note reference.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:980-981 – "S0b finding" and "unit c" are internal references; drop them in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:987 – "Fork A = A1, pre-check from Fork C, unit a" is an internal planning reference; drop it, keep only the decision note reference.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:1006 – "Fork C" is an internal planning reference; drop it in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:1019 – "S0b" is an internal finding reference; drop it in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:1039 – "P5 unit b" is an internal codename; drop it in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:1050 – "Fork C = R, unit b" is an internal planning reference; drop it, keep only the decision note reference.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:1064 – "unit a" is an internal codename; drop it in the final form.~~
ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:1078 – "TODO P5" marks an unbuilt feature; surface terminal failure to UI/FCM needs to be implemented before launch.
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:1081 – "P5 unit b" is an internal codename; drop it in the final form.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:1098 – "Fork C = R, unit b" is an internal planning reference; drop it, keep only the decision note reference.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Upload/BlobUploadManager.swift:1121 – "Relaunch path re-enqueues via enqueuePhasOneBlobs" is inaccurate for bundle.pb; it's re-enqueued via onAllBlobsUploaded → enqueueBundlePb instead.~~
~~BlobUploadDelegate.swift:7-17 – "M1 fix", "M2 fix", "HOP 1" are patch/session codenames with no stable meaning~~
~~AppDelegate.swift:21 – "0041 (D2 AppDelegate wiring seam)" — "D2" is a patch codename.~~
~~CaptureStorageSweeper.swift:24 – decision ref cites 0040 (irrelevant) and misdescribes 0043; fix to /// Decisions: 0043 (sweep design, race guard, 300s threshold)~~
~~CaptureStorageSweeper.swift:3,50 – <bundleId> in path comments refers to the app's bundle identifier (Bundle.main.bundleIdentifier), but bundleId everywhere else in the codebase means the capture UUID — rename placeholder to <app-bundle-id> to avoid confusion.~~
~~CaptureStorageSweeper.swift:80 – comment "the actor serializes calls" implies actor isolation is what makes sweep safe during concurrent uploads; it isn't — safety comes from the age threshold and record presence check; the actor only prevents concurrent sweep() invocations, which can't happen anyway since sweep() has a single call site at launch.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Scene/SceneStatus.swift:7-8 – "P2 contract, revision api-public-00006-quw" is a phase codename + stale deployment revision; remove both, leave only the structural contract statement.~~
ios/RoomStudioCapture/RoomStudioCapture/Scene/ScenePoller.swift:53 – Unbuilt seam: when pollState transitions to .recoverable(missingPaths:), nothing triggers a re-upload; need a coordinator (SceneStatusView or equivalent) to watch this state and call into BlobUploadManager with the missing paths.
ios/RoomStudioCapture/RoomStudioCapture/Scene/ScenePoller.swift:69-75 – Cadence constants are placeholder guesses; retune cadenceShortWindow, cadenceMediumWindow, cadenceShort, cadenceMedium, cadenceLong once real perception latencies are known from production data.
~~ios/RoomStudioCapture/RoomStudioCapture/Scene/ScenePoller.swift:133-136 – Comment "re-callability IS the seam for the re-upload front" is misleading — the re-upload trigger (coordinator watching .recoverable → calling BlobUploadManager) is unbuilt; comment overstates completeness and obscures what "the re-upload front" means.~~
~~ios/RoomStudioCapture/RoomStudioCapture/Scene/ScenePoller.swift:178 – §6 is a dangling reference; the foreground-gating rationale lives in docs/decisions/0046-p5a-poll-client-design.md and 0047-p5a-scene-poll-design.md but neither has a numbered section 6~~
~~ios/RoomStudioCapture/RoomStudioCapture/Scene/SceneStatusView.swift:35 – setVisible(true) is redundant; resume() on line 36 already sets isVisible = true internally~~
~~ios/RoomStudioCapture/RoomStudioCapture/Scene/SceneStatusView.swift:106 – reconsider whether a .bordered/.secondary "Check now" button fits the premium product identity; consider a text link, pull-to-refresh, or dropping it entirely in favour of polling cadence + FCM~~
~~ios/RoomStudioCapture/RoomStudioCapture/Scene/SceneStatusView.swift:126 – resultUri is optional but should always be present on success; silent if let hides a backend contract violation~~
~~ios/RoomStudioCapture/RoomStudioCapture/ContentView.swift:39 – gating SceneStatusView on pollState != .idle deadlocks both poll-start paths: the kick needs isVisible (only set by the view) and onAppearAsync's .idle guard (SceneStatusView.swift:205) always fails once the view is visible, making findCompletedBundleId() unreachable — polling can never start on a cold foregrounded flow~~
~~ios/RoomStudioCapture/RoomStudioCapture/ContentView.swift:4-11 – doc comment block mixes phase-codename changelog notes ("P2 additions", "P3 additions") and a stale one-time P3-sign-off manual QA checklist, neither of which describes current behavior; changelog/verification history belongs in docs/decisions/ per CLAUDE.md, not inline — should be rewritten to state what the view does now (tier badge, bundle.pb path display, upload session state badge, coordinator trigger on bundlePath) with the checklist removed.~~
~~ios/RoomStudioCapture/RoomStudioCapture/ContentView.swift:30-32,129-139 – `gravityDebugHUD` (view usage at 30-32, definition at 129-137) was a one-time on-device sign/axis verification tool for chunk C sign-off (already closed per CLAUDE.md); now dead verification scaffolding left in mainline source, candidate for removal now that its gate has passed.~~
~~ios/RoomStudioCapture/RoomStudioCapture/ContentView.swift:141 – doc comment references phase codename "P3 pipeline" instead of describing current behavior; should read something like "shown after stop-capture triggers the upload session pipeline."~~

~~packages/api-core/roomstudio_api_core/scene.py:3-7 – docstring phrased as change-history ("Moved from...") rather than current-state description; reword to state the present architecture (canonical location + who re-exports it), per CLAUDE.md's own history-goes-in-decisions convention.~~
~~packages/api-core/roomstudio_api_core/scene.py:49-50 – comment cites a specific test-fixture byte count in production code; drop the number (already stale) and just describe the category ("tiny synthetic test fixtures") to avoid future drift.~~
~~packages/api-core/roomstudio_api_core/scene.py:55-72,131-136,159,177-180,205,217 — DeviceIdSource (PROVIDED / FALLBACK_HARDWARE_ID) exists to track a migration that hasn't started: device.deviceID is never populated by iOS (BundleAssembler.swift:74, "deferred to a later chunk"). Pre-launch, with zero real users, the documented removal trigger ("≥99% of captures over a 7-day window") is meaningless — there's no traffic to measure. Populate deviceID in iOS now and drop DeviceIdSource/FALLBACK_HARDWARE_ID entirely before launch: the enum (55-72), the device_id/device_id_source docstring (131-136), the field declaration (159), the __post_init__ type check (177-180), and the new_scene() parameter/pass-through (205, 217).~~
~~packages/api-core/roomstudio_api_core/scene.py:152,211 – "step 3" doesn't match any numbered step in this file or in services/api-internal/ingest_server.py (dispatch/enqueue is actually step 7 there, ingest_server.py:505); stale/wrong cross-reference, fix or reword to avoid an unpinned step number.~~
~~packages/api-core/roomstudio_api_core/scene.py:210-211 – docstring's idempotency guidance ("generate and pass your own scene_id") doesn't match actual usage; ingest_server.py achieves idempotency via get_by_bundle_id lookup-and-reuse of existing_scene_id, not by passing a deterministic ID into new_scene(). Also repeats the stale "step 3" reference (see scene.py:152 finding). Reword or remove.~~
~~services/api-internal/repository.py:99-108,142-146 – InMemorySceneRepository doesn't multiply-inherit from InMemorySceneReadRepository the way FirestoreSceneRepository does from FirestoreSceneReadRepository; both get() (105-108) and get_by_bundle_id() (142-146) are duplicated verbatim from scene_read_repo.py:77-80,82-86 instead of reused via InMemorySceneRepository(InMemorySceneReadRepository, SceneRepository).~~
~~packages/api-core/roomstudio_api_core/scene_read_repo.py:119,125 – DeviceIdSource deserialization is a leftover from before the iOS app existed (fallback hardware-ID path); candidate for removal now that iOS always provides a real device_id.~~
~~services/api-public/auth.py:10 – "Consumers: server.py" is stale; renamed to public_server.py (per CLAUDE.md rename note). Endpoint path itself is correct.~~
~~services/api-public/auth.py:39-40 – "(e.g. by fcm.py)" is unfounded; api-public has no fcm.py and constructs FirebaseTokenVerifier as a lazy singleton (public_server.py:139-140), so the reuse branch never fires in this service. Reword to drop the cross-service reference.~~
~~services/api-internal/fcm.py:64-65 – "(e.g. by auth.py)" is unfounded; api-internal has no auth.py (IAM-gated per decision 0016, not Firebase-JWT-verified) and no other firebase_admin.initialize_app() caller. Reword to drop the cross-service reference.~~
~~services/api-public/public_server.py:19 – comment only mentions the smoke tool as the consumer, omitting that ScenePoller.swift (iOS) is the actual production consumer of this endpoint — misleading about the endpoint's purpose.~~
~~services/api-public/public_server.py:52-56 – dev-only sys.path shim runs unconditionally with no ENVIRONMENT guard; comment says "in dev" but it also executes in production where it's redundant (packages are properly pip-installed per the Dockerfile) — should be gated or removed as prod hardening.~~
~~services/api-public/public_server.py:58 – # noqa: E402 implies this import must come after the sys.path patch above it, but auth.py (checked) has no dependency on packages/schemas/packages/api-core — it could be moved above the sys.path block with the rest of the top-level imports, dropping the noqa.~~
~~services/api-public/public_server.py:132,274 – bucket re-reads GCS_CAPTURES_BUCKET at request time falling back to _GCS_CAPTURES_BUCKET, which already resolved the same env var (with the same default) at import time — redundant double-read; collapse to one.~~
~~services/api-public/public_server.py:193,297 – CONFIRMED: both handlers are async def with zero await, and their full call chain (firebase_admin.auth.verify_id_token, google.cloud.firestore.Client.get/set/stream, google.auth.transport.requests.AuthorizedSession.post) is synchronous/blocking — no async client used anywhere in api-core/auth.py. Every request blocks the entire event loop for the duration of its Firebase+Firestore+GCS round-trips. Fix: declare both as plain def (FastAPI/Starlette will threadpool them automatically) or migrate the whole chain to async clients.~~
~~services/api-internal/scene.py:1-8 – docstring frames the shim as compat-preservation ("without requiring changes to existing imports") when there's no prior state to preserve in a first-draft project; reword to state current purpose only (re-export for a shorter import path), not migration framing.~~
~~services/api-internal/scene.py:11 – re-exports DeviceIdSource, which is already slated for removal~~
~~services/api-internal/fcm.py:17 – "Consumers: server.py (POST /ingest/eventarc existence-check failure path)" is stale; the file is services/api-internal/ingest_server.py, and the actual call site is _handle_failed_incomplete (ingest_server.py:603).~~
~~services/api-internal/validation.py:17 – stale filename reference: says "server.py (the FastAPI ingester)" but that module was renamed to ingest_server.py; update the docstring.~~
services/api-internal/validation.py:27-50 – validate_bundle(bundle) has no check cross-referencing bundle.bundle_id (capture_bundle.proto:62) against the URI-derived bundle_id; also structurally can't today since the function signature only takes bundle, not the URI/bundle_id — would need a signature change (and updating the call site at services/api-internal/ingest_server.py:385) to add this check.
~~services/api-internal/blob_validator.py:11,42-44 – docstring/comment hardcodes "21-byte test fixture" size in mainline code at two places (module docstring + MIN_IMAGE_SIZE_BYTES constant comment); couples prod comments to a test fixture's specific value, will go stale if the fixture changes — reword both to drop the magic number.~~
~~services/api-internal/dispatcher.py:18-19 – "This endpoint does not exist yet; it is the subject of the next session" reads as a session-diary note left in a permanent docstring; per CLAUDE.md perception-obj /process is deployed, so this is also factually stale.~~
~~services/api-internal/dispatcher.py:21-22 – "server.py falls back to InMemoryTaskDispatcher" should read "ingest_server.py" (renamed, see CLAUDE.md).~~
~~services/api-internal/dispatcher.py:24 – "Consumers: server.py (POST /ingest dispatch step)" should read "ingest_server.py".~~
~~services/api-internal/dispatcher.py:100-102 – class docstring says "No OIDC auth is attached... Auth is a future concern," but this is false: enqueue() attaches an OIDC token (lines 137-146) and perception-obj's oidc.py/server.py verify it, both wired via CLOUD_TASKS_INVOKER_SA which is set in production (infra/deploy_perception.sh:85, infra/api-internal.env.yaml:16). Docstring should describe the conditional-OIDC design, not deny it exists.~~
~~services/api-internal/repository.py:19-20 – comment frames re-export as backward-compat ("existing imports continue to work") on a first-draft project with no legacy callers; just state it's re-exported for callers that prefer importing from repository.py.~~
~~services/api-internal/repository.py:42-43 – same backward-compat framing repeated ("continues to work without changes in test files"); redundant with line 38's inline comment too.~~
~~services/api-internal/repository.py:25 – "Consumers: server.py" is a stale filename; module was renamed to ingest_server.py.~~
~~services/api-internal/repository.py:34 – imports Scene model directly from roomstudio_api_core.scene while every other api-internal file (ingest_server.py, test_scene.py, test_ingest.py, test_ingest_eventarc.py) goes through the local scene.py shim — inconsistent import style within the same service, worth picking one convention.~~
~~services/api-internal/repository.py:34 – InvalidTransitionError imported but never used as a name in code (only in docstrings); dead import.~~
~~services/api-internal/repository.py:37 – InMemorySceneReadRepository imported but unused; InMemorySceneRepository (line 99) duplicates get()/get_by_bundle_id() logic instead of inheriting from it, unlike FirestoreSceneRepository which does inherit from its read counterpart (line 153) — looks like an inconsistency/miss, not intentional.~~
~~services/api-internal/repository.py:38,42-49 – __all__ has no effect: nothing in the codebase does from repository import * or import repository; all consumers use explicit named imports which ignore __all__. Either dead code or just unused documentation-by-convention.~~
~~services/api-internal/repository.py:66-70,110-115,166-174 – create()'s return value has no real consumer: all 3 production call sites (ingest_server.py:497,580,662) discard it, and only one test (test_scene.py:284-289) reads it, scoped to TestInMemoryRepository only — the Firestore aliasing gap flagged earlier is untested. Consider whether create() needs to return anything at all, or if it does, add a matching Firestore-side isolation test.~~
~~services/api-internal/repository.py:232 – Remove device_id_source from Firestore serialization (_to_dict) before final/production schema.~~

~~services/api-internal/ingest_server.py:26 – docstring says uvicorn server:app --reload --port 8081, but the module was renamed to ingest_server.py (per CLAUDE.md); should be uvicorn ingest_server:app ....~~
~~services/api-internal/ingest_server.py:34-37 – brace-grouping implies only 3 vars matter for "real dispatch"; should clarify CLOUD_TASKS_INVOKER_SA is required for the dispatched task to be accepted by perception-obj (auth), even though it's not required for CloudTasksDispatcher to be selected/constructed — conflating "task gets sent" with "task gets accepted" is misleading to a reader deciding what's safe to omit.~~
~~services/api-internal/ingest_server.py:67 – CaptureTier imported but never used in this file; dead import, safe to drop.~~
~~services/api-internal/ingest_server.py:69,224-241,401,409,433,448,469,491,499,501,556,575,617,657 – DeviceIdSource fallback path (hardware_id) is a stopgap for iOS not yet populating device_id; remove once iOS ships real device identity.~~
~~services/api-internal/ingest_server.py:124-126 (IngestRequest) – becomes unused once the /ingest route is removed; IngestAck/IngestError stay (reused by /ingest/eventarc), but IngestRequest has no remaining reference — delete alongside the route.~~
~~services/api-internal/ingest_server.py:275-279 – _blob_exists constructs a new storage.Client() per call inside a thread-pool loop instead of reusing one; same pattern duplicated in services/api-internal/blob_validator.py:79, and a third separate client is made in _fetch_bundle_bytes (ingest_server.py:259). Net effect: a single incoming request with N blob paths currently constructs ~1+N+M separate clients (one per GCS call), not one shared client — e.g. ~200 for a 50-keyframe bundle (150 in the step-5 existence check, one per RGB/depth/confidence path, plus 50 in step 5b's image validation, one per RGB frame). Fix: reuse one client per request (or per-thread via threading.local()), not one shared client across all threads — cross-thread safety of storage.Client()/AuthorizedSession isn't documented, only inferred from engineering evidence (see requests library issue psf/requests#2766).~~
~~services/api-internal/ingest_server.py:393-395 – comment cites "1.0.0" as if describing a real stale-client cohort, but no external users have ever existed yet (project is pre-launch, per CLAUDE.md); the underlying reasoning (avoid Pub/Sub retry storms) is sound but should be reworded as a general future-schema-bump concern, not tied to the specific version string from the internal bug fixed in decision 0031.~~
~~services/api-internal/ingest_server.py:358,399,420-429,441,462 – with /ingest removed, _run_ingest's sole caller (ingest_eventarc) already guarantees a path match before calling in, so bundle_id can never be None here anymore. The three if bundle_id: branches and the bare-400 "no bundle_id" fallback (420-429) are now dead/tautological. Simplify by removing the None-handling entirely rather than documenting it — treat bundle_id as always-present.~~
~~services/api-internal/ingest_server.py:432-438 (resolve_device_id failure handling in _run_ingest) – returns a bare 400 unconditionally, unlike every other failure branch (steps 3, 5, 5b) which return 200 + terminal Scene state specifically to avoid Pub/Sub retry storms (per decision 0023, 400 → exponential-backoff redelivery, no dead-letter topic configured). A bundle.pb with both device_id and hardware_id empty — from any source, not just direct /ingest calls — would retry indefinitely via /ingest/eventarc since the GCS object never changes.~~
~~services/api-internal/ingest_server.py:513-531 – dispatch failure sets Scene status to plain FAILED and returns 500, but ingest_eventarc's idempotency check (lines 784-825) has no branch for FAILED — Pub/Sub redelivery on the resulting 500 falls through to a fresh _run_ingest with existing_scene_id=None, creating a duplicate Scene per retry instead of retrying the existing one.~~
~~services/api-internal/ingest_server.py:496,579,661 – decision 0036's bundle.user_id fallback existed only for the legacy /ingest route; with that route removed, no remaining caller lacks an upload_session record, so user_id should always come from _get_upload_session_repo().get_user_id(bundle_id). Simplify line 496 to match 579/661 exactly (drop bundle.user_id entirely) rather than adding a fallback chain — makes all three Scene-creation sites identical.~~
~~services/api-internal/ingest_server.py:697-715 (POST /ingest) – no confirmed caller today beyond its own test suite (test_ingest.py); production uses only /ingest/eventarc, the smoke tool uploads via GCS+Eventarc not this route, and "admin re-ingest" (decisions 0022/0036) is a cited rationale with no actual tool built. Candidate for removal (keep _run_ingest importable for tests) — also removes one of the two divergent branches (route vs. success/failure split) that decision 0022 already flagged as recurring-bug-prone.~~
~~services/api-internal/ingest_server.py:739-744 – idempotency docstring lists only 2 of the 4 real status branches (QUEUED/PROCESSING/READY skip, FAILED_INCOMPLETE retry) and omits FAILED_INVALID (line 797, its own skip branch) entirely; "Otherwise, run a fresh ingest" undersells that this silently also catches plain FAILED, which is the duplicate-Scene bug already tracked at lines 513-531 — the docstring should enumerate all four states so the gap is visible from the doc alone.~~
~~packages/schemas/capture_bundle.proto:65-66 – comment says user_id format is "Sign-in-with-Apple subject claim today," but per decision 0036 and BundleAssembler.swift:30, the actual value is the Firebase anonymous UID; stale, never updated when auth landed.~~
~~services/perception-obj/oidc.py:5-6 – "commit 5" is a non-durable reference (no hash/decision note); combined with the stale services/api/dispatcher.py path, this whole parenthetical should probably just be deleted or replaced with a docs/decisions/ pointer.~~
~~services/perception-obj/receiver_repo.py:12,18 – docstring references services/api/scene.py, but per CLAUDE.md that path was removed in the two-service split; Scene model now lives in packages/api-core/roomstudio_api_core/scene.py (re-exported from services/api-internal/scene.py) — stale path references.~~
~~services/perception-obj/receiver_repo.py:3 — reword docstring: "that the ingester's SceneRepository does not need" → "...is not responsible for" (or equivalent), to state scope positively rather than via negation.~~
~~services/perception-obj/receiver_repo.py:27 – field imported from dataclasses but never used in the file — dead import, safe to remove.~~
~~services/perception-obj/receiver_repo.py:83-84 – "idempotency or stale task" justification for NOT_FOUND isn't tied to any actual scene-deletion mechanism in the current codebase (no TTL on scenes collection per CLAUDE.md gap F6); comment reads as defensive-programming rationale rather than a real observed case — worth clarifying what's expected to trigger this, or noting it's currently unreachable in practice.~~
~~services/perception-obj/receiver_repo.py:113-124,229-256,388-436 – release_ready/release_failed don't check lease_holder_id, unlike release_lease (docstring 88-97, in-memory impl ~210-215, Firestore impl ~352-356) and release_queued (docstring 100-110, in-memory impl ~220-226, Firestore impl ~376-381), which no-op on holder mismatch — so a reclaimed-from worker that's actually still alive (slow past TTL, not crashed) can overwrite a reclaiming worker's terminal state with no guard; likely should take holder_id and no-op/raise on mismatch like the other two release methods.~~
~~services/perception-obj/receiver_repo.py:283,339,363,389,414 – redundant local re-imports of firestore; since __init__ (line 278) already imports it before any instance can exist, these could just reference a module-level import or an attribute set once in __init__ instead of repeating the deferred-import line in every method.~~
~~services/perception-obj/receiver_repo.py:289,293,335-336 – nonlocal result + outer default init is more complex than necessary; _fs.transactional-decorated functions propagate their return value through the call, so _txn could just return ClaimResult(...) in each branch and the caller could do result = _txn(self._db.transaction(), ref) directly, removing the outer variable, the nonlocal, and the default-value question altogether.~~
~~services/perception-obj/process_receiver.py:6 – comment says "the ingester OIDC patch" and points to services/api/dispatcher.py; path is stale (now services/api-internal/dispatcher.py) and "patch" wording implies an after-the-fact fix rather than original implementation — reword to plain "OIDC verification in services/api-internal/dispatcher.py".~~
~~services/perception-obj/process_receiver.py:180 – docstring "Return the gs:// prefix for paths relative to bundle_uri" is ambiguous/misleading — function returns an absolute directory prefix used to resolve other relative paths, not a relative path itself.~~
~~services/perception-obj/process_receiver.py:262 — reword "Mirrors the per-object loop in /objects but keyed by scene_id/frame" once services/perception-obj/server.py:392's /objects route is removed; the comparison will reference a route that no longer exists. Replace with a self-contained description of what the loop does (segment + reconstruct + upload per detected object), not a diff-against-a-sibling-route framing.~~
~~services/perception-obj/process_receiver.py:266 – import torch inside _process_frame is unused; the function's actual CUDA calls each do their own local import torch as _torch (lines 330, 332, 371) instead of using this one — dead import, likely edit leftover.~~
~~services/perception-obj/process_receiver.py:273-274 – redundant double existence-check (_gcs_blob_exists then _gcs_blob_exists_and_get, which re-checks existence internally) — collapse to one _gcs_blob_exists_and_get() call and branch on None.~~
~~services/perception-obj/process_receiver.py:344 – tmp.flush() flushes the wrong file handle; gs.save_ply(tmp.name) writes via its own independently-opened handle on that path, not through tmp, so this flush call doesn't actually guarantee the write is persisted before the following read — likely a no-op that looks like a safety measure but isn't one.~~
~~services/perception-obj/process_receiver.py:250-374 – _process_frame's per-object loop lacks the catch-and-continue that its mirrored source (server.py:495-501, /objects) has; one object's SAM 3D failure aborts the whole frame/scene instead of soft-failing that object alone. Given the field it carries ("ok") implies per-object granularity, this is a real behavioral divergence, not just a stale comment.~~
~~services/perception-obj/process_receiver.py:318,354,394,463 – "ok" field (always True, read only at line 463 for a log count) currently has zero consumers outside this file — nothing downstream (api-internal, api-public, iOS, tools) ever parses scenes/{scene_id}/manifest.json content; result_uri is treated as an opaque string everywhere it's used.~~ *(resolved: no code change — the per-object soft-fail fix (entry above) makes `ok:false` reachable, so the field now carries real per-object outcome for the future web app; manifest content remains unparsed by current consumers by design)*
~~services/perception-obj/process_receiver.py:641-648 – repo.release_failed() failure is only logged, but this is the last opportunity to mark the scene failed/clear its lease on both call paths (PoisonError → 200 drain, EnvironmentalError final attempt → maxAttempts exhausted) — a transient Firestore error here permanently strands the scene in processing with no further automatic retry. Not flagged in CLAUDE.md's pre-launch gap list; worth an explicit decision (alerting/reconciliation) rather than implicit accept-as-best-effort.~~
~~services/perception-obj/server.py:19-21 – "(or /segment / /objects call)" becomes stale when /segment//objects are removed.~~
~~services/perception-obj/server.py:23-25 – VGGT/call_perception.py placement paragraph describes a caller that no longer exists when the old photo-upload path is removed; delete rather than reword.~~
~~services/perception-obj/server.py:34 – hashlib import is solely for /objects's cache-key hashing (line 415); dead once /objects (392-541) is removed.~~
~~services/perception-obj/server.py:57-61 – confirmed: /process never receives HEIC (iOS frames are JPEG; even build_test_bundle.py's HEIC test fixtures are decoded client-side, registering their own pillow_heif opener at tools/build_test_bundle.py:46). This registration only serves the old /segment//segment-raw//objects handlers — safe to remove alongside them.~~
~~services/perception-obj/server.py:89-96 – PERCEPTION_OUTPUTS_BUCKET var itself stays (still used by /process at line 583), but the comment needs rewriting once /objects is removed: the "/objects falls back to the in-memory zip path" sentence describes a handler that will no longer exist (and was already inaccurate — no such fallback exists today)~~
~~services/perception-obj/server.py:98-154 – _bucket(), _gcs_upload(), _gcs_get_bytes(), _gcs_blob_size(), and the _gcs_bucket/_gcs_lock globals are called only from the /objects handler (392-541). process_receiver.py (the /process path) has its own separate _gcs_upload_for_scene (process_receiver.py:211) and never calls these. Entirely dead once /objects is removed — remove alongside it.~~
~~services/perception-obj/server.py:178-181 – docstring juxtaposes "Blocks the calling request" and "Safe under concurrency=1" as if the second qualifies the first; "safe" actually refers only to the double-checked-locking correctness (no race on _sam3/_sam3_error/_sam3_loading), not to the separate event-loop-starvation effect, which concurrency=1 doesn't mitigate. Worth a clarifying reword.~~
~~services/perception-obj/server.py:212-215 – same juxtaposition problem as get_sam3()'s docstring: "Blocks the calling request" and "Safe under concurrency=1" read as related, but "safe" refers only to double-checked-locking correctness (lines 217-241), not the separate event-loop-starvation effect. Same clarifying reword needed as the get_sam3 finding.~~
~~services/perception-obj/server.py:175-206 – get_sam3() runs SAM3Model() (line 198) synchronously inline on the event loop thread, starving /health and any other request for ~100s. Wrap the model construction in asyncio.to_thread/run_in_threadpool so it runs off the event-loop thread; note get_sam3 itself would need to become async def (or be called via threadpool from callers), which touches all call sites (lines 362, 434, 475, 482, 584).~~
~~services/perception-obj/server.py:209-241 – get_sam3d() has the identical issue with SAM3DModel() (line 233, ~95s load) and needs the same fix; call sites at lines 475, 482, 585.~~
~~services/perception-obj/server.py:259 – RECEIVER_URL/CLOUD_TASKS_INVOKER_SA imported but unused in _get_receiver_repo(); likely copy-pasted from _get_oidc_verifier (line 283) which does use them. Remove the import.~~
~~services/perception-obj/server.py:352-366 – /segment handler: old photo-upload path only; remove when that path is removed.~~
~~services/perception-obj/server.py:369-389 – /segment-raw handler: old photo-upload path only; remove when that path is removed.~~
~~services/perception-obj/server.py:392-541 – /objects handler: old photo-upload path only; remove when that path is removed.~~
~~services/perception-obj/server.py:569,576 – comments claim request-timeout=600s and a 30-min Cloud Tasks dispatch deadline; actual deploy script sets Cloud Run --timeout=900 (deploy_perception.sh:79) and dispatcher.py's enqueue() never sets dispatch_deadline (defaults to 10 min, not 30) — both figures are stale/wrong, and the mismatch (Cloud Run 900s > Cloud Tasks default 600s) means Cloud Tasks can retry a still-running attempt.~~
~~services/perception-obj/server.py:594-619 – _pack_segments: only used to pack /segment-raw output; dead once that handler is removed.~~
~~services/perception-obj/server.py:622-667 – _pack_objects: already dead code today — not called anywhere in this file. Remove regardless of old-path decision.~~
~~services/perception-obj/server.py:676 – tmp.flush() operates on the NamedTemporaryFile's own handle, but nothing is ever written through it — gs.save_ply(tmp.name) (line 676) writes via its own separate path-based open inside the external SAM 3D library. This line is a no-op; remove, or if there's a real flush concern it needs to be on save_ply's side, not here.~~

~~Fix FCM token contract (perception-obj sends garbage tokens) — add fcm_token to Scene, thread it through the claim path, rename perception-obj's notifier param from device_id to fcm_token. Requires changes to:~~
~~packages/api-core/roomstudio_api_core/scene.py:158 — add fcm_token: Optional[str] = None field to Scene~~
~~services/api-internal/ingest_server.py:487 — capture fcm_token (via _get_upload_session_repo().get_fcm_token(bundle_id)) on the happy-path new_scene(...) call, not just in _handle_failed_incomplete~~
~~services/perception-obj/receiver_repo.py:196,202 — ClaimResult must carry fcm_token alongside device_id~~
~~services/perception-obj/process_receiver.py:545,614,650 — read fcm_token off claim_result, pass it to notify_ready/notify_failed instead of device_id~~
~~services/perception-obj/fcm.py:32,36 — rename device_id param to fcm_token in notify_ready/notify_failed signatures and docstrings~~
~~services/perception-obj/fcm.py:60-63 — docstring describes a Firestore-key contract that was never built and is now superseded by the fcm_token-on-Scene plan; rewrite once the fcm_token threading lands (or delete if no longer accurate).~~

~~services/perception-obj/fcm.py:23 — Optional imported from typing but never referenced in the file; delete the import.~~

~~services/perception-obj/models/sam3.py:62 – zip(masks, boxes, scores, strict=False) silently truncates on length mismatch between three arrays that should always be parallel (one per detected instance); no log/skip, unlike the explicit skip-and-print two lines below for malformed mask shape — consider strict=True or an explicit length check to surface an upstream anomaly instead of silently dropping/misindexing detections.~~
~~services/perception-obj/models/sam3.py:71 – uses print(..., flush=True) instead of logger.warning(...), inconsistent with the rest of the service (server.py, process_receiver.py, fcm.py all use logging.getLogger(__name__), and server.py explicitly configures the root logger for Cloud Logging); loses severity distinction and isn't filterable/alertable.~~
~~services/perception-obj/models/sam3.py:88 – same issue: print(f"[sam3] class '{cls}' raised: {e}", flush=True) swallows an exception with no severity/traceback captured, should be logger.exception(...) or logger.error(...) per the service's established pattern.~~